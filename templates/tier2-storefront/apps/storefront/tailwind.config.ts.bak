import type { Config } from "tailwindcss"

/**
 * Tailwind v4 config — kept minimal. The `@tailwindcss/postcss` plugin handles
 * the actual compilation; this file exists mostly to scope content paths and
 * to declare any custom theme tokens.
 */
const config: Config = {
  content: [
    "./src/app/**/*.{ts,tsx}",
    "./src/components/**/*.{ts,tsx}",
    "./src/lib/**/*.{ts,tsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        sans: ["system-ui", "-apple-system", "BlinkMacSystemFont", "Segoe UI", "Roboto", "sans-serif"],
      },
      colors: {
        // Tier2-storefront brand (deliberately neutral; Path A swap keeps these)
        brand: {
          50: "#f5f5f4",
          500: "#78716c",
          900: "#1c1917",
        },
      },
    },
  },
  plugins: [],
}

export default config
