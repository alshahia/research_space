/**
 * Optional-modules.d.ts — type stubs for packages that are optional or
 * dynamically imported (Stripe, medusa-js sub-clients, etc).
 *
 * This file mirrors `templates/tier2-mobile/src/types/optional-modules.d.ts`
 * (Phase 3.3 pattern) — declared in coder summary §Deviations.
 *
 * Kept empty for the scaffold; extend with module declarations if a downstream
 * project adds a stripe sub-client or a Medusa admin widget.
 */
export {}
