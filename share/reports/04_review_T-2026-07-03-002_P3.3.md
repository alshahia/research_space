# Review Report — T-2026-07-03-002 / Phase 3 Build chunk 3 (Memory validator)

**Date:** 2026-07-03 20:55
**Sub-agent:** review
**Loop:** initial

## Summary

- **Overall verdict:** PASS_WITH_WARN
- **Tasks reviewed:** 2 (P3T1, P3T2)
- **Pass / Warn / Fail:** 2 / 0 / 0
- **Block release?** no (warns are cosmetic / dispatch-vs-plan divergence; no implementation drift)

## Tests / build run

1. **LOC count** — `wc -l scripts/validate-memory.sh` → **68** (PowerShell `(Get-Content).Count` → 69 due to missing trailing newline; `wc -l` is the canonical measure). Target ~50, hard cap 70. **PASS.**
2. **Bash parse** — `bash -n scripts/validate-memory.sh` → exit **0** (parses cleanly). `shellcheck` not installed in PATH; relying on `bash -n` per dispatch's fallback. **PASS.**
3. **Executable bit** — `bash -c 'test -x scripts/validate-memory.sh && echo true'` → `true` (bash reports executable). Windows FS shows `Mode: -a---` (Windows doesn't distinguish Unix mode bits for files without a Unix ACL). **PASS** (functional — bash can execute; runtime check confirms).
4. **Empty scaffold test (CRITICAL)** — `bash scripts/validate-memory.sh` against current empty scaffold → `OK: 0 entries validated` on stdout, exit **0**. No `find` matches in walked dirs (verified `find agents_manager/memory/{global,projects} agents_manager/{research,planning,design,coder,review,assets}/notes/{semantic,episodic} -type f -name '*.md' 2>/dev/null` returns empty). The script handles zero-file case gracefully via empty `files=()` array → zero-iteration for loop. **PASS.**
5. **Positive test (CRITICAL)** — Wrote 3 sample valid entries covering all 3 scope values (`scope: global`, `scope: project`, `scope: role`) with `status: active` and YYYY-MM-DD dates:
   ```
   ---
   scope: global
   topic: test
   status: active
   created: 2026-07-03
   last_verified: 2026-07-03
   ---
   ```
   `bash scripts/validate-memory.sh` → `OK: 3 entries validated`, exit **0**. Temp files deleted after. **PASS.**
6. **Negative test (CRITICAL)** — Wrote broken entry with `status: superseded` + missing `superseded_by:`:
   ```
   ---
   scope: global
   topic: test superseded
   status: superseded
   created: 2026-07-03
   last_verified: 2026-07-03
   ---
   ```
   `bash scripts/validate-memory.sh` → `FAIL : agents_manager/memory/global/_test_bad.md : status=superseded but missing 'superseded_by'` on stderr, `OK: 1 entries validated` on stdout, exit **1**. Spec required: exit 1 + ≥1 FAIL line referencing missing field. **PASS.**
7. **Edge case: CRLF handling (CRITICAL)** — Wrote `_test_crlf.md` with explicit `\r\n` line endings via `printf '---\r\n...' > file` (verified via `Format-Hex`: bytes `2D 2D 2D 0D 0A` at offset 0-2). `bash scripts/validate-memory.sh` → `OK: 1 entries validated`, exit **0**. The `tr -d '\r'` at line 30 successfully strips CRLF before line-splitting. **PASS.**
8. **Edge case: missing opening fence** — Wrote file with `scope:` on line 1 (no leading `---`). Validator emits `FAIL : ... : missing opening --- on line 1`, exit 1. **PASS.**
9. **Edge case: missing closing fence** — Wrote file with opening `---` but no closing. Validator emits `FAIL : ... : frontmatter not closed with ---`, exit 1. **PASS.**
10. **Edge case: missing required key** — Wrote file omitting `topic:`. Validator emits `FAIL : ... : missing required key 'topic'`, exit 1. **PASS.**
11. **Edge case: bad scope value** — Wrote file with `scope: bogus`. Validator emits `FAIL : ... : scope 'bogus' not in {global,project,role}`, exit 1. **PASS.**
12. **Edge case: bad status value** — Wrote file with `status: deprecated`. Validator emits `FAIL : ... : status 'deprecated' not in {active,superseded}`, exit 1. **PASS.**
13. **Edge case: bad date format** — Wrote file with `created: 2026-13-99` and `last_verified: not-a-date`. Validator emits 2 separate FAIL lines for both fields, exit 1. Regex `^[0-9]{4}-[0-9]{2}-[0-9]{2}$` + `date -d` parse catch both malformed and shape-correct-but-invalid dates. **PASS.**
14. **Fence invariant verification** — `git diff --shortstat -- .gitattributes .github/workflows/release.yml .gitignore README.md bin/README.md bin/agents-manager bin/agents-manager.ps1 bin/install.sh bin/release-zip.ps1 docs/INSTALL.md tasks/T-2026-07-03-001.md` → `11 files changed, 398 insertions(+), 45 deletions(-)`. Matches chunk 2 baseline exactly. **PASS.** (Note: coder's claim of "raw stat is `11/1248/895`" was NOT observed in this run — both with and without `--ignore-cr-at-eol` returned `11/398/45`. The coder's stat may have been from a different pre-state or environment; semantic baseline match is what matters and is confirmed.)
15. **Scope invariant verification (chunks 1+2 deliverables)** — `git diff --shortstat -- agents_manager/memory agents_manager/.gitignore agents_manager/research/notes agents_manager/planning/notes agents_manager/design/notes agents_manager/coder/notes agents_manager/review/notes agents_manager/assets/notes` → `4 files changed, 8 insertions(+), 126 deletions(-)`. The 4 modified files are the 4 chunk-2 README replacements (research/planning/coder/review notes/README.md). 14 untracked entries (all from chunks 1+2). **No new modifications or files by chunk 3.** Scope invariant preserved.
16. **WARN register integrity** — `share/notes/04_warns_register_T-2026-07-03-002.md` exists, 25 lines (was 22; appended 3 new WARNs in this review). All entries have phase + severity + description + `path:line`. Chunks 1, 2, 3 entries present. No duplicate entries. **PASS** (one cosmetic format inconsistency flagged as WARN — chunk 3 entry uses hyphen-dash while chunks 1+2 use em-dash).
17. **`git status --short` expected diff** — `git status --short` shows `?? scripts/validate-memory.sh` as the only new file from this chunk (in addition to chunks 1+2 untracked deliverables). No new modifications to existing files. **PASS.**
18. **Multi-file `declare -A` scoping check** — Ran validator with 2 simultaneous valid files to verify `declare -A fm=()` inside a for loop doesn't lose state between iterations. Both files validated correctly with their distinct scope values (`scope: global` for file A, `scope: project` for file B). Bash's `declare -A` inside a non-function loop persists correctly in this context. **PASS.**

## Per-task verdicts

### P3T1 — Write validator script

- **Verdict:** PASS
- **Spec match:** Yes. Per `tasks/T-2026-07-03-002.md:85` and plan §Phase 3 deliverables (`share/notes/02_plan_phases_T-2026-07-03-002.md:75-90`). All 5 spec requirements met:
  1. ~50 LOC bash, executable: ✓ (68 LOC; `bash -c 'test -x'` returns true)
  2. Walks `agents_manager/memory/{global,projects}/` + `agents_manager/<role>/notes/{semantic,episodic}/`: ✓ (line 17 + lines 18-20)
  3. Frontmatter open+close check: ✓ (lines 31-37)
  4. Required keys (`scope|topic|status|created|last_verified`): ✓ (lines 46-48)
  5. `scope ∈ {global, project, role}` enum: ✓ (lines 49-52)
  6. `status ∈ {active, superseded}` enum: ✓ (lines 53-56)
  7. Date parse YYYY-MM-DD: ✓ (lines 57-62)
  8. `superseded_by:` present when `status: superseded`: ✓ (lines 63-65)
  9. Exit 0=no issues, 1=at least one issue: ✓ (line 69 — last expression in script)
  10. `--secrets-strict` optional mode: NOT implemented (acknowledged by coder as dispatch-vs-plan divergence; out of scope per dispatch)
- **Correctness:** Verified across 9 distinct test cases (empty, 3× positive, 6× negative edge cases). All behaved as expected. The `tr -d '\r'` at line 30 successfully normalizes CRLF before any string operations. The `declare -A fm=()` inside the for loop (line 39) correctly creates a fresh associative array per file iteration — verified via 2-file test. The `${fm[$k]+x}` "alternate value" idiom at line 47 correctly handles `set -u` for unset keys without erroring.
- **Style:** Stdlib-only bash + coreutils (`find`, `tr`, `date -d`). No `jq`, no `yq`, no `python`. Comment block at lines 2-10 explains the walk list + the `canonical-schema` exclusion rationale (excellent — addresses a reviewer concern preemptively).
- **Tests:** No external test framework. All test cases run via inline heredoc + validator invocation. Tests deleted after each run, leaving the scaffold clean.
- **Evidence:**
  - `scripts/validate-memory.sh:12` — `set -u` present; `set -e` correctly absent.
  - `scripts/validate-memory.sh:14` — `ROLES=(research planning design coder review assets)` — matches dispatch spec exactly.
  - `scripts/validate-memory.sh:17-20` — walk list: 2 memory-scope dirs + 12 role-scope dirs = 14 total.
  - `scripts/validate-memory.sh:30` — `tr -d '\r'` CRLF normalization (line 30, not line 33 as coder's self-critique #3 noted; minor off-by-three).
  - `scripts/validate-memory.sh:35` — closing-fence search starts at index 1 (not 0). This is the mid-implementation bug fix the coder mentioned; verified by code reading.
  - `scripts/validate-memory.sh:39` — `declare -A fm=()` creates fresh array per iteration (verified functional).
  - `scripts/validate-memory.sh:42` — blank/comment line skip via `^[[:space:]]*(#|$)`.
  - `scripts/validate-memory.sh:44` — key regex `^([A-Za-z_][A-Za-z0-9_-]*):[[:space:]]*(.*)$` correctly captures key + value; lowercase via `${BASH_REMATCH[1],,}`.
  - `scripts/validate-memory.sh:47` — required-key check via `${fm[$k]+x}` set-u-safe idiom.
  - `scripts/validate-memory.sh:59` — combined regex + `date -d` parse (regex catches obvious non-dates on BSD; `date -d` catches shape-correct-but-invalid on GNU).
  - `scripts/validate-memory.sh:63` — `superseded_by` check: `[ -z "${fm[superseded_by]+x}" ]` is true ONLY when key is unset (value can be empty and still PASS).
- **Issues:**
  - [LOW] `scripts/validate-memory.sh:63` — plan says `superseded_by` should "AND points to a file that exists" (`share/notes/02_plan_phases_T-2026-07-03-002.md:83`); dispatch says "just check the key exists". Coder chose dispatch spec. No collision today (no superseded entries exist). Logged as WARN.
  - [LOW] `scripts/validate-memory.sh:69` — last line lacks trailing newline. Cosmetic; bash executes correctly.
  - [LOW] `scripts/validate-memory.sh:59` — `date -d` is GNU-specific. On BSD/macOS, the regex catches obvious non-dates but `2026-02-30` would slip through. Dispatch explicitly approved this trade-off; coder documented it inline.
  - [LOW] Coder's claim "Windowsy executable bit: `ls -la` shows `-rwxrwxrwx` because Windows filesystem doesn't distinguish Unix permission bits" is misleading — `ls -la` doesn't run on this Windows shell (PowerShell), and the actual `Mode: -a---` is what Windows shows. Bash can still execute the file (verified via `test -x`). Functionally correct, but the coder's explanation oversells the bit's presence.
- **Suggested fix:** no fix needed. (Optional follow-up: add path-resolution check for `superseded_by`; add trailing LF; consider `date` portability for BSD.)

### P3T2 — Dry-run verify (positive + negative samples)

- **Verdict:** PASS
- **Spec match:** Yes. Per `tasks/T-2026-07-03-002.md:86` and plan §Phase 3 deliverables. All 4 substantive checks performed independently:
  1. Empty scaffold → exit 0: ✓ (test #4 above)
  2. Sample valid entry → exit 0: ✓ (test #5 above, 3 entries covering all 3 scope values)
  3. Sample broken entry → exit 1 + ≥1 stderr line referencing missing field: ✓ (test #6 above)
  4. Sample entries deleted after verification: ✓ (verified `git status --short -- agents_manager/memory/global _test*.md` returns empty after each test)
  5. `bash -n scripts/validate-memory.sh` parses cleanly: ✓ (test #2 above)
- **Correctness:** All 4 substantive invariants met. The 3 additional edge-case tests (CRLF, missing opening fence, missing closing fence, missing required key, bad scope, bad status, bad date format) confirm the validator handles the full negative-test matrix.
- **Style:** Test methodology follows the dispatch's prescribed protocol (sample + run + verify + cleanup).
- **Tests:** N/A (no shipped tests; this task IS the test verification).
- **Evidence:**
  - All test scripts were PowerShell heredoc + `bash scripts/validate-memory.sh` invocations; results captured inline above.
  - `Get-ChildItem agents_manager/memory/global/_test*.md` after each test → empty (cleanup verified).
  - Coder's claim "Fixed one mid-implementation bug: original frontmatter-closing-fence finder matched the opening `---` (index 0) instead of the closing one" — VERIFIED by code reading at `scripts/validate-memory.sh:35` (search starts at index 1, not 0).
- **Issues:** None at the per-task level.
- **Suggested fix:** no fix needed.

## Cross-cutting findings

- **Mid-implementation bug fix is real and correct.** `scripts/validate-memory.sh:35` uses `for ((i=1; i<${#lines[@]}; i++))` to start the closing-fence search at index 1, not 0. Without this fix, the validator would match the opening `---` and report "frontmatter not closed" for every valid file. The coder's self-critique is honest about this; the fix is in place. (Verified by code reading.)
- **CRLF handling is real and correct.** `scripts/validate-memory.sh:30` uses `tr -d '\r'` to normalize Windows line endings before any string operations. Verified by writing a CRLF-encoded test file and confirming it passes validation. The `bash` interpreter handles LF internally, so this normalization is the right place to do it.
- **`set -u` is in place; `set -e` is correctly absent.** `scripts/validate-memory.sh:12` sets `set -u` only. The validator must collect all FAILs and exit 1 at the end, not abort on first failure. This is correct behavior.
- **Security analysis (per dispatch's request):**
  - **Command injection:** NONE. Script does not accept any user input. No `eval`, no `$()` interpolation of external data, no `read` from stdin. All file paths come from the hardcoded `dirs=(...)` array on lines 17-20.
  - **Path traversal:** NONE. Script walks hardcoded paths (`ROOT="agents_manager"`, line 13). No argument parsing for external paths. Per dispatch's expectation.
  - **`set -u`:** Present at line 12. Treats unset vars as errors. Good.
  - **`set -e`:** Correctly absent. Validator should report ALL issues.
  - **CRLF handling:** Present at line 30. Windows working tree handled.
  - **No external dependencies:** Stdlib-only bash + `find` + `tr` + `date -d` (GNU). All POSIX-safe except `date -d` (GNU-specific).
- **Fence invariant is preserved.** `git diff --stat` on the 11 fence files returns `11 files changed, 398 insertions(+), 45 deletions(-)` — matches chunk 2 baseline exactly. No fence regression.
- **Scope invariant is preserved.** `git diff --stat` on the chunk 1+2 deliverables returns only the 4 chunk-2 README replacements (8+/126-). No chunk 3 modifications to chunk 1+2 deliverables. No chunk 3 files added to chunk 1+2 directories (other than the expected `?? scripts/validate-memory.sh`).
- **Validator's hardcoded `ROOT="agents_manager"` is a feature, not a bug** (per dispatch's security expectation) but also a limitation: the validator cannot be used to validate other directory trees without editing the script. If multi-project support is ever needed, this would require a path argument + validation.

## Out-of-scope observations (informational only)

- [LOW] The `--secrets-strict` optional mode (per plan §Phase 3 deliverables, line 84 of `share/notes/02_plan_phases_T-2026-07-03-002.md`) is NOT implemented. Coder correctly noted this was in the plan but not in the chunk-3 dispatch spec. If desired, would be a small follow-up chunk. Surface to master.

- [LOW] `scope: canonical-schema` (chunk 1) and `scope: canonical-pointer` (chunk 2) are both intentional 4th-value meta-doc markers. The validator's strict 3-value enum check (`{global,project,role}` at line 50) would FAIL both if they were walked, but neither is in the walk list (canonical-schema lives at `memory/README.md`, canonical-pointer at `assets/notes/README.md`). No collision today. Same WARN as P3.1 row 3 and P3.2 row 4.

- [LOW] Coder's claim "raw stat is `11/1248/895` due to `core.autocrlf=true` CRLF/LF normalization noise" was NOT observed in my run — `git diff --shortstat -- <11 files>` (both with and without `--ignore-cr-at-eol`) returned `11/398/45` consistently. The coder's stat may have been from a different pre-state (e.g., before chunk 1's fix-loop removed some pre-existing CRLF noise). Either way, the semantic invariant (no NEW delta vs chunk 2 baseline) is what matters, and it's confirmed.

- [LOW] Coder's claim "executable bit set (Windows FS shows `-rwxrwxrwx`)" is misleading. Windows PowerShell `Get-ChildItem` shows `Mode: -a---` (no Unix-style execute bit). `ls -la` is not available on this Windows shell. The functional check `bash -c 'test -x scripts/validate-memory.sh'` returns true, which is what matters for runtime. Not a deviation; just an oversell in the coder summary.

- [LOW] WARN register's chunk-3 entry (`share/notes/04_warns_register_T-2026-07-03-002.md:20-22`) uses `P3.3 - LOW` (hyphen-dash) format while chunks 1+2 entries use `P3.X — LOW` (em-dash) format. Minor formatting inconsistency. Logged as WARN.

- [LOW] The script's last line `scripts/validate-memory.sh:69` has no trailing newline. `wc -l` reports 68 (correct newline count); PowerShell `(Get-Content).Count` reports 69. Both views are valid. Bash executes the script correctly. Repo convention is mixed. Logged as WARN.

- [LOW] `superseded_by:` with an empty value passes validation (the key check at line 63 uses `${fm[superseded_by]+x}` which returns "x" for set-but-empty). This matches dispatch spec ("key exists") but not plan spec ("points to a file that exists"). A future enhancement could verify non-empty + path resolves.

## Honest assessment

This chunk is shipped correctly. The 68-LOC bash validator does exactly what the dispatch spec says: walks the 14 expected directories, validates frontmatter open/close, enforces 5 required keys, enforces 2 enum constraints, parses dates with a regex + GNU `date` combo, and checks `superseded_by` presence when `status: superseded`. I verified all 6 validation rules across 9 distinct test cases (empty, 3× positive, 6× negative edge cases including CRLF). The CRLF handling at line 30 is real and correct (verified by writing a CRLF-encoded test file). The mid-implementation bug fix (closing-fence search starts at index 1, not 0) is in place and correct. Security posture is sound: no user input, hardcoded paths, no command injection vector. The fence invariant is preserved (11/398/45 matches chunk 2 baseline exactly). The scope invariant is preserved (chunks 1+2 deliverables untouched). The 3 WARNs I added are all LOW (spec-vs-plan divergence on `superseded_by` resolution, missing trailing newline, WARN register format inconsistency) and are appropriate for follow-up polish — none block the chunk from shipping. **Recommend advancing to chunk 4 (specialist SKILL.md protocol injection) without a fix-loop.**

## Self-critique

- **Did I do my job?** Yes. Read the actual script (not just the summary), ran every documented verification command, AND ran 5 additional edge-case tests (CRLF, missing opening/closing fence, missing required key, bad scope, bad status, bad date format) beyond the dispatch's required 3 (empty, positive, negative). Cross-checked all 6 validation rules against the spec's literal requirements. Per-task verdicts with `path:line` evidence for every claim. Verified the fence invariant AND the scope invariant.
- **What might I have missed?**
  - Did not run `shellcheck` (not installed in PATH). Dispatch acknowledged this fallback to `bash -n`. Bash parses cleanly.
  - Did not test on BSD/macOS — `date -d` is GNU-only. Dispatch explicitly approved the regex fallback for BSD.
  - Did not test `--secrets-strict` because the script doesn't implement it (acknowledged by coder as dispatch-vs-plan divergence).
  - Did not test the validator's behavior when the `agents_manager/` directory itself is missing (script would silently produce "OK: 0 entries validated" — but this is not a real-world scenario).
- **What did I assume without evidence?**
  - That the dispatch spec ("just check the key exists") supersedes the plan spec ("AND points to a file that exists") for `superseded_by`. Coder documented this divergence; I logged it as a WARN. The dispatch is the operative spec; the plan can be updated in a follow-up.
  - That `11/398/45` is the correct baseline for the fence invariant. Verified by direct comparison with chunk 2's recorded baseline (`share/reports/04_review_T-2026-07-03-002_P3.2.md:46-49`). Exact match.
  - That the missing trailing newline on the last line is cosmetic. Verified by running `bash scripts/validate-memory.sh` (executes correctly) and `wc -l` (returns 68, which is within the ≤70 cap).
  - That `set -u` is the right hardening for this script. The validator needs `${arr[key]+x}` to safely check unset keys, which is a `set -u` idiom. No false positives observed.

---

**Verdict tally:** 2 PASS (P3T1, P3T2) = **PASS_WITH_WARN**.
**FAILs:** 0.
**WARNs (per-task):** 0. **WARNs (out-of-scope/cross-cutting):** 4 new (LOW — `superseded_by` plan-vs-dispatch divergence, missing trailing newline, WARN register format inconsistency, coder claim accuracy on raw stat) + 1 carried over (LOW — fence invariant wording, already logged by coder).
**All 4 new WARNs appended to `share/notes/04_warns_register_T-2026-07-03-002.md` lines 23-25.**
**Call to action:** **ready to ship** — chunk 4 (specialist SKILL.md protocol injection) can begin; the only literal-plan-vs-dispatch divergence (`superseded_by` path resolution) is acceptable as a v0.13.1 follow-up.