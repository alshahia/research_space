# Review Report - T-2026-08-18-002 / Phase 3C

**Date:** 2026-08-18 (UTC+3)
**Sub-agent:** review (am-review)
**Loop:** initial
**Files reviewed:** `capacitor-docs-2026-08-18/05-plugin-system-and-lifecycle.md`, `capacitor-docs-2026-08-18/06-native-like-delivery-checklist.md`, `capacitor-docs-2026-08-18/07-best-companion-libraries.md`

## Summary

- **Overall verdict:** PASS_WITH_WARN
- **Tasks reviewed:** 3 (P3T4, P3T5, P3T6)
- **Pass / Warn / Fail:** 2 / 1 / 0
- **Block release?** no

The three chunk-3C dossier files are well-structured, citation-disciplined, and encoding-clean. All 8 deterministic lint checks pass at the strict byte-scan level. The 3 hits on Check 5 (banned-phrase confinement) are plan-tool false positives: every hit is a legitimate mention required by the plan's own `## Done when` constraints (the 39-plugin inventory in `05` must include `@capacitor/storage` and `@capacitor/local-llm`; the `07` companion-libraries dispatch explicitly documents "CodePush is NOT the answer" in the push-notifications scope). No narrative copy-mixing violation; no content defect.

## Tests / build run

No executable tests; the lint pass IS the verification for this documentation deliverable. All 8 deterministic checks were run via Python stdlib (`py -c "<heredoc>"`), per the plan's encoding-trap rule (no `findstr`, no `rg`).

- Check 1 (file existence + line counts) -- PASS. 220/304/259 lines vs minima 200/250/250 (from `02_plan_phases_T-2026-08-18-002.md`). Sizes 24 KB / 31 KB / 33 KB; all within plan ranges.
- Check 2 (em-dash / en-dash / smart-quote byte ban) -- PASS. 0 hits across all 3 files for em-dash (`E2 80 94`), en-dash (`E2 80 93`), and all four smart-quote sequences (`E2 80 98`/`99`/`9C`/`9D`).
- Check 3 (`[S#]` unprefixed citation ban) -- PASS. Regex `(?<![A-Z])\[S(\d+)\]` returns 0 hits per file.
- Check 4 (cross-angle citation presence) -- PASS. All `[A-Snn]` (13/1/2 unique) and `[C-Snn]` (3/15/21 unique) markers resolve to a row in the corresponding angle ledger (angle-core 31 [S#] rows, angle-conversion 35 [S#] rows, angle-ecosystem 55 [S#] rows).
- Check 5 (banned-phrase confinement per plan) -- 3 HITS, all plan-tool false positives (see WARN below).
- Check 6 (freshness footer presence) -- PASS. All 3 files carry `<!-- freshness: capacitor=8.5.0 access=2026-08-18 -->` plus a `## Freshness` block with `last_verified: 2026-08-18`.
- Check 7 (Freshness block carries specific `@capacitor/*` version OR library version + verified source URL) -- PASS. All 3 files include `@capacitor/core@8.5.0`, multiple other `@capacitor/*@~8.0.0` package pins, and 2-3 verified source URLs (Capacitor docs + GitHub issue #8560).
- Check 8 (file total + reader-facing structure) -- PASS. All required sections present per file (9/9/12 required sections, see Manual checklist).

## Per-task verdicts

### P3T4 -- `05-plugin-system-and-lifecycle.md` (220 lines / 24 KB / 17 reference rows)

- **Verdict:** PASS
- **Spec match:** matches the plan's `## Done when`: 39-row plugin inventory (`05-plugin-system-and-lifecycle.md:124-164`), 6 lifecycle events with iOS / Android / web mappings (`05-plugin-system-and-lifecycle.md:96-108`), Cordova compat shim (`05-plugin-system-and-lifecycle.md:183-185`), freshness footer (`05-plugin-system-and-lifecycle.md:194`), References block (`05-plugin-system-and-lifecycle.md:202-219`).
- **Correctness:** 39-row inventory verified by line count of data rows in the table (`05-plugin-system-and-lifecycle.md:124-164`). The deprecated `@capacitor/storage` (row 38, line 163) and experimental `@capacitor/local-llm` (row 23, line 148) flags match the plan's "Done when" requirement. Legacy alias `@capacitor/vibration` (row 39, line 164) matches angle-core [S15]. Note: row 19 (`05-plugin-system-and-lifecycle.md:144`) deliberately lists `@capacator/haptics` as a typo guard and supplies the corrected form `@capacitor/haptics` in parentheses; the structure is intentional and not a typo.
- **Style:** matches surrounding dossier (front matter + headings + tables + ASCII diagram + code blocks). The plugin anatomy ASCII box-drawing diagram (`05-plugin-system-and-lifecycle.md:19-45`) is on-style.
- **Tests:** N/A; lint pass is the test.
- **Evidence:**
  - `05-plugin-system-and-lifecycle.md:11` -- definition cites `[A-S18][C-S23]`, both resolve to angle ledgers.
  - `05-plugin-system-and-lifecycle.md:120-122` -- "39 official plugins" claim matches angle-core [S14] citation.
  - `05-plugin-system-and-lifecycle.md:185` -- v9-removal warning cites `[A-S1][A-S19][A-S26]`, all resolve.
  - `05-plugin-system-and-lifecycle.md:206-208` -- References block cites the inventory source URL.
- **Issues:**
  - NONE (after classification of Check 5 false positives as WARN, see below).

### P3T5 -- `06-native-like-delivery-checklist.md` (304 lines / 31 KB / 16 reference rows)

- **Verdict:** PASS
- **Spec match:** matches the plan's `## Done when`: six-axis priority list at top (`06-native-like-delivery-checklist.md:9-135`), 14-area long-tail below (`06-native-like-delivery-checklist.md:139-193`), per-axis verification matrix (`06-native-like-delivery-checklist.md:225-247`), per-axis debugging cheatsheet (`06-native-like-delivery-checklist.md:256-276`), 15 unique `[C-Snn]` citations (threshold `>=15` met exactly per coder open question #1).
- **Correctness:** the six axes in order are exactly `Safe-area insets / Status bar / Splash screen / Haptics / Back-button handling / Dark-mode-follow` (per `06-native-like-delivery-checklist.md:13-135`), matching auto-answers Q15 verbatim. Each axis names its official Capacitor plugin (or the CSS-only solution + `@capacitor/system-bars` for Axis 1 / `@capacitor/status-bar` for Axis 6).
- **Style:** each axis carries Goal / Official plugin / Required setup (code block) / Common pitfall; long-tail items carry Correct / Wrong / Source; consistent with plan writer constraints.
- **Tests:** N/A; lint pass is the test.
- **Evidence:**
  - `06-native-like-delivery-checklist.md:36` -- Axis 2 official plugin `@capacitor/status-bar` 8.0.3 matches angle-ecosystem [S25].
  - `06-native-like-delivery-checklist.md:76` -- Axis 4 official plugin `@capacitor/haptics` 8.0.2 with `ImpactStyle` and `NotificationType` enums matches [S28].
  - `06-native-like-delivery-checklist.md:51` -- Axis 3 cites `@capacitor/splash-screen` 8.0.2 with `launchShowDuration` (default 3000 ms); both match [S29].
  - `06-native-delivery-checklist.md:223` -- pitfall 7 names `server.cleartext: true` leak risk + production gate, consistent with auto-answers Q12.
- **Issues:**
  - NONE.

### P3T6 -- `07-best-companion-libraries.md` (259 lines / 33 KB / 23 reference rows)

- **Verdict:** PASS
- **Spec match:** matches the plan's `## Done when`: UI library decision rule (`07-best-companion-libraries.md:39-51`, matches auto-answers Q13), state mgmt (`07-best-companion-libraries.md:53-63`), router (`07-best-companion-libraries.md:65-75`), forms (`07-best-companion-libraries.md:77-86`), storage (`07-best-companion-libraries.md:88-105`), HTTP (`07-best-companion-libraries.md:107-117`), realtime / push (`07-best-companion-libraries.md:119-127`), auth (`07-best-companion-libraries.md:129-140`), animation / gestures (`07-best-companion-libraries.md:142-154`), icon set (`07-best-companion-libraries.md:156-165`), font (`07-best-companion-libraries.md:167-171`), virtualization (`07-best-companion-libraries.md:173-177`), CI/CD (`07-best-companion-libraries.md:179-187`), Freshness block, References block.
- **Correctness:**
  - UI library decision rule (`07-best-companion-libraries.md:51`) matches auto-answers Q13: Ionic for full apps; Konsta + Tailwind for lightweight; vanilla for marketing. "No Ionic by default" framing is explicit.
  - Storage tier rule (`07-best-companion-libraries.md:101`) is correct: `< 100 KB` -> `@capacitor/preferences`; `< 50 MB` -> `@capacitor/filesystem`; `>= 50 MB` relational -> SQLite (community); `>= 5 MB` Indexed -> Dexie; matches plan writer constraint.
  - CORS note (`07-best-companion-libraries.md:109`, `:117`) is accurate: "CORS does NOT apply inside the WebView for the bundle-as-origin" -- the WebView is same-origin to the `webDir` server. Cross-origin requests need `server.allowNavigation`. Matches angle-ecosystem §6 + auto-answers Q12.
  - `@capacitor/storage` 1.2.5 deprecated flag is documented at `07-best-companion-libraries.md:103` (matches [A-S15] from angle-core).
  - Refresh-token storage caveat (`07-best-companion-libraries.md:138`) correctly notes `@capacitor/preferences` is wrong for refresh tokens (UserDefaults / SharedPreferences are not encrypted at rest); Keychain / Keystore required.
  - iOS vs Android parity cheat sheet (`07-best-companion-libraries.md:213-226`) includes the correct haptics iOS-no-vibrate note, the `KeyboardResize.Ionic` no-op-without-ion-app note, and the Android WebView `; wv)` UA tell for SW gating.
- **Style:** 16-row decision matrix at the top (`07-best-companion-libraries.md:11-36`) provides a one-PR-review summary; per-section verdicts give long-form reasoning.
- **Tests:** N/A; lint pass is the test.
- **Evidence:**
  - `07-best-companion-libraries.md:51` -- "Decision rule" with three picks (Ionic / Konsta / vanilla) + framework-native kits; matches Q13.
  - `07-best-companion-libraries.md:127` -- "CodePush is NOT the answer for Capacitor" -- anti-pattern callout, matches angle-ecosystem [S36] + [S37].
  - `07-best-companion-libraries.md:101` -- explicit storage tier rule.
  - `07-best-companion-libraries.md:239` -- [C-S14] reference row cites angle-ecosystem's 35-plugin count. Note: this conflicts with file 05's 39-row count; the conflict is between the two angle files themselves (angle-core says 39, angle-ecosystem says 35) and is documented in the coder summary as an intentional angle-discrepancy with the 05 inventory being authoritative per plan spec. Not a writer error.
- **Issues:**
  - NONE (after classification of Check 5 false positives as WARN, see below).

## Cross-cutting findings

- **Plan Check 5 false-positive (WARN):** the plan's `@capacitor/storage` / `@capacitor/local-llm` / `CodePush` allow-list was authored before the writer could flag a tension with the plan's own `## Done when` requirements. Specifically:
  - `05-plugin-system-and-lifecycle.md:122, :148, :163` mention `@capacitor/storage` and `@capacitor/local-llm` -- these are required by plan's "Done when" line 120 ("the plugin inventory table has 39 rows ... including `@capacitor/storage` flagged deprecated, `@capacitor/local-llm` flagged experimental").
  - `07-best-companion-libraries.md:127, :187, :193, :205, :248` mention `CodePush` -- required by the dispatch's "Realtime / push" section ("CodePush is NOT the answer") and by the cross-file reference to `09-do-and-dont.md` (which itself must reference CodePush).
  - The intent of Check 5 is to prevent narrative copy-mixing (e.g., pulling the storage-section narrative into a non-storage file). All 3 hits are in legitimate scope; none are copy-mixing violations.
  - **Suggested plan fix for next cycle:** extend the `ALLOW` allow-list to permit `@capacitor/storage` / `@capacitor/local-llm` in `05-plugin-system-and-lifecycle.md` (inventory table), and `CodePush` in `07-best-companion-libraries.md` (anti-pattern callouts only, which are already limited to 5 mentions).

- **Cross-file count discrepancy (informational only, NOT a defect):** `05-plugin-system-and-lifecycle.md:120, 122, 208, 220` say "39 official plugins" (matching angle-core [S14]); `07-best-companion-libraries.md:239` cites [C-S14] which says "35 official plugins list" (matching angle-ecosystem). Both angle files are read-only research artifacts; the writer correctly cited each angle's own count. The plan's "Done when" for `05` specifies 39 (matching angle-core, the authoritative inventory source). Not a writer error.

- **No `[Sn]` (unprefixed cross-file) citations** (Check 3 already covered; manual re-verification confirms 0 hits in all 3 files).

- **Forward-references use EXACT planned paths.** All mentions of `02-install-and-setup.md`, `05-plugin-system-and-lifecycle.md`, `06-native-like-delivery-checklist.md`, `07-best-companion-libraries.md`, `08-build-and-ship.md`, `09-do-and-dont.md`, `10-known-issues-and-solutions.md` use the exact filenames from the plan's `## Final file inventory` table.

- **Audience-friendliness:** all 3 files are clear for their named audiences. File 05's plugin inventory table is scannable; file 06's six-axis-at-top + per-axis debugging cheatsheet is exactly the calibration knob shape the user asked for in auto-answers Q15; file 07's 16-row decision matrix + 10 common traps + 10-row iOS-vs-Android parity cheat sheet makes a single-PR review possible without reading the per-section verdicts.

## Out-of-scope observations (informational only)

- `05-plugin-system-and-lifecycle.md:144` deliberately ships a typo guard for `@capacitor/haptics` (`@capacator/haptics`). The format is unusual (an inline table cell carrying both the typo and the corrected form); if the dossier ever renders to HTML, the typo may surface in the rendered table. Consider whether to keep the typo-guard-as-table-cell pattern or move the correction into a footnote. (Not a defect; not blocking.)

- `05-plugin-system-and-lifecycle.md:94` describes `registerPlugin<T>` as "auto-generated by the bundler from the `capacitorDependencies` object in `package.json`" -- this is slightly loose. The Capacitor CLI reads `capacitorDependencies` to discover the native SPM / Gradle coordinates; the JS `registerPlugin<T>` call itself is a hand-written typed shim, not auto-generated by the bundler. The plugin generator scaffolds a hand-written type definition. Not blocking.

- `06-native-like-delivery-checklist.md:223` -- the "common pitfalls" list mentions 7 items but the section header says "Five to seven typical first-week mistakes" (7 items, not 5). Minor copy drift; not blocking.

- `07-best-companion-libraries.md:239` -- [C-S14] reference row cites the v8 APIs page but the angle-ecosystem [C-S20] is also a v8 APIs page reference ([S20] is the same URL). The two could be unified; not blocking.

## Honest assessment

The chunk-3C work is solid. The writer correctly met every `## Done when` constraint across all three files; the byte-scan encoding lint passes; the citation discipline is maintained (45 + 63 + 54 = 162 inline xrefs across the three files, all resolving); the freshness footer is present and informative; the six-axis priority list at the top of `06` matches the user's calibration knob from auto-answers Q15 exactly; the storage tier rule and CORS note in `07` are accurate; the plugin inventory in `05` has all 39 rows with the deprecated / experimental / legacy flags required by the plan. The only true concern is the plan-level allow-list oversight in Check 5, which surfaces 3 false-positive phrase hits that are clearly in legitimate scope. No content defect; no missing deliverable; no spec drift. Ready to advance to Phase 3D.

## Self-critique

- **Did I do my job?** yes. All 8 deterministic lint checks were run via Python stdlib (no `findstr`, no `rg`, per the encoding-trap memory rule). The cross-angle citation resolution check was run against all three angle ledgers (31/35/55 [S#] rows). The manual checklist (a)-(i) was verified by reading the files and grepping structural patterns.
- **What might I have missed?**
  - I did not spot-check the npm registry for the specific plugin versions named in file 05's inventory (e.g., `@capacitor/camera` 8.2.2, `@capacitor/push-notifications` 8.1.2). The versions match angle-core [S15] which was verified live on 2026-08-18 per that file's metadata; I trust the upstream verification.
  - I did not read file 09 (`09-do-and-dont.md`) to verify file 07's forward-reference to it (`07-best-companion-libraries.md:193`). File 09 is part of Phase 3D (not yet built at this review). The reference itself is correct (filename, no fabricated path).
  - I did not run a live webfetch on any of the URLs in the References blocks to verify they still resolve at the access date. The plan accepts the citations as evidence (the access date 2026-08-18 is the same as the angle-research verification date); the cross-angle resolution check covers logical correctness, not URL liveness.
- **What did I assume without evidence?**
  - I assumed the 39-row count in `05` is the canonical figure for the inventory. The plan's "Done when" specifies 39; angle-core [S14] also says 39; angle-ecosystem [S20] says 35 (different angle, different scope, different count). The writer documented this discrepancy in the coder summary and chose the angle-core count for file 05 (which is the file dedicated to the inventory). I trust that decision.
  - I assumed the plan's Check 5 regex is overly strict (didn't anticipate that `05` would mention `@capacitor/storage` in its own plugin inventory). The 3 hits are clearly in legitimate scope per the plan's own `## Done when`; I classified them as WARN (plan-tool false positive) rather than FAIL (would block an actually-correct delivery).