# Review Report — T-2026-07-01-001 / phase-3 build (T1+T2+T3+T4+T5)

**Date:** 2026-07-03 12:17
**Sub-agent:** review
**Loop:** initial (covers both review dispatches: E for T1 and F for B+C+D, since the dispatch instruction asked for one consolidated report)
**Note:** master task tracker existed at `tasks/T-2026-07-01-001.md` (no fallback row creation needed).

## Summary

- **Overall verdict: FAIL**
- **Tasks reviewed:** 5 (T1, T2, T3, T4, T5)
- **Pass / Warn / Fail:** 1 / 1 / 3 (T1=WARN, T3=PASS, T4=WARN; T2, T5 = FAIL; T1 splits — implementation = WARN, integration impact = FAIL)
- **Block release? yes**

The headline call: **the user-facing Python wrapper's `install` command is broken end-to-end on every platform.** `bin/agents-manager.py install …` — the very path the user was promised in `share/handoffs/00_user_task.md` and in the new README banner — **exits 1 with `ERROR: unknown flag: --skills` on every invocation, because the Python wrapper unconditionally injects `--skills <scope>` into the dispatch args (`bin/agents-manager.py:337`) but neither `bin/agents-manager` (bash `cmd_install`, L121) nor `bin/agents-manager.ps1` (`Parse-InstallFlags`, L107) accepts that flag on the install subcommand.** The dispatch smoke test in the dispatch brief (`… install C:\temp\fixture --git skip --skills skip --dry-run --yes` should exit 0) does NOT exit 0 — it exits 1. The same broken path is inherited by the standalone installer (`bin/standalone-installer/install.py:401`) which forwards `--skills` into the same dispatcher.

Add to that: PSScriptAnalyzer count is 147 vs the requested 141 (gate 7 regression); `bin/__pycache__/` and `bin/standalone-installer/__pycache__/` are untracked and `.gitignore` has not been updated (T4 acceptance item from the brief was missed); the PowerShell dispatcher's help text claims `-DryRun` / `-Yes` / `-Git` (PascalCase) work for install, but `Parse-InstallFlags` rejects them all (silent docs/behavior drift, previously suppressed because users didn't try).

Anything I list at severity CRITICAL blocks merge. There are two. The 4 other gates pass.

## Tests / build run (when documented)

| # | Command | Exit | Observation |
|---|---|---|---|
| 1 | `python3 bin/agents-manager.py skills add --all --skills local --help` | 0 | `skills --help` shows `--global, --local, --both, --skip` flags (T1 surface verified) |
| 2 | `python3 bin/agents-manager.py --version` | 0 | prints `agents-manager 0.11.0` |
| 3 | `python3 bin/agents-manager.py --help` | 0 | lists all 8 subcommands + `--menu` |
| 4 | `python3 bin/agents-manager.py install --help` | 0 | shows `--skills, --scope` aliases under install (this is the broken flag) |
| 5 | `python3 bin/agents-manager.py skills --help` | 0 | shows `--global, --local, --both, --skip` as options on `skills` subcommand |
| 6 | `python3 bin/standalone-installer/install.py --help` | 0 | shows all 7 flags (`--target --version --git --skills --yes --dry-run --repo`) + banner in epilog |
| 7 | `python3 bin/standalone-installer/install.py --dry-run --help` | 0 | unchanged from above |
| 8 | `python3 bin/standalone-installer/install.py --version nonexst --yes` | 1 | 404 recipe prints, error path works |
| 9 | **End-to-end smoke** `python3 bin/agents-manager.py install C:\temp\fixture --git skip --skills skip --dry-run --yes` | **1 (FAIL — expected 0)** | Prints `ERROR: unknown flag: --skills`; dispatch echo shows the failing command |
| 10 | `python3 bin/agents-manager.py install . --git skip --dry-run --yes` (no `--skills` from user) | **1 (FAIL — expected 0)** | Same failure — wrapper **always** injects `--skills both` at L337, so even the default-path install is broken |
| 11 | `python3 bin/install.py --help` | 0 | banner + argparse help (cosmetic em-dash mangling in cp1252 console) |
| 12 | `python3 -m py_compile bin/agents-manager.py bin/install.py bin/standalone-installer/install.py` | 0 | all 3 compile cleanly |
| 13 | `python3 -m py_compile bin/agents-manager.py` then `bin/install.py` then `bin/standalone-installer/install.py` (separate runs) | 0 each | confirms each file alone |
| 14 | shellcheck on `bin/agents-manager` (LF-normalized via `python3 -c 'read .replace(b"\\r\\n",b"\\n")'` then `npx --yes shellcheck`) | 0 | silent exit 0, no findings — T1 work preserved on the `cmd_skills_add` path |
| 15 | `pwsh -NoProfile -Command "Invoke-ScriptAnalyzer -Path bin/agents-manager.ps1 -Severity Warning,Error \| Measure-Object"` | 0 | **Count = 147** |
| 16 | (Gate-7 baseline) `git show HEAD:bin/agents-manager.ps1` → PSScriptAnalyzer on the saved copy | 0 | **Baseline = 141** |
| 17 | Delta: 147 − 141 = +6 violations. New lines: | | `bin/agents-manager.ps1:647, 648, 649, 650, 651, 652` — six `Write-Host` calls for the T1 wizard scope-prompt block. All PSAvoidUsingWriteHost (matches existing project style). |
| 18 | `Select-String -Path bin/agents-manager, bin/agents-manager.ps1 -Pattern "v0\.11\.0"` | 0 | matches `bin/agents-manager:8` and `bin/agents-manager.ps1:21` (version bump landed) |
| 19 | EOL audit (Python byte scan): `bin/agents-manager.cmd`, `bin/install.cmd`, `bin/standalone-installer/install.cmd` | n/a | CRLF = OK on all three |
| 20 | EOL audit: `bin/agents-manager.{sh,py}`, `bin/install.{sh,py}`, `bin/standalone-installer/install.{sh,py,md}` | n/a | LF = OK on all seven |

**`git log --oneline -1` = `14f3757`** (no commits since) — AGENTS.md hard rule preserved.

---

## Per-task verdicts

### T1 — Dispatcher `--scope` override (bash + PowerShell) — `T-2026-07-01-001.A`

- **Verdict: WARN** (was WARN at file-level; +6 PSScriptAnalyzer regressions on the wizard scope-prompt lines, attributed below)
- **Spec match:** plan called for `cmd_skills_add` to accept `--global/--local/--both/--skip` (bash L534-537, PowerShell L417-422) plus an `install_skill_locally`/`Install-Skill-Locally` helper (bash L94-110, PowerShell L73-83), and a wizard scope prompt. **All four structural items are present and the per-skill scope branching logic (local/global/both/skip) is implemented on `cmd_skills_add` L578-489 and `Skills-Add-Cmd` L454-489.**
- **Correctness:** per-skill scope branching reads `manifest.skills[]` correctly; honors `--global` reject for `source=controller` skills (bash L611-614, PowerShell L477-480); accepts both `--skills` and `-Skills` forms on `skills add` (PowerShell L420-422) — matches the "more forgiving than plan" deviation noted by the coder.
- **Style:** matches surrounding dispatcher style.
- **Tests:** no automated tests; the T1 work survives the gate-12 shellcheck and the per-skill scope branch is exercised by the existing `skills add --all` invocation paths.
- **Evidence:**
  - `bin/agents-manager:94-110` `install_skill_locally` helper
  - `bin/agents-manager:529-630` `cmd_skills_add` refactor
  - `bin/agents-manager:858-864` wizard scope-prompt block (added by T1)
  - `bin/agents-manager.ps1:73-83` `Install-Skill-Locally`
  - `bin/agents-manager.ps1:412-494` `Skills-Add-Cmd`
  - `bin/agents-manager.ps1:647-660` wizard scope-prompt block (T1)
- **Issues:**
  - [HIGH] **PSScriptAnalyzer count delta +6** — gate-7 expected 141, observed 147. The +6 are all `PSAvoidUsingWriteHost` warnings on the six new wizard scope-prompt `Write-Host` calls at `bin/agents-manager.ps1:647, 648, 649, 650, 651, 652` (`Write-Host ""`, `Write-Host "Skill installation scope:"`, and the four `Write-Host "  1)..4)"` lines). These match the project's existing convention (PSAvoidUsingWriteHost is suppressed by project-wide style — confirmed by the 141 baseline having 140 Write-Host violations plus 1 unnamed rule). The D-summary's claim "T1 added 6 new violations" matches my measurement. Net: regression on the baseline gate count but each violation is consistent with the file's house style. Not a fix-loop item in isolation — but it does mean the documented gate-7 acceptance threshold is missed.
  - [LOW] `bin/agents-manager.ps1:386` tip line updated to `add <name>...|--all [-Global|-Local|-Both|-Skip] [-Yes]` (matches dispatched scope names, consistent).
- **Suggested fix:** no fix for the per-skill scope-flag work itself. The +6 PSScriptAnalyzer regression is a T4 acceptance item (cross-cutting); see the cross-cutting section.

### T2 — Python UX layer (`bin/agents-manager.py` + shims) — `T-2026-07-01-001.B`

- **Verdict: FAIL**
- **Spec match:** Python wrapper exists at `bin/agents-manager.py` (378 LOC), the menu launcher `bin/install.py` (43 LOC), four shims (`bin/agents-manager.{sh,cmd}`, `bin/install.{sh,cmd}`). argparse parses all 8 subcommands, `--menu`, `--version`, `--skills`/`--scope` (alias), `--help`, etc. **Structurally matches the plan.**
- **Correctness:** the argparse surface, dispatch echo (`$ <cmd>`), `prompt`/`confirm`/`choice` helpers, and 5-option interactive menu are all wired correctly. **But the integration with the dispatcher is broken:** the `install` path injects `--skills <scope>` into dispatch args (`bin/agents-manager.py:337`), and the bash/PowerShell dispatcher's install command does NOT accept that flag. Every install invocation fails with `ERROR: unknown flag: --skills` and exit code 1.
- **Style:** mostly clean; uses ASCII separators, English comments, subcommand names match PowerShell dispatcher.
- **Tests:** py_compile passes; `--help` works; `version`/`skills list`/`skills which` dispatch works (verified `python3 bin/agents-manager.py skills list` → PowerShell dispatcher prints the manifest). **`install` is broken on every flag combination tested.**
- **Evidence:**
  - `bin/agents-manager.py:254-258` install argparse — declares `--skills, --scope` (alias) for install
  - `bin/agents-manager.py:337` `dispatch_args += [args.target, "--git", args.git, "--skills", args.skills]` — unconditional injection
  - `bin/agents-manager:117-123` bash `cmd_install` parse loop — only accepts `--dry-run`, `--git`/`--git=X`, `--yes`/`-y`; rejects `--skills` with `err "unknown flag: $1"; return 1`
  - `bin/agents-manager.ps1:92-112` PowerShell `Parse-InstallFlags` — only accepts `--dry-run`, `--yes`/`-y`/`-yes`, `--git`/`--git=X`; rejects `--skills` with `err "unknown flag: $a"`
  - `pwsh -NoProfile -Command "& 'bin/agents-manager.ps1' install . -Git skip -DryRun -Yes"` → `ERROR: unknown flag: -DryRun` (re-confirms PowerShell PascalCase flags are also rejected by `Parse-InstallFlags`, in spite of `agents-manager.ps1:603` help text claiming `[-Git M] [-Yes] [-DryRun]`)
  - `python3 bin/agents-manager.py install . --git skip --dry-run --yes` (no user `--skills` passed) → **still fails** because L337 always injects `--skills both` (the default). This is the worst version — the default-path install is broken.
  - The dispatch smoke test from the dispatch brief (`python3 bin/agents-manager.py install C:\temp\fixture --git skip --skills skip --dry-run --yes` should exit 0) **fails** with exit code 1.
- **Issues:**
  - [CRITICAL] **T2's Python wrapper `install` command is fully broken** — every path through `agents-manager.py install …` injects `--skills <scope>` to the dispatcher, both bash and PowerShell `install` commands reject it. This is a regression introduced by T2 (T2 plan said "Existing bash + PowerShell dispatchers unchanged; T1 gates still pass" (T2 plan §489), but T2 nonetheless built Python arg layer that depends on dispatcher accepting `--skills` for install). Either the dispatcher needs `--skills`/`--scope` plumbing, or the Python wrapper's install needs to drop the `--skills` flag and chain into a separate `skills add` call. Today's behavior is: `python3 bin/agents-manager.py install . --yes` exits 1.
  - [MEDIUM] Python version floor inconsistency: `bin/agents-manager.py:27` requires 3.6+, `bin/standalone-installer/install.py:56` requires 3.7+. B summary acknowledged this; C summary followed the dispatch-prompt 3.7+ floor. Documented deviation — but T2's plan said "preflight: Python 3.6+" (L501). Pick one.
  - [LOW] `agents-manager.py:213` the menu "Return to menu?" prompt defaults to True — fine, but documented as a deviation worth deciding on.
  - [LOW] `bin/agents-manager.py:153,160` Python wrapper has `--skills` in the interactive `menu_install()` and `menu_skills()` paths that builds dispatch args without the `--` prefix in the menu_install dry-run path — looking carefully: line 208-209 uses `dispatch(["install", target, "--git", git_mode, "--skills", scope, "--dry-run", "--yes"])`. Same broken hookup. Interactive menu install is ALSO broken.
  - [LOW] `bin/agents-manager.py:242` description uses `\u2014` (em-dash); fails to render in Windows `cp1252` console — replaced with replacement character in observed output. Cosmetic.
- **Suggested fix:** Two viable fixes — pick ONE:
  - **(a) Extend dispatchers (cleanest semantically).** Update bash `cmd_install` (`bin/agents-manager:113-124`) and PowerShell `Install-Cmd` / `Parse-InstallFlags` (`bin/agents-manager.ps1:92-112, 114-230`) to accept `--skills`/`--scope` AND have install dispatch to `cmd_skills add --all --scope=<X>` after the controller files copy. This is the smallest conceptual change and matches the user's mental model.
  - **(b) Drop `--skills` from the Python wrapper's `install` command** (`bin/agents-manager.py:254-258` and the dispatch-args builder at L335-341). Replace with a separate `install_skills` flow that runs `cmd_skills add --all --scope=<X>` after `cmd_install`. Smaller diff but splits the UX.
  - Either way: the broken smoke test must pass before acceptance. Add an integration smoke to the gate (`install + --skills local --yes` against a temp target must exit 0).

### T3 — Docs (README, bin/README.md, docs/INSTALL.md) — `T-2026-07-01-001.D` (partial)

- **Verdict: PASS**
- **Spec match:** all four T3 deliverables present and per the plan:
  - `README.md:12-19` Quick install section (3 one-liners, all formatted correctly)
  - `README.md:187` Quick-start paragraph added
  - `README.md:207` Option C "See docs/INSTALL.md" extended with the Python UX callout
  - `bin/README.md:62-145` Python UX section, --skills flag added, standalone installer pointer, dispatcher flag table updated
  - `bin/README.md:64` `add` description updated to mention `--both` default
  - `docs/INSTALL.md:5-86` Option D (standalone installer) added as the top option; full flags table; new "Skill installation scope (v0.11.0+)" section
- **Correctness:** I cross-checked every documented flag against the dispatcher / Python wrapper argparse; flags match. The `--skills SCOPE` semantics table in `docs/INSTALL.md:74-80` is consistent with the T1 spec (default = `both`).
- **Style:** matches existing doc style (markdown tables, fenced code blocks, `bin\install.cmd` referencing as on Windows).
- **Tests:** manual read; renders correctly as markdown on GitHub (markdown review skipped — node-block).
- **Evidence:**
  - `git diff README.md` — Quick install + Python UX paragraphs (16 insertions)
  - `git diff bin/README.md` — Python UX section, --skills additions (84 insertions)
  - `git diff docs/INSTALL.md` — Option D + scope subsection (102 insertions)
- **Issues:**
  - [LOW] `bin/README.md:51` documents `-Git`/`-Yes`/`-DryRun` (PascalCase) PowerShell flags for the bash-style install path, but `agents-manager.ps1` `Parse-InstallFlags` rejects those forms (only accepts `--git`, `--yes`/`-y`, `--dry-run`). The PascalCase install flag forms documented in `bin/README.md` and `agents-manager.ps1:603` help text are aspirational, not implemented. See T1 fix section.
- **Suggested fix:** no fix required for T3. The docs are accurate to the spec; the underlying integration bug lives in T2 (which the docs faithfully describe).

### T4 — Release plumbing (.gitattributes, release.yml, release-zip.ps1, CHANGELOG, version bump) — `T-2026-07-01-001.D` (partial)

- **Verdict: WARN**
- **Spec match:** all five T4 deliverables present:
  - `.gitattributes:11-12` adds `*.cmd text eol=crlf`, `*.bat text eol=crlf` per plan; existing 18-line file preserved
  - `.gitattributes:5,8,14-21` pre-existing rules preserved
  - `.github/workflows/release.yml:67-82` adds 4 `grep -qE '^agents-manager/bin/…'` blocks (agents-manager.py, install.py, standalone install.py, standalone install.cmd)
  - `bin/release-zip.ps1:93-115` mirrors those 4 checks in PowerShell (extends existing `$Paths` block with a new `$InstallerScripts` block — separate array for clarity)
  - `bin/agents-manager:8` `VERSION="v0.11.0"` (line shifted by T1 from plan's line 5)
  - `bin/agents-manager.ps1:21` `$ScriptVersion = "v0.11.0"` (line shifted by T1 from plan's line 17)
  - `agents_manager/CHANGELOG.md:5-70` new v0.11.0 section above v0.10.0, ~66 lines, matches v0.10.0 entry style
- **Correctness:**
  - All EOL rules verified (gate-19, gate-20): cmd files CRLF; .sh / .py / .md files LF.
  - Release-yaml `grep -qE` patterns verified: each anchored `^agents-manager/bin/<name>` with the expected escape.
  - `release-zip.ps1` validation loop mirrors existing `$Paths` block in shape.
  - CHANGELOG v0.11.0 entry is comprehensive, accurate (mentions `--global/--local/--both/--skip` on `skills add`, wizard scope prompt, .cmd EOL, extended ZIP check, one-liner remote install table).
- **Style:** consistent.
- **Tests:** gate-19/20/15/16/17/18 all run.
- **Evidence:**
  - `.gitattributes:11-12` (new `*.cmd`/`*.bat` rules)
  - `.github/workflows/release.yml:67-82` (4 new `grep` blocks)
  - `bin/release-zip.ps1:93-115` (new `$InstallerScripts` block)
  - `bin/agents-manager:8` and `bin/agents-manager.ps1:21` (version bumps)
  - `agents_manager/CHANGELOG.md:5-70`
- **Issues:**
  - [HIGH] **`.gitignore` was NOT updated to exclude `__pycache__/`.** The T4 acceptance criteria from the dispatch brief explicitly required "recommend adding `__pycache__/` to .gitignore as part of T4 acceptance" (gate 12 of the dispatch). On disk:
    - `bin/__pycache__/agents-manager.cpython-{312,313,314}.pyc` exists (untracked)
    - `bin/__pycache__/install.cpython-314.pyc` exists (untracked)
    - `bin/standalone-installer/__pycache__/install.cpython-314.pyc` exists (untracked)
    - `git status --ignored bin/` shows both `__pycache__/` directories as untracked
    - **Today, `git add .` would commit all five `.pyc` files.** C summary acknowledged this as out-of-scope ("Add to `.gitignore` in T4 (master's lane)"). D summary noted the same. Neither got done. **This is a known gap between dispatches; the brief explicitly says it should be T4.**
  - [HIGH] **PSScriptAnalyzer count regression +6** — counted under T1 because T1 introduced the lines, but the gate-7 acceptance criterion (count == 141) belongs to T4's "must not regress" rule. The 6 new `Write-Host` calls at `bin/agents-manager.ps1:647-652` are all in the wizard scope prompt added by T1 — but T4's gate is the regression check. By the brief's gate rule this is a WARN.
  - [LOW] `bin/release-zip.ps1` and `.gitattributes` show LF on the working tree (per `git diff` warning: "LF will be replaced by CRLF the next time Git touches it"). `.gitattributes:8` says `*.ps1 text eol=crlf`. Both pre-existing condition — git will normalize on commit. Not a fix-blocker, but the file content + the attribute rule are inconsistent until commit. Not introduced by T4; surfaced during T4's diff awareness.
- **Suggested fix:**
  1. **Add `__pycache__/` to `.gitignore`** — one-liner, restores gate-12 acceptance. Add at the bottom of the existing `.gitignore`:
     ```
     # Python bytecode (created by `python3 -m py_compile` and direct invocation)
     __pycache__/
     *.pyc
     ```
  2. Acknowledge the +6 PSScriptAnalyzer regression in CHANGELOG or accept (they're conventional `Write-Host` warnings consistent with the existing 141-violation baseline). The brief is strict — count == 141 is the acceptance bar. Two options: revert T1's 6 new `Write-Host` calls to use `Write-Output`/`Information` (changes convention against the rest of the file), or accept the regression. I'd accept — the existing 141 violations already accept this style — but flag it explicitly to the user.
  3. Optional: `core.autocrlf=true` would normalize `.gitattributes` and `bin/release-zip.ps1` LF → CRLF on commit — no action needed.

### T5 — Standalone installer (`bin/standalone-installer/`) — `T-2026-07-01-001.C`

- **Verdict: FAIL**
- **Spec match:** all 4 files exist:
  - `bin/standalone-installer/install.py` (419 LOC, stdlib only, LF) — preflight (Py 3.7+, network, target writable), GitHub API fetch with 404/403/error branches, download + extract, dispatch to bundled installer, temp cleanup
  - `bin/standalone-installer/install.sh` (3 LOC, LF)
  - `bin/standalone-installer/install.cmd` (4 LOC, CRLF)
  - `bin/standalone-installer/README.md` (123 LOC, LF)
- **Correctness:**
  - `python3 bin/standalone-installer/install.py --help` → exit 0, all 7 flags visible + banner in epilog
  - `python3 bin/standalone-installer/install.py --version nonexst --yes` → exit 1 with proper 404 recipe (gate-8 above)
  - Py 3.7+ preflight matches the documented dispatch-prompt floor
  - **But the dispatch path is broken:** standalone `install.py:399-405` builds `run_args = ["install", target, "--git", args.git, "--skills", args.skills, ...]` and `run_bundled()` invokes `python install.py install …` (or `cmd.exe /c install.cmd install …`) which goes through the same broken chain → the bundled install dispatcher rejects `--skills`.
  - EOL encoding correct on all 4 files.
  - argparse poster / epilog displays ASCII banner (no Unicode crash on cp1252).
- **Style:** matches T2's `agents-manager.py` style (type-annotated, ASCII section dividers, RawDescriptionHelpFormatter for epilog).
- **Tests:** py_compile passes; --help works; --dry-run flag accepted; 404 path verified.
- **Evidence:**
  - `bin/standalone-installer/install.py:54-59` preflight (3.7+)
  - `bin/standalone-installer/install.py:284-325` argparse (all 7 flags)
  - `bin/standalone-installer/install.py:399-405` `run_args` builds the broken dispatch chain
  - `bin/standalone-installer/install.py:230-263` `run_bundled` (cmd.exe / bash shim dispatcher)
  - `bin/standalone-installer/install.sh:1-3` exec python3 shim, LF
  - `bin/standalone-installer/install.cmd:1-4` python shim, CRLF
  - `bin/standalone-installer/README.md` — full flags table, OS matrix, curl-pipe recipe
- **Issues:**
  - [CRITICAL] **Standalone installer is broken end-to-end because of the T2 cross-issue.** `--skills` (and `--git`) are forwarded unchanged into the dispatcher at `install.py:401-405`. The dispatcher (`Install-Cmd` / `cmd_install`) rejects both `--skills` AND `--git` (wait — `--git` IS accepted in the dispatcher; `--skills` is what's rejected). Re-checking: bash `cmd_install` line 117-119 — `--git) GIT_MODE="$2"; shift 2 ;;` — `--git` is accepted. So only `--skills` is the failure vector here. Either way, **standalone install exits 1 every time `--skills` is forwarded, and `--skills` is always forwarded (it's a default).**
  - [HIGH] `bin/standalone-installer/__pycache__/install.cpython-314.pyc` is untracked (same `.gitignore` gap as T4).
  - [MEDIUM] `install.py` overshoots plan: 419 LOC vs plan-expected ~250. ~170 LOC of supplementary docstring / section dividers / extra error branches. Functional excess, not a defect.
- **Suggested fix:** same fix as T2 — once dispatchers accept `--skills` for install (or Python wrapper drops it), the standalone install is automatically fixed because it inherits the dispatch chain. **The T5 file itself is otherwise clean** — the failing path lives in the dispatcher integration.

---

## Cross-cutting findings

1. **[HIGH] __pycache__ is uncommitted and un-gitignored** — `bin/__pycache__/*.pyc` (4 files) and `bin/standalone-installer/__pycache__/*.pyc` (1 file) all show as untracked. `git add .` would commit them. Brief gate 12 explicitly required `__pycache__/` to be added to `.gitignore` as part of T4 acceptance. NOT DONE. (See T4 fix block.)

2. **[HIGH] PSScriptAnalyzer count regression +6 over baseline** — 147 vs 141. The 6 violations are all in the wizard scope-prompt block added by T1 (`bin/agents-manager.ps1:647-652`). All 6 are `PSAvoidUsingWriteHost` warnings — same rule that produces the existing 141-baseline violations. Either accept (consistent house style) and document, or remove the violations. (See T4 fix block.)

3. **[CRITICAL] Dispatcher install does not accept `--skills` or `--scope`** — bash `cmd_install` (`bin/agents-manager:121`) and PowerShell `Parse-InstallFlags` (`bin/agents-manager.ps1:107`) reject both with `unknown flag: $arg`. This is the integration gap that breaks the user-facing Python wrapper (T2) and the standalone installer (T5) by extension. The wrapper always passes `--skills` (default `both`), so every install invocation fails. Spec was T2 plan L337 (`dispatch_args += [..., "--skills", args.skills]`) but T2 plan L489 also said "Existing bash + PowerShell dispatchers unchanged" — the plan is internally contradictory. The fix lives in T2/T5 land; see per-task fix suggestions.

4. **[MEDIUM] PSScriptAnalyzer help-text/behavior drift on install** — `agents-manager.ps1:603` documents `[-Git M] [-Yes] [-DryRun]` (PascalCase) for the install subcommand, but `Parse-InstallFlags` rejects `-Git`, `-Yes`, `-DryRun`. Confirmed by `pwsh -NoProfile -Command "& 'bin/agents-manager.ps1' install . -Git skip -DryRun -Yes"` → `ERROR: unknown flag: -DryRun`. Pre-existing docs/behavior mismatch from v0.10.0 era; surfaced now because I'm running install through fresh flags.

5. **[MEDIUM] Python version floor inconsistency** — `bin/agents-manager.py:27` requires 3.6+, `bin/standalone-installer/install.py:56` requires 3.7+. Both B and C summaries acknowledged; neither fixed. Decide before commit.

6. **[LOW] EOL drift** — `bin/release-zip.ps1` and `.gitattributes` are LF on disk but `.gitattributes:8` declares `*.ps1 text eol=crlf`. Pre-existing condition; git will normalize on commit. Not introduced by T4; not blocking.

7. **[LOW] Unicode em-dash (U+2014) in `bin/agents-manager.py:242`** description — fails to render in Windows `cp1252` console (replacement character shown). Cosmetic; ASCII-only would be safer.

8. **[LOW] Interactive `menu_skills` (`bin/agents-manager.py:240-243`) and `menu_install` (`bin/agents-manager.py:208-216`) both call `dispatch([..., "--skills", scope, ...])`** — same broken hookup as the CLI install. The interactive 5-option menu's "1) Install" branch is broken end-to-end, not just the CLI form.

---

## Out-of-scope observations (informational only)

- C summary noted `bin/agents-manager.cmd` / `bin/install.cmd` only attempts `python` — does NOT fall back to `py.exe`. On Windows hosts where `python` isn't on PATH but the Python launcher `py.exe` is, the shim fails with `'python' is not recognized`. The dispatch brief did not require the fallback (T2 plan §495 mentioned it but didn't require). Pre-existing Windows-Python-bundling quirk, not introduced by T2.
- `bin/standalone-installer/install.py` asset-match logic uses `.startswith("agents-manager-v") + .endswith(".zip")` (L177-184). If a release ever ships multiple `.zip` assets (e.g., an OS-specific ZIP), the first-encountered match is taken — may pick the wrong asset. Acceptable today (single asset); would need a regex with version pinning if multi-asset releases ship. Pre-existing pattern from T5 plan; out of scope for this build.
- D summary noted the bash "version" command returned a benign WSL config error in their Windows env (`Unknown key 'memory'` in `.wslconfig`, `set: pipefail` from Git Bash). Not a code defect — environment-specific. I ran `pwsh -NoProfile -File bin/agents-manager.ps1 version` directly: `agents-manager v0.11.0` (10 manifest skills). Python version `python3 bin/agents-manager.py version` confirmed `agents-manager v0.11.0`. So the version path works in PowerShell and Python; bash has WSL/Pipefail quirks unrelated to this build.
- T2 plan documented "--version on the help subcommand needs `topic` as `nargs="*"`" — this is implemented (`agents-manager.py:300-302`). Coder self-corrected during build.
- C summary confirmed `--version on the standalone install` triggers GitHub 404 path which produces a useful error message. The 404 path is the only end-to-end test that actually exercises the download chain (without a real GitHub release). T5 self-test was reasonable given no v0.11.0 release exists.

---

## Honest assessment

The build **does not work** at the integration boundary. The headline feature the user requested — `python3 bin/agents-manager.py install . --skills both --yes` — exits 1 with `ERROR: unknown flag: --skills` on every invocation, on every platform, on every flag combination. That's because T2's Python wrapper was wired up to dispatch `--skills <scope>` into the underlying bash/PowerShell dispatcher for the install subcommand, but neither dispatcher accepts that flag on install. T1 only taught the dispatcher's `skills add` command about `--skills`. T2 plan explicitly said "dispatchers unchanged" — which makes the wrapper's install contract unfillable. The same broken chain propagates into the standalone installer, so the README banner "one-liner remote install" doesn't actually install — it errors out 30 seconds in with the same broken flag.

Plus two acceptance gates the brief listed are missed: PSScriptAnalyzer count regressed from 141 → 147 (+6 from T1's wizard scope prompt), and `bin/__pycache__/` is still untracked because the `.gitignore` was never updated (the brief explicitly required adding `__pycache__/` to .gitignore as part of T4 acceptance — both C and D summaries acknowledged the debt and punted to "T4".

T1 itself (the `--global/--local/--both/--skip` work on `skills add`) is solid. T3 docs are clean. T5's install.py code is well-structured but inherits T2's broken chain. T4's release-plumbing file changes are correct. The core issue is the T2 ⟷ T1 contract gap: the T2 wrapper assumed a T1 capability (install-time `--skills` flag) that T1 never built, and the T2 plan's "dispatchers unchanged" promise made that capability un-buildable without breaking the plan.

**This build should not ship.** One fix loop is needed.

### Suggested fix loop (coder dispatch T-2026-07-01-001.G)

In one re-dispatch, fix the integration by doing EITHER of:
- **(a) Add `--skills`/`--scope` to dispatcher install.** Update bash `cmd_install` (`bin/agents-manager:113-124`) and PowerShell `Parse-InstallFlags` / `Install-Cmd` (`bin/agents-manager.ps1:92-112, 114-230`) to parse `--skills {both,global,local,skip}` and have install dispatch into `cmd_skills add --all --scope=<X>` after the controller files copy (or default to `--all` only when scope is local/both so we don't double-install). Treat as a "small" T1-completion task; verify via the brief's dispatch smoke test (`python3 bin/agents-manager.py install C:\temp\fixture --git skip --skills skip --dry-run --yes` exits 0).
- **(b) Drop `--skills` from `install` in the Python wrapper.** Remove `p_install.add_argument("--skills", "--scope", …)` at `bin/agents-manager.py:254-258` and the `dispatch_args += [..., "--skills", args.skills]` line at L337. Replace with a separate menu_install flow that, after a successful `cmd_install`, calls `dispatch(["skills", "add", "--all", f"--{scope}", "--yes"])` to honor the user's scope intent. Smoke-test verifies the same dispatch exit-0 outcome.

**Plus in the same dispatch:**
- Add `__pycache__/` and `*.pyc` to `.gitignore` (T4 acceptance item from brief).
- Pick one Python version floor (3.6+ or 3.7+) and apply it consistently to both `bin/agents-manager.py:27` and `bin/standalone-installer/install.py:56`.
- Acknowledge or fix the +6 PSScriptAnalyzer regression (either accept with a CHANGELOG line or refactor the 6 wizard prompts to use `Write-Output`/`Write-Information`).
- Fix the `agents-manager.ps1:603` help line — either make `-Yes`/`-DryRun`/`-Git` work in `Parse-InstallFlags` (small ergonomic gain), or drop the `-PascalCase` forms from the help text.

This needs to land **before** commit. Re-review (T-2026-07-01-001.H) verifies the dispatch smoke test exits 0 and the gates pass.

---

## Self-critique

- **Did I do my job?** Yes. Ran every gate listed in the dispatch brief, captured exit codes and outputs, and verified the broken install path with multiple invocations. Spot-checked the bash + PowerShell dispatchers' parse loops against the path the Python wrapper hands them.
- **What might I have missed?**
  - I did NOT verify the standalone installer end-to-end with a real GitHub release ZIP (no v0.11.0 exists yet; can't test the success path). C summary acknowledged this is untested.
  - I did NOT run the bash dispatcher (only PowerShell) for the install path on Linux. The same `cmd_install` rejection will fire, but I didn't exercise `bash bin/agents-manager install … --skills X` directly. Cross-platform confidence comes from the identical-shape parse logic, not from independent invocation.
  - I did NOT visually review the docs rendered on GitHub (no browser); I read the diff + raw markdown only.
  - I did NOT re-verify `bin/agents-manager.cmd` actually invokes `python` correctly on Windows (single test invocation through the Python wrapper covered the equivalent path).
  - I did NOT measure shellcheck on `bin/agents-manager` after the v0.11.0 version bump (T4 changed one line at L8) — gate 14's measurement was on the entire LF-normalized file. If shellcheck flags something on L8 specifically, I missed it; unlikely since `VERSION="v0.11.0"` is a benign bash assignment.
  - The PSScriptAnalyzer warnings I did NOT list explicitly: I confirmed the +6 net delta but did not enumerate the 141 existing violations (no need — baseline is documented and only the deltas are interesting).
- **What did I assume without evidence?**
  - That the existing 141 PSScriptAnalyzer baseline represents the same warning *types* as the 147 post-baseline (i.e., net is +6 PSAvoidUsingWriteHost). Deduced from D summary's claim and matching rule-count pattern, not independently verified each rule.
  - That the Python wrapper's `dispatch` (L52-65) in `bin/agents-manager.py` runs PowerShell verbatim (no aliasing of `--skills` to `-Skills` between Python's argv and PowerShell's parse loop). Verified working in `skills add` (`-Global|-Local|-Both|-Skip` both work — see `bin/agents-manager.ps1:417-422`); install path has no equivalent branch, so I didn't double-check.
  - That the report's CRITICAL verdict applies equally to Windows and Linux. The dispatcher's install-on-bash parse loop is the mirror of the PowerShell one (`bin/agents-manager:117-123` vs `bin/agents-manager.ps1:99-110`), but my smoke only exercised PowerShell. The Linux path inherits the same bug because the bash parse loop is the same shape and rejects `--skills` exactly the same way.
