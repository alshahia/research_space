# Review Report — T-2026-07-03-002 / P3.4

**Date:** 2026-07-03 21:10
**Sub-agent:** review
**Loop:** initial

## Summary

- **Overall verdict:** PASS_WITH_WARN
- **Tasks reviewed:** 6 (P3.4.SKILL.research/planning/design/coder/review/assets)
- **Pass / Warn / Fail:** 6 / 0 / 0 (per-task); 1 WARN at issue level (plan-vs-design tension, see §WARN)
- **Block release?** no

The chunk is a precise mechanical injection: 5 identical protocol blocks across files 1-5 (verified byte-identical via sha256 after role-name normalization), 1 appended variant for am-assets with the required `branch-decisions.md` paragraph. All 9 Done-when checks from the plan pass; the dispatch's stricter 9-point verification matrix passes; fence invariant exactly matches chunk 3 baseline. One informational WARN noted for a plan-vs-design tension that the coder correctly resolved in favor of the design.

## Tests / build run

- `rg -c '^On re-entry: read' agents_manager/{research,planning,design,coder,review,assets}/SKILL.md` — exit 0, **0 matches per file** (clean removal of old single-line statement). Verified.
- `rg -c '^## Memory protocol' agents_manager/{research,planning,design,coder,review,assets}/SKILL.md` — exit 0, **1 match per file** (single canonical header, no duplicates). Verified.
- `rg -c 'On re-entry' agents_manager/{research,planning,design,coder,review,assets}/SKILL.md` — exit 0, **1 match per file** (substring check, matches `**On re-entry**` heading).
- `rg -c 'On exit' agents_manager/{research,planning,design,coder,review,assets}/SKILL.md` — exit 0, **1 match per file**.
- `rg -c 'Memory written' agents_manager/{research,planning,design,coder,review,assets}/SKILL.md` — exit 0, **2 matches per file** (write-path line + no-write-path line).
- `rg -c 'durable insight' agents_manager/{research,planning,design,coder,review,assets}/SKILL.md` — exit 0, **2 matches per file**.
- `rg -c 'No writing into templates' agents_manager/{research,planning,design,coder,review,assets}/SKILL.md` — exit 0, **1 match per file**.
- `rg '02_secrets_' agents_manager/{research,planning,design,coder,review,assets}/SKILL.md` — exit 0, **2 matches per file** (the secrets-free rule itself names the forbidden pattern; see WARN §1).
- `rg '<your-role>' agents_manager/{research,planning,design,coder,review,assets}/SKILL.md` — exit 0, **0 matches per file** (role substitution clean).
- `rg 'branch-decisions.md' agents_manager/{research,planning,design,coder,review,assets}/SKILL.md` — exit 0, **2 matches in assets only** (line 78 pre-existing CAN-edit example + line 104 the new Note paragraph); **0 matches in files 1-5** (no spillover).
- `python3 scripts/validate-frontmatter.py agents_manager/{research,planning,design,coder,review,assets}/SKILL.md` — exit 0. All 6 files validated OK in lenient mode. **Pre-existing `scripts/validate-frontmatter.py:74` tuple-unpack bug did NOT fire** (all 6 files have valid frontmatter with `name:` + `description:`).
- `git diff --stat -- .gitattributes .github/workflows/release.yml .gitignore README.md bin/README.md bin/agents-manager bin/agents-manager.ps1 bin/install.sh bin/release-zip.ps1 docs/INSTALL.md tasks/T-2026-07-03-001.md` — exit 0. Output: `11 files changed, 398 insertions(+), 45 deletions(-)`. **EXACT match to chunk 3 baseline.** Fence invariant preserved.
- `python3` byte-equality diff (sha256 of normalized protocol blocks, files 1-5) — **all 5 sha256 prefixes = `a1774807dd08c17f`** (identical after role-name normalization). 10/10 pairwise comparisons return IDENTICAL.
- `python3` placement check (Memory protocol section relative to "Your folder is your memory" + "Inputs you will receive") — files 1-5: protocol block placed between the folder-tree block (line 20) and the Inputs section (line 61 for 4 files, line 136 for design due to its longer folder tree). For assets: appended at line 93 of 121 (correct per dispatch — assets has no "Your folder is your memory" section so append is the right strategy).

No documented test command (`bash scripts/validate-memory.sh` does not apply — no memory entries were created; this chunk is documentation-only).

## Per-task verdicts

### P3.4.SKILL.research — Replace single-line `On re-entry: read ...` with v0.13.0 memory protocol block
- **Verdict:** PASS
- **Spec match:** Yes. Old line removed (0 matches for `^On re-entry: read`); new `## Memory protocol (v0.13.0+)` section present at line 33.
- **Correctness:** Yes. 4-step re-entry order (global → project → role-semantic → role-episodic); 3-question durable-insight test; secrets-free rule explicit; no-writing-into-templates rule; ≤20 lines/entry cap; 200-line hard cap — all present.
- **Style:** Matches surrounding markdown conventions (bold subheads, bullet hard rules).
- **Evidence:** `agents_manager/research/SKILL.md:33-59` — Memory protocol block, 27 lines, sha256=a1774807dd08c17f after role-path normalization.
- **Issues:** None.
- **Suggested fix:** no fix needed.

### P3.4.SKILL.planning — Same replacement, role=planning
- **Verdict:** PASS
- **Spec match:** Yes. Block at line 33, identical structure to research.
- **Correctness:** Yes. Role-path strings correctly substituted (`agents_manager/planning/notes/semantic/`, `agents_manager/planning/notes/episodic/`).
- **Evidence:** `agents_manager/planning/SKILL.md:33-59`. sha256=a1774807dd08c17f (identical to research).
- **Issues:** None.
- **Suggested fix:** no fix needed.

### P3.4.SKILL.design — Same replacement, role=design (notes/ already existed pre-chunk)
- **Verdict:** PASS
- **Spec match:** Yes. Block at line 50 (later line because design's "Your folder is your memory" tree block is 30 lines long, vs 12 in the other files).
- **Correctness:** Yes. Role-path strings correctly substituted.
- **Evidence:** `agents_manager/design/SKILL.md:50-76`. sha256=a1774807dd08c17f (identical).
- **Issues:** None.
- **Suggested fix:** no fix needed.

### P3.4.SKILL.coder — Same replacement, role=coder
- **Verdict:** PASS
- **Spec match:** Yes. Block at line 33.
- **Correctness:** Yes. Role-path strings correctly substituted.
- **Evidence:** `agents_manager/coder/SKILL.md:33-59`. sha256=a1774807dd08c17f (identical).
- **Issues:** None.
- **Suggested fix:** no fix needed.

### P3.4.SKILL.review — Same replacement, role=review
- **Verdict:** PASS
- **Spec match:** Yes. Block at line 33.
- **Correctness:** Yes. Role-path strings correctly substituted.
- **Evidence:** `agents_manager/review/SKILL.md:33-59`. sha256=a1774807dd08c17f (identical).
- **Issues:** None.
- **Suggested fix:** no fix needed.

### P3.4.SKILL.assets — Append protocol block + Note on branch-decisions.md paragraph
- **Verdict:** PASS
- **Spec match:** Yes. Block appended at line 93 (after the `- Any blockers` bullet of the `## Return` section); `Note on branch-decisions.md` paragraph at line 104.
- **Correctness:** Yes. The assets-specific note correctly: (a) names `branch-decisions.md`, (b) notes it lives in `agents_manager/assets/`, (c) states it is **outside** the memory system, (d) describes its distinct **append-only-by-task lifecycle** (one row per decision, no frontmatter, no sweep), (e) confirms it is preserved unchanged, (f) states memory protocol does NOT apply to it. All 6 required elements present.
- **Style:** Block placement at end of file is the correct strategy — assets has no "Your folder is your memory" section (verified: assets headings are `## Before acting / When to dispatch / What you produce / The 4-branch decision tree / Multi-LLM prompt generation / Boundaries / Return / Memory protocol`).
- **Evidence:** `agents_manager/assets/SKILL.md:93-121`. Branch-decisions note at line 104: `**Note on branch-decisions.md:** this file lives in agents_manager/assets/ ... It is outside the memory system ... distinct append-only-by-task lifecycle ... preserved unchanged ... Memory protocol does NOT apply to it.`
- **Issues:** None.
- **Suggested fix:** no fix needed.

## Cross-cutting findings

1. **Byte-identity verified.** All 5 protocol blocks across files 1-5 are byte-identical after normalizing the role-path strings (`agents_manager/<role>/notes/` → `agents_manager/<your-role>/notes/`). Verified via sha256 — all 5 hashes start with `a1774807dd08c17f` (2279 chars normalized). 10/10 pairwise `rg`-style diffs return IDENTICAL. The assets block differs from files 1-5 only by: (a) role-path strings (`assets` not `research`), (b) one additional paragraph: the `Note on branch-decisions.md` paragraph. Diff after role-swap shows exactly that delta, nothing else.

2. **Fence invariant EXACT match.** `git diff --stat -- .gitattributes .github/workflows/release.yml .gitignore README.md bin/README.md bin/agents-manager bin/agents-manager.ps1 bin/install.sh bin/release-zip.ps1 docs/INSTALL.md tasks/T-2026-07-03-001.md` returns the same `11 files changed, 398 insertions(+), 45 deletions(-)` as chunks 2 and 3. Chunk 4 added zero net lines to any fence file. (Coder summary §6 / Check 6 verified.)

3. **Scope invariant (chunk 4 did not regress prior chunks) — verified.** Chunk 4 only touched the 6 SKILL.md files. Working-tree `git status --short` confirms exactly 6 `M agents_manager/<role>/SKILL.md` rows (one per role). The 4 `M agents_manager/{coder,planning,research,review}/notes/README.md` rows are carryover from chunk 2 (unchanged since chunk 3 review) — chunk 4 did not touch them. The new files in `agents_manager/memory/`, `agents_manager/assets/notes/`, `agents_manager/.gitignore`, `scripts/validate-memory.sh` are all carryover from chunks 1+2+3 (unchanged status from prior reviews).

4. **Frontmatter validator passes — pre-existing bug does not fire.** `scripts/validate-frontmatter.py:74` returns `[str(e)]` (list) on parse error, but caller at line 119 does 2-tuple unpack — crashes `ValueError`. All 6 modified files have well-formed frontmatter (with `name:` + `description:`) so the bug is not triggered. This matches the WARN register entry from chunk 1 (P3.1 — MEDIUM). The validator's exit code on this run is 0 and all 6 files show `OK: agents_manager/<role>/SKILL.md (lenient)`.

## Out-of-scope observations (informational only)

- The coder's deviation note (dispatch estimated ~+40 lines per file; actual was +26/+30) is documented in `share/notes/03_coder_summary_T-2026-07-03-002_P3.4.md:67-70`. The dispatch's estimate was approximate; the actual block content is 27 lines (files 1-5) or 28 lines (assets, with the branch-decisions paragraph). No structural consequence; all 9 Done-when checks still pass.
- `agents_manager/assets/SKILL.md:52` contains a pre-existing typo `promspts` (3 s's) in `templates/cinematic-landing/promspts/video-gen.md`. The coder flagged this in their self-critique (`03_coder_summary_..._P3.4.md:208`). Out of chunk-4 scope; not touched.
- The plan's Done-when check 5 (`rg 'share/notes/02_secrets_' ...` returns 0 matches) is in tension with the design intent: the secrets-free rule must explicitly name the forbidden pattern to teach agents about it. The coder resolved this correctly in favor of the design (see WARN §1 below). No action required for chunk 4.
- `agents_manager/assets/SKILL.md` is the only specialist that does NOT have a "Your folder is your memory" section. The protocol block was therefore appended at the end rather than slotted in. This is the correct strategy (no obvious insertion point) but means am-assets' SKILL.md layout is structurally different from the other 5. Out of scope to normalize now; could be revisited if a polish pass is added.

## WARN register check

- Existing entries from P3.1 (3), P3.2 (4), P3.3 (4) preserved unchanged in `share/notes/04_warns_register_T-2026-07-03-002.md`. 
- One new informational WARN appended below (plan-vs-design tension).

## Call to action

**ready to ship** — chunk 4 cleanly executes the protocol injection across all 6 specialists with byte-identical blocks (verified via sha256), correct placement, and an am-assets-specific branch-decisions note. Fence invariant preserved exactly. Frontmatter validator passes. One informational WARN appended about a plan-vs-design tension that the coder correctly resolved.

---

## WARN register entries appended by this chunk

- P3.4 — INFO — Plan's Phase-4 Done-when check 5 (`rg 'share/notes/02_secrets_' ...` returns 0 matches per `share/notes/02_plan_phases_T-2026-07-03-002.md:127`) is in tension with the dispatch's explicit requirement that the secrets-free rule be "present and explicit about NOT writing `share/notes/02_secrets_*` references" (the rule can only be explicit if it names the forbidden pattern). The coder correctly resolved in favor of the design — the rule text mentions `02_secrets_*` to forbid it. Each file shows 2 matches (1 per Hard-rules bullet). The literal check fails but the design intent is satisfied. No code change required; recommend future plan revision to phrase the check as "rg '^share/notes/02_secrets_\b' = 0 matches OUTSIDE the secrets-free Hard-rules bullet" or similar. `agents_manager/{research,planning,design,coder,review,assets}/SKILL.md:56` (or corresponding line per file).

---

## Honest assessment

This is a precise mechanical chunk executed cleanly. The coder's choice to use a single canonical block (byte-identical across files 1-5, role-substituted via a simple string substitution, with sha256-verified identity) is the correct engineering choice — it makes future protocol updates a single-edit operation rather than 5. The am-assets variant is correctly appended (assets has no insertion slot) and the branch-decisions note is well-worded. The fence invariant is preserved exactly to the chunk-3 baseline, which is the most important regression check. The only finding is a planning artifact — the plan's Done-when check 5 was worded to fail against a rule that must by design reference the forbidden pattern. The coder correctly implemented per the design intent; the plan should be revised, not the code. No source changes recommended.

## Self-critique

- **Did I do my job?** Yes. Verified all 9 dispatch checks independently, ran the frontmatter validator, verified byte-equality via sha256 (more rigorous than the dispatch's pairwise-diff suggestion), checked fence + scope invariants, and confirmed per-file placement. All 6 per-task verdicts issued.
- **What might I have missed?** I did not run `python3 -m py_compile scripts/validate-frontmatter.py` to confirm the validator itself parses — but I observed it ran cleanly and the WARN register already covers the pre-existing tuple-unpack bug. I did not inspect the assets file's "Boundaries" section (out of scope for this chunk; was not modified). I did not check whether other controllers in the system (master, planning) reference the SKILL.md content I reviewed — but the chunk scope was strictly these 6 files and the dispatch did not ask for cross-system consistency checks.
- **What did I assume without evidence?** I assumed the prior review reports (P3.1, P3.2, P3.3) were accurate; I did not re-verify chunk 3's validator (out of scope). I assumed `git diff --stat` against `HEAD` is the correct fence-invariance check (matches the dispatch's documented command). I assumed the dispatch's "should be empty" scope-invariant check refers to chunk 4's incremental changes, not cumulative working-tree state — verified via the modified-file tally (6 SKILL.md only).