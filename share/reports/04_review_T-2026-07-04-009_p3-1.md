# Review Report — T-2026-07-04-009 / Phase 3.1

**Date:** 2026-07-04
**Sub-agent:** review (am-review)
**Loop:** initial
**Scope:** sub-phase 3.1 — `templates/EXTRACTION.md` (NEW) · `agents_manager/extract/SKILL.md` (NEW) · `agents_manager/extract/rules.md` (NEW) · `agents_manager/memory/.extract-config.yaml.example` (NEW) · `templates/CONTRIBUTING.md` (MOD, +22L) · `agents_manager/memory/.gitignore` (MOD, +2L, master's MEDIUM fix) = **6 files**
**Master fix:** the `.gitignore` MEDIUM concern was flagged in 3.1's coder summary; master applied the fix in-scope for this review.

## Summary
- **Overall verdict:** PASS_WITH_WARN
- **Tasks reviewed:** P3T1 (5 files) + P3T2 (4 files, of which 1 is master fix) — 2 task rows
- **Pass / Warn / Fail:** 6 / 2 (LOW) / 0
- **Block release?** no — LOW WARNs are cosmetic cross-references + documented deviation

## Tests / build run

- `python scripts/validate-frontmatter.py agents_manager/extract/SKILL.md agents_manager/memory/README.md agents_manager/SKILL.md` → **exit 0**, all 3 files OK in lenient mode. Frontmatter validator passes the 3 frontmatter-bearing files (CONTRIBUTING.md has no frontmatter; EXTRACTION.md has no frontmatter; rules.md has no frontmatter; .extract-config.yaml.example has YAML not frontmatter).
- `python -c "import yaml; d=yaml.safe_load(open('agents_manager/memory/.extract-config.yaml.example').read())"` → **exit 0**, returns `{'extract_enabled': False}` — valid YAML, schema confirmed.
- `python -c "import yaml,re; t=open('agents_manager/extract/SKILL.md').read(); fm=re.split(r'\A---\n',t,maxsplit=1)[1].split('\n---\n',1)[0]; d=yaml.safe_load(fm); assert 'description' in d and 'paths' in d"` → **exit 0**; frontmatter keys = `['name', 'description', 'paths']`; `paths` = `['templates/**', 'agents_manager/memory/**', 'share/templates/drafts/**']`.
- `git diff --name-only HEAD` → 4 files modified: `agents_manager/SKILL.md`, `agents_manager/memory/.gitignore`, `agents_manager/memory/README.md`, `templates/CONTRIBUTING.md`. **No forbidden-zone file in diff.**
- `git ls-files --others --exclude-standard` → 4 NEW files: `agents_manager/extract/SKILL.md`, `agents_manager/extract/rules.md`, `agents_manager/memory/.extract-config.yaml.example`, `templates/EXTRACTION.md`. **All expected per scope A.**
- `git diff HEAD opencode.jsonc CLAUDE.md templates/AUTHORING.md scripts/validate-template.sh` → **fatal: scripts/validate-template.sh: no such path in the working tree** (correct — deferred to v0.15.x per plan §Future work); `opencode.jsonc`/`CLAUDE.md`/`templates/AUTHORING.md` unchanged. **0 forbidden-zone modifications.**
- `wc -l` (via Python): `templates/EXTRACTION.md` = **644L** (plan ≥500), `agents_manager/extract/SKILL.md` = **160L** (plan 200-250), `agents_manager/extract/rules.md` = **93L** (plan 100-150), `.extract-config.yaml.example` = **13L** (plan 5-15), `templates/CONTRIBUTING.md` = 123L (was 101, +22 matches dispatch claim). All within or exceeding budgets.
- Forbidden-zone guard: regex filter on `git diff --name-only HEAD` against `^(opencode\.jsonc|CLAUDE\.md|templates/AUTHORING\.md|scripts/validate-template\.sh|agents_manager/(research|planning|design|coder|review)/(SKILL|rules)\.md)$` → **0 matches**.

## Per-task verdicts

### P3T1 — `templates/EXTRACTION.md` (NEW, 644L)

- **Verdict:** PASS
- **Spec match:** rulebook mirrors `templates/AUTHORING.md` style; contains all required sections per Phase 3.1 Done-when #1.
- **Correctness:** TL;DR 8 rules at `EXTRACTION.md:40-68`; Rule details at `:119-198`; "What extraction produces" at `:72-116`; pre-flight checklist (PF-1..PF-7) at `:201-322` with explicit "Pre-flight FAIL-condition coverage map" table at `:308-319`; 9-step recipe at `:326-392` parallel to `AUTHORING.md:248-290`; worked example at `:396-454`; "what the agent can't decide" at `:457-486`; refusals/WARN table at `:490-516`; companion-files cross-refs at `:556-573`; versioning block at `:577-592`; audit-log template at `:596-643`.
- **Style:** matches `AUTHORING.md` voice (numbered rules, "Bad/Good" framing in Rule details, refusal/WARN summary table).
- **Tests:** N/A — docs/skill authoring. The PF-* gates encode the future-test commands.
- **Evidence:** `templates/EXTRACTION.md:40-68` (TL;DR), `:201-322` (pre-flight), `:308-319` (FAIL-condition coverage map — all F1..F8 enumerated with PF + R mapping).
- **Issues:**
  - [LOW] `EXTRACTION.md:386-387` says the cinematic-landing "version path was 0.12.0 → 0.13.0 → v0.14.0" — minor factual drift from git log which shows `3a88932 feat(templates): v0.14.0` and `700f3fb docs(templates): v0.14.1`. The exact version path is illustrative not normative; no spec violation.
- **Suggested fix:** no fix needed.

### P3T2a — `agents_manager/extract/SKILL.md` (NEW, 160L)

- **Verdict:** PASS_WITH_WARN
- **Spec match:** YAML frontmatter present with `name:`, `description:`, `paths:` keys (verified via Python YAML parse). Body describes the 8-step procedure. Plan Done-when #2: "YAML frontmatter parses successfully; description contains trigger phrases; paths lists templates/**, agents_manager/memory/**, share/templates/drafts/**" — all confirmed.
- **Correctness:** opt-in gating clause at `:60-67` mirrors the master's `.extract-config.yaml` check verbatim; 8-step procedure at `:69-122` parallel to EXTRACTION.md 9-step recipe; "what you cannot do" at `:130-139` enumerates the lane boundaries (no source edit, no controller memory in `templates/<slug>/memory/`, no auto-`global/`, no overwrite, no `opencode.jsonc`/`CLAUDE.md`/other-specialist edits).
- **Style:** matches existing `agents_manager/research/SKILL.md` shape (goal/backstory/ground-truth/procedure). The "Non-standard frontmatter note" callout at `:12-20` honestly documents the `paths:` extension as project-specific.
- **Tests:** N/A — skill authoring.
- **Evidence:** `agents_manager/extract/SKILL.md:1-8` (frontmatter), `:12-20` (deviation callout), `:69-122` (8-step procedure), `:130-139` (lane boundaries).
- **Issues:**
  - [LOW] step 2 at `:82-86` enumerates only PF-1..PF-5 inline; PF-6 (placeholder/trigger hygiene, F1+F3+F4+F7) and PF-7 (memory schema) are reflected in subsequent steps 4-5 but not labeled with their PF-* codes in the step-2 list. Substantive coverage is intact (F1 placeholder grep, F3/F4 `USE THIS WHEN` H1, F7 INDEX trigger phrases all cited in step 5 at `:96-106`; PF-7 memory schema in step 4 at `:96-100` and step 6 at `:110`); the gap is the label consistency only.
- **Suggested fix:** either (a) re-label step 2 to "PF-1..PF-5 plus the gates PF-6 and PF-7 describe below" with one-line pointer to subsequent steps, or (b) defer to v0.15.x as a cosmetic polish. Not a blocker.

### P3T2b — `agents_manager/extract/rules.md` (NEW, 93L)

- **Verdict:** PASS
- **Spec match:** 8 numbered hard rules R1-R8 each matching synthesis v2 spec verbatim (R1 no `templates/<name>/memory/` writes; R2 no secrets; R3 LICENSE with attribution; R4 invoke `tests/verify.sh`; R5 run `scripts/validate-memory.sh`; R6 never auto-default to `global/`; R7 never overwrite, suffix on collision; R8 emit audit log at `share/notes/03_extracted_<task-id>.md`).
- **Correctness:** R2 denylist at `:25-28` matches synthesis v2 verbatim (`.env*`, `*.pem`, `*.key`, `id_rsa*`, `02_secrets_*` + content regexes); R3 whitelist at `:40-41` matches synthesis (MIT, Apache-2.0, BSD-2-Clause, BSD-3-Clause, ISC, CC0, CC-BY); R7 suffix format `<slug>-v2`, `<slug>-v3` at `:82`; R8 audit path at `:84`. Each rule (R2/R3/R4/R5/R6/R7/R8) cites the FAIL condition it gates; R1 has no FAIL but explicitly justifies the lane split.
- **Style:** matches the standing-rules pattern used by other specialists (`agents_manager/coder/rules.md`, etc.).
- **Tests:** N/A.
- **Evidence:** `agents_manager/extract/rules.md:12` (R1), `:23` (R2 → F6), `:36` (R3 → F5), `:47` (R4 → F1/F2/F3/F4/F7), `:56` (R5), `:66` (R6), `:75` (R7), `:84` (R8 → F8).
- **Issues:** none.
- **Suggested fix:** no fix needed.

### P3T2c — `agents_manager/memory/.extract-config.yaml.example` (NEW, 13L)

- **Verdict:** PASS
- **Spec match:** valid YAML, `extract_enabled: bool` field present.
- **Correctness:** field set to `false` (the safe default; matches synthesis v2 "default false" pattern).
- **Style:** comment block at `:1-11` documents the copy/gitignore/Phase-5-trigger contract.
- **Tests:** Python YAML parser exits 0, returns `{'extract_enabled': False}`.
- **Evidence:** `agents_manager/memory/.extract-config.yaml.example:13` (only field).
- **Issues:** none.
- **Suggested fix:** no fix needed.

### P3T2d — `templates/CONTRIBUTING.md` (MOD, +22L)

- **Verdict:** PASS
- **Spec match:** new `### I want to extract a finished project into a template` subsection inserted between existing "I want to add a new template" (line 10) and "I want to fix a bug in an existing template" (line 58); references `EXTRACTION.md` and `agents_manager/extract/SKILL.md` + `agents_manager/extract/rules.md`; contrasts extraction (additive) vs manual authoring (full write).
- **Correctness:** 3-step flow covers (1) read EXTRACTION.md, (2) load skill, (3) contrast with manual authoring; routes to opt-in config + Phase 5 menu.
- **Style:** matches the existing "I want to…" subsection pattern.
- **Tests:** `rg -n 'extract' templates/CONTRIBUTING.md` returns 9 matches (≥2 gate). Plan Done-when #5: "extract references in CONTRIBUTING.md returns ≥2 matches" — **PASS** (9 matches).
- **Evidence:** `templates/CONTRIBUTING.md:36-56` (new subsection), `:58` (existing "fix a bug" preserved).
- **Issues:** none.
- **Suggested fix:** no fix needed.

### P3T2e — `agents_manager/memory/.gitignore` (MOD, +2L, master's MEDIUM fix)

- **Verdict:** PASS
- **Spec match:** master added `**/.extract-config.yaml` ignore + `!.extract-config.yaml.example` exception to address coder's 3.1 MEDIUM concern (".extract-config.yaml is not gitignored — out-of-scope for 3.1").
- **Correctness:** the ignore pattern `**/.extract-config.yaml` catches the operator-created real config; the `!.extract-config.yaml.example` exception preserves the checked-in example. Together they match the pattern of `**/*.md` + `!README.md` (the pre-existing ignore/exception pair).
- **Tests:** manual verification: the pattern would ignore any `.extract-config.yaml` created from the example while keeping the example committed. (Cannot run actual git-ignore behavior in this review.)
- **Evidence:** `agents_manager/memory/.gitignore:2` (new ignore), `:5` (new exception).
- **Issues:** none.
- **Suggested fix:** no fix needed.

## Cross-cutting findings

- **F1-F8 forward-looking encoding (primary gate).** Every am-review FAIL condition is encoded at TWO layers — `templates/EXTRACTION.md` pre-flight gates (PF-1..PF-7) and `agents_manager/extract/rules.md` (R1-R8). Coverage map at `EXTRACTION.md:308-319`:
  - F1 → PF-6 / R4 ✓
  - F2 → PF-5 / R4 ✓
  - F3 → PF-6 / R4 ✓
  - F4 → PF-6 / R4 ✓
  - F5 → PF-1 / R3 ✓
  - F6 → PF-2 / R2 ✓
  - F7 → PF-6 / R4 ✓
  - F8 → PF-4 / R8 ✓
  All 8 conditions have a forward-looking PASS via observable encoding in the build. No FAIL.
- **Forbidden-zone preservation.** `git diff --name-only HEAD` shows 4 modified files; none match `opencode.jsonc`, `CLAUDE.md`, `templates/AUTHORING.md`, `scripts/validate-template.sh`, or any `agents_manager/{research,planning,design,coder,review}/SKILL.md|rules.md`. No soft-wall erosion. ✓
- **Cross-references resolve.** Every file path cited from new files exists at that path (verified via `os.path.exists` for all 8 build files; `EXTRACTION.md` cited from `agents_manager/SKILL.md:413` exists; `agents_manager/extract/SKILL.md` cited from `agents_manager/memory/README.md:63` and `templates/CONTRIBUTING.md:46` exists; `agents_manager/extract/rules.md` cited from `agents_manager/extract/SKILL.md:52` and `templates/EXTRACTION.md:569` exists).
- **EOL preservation (BONUS check).** Em-dash U+2014 in the master SKILL.md Phase 5b options 1-4 is byte-preserved vs HEAD (Python byte comparison at index 23 = 0x2014 in both old and new). The visible "—" → "-" diff in earlier terminal output was a display artifact.
- **Coder-flagged CI gap (LOW, acknowledged).** `.github/workflows/ci.yml:49-56` does not list the new `agents_manager/extract/SKILL.md`. Per coder summary, this is a v0.15.x follow-up; not a blocker for v0.15.0.

## Out-of-scope observations (informational only)

- The retroactive cinematic-landing worked-example trace is deferred per `share/notes/02_plan_phases_T-2026-07-04-009.md` "Future work" #4. The rulebook references the exemplar inline (`EXTRACTION.md:396-454`) rather than producing a full reverse-trace. Acceptable per plan.
- `scripts/validate-template.sh` does NOT exist in the working tree — confirmed via `git diff HEAD scripts/validate-template.sh` → fatal "no such path". Correct per plan §Future work; not a Phase 3 deliverable.

## Honest assessment

This is a solid, well-structured build. The 4 new files form a coherent capability: the rulebook (`EXTRACTION.md`) is the ground truth, the skill (`extract/SKILL.md`) is the loadable procedure, the rules (`extract/rules.md`) are the enforceable contract, and the config flag (`.extract-config.yaml.example`) is the opt-in gate. Every one of the 8 am-review FAIL conditions F1-F8 is encoded at both layers with clear citations; the FAIL-condition coverage map table at `EXTRACTION.md:308-319` is the kind of auditable, single-source-of-truth artifact reviewers want to see. The forbidden-zone preservation is clean — no soft-wall erosion, no controller-fence edits. The two LOW WARNs are real but cosmetic (PF-label consistency in SKILL.md step 2; non-standard `paths:` documented inline). Master-applied `.gitignore` fix addresses a real risk that the coder correctly surfaced as out-of-scope. Ready to ship.

## Self-critique

- **Did I do my job?** yes — read all 6 files in scope + 1 modified file (`agents_manager/SKILL.md`); ran Python YAML parse on config example; ran frontmatter validator on the 3 frontmatter-bearing files; ran `git diff --name-only HEAD` + regex filter against forbidden-zone patterns; verified cross-references resolve; confirmed em-dash U+2014 byte-preservation vs HEAD.
- **What might I have missed?** I did not actually execute a gitignore pattern test (no `mkdir .extract-config.yaml` + `git check-ignore`) — relied on visual inspection of the regex. The pattern is straightforward (`**/.extract-config.yaml` + `!.extract-config.yaml.example`); matching the pre-existing `**/*.md` + `!README.md` pattern, so HIGH confidence.
- **What did I assume without evidence?** That the cinematic-landing version path "0.12.0 → 0.13.0 → v0.14.0" in `EXTRACTION.md:386-387` is illustrative only — git log shows `3a88932 feat(templates): v0.14.0` + `700f3fb docs(templates): v0.14.1`, so the exact version path is slightly inaccurate. Not normative; no spec violation. Filed as LOW observation only.

## Status signal

**DONE_WITH_CONCERNS** — sub-phase 3.1 builds clean. All 6 files at expected paths with expected content. F1-F8 forward-looking encoding complete. Forbidden-zone preservation confirmed. 2 LOW WARNs (cosmetic cross-reference + documented non-standard frontmatter) are auto-acceptable; master consolidates in `share/notes/04_warns_register_T-2026-07-04-009.md`.