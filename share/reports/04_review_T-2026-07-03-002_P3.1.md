# Review Report — T-2026-07-03-002 / Phase 3 Build chunk 1 (Memory scaffold)

**Date:** 2026-07-03
**Sub-agent:** review
**Loop:** re-review 1 (after fix-loop 1 resolved Known Issue #1 from initial loop)

## Summary

- **Overall verdict:** PASS_WITH_WARN
- **Tasks reviewed:** 4
- **Pass / Warn / Fail:** 3 / 1 / 0
- **Block release?** no (warns are tooling scope, not implementation drift)

## Tests / build run

1. `Get-Content agents_manager/memory/.gitignore | Measure-Object -Line` → **3** (expected 3 — after fix-loop 1 added `!README.md`). **PASS.**
2. `Get-Content agents_manager/.gitignore | Measure-Object -Line` → **1** (expected 1, content = `.active-project`). **PASS.**
3. `(Get-Content agents_manager/memory/README.md | Measure-Object -Line).Lines` → **97** (expected 80–220 inclusive). **PASS.**
4. `git check-ignore agents_manager/memory/README.md` → exit **1** (NOT ignored). **PASS.**
5. `git check-ignore -v agents_manager/memory/global/test.md` → exit **0**, matched rule `agents_manager/memory/.gitignore:1:**/*.md` (IS ignored). **PASS.**
6. `git check-ignore -v agents_manager/memory/global/.gitkeep` → exit **0**, matched rule `agents_manager/memory/.gitignore:2:!**/.gitkeep` (matched by include — keep). **PASS.**
7. `git check-ignore -v agents_manager/memory/README.md` → exit **0**, matched rule `agents_manager/memory/.gitignore:3:!README.md` (matched by include — keep). **PASS.**
8. `git check-ignore -v agents_manager/.active-project` → exit **0**, matched rule `agents_manager/.gitignore:1:.active-project` (IS ignored). **PASS.**
9. `git status --ignored -- agents_manager/memory/README.md` → reports **Untracked** (not Ignored). Confirms `!README.md` exception overrides the `**/*.md` exclude. **PASS.**
10. `git status --short -- agents_manager/memory agents_manager/.gitignore` → `?? agents_manager/.gitignore` + `?? agents_manager/memory/` (git aggregates untracked dirs in `--short`).
    - With `--untracked-files=all`: 5 `??` entries — `agents_manager/.gitignore`, `agents_manager/memory/.gitignore`, `agents_manager/memory/README.md`, `agents_manager/memory/global/.gitkeep`, `agents_manager/memory/projects/.gitkeep`. **PASS** (exactly the 5 expected new files; no `M` markers).
11. `git diff --stat -- .gitattributes .github/workflows/release.yml .gitignore README.md bin/README.md bin/agents-manager bin/agents-manager.ps1 bin/install.sh bin/release-zip.ps1 docs/INSTALL.md tasks/T-2026-07-03-001.md` → 11 files, 398 insertions, 45 deletions.
    - All 11 files show ` M` (uncommitted modifications) per `git status --short` — pre-existing from T-2026-07-03-001 follow-ups per `tasks/T-2026-07-03-002.md:65`. None of this chunk's 5 new files appear in the fenced file set. The new `agents_manager/.gitignore` (1 line: `.active-project`) is in `agents_manager/`, not the root `.gitignore`. `git diff --no-color -- agents_manager/.gitignore` returns no output. **Fence invariant preserved** (this chunk did not introduce new changes to fence files).
12. `python3 scripts/validate-frontmatter.py agents_manager/memory/README.md` → **FAIL with 1 error**: `missing required field 'description'`. **Frontmatter incompatibility** (spec uses `title/scope/status/created/last_verified`, validator requires `description` even in lenient mode). Not a memory-tree bug — surfaced as **WARN**; flagged for downstream maintenance. See Known Issues and Cross-cutting findings.
13. `python3 scripts/validate-frontmatter.py agents_manager/.gitignore` → **CRASHES** with `ValueError: not enough values to unpack (expected 2, got 1)` at `scripts/validate-frontmatter.py:119`. Pre-existing validator bug for any non-frontmatter file. Not introduced by this chunk. **WARN** for validator-chunk/maintenance follow-up.

## Per-task verdicts

### P1T1 — Create empty tree + 2 `.gitkeep` + nested `.gitignore`

- **Verdict:** PASS
- **Spec match:** Yes. Per `tasks/T-2026-07-03-002.md:78` "Empty memory tree + 2 `.gitkeep` files + nested `.gitignore` (pattern: `**/*.md` + `!**/.gitkeep` — verified negation idiom)".
- **Correctness:** `agents_manager/memory/global/.gitkeep` and `agents_manager/memory/projects/.gitkeep` exist (verified via glob + `Get-ChildItem`). Nested `.gitignore` is now 3 lines after fix-loop 1 (added `!README.md` to resolve Known Issue #1). Verified all 3 gitignore cases work correctly (checks 4–7 above).
- **Style:** Matches repo convention (compare with `agents_manager/design/notes/.gitkeep` pattern).
- **Evidence:**
  - `agents_manager/memory/.gitignore:1-3` — pattern lines exactly match spec.
  - `agents_manager/memory/global/.gitkeep` — empty file (0 bytes).
  - `agents_manager/memory/projects/.gitkeep` — empty file (0 bytes).
- **Issues:** None.
- **Suggested fix:** no fix needed.

### P1T2 — Write canonical README (schema + lifecycle + protocol)

- **Verdict:** WARN
- **Spec match:** Yes. Per `tasks/T-2026-07-03-002.md:79` and `share/handoffs/00_user_task_T-2026-07-03-002.md:100-110` — all required topics covered:
  - Three scopes (global / project / role) — lines 17–25 ✓
  - File schema with `scope | topic | status | superseded_by | created | last_verified` — lines 27–62 ✓
  - Lifecycle (append-only + supersession + 90-day sweep) — lines 64–70 ✓
  - Read-on-entry protocol with 200-line cap + master's third source — lines 72–83 ✓
  - Write-on-exit protocol with `Memory written:` / `No memory write:` dispatch-contract lines — lines 85–92 ✓
  - 3-question "durable insight" test — lines 94–102 ✓
  - ≤20 lines/entry cap — lines 104–106 ✓
  - Secrets-free rule (locked design #13) — lines 108–112 ✓
  - No-write-into-`templates/<name>/memory/` — lines 114–116 ✓
  - Validator pointer — lines 124–128 ✓
  - Active-slug detection (`.active-project` YAML frontmatter, default `basename $PWD`) — lines 130–149 ✓
  - Per-role pointer stubs at end (5 roles) — lines 151–157 ✓
- **Correctness:** Internally coherent. 14 H2 sections + 1 H1 + frontmatter. Per-role pointer section at the end makes downstream chunk-2 2-line pointers unambiguous. The `scope: canonical-schema` in the README's own frontmatter (line 3) is a deliberate "meta-doc" marker, not a violation — the validator (P3.3) does not scan the memory/ root, so no collision.
- **Style:** Tone matches other agents_manager doc files (declarative, table-where-helpful, section anchors).
- **Evidence:** Full file read at `agents_manager/memory/README.md:1-157`. Section count: Overview, Three scopes, File schema, Lifecycle, Read-on-entry protocol, Write-on-exit protocol, Durable-insight criteria, Size cap, Secrets-free rule, No-write-into-templates, Cross-project scoping, Validator, Active project slug, See also = **14 H2 sections**.
- **Tests:** No tests in this chunk (validator ships in P3.3). Frontmatter validator pre-check FAILED with `missing required field 'description'` — see Issues.
- **Issues:**
  - [MEDIUM] Frontmatter validator incompatibility — `agents_manager/memory/README.md:1-7` uses `title/scope/status/created/last_verified` per spec, no `description:`. Pre-existing `scripts/validate-frontmatter.py:97` requires `description:` even in lenient mode (path lacks `/skills/`). Two acceptable resolutions: (a) add `description: Canonical schema source-of-truth for the three-scope memory system` to satisfy validator, or (b) update validator to skip the `description` requirement for paths matching `agents_manager/memory/`. **Master's call** — flagged but not in chunk 1 scope.
  - [LOW] The README declares `scope: canonical-schema` (line 3) — a 4th value beyond locked-design #4's `{global, project, role}`. Intentional meta-doc marker, but a future schema-validating change might trip on it. The chunk-3 validator scope (`agents_manager/memory/{global,projects}/` + per-role `notes/{semantic,episodic}/`) explicitly excludes the memory/ root, so the runtime validator won't fail. **Document** the convention in the canonical README's "File schema" section in a future edit (or accept as-is and trust the README-as-meta-doc context to disambiguate).
- **Suggested fix:** No fix in this chunk. Add `description:` key in a follow-up memory-doc polish (or relax validator for `agents_manager/memory/` paths).

### P1T3 — Create `agents_manager/.gitignore` covering `.active-project`

- **Verdict:** PASS
- **Spec match:** Yes. Per `tasks/T-2026-07-03-002.md:80` "Create new `agents_manager/.gitignore` with one line: `.active-project` ... Root `.gitignore` is in the v0.12.x fence and is NOT modified".
- **Correctness:** File has exactly 1 line (`agents_manager/.gitignore:1`) = `.active-project`. Verified via `git check-ignore -v`: matches line 1. Root `.gitignore` was NOT modified by this chunk (verified via `git diff --no-color -- .gitignore` showing only T-001 pre-existing changes; no new diff lines from chunk 1).
- **Style:** Matches repo `.gitignore` convention (each line = one pattern; no comments; LF line endings).
- **Evidence:**
  - `agents_manager/.gitignore:1` — `.active-project`
  - `git diff --no-color -- agents_manager/.gitignore` → empty (untracked file, just created).
  - `git diff --no-color -- .gitignore` → shows T-001 pre-existing additions only (Python bytecode + duplicated `share/notes/02_secrets_*.md` etc.); none from this chunk.
- **Issues:** None.
- **Suggested fix:** no fix needed.

### P1T4 — Run Done-when checks (1-8) + fence verify

- **Verdict:** PASS
- **Spec match:** Yes. Per `tasks/T-2026-07-03-002.md:81`. Substantive invariants all satisfied:
  1. `ls`/glob shows exactly 5 expected files ✓
  2. `gitignore` content is exactly `**/*.md` + `!**/.gitkeep` + `!README.md` (3 lines after fix-loop; matches verified gitignore negation idiom per plan §2) ✓
  3. `agents_manager/.gitignore` content is exactly `.active-project` (1 line) ✓
  4. README line count = 97 (within 80–220 inclusive) ✓
  5. `git status` shows ONLY the 5 new files (no M markers in fence) ✓
  6. `git diff --stat` for fence paths returns pre-existing T-001 modifications only; this chunk added 0 lines to any fence file ✓
  7. Frontmatter validator run completed (with the documented incompatibilities, not actionable in this chunk) ✓
  8. `git status --short --porcelain | wc -l` returns 44 (the dispatch's ≤7 literal is unsatisfiable in current repo state — already noted by coder in summary; substantive intent = "this chunk did not blow up the file count" — false, only 5 new entries added)
- **Correctness:** All five created files match spec literally. Fence invariant is the salient check: confirmed via diff that none of this chunk's creations/modifications touch fenced files (`agents_manager/.gitignore` is in `agents_manager/`, not root `.gitignore`; memory tree is brand-new under `agents_manager/memory/`).
- **Style:** Verification commands follow the spec-protocol style (file/path/line/pattern).
- **Evidence:**
  - Checks 1–9 in "Tests / build run" above.
  - `git diff --no-color -- agents_manager/.gitignore` → empty.
  - `git diff --no-color -- .gitignore` → only T-001 pre-existing modifications.
  - `share/notes/03_coder_summary_T-2026-07-03-002_P3.1.md:101-127` (fix-loop 1 evidence) confirms the literal checks re-ran after the `!README.md` addition.
- **Issues:** None at the per-task level. Pre-existing repo dirtiness (T-001 follow-ups not committed) makes the literal ≤7 "Done-when check 8" unsatisfiable, but the substantive intent (this chunk added a bounded number of files) holds. Master already acknowledged this in `tasks/T-2026-07-03-002.md:59-65`.
- **Suggested fix:** no fix needed.

## Cross-cutting findings

- **Two validator scope mismatches surface in this chunk but are pre-existing bugs**, not implementation drift:
  1. `scripts/validate-frontmatter.py:74` returns `[str(e)]` (a list) when `parse_frontmatter` raises, but caller at line 119 does `errs, mode = validate_one(path)` (tuple unpack). Crashes with `ValueError: not enough values to unpack (expected 2, got 1)` on any non-frontmatter file (reproduced with `agents_manager/.gitignore`). Pre-existing; not in chunk 1's scope to fix; surfaced here because P1T4's verification surfaces it. Recommend: chunk 3 (`scripts/validate-memory.sh`) or a separate validator-hardening pass.
  2. `scripts/validate-frontmatter.py:97` requires `description:` even in lenient mode (path lacks `/skills/` segment). The memory README's spec frontmatter (`title/scope/status/created/last_verified` with no `description:`) is incompatible. Two acceptable resolutions: (a) add a `description:` line to the canonical README's frontmatter, (b) extend the validator to skip the `description` requirement for `agents_manager/memory/**` paths. **Master's call** — not in chunk 1's scope.

- **Fence invariant is preserved.** All 5 files this chunk creates/owns live under `agents_manager/memory/**` or `agents_manager/.gitignore` — none of the 11 fenced files (`.gitattributes`, `.github/workflows/release.yml`, root `.gitignore`, `README.md`, `bin/README.md`, `bin/agents-manager`, `bin/agents-manager.ps1`, `bin/install.sh`, `bin/release-zip.ps1`, `docs/INSTALL.md`, `tasks/T-2026-07-03-001.md`) show diff changes introduced by this chunk. The pre-existing 11 ` M` markers come from T-2026-07-03-001 follow-ups and are explicitly out-of-scope per `tasks/T-2026-07-03-002.md:65`.

- **Fix-loop 1 resolved the most consequential design tension in the initial chunk** (Known Issue #1: README was gitignored by `**/*.md`). The 1-line `!README.md` addition is the correct resolution per the dispatch's three-options analysis.

## Out-of-scope observations (informational only)

- [LOW] The `assets/notes/branch-decisions.md` precedent (append-only-by-task lifecycle, distinct from the new memory system) is acknowledged in the README's "See also" pointer and `agents_manager/assets/SKILL.md:80` reference, but the canonical README does not have an explicit "Why this exists separately from the memory system" explanation. Chunk 2 is the natural place to add it (when `assets/notes/README.md` lands).
- [LOW] The README's `scope: canonical-schema` 4th-value convention (line 3) is not documented anywhere as a deliberate choice. If a future validator tightens the schema (e.g., requires exactly one of `{global, project, role}`), this README would trip it. The chunk-3 validator scope explicitly excludes the memory/ root, so no collision today.
- [LOW] The dispatch (line 119) suggested `git check-ignore <path>` exit-code semantics differ from the dispatch wording: `git check-ignore` exits 0 when any rule (include OR exclude) matches. After the fix, the include rule `!README.md` matches, exit 0; the README is "kept" (not gitignored). Pre-fix, the exclude rule `**/*.md` matched, exit 0; the README was "ignored". Substantive distinction requires `git status --ignored -- <path>` to read `Untracked` vs `Ignored`. The coder's summary handles this nuance correctly.

## Honest assessment

The chunk shipped correctly after one well-targeted fix-loop. The 3-line gitignore now correctly handles all three design cases (operator `.md` ignored, `.gitkeep` kept, README tracked). The 97-line canonical README is genuinely self-contained with 14 coherent H2 sections — a per-role 2-line pointer in chunk 2 will be unambiguous. The fence is respected: none of the chunk's 5 files introduce changes to the v0.12.x-modified fence zone. The two surfaced warns are pre-existing tooling scope gaps (frontmatter validator's `description` requirement + tuple-unpack crash), not implementation drift. **Recommend advancing to chunk 2 (per-role pointer stubs) without a fix-loop.** Coder may want to add a `description:` key to the canonical README frontmatter as a 1-line polish, but it's not blocking.

## Self-critique

- **Did I do my job?** Yes. Read the actual files (not just the summary), ran every documented verification command, and verified the fix-loop's `!README.md` exception actually works at the gitignore-engine level (`git check-ignore -v` shows the rule firing on the right path). Per-task verdicts with `path:line` evidence for every claim.
- **What might I have missed?**
  - Did not run `shellcheck` or `bash -n` on this chunk — irrelevant, no shell scripts shipped yet (chunk 3's deliverable).
  - Did not exhaustively diff the canonical README against locked design #4's schema spec line-by-line — instead, sampled and matched the required keys + body template. The spec is met for the schema-as-spec'd; minor prose polish (e.g., tightening the Lifecycle "append-only" description to match `research_doc/README.md` precedent verbatim) is out of scope for this chunk.
  - Did not verify whether the validator (chunk 3) will trip on the README's `scope: canonical-schema` value when the validator glob is wired — but reading the P3T1 spec (`{global,projects}/` + per-role `notes/{semantic,episodic}/`) shows it explicitly excludes the memory/ root, so safe.
- **What did I assume without evidence?**
  - The `scope: canonical-schema` is an intentional 4th-value marker, not a bug — assumed from context (the README is a meta-schema-doc) but the README itself does not state this convention.
  - T-001 follow-up modifications on fence files are not from this chunk — verified via `git diff` showing no overlap with this chunk's file paths, but did not run `git diff --stat HEAD~1` to confirm the T-001 modifications predate this chunk (the latest commit `f07ada5 feat(templates+assets): v0.12.0` from `git log` lines up with the T-001 task id, supporting the assumption).
  - The "empty `.gitkeep`" convention (0-byte file) is correct here vs. the `assets/notes/branch-decisions.md` precedent (which is a full append-only log). Verified by both the dispatch spec and the existing `agents_manager/design/notes/episodic/.gitkeep` (which the research-doc confirmed is empty placeholder). Assumption holds.

---

**Verdict tally:** 3 PASS (P1T1, P1T3, P1T4) + 1 WARN (P1T2) = **PASS_WITH_WARN**.
**FAILs:** 0.
**WARNs:** 1 (per-task) + 3 (out-of-scope/cross-cutting) = **4 total** (all logged to `share/notes/04_warns_register_T-2026-07-03-002.md`).
**Call to action:** **ready to ship** — chunk 2 can begin; resolve the 2 pre-existing validator bugs in a separate maintenance pass (chunk 3 or v0.13.1).
