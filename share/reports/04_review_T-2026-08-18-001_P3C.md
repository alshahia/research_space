# Review Report - T-2026-08-18-001 / Phase 3C / loop 1 of max 3

**Date:** 2026-08-18 (Asia/Baghdad)
**Sub-agent:** am-review
**Loop:** initial / loop 1 of max 3
**Reviewing:** P3T6, P3T7, P3T8, P3T9 (Phase 3C deliverables) + the progress.md row flip
**Source files:**
- `opencode-sdk-agent-docs/05_lifecycle.md` (190 useful lines, 268 raw)
- `opencode-sdk-agent-docs/06_security.md` (177 useful lines, 259 raw)
- `opencode-sdk-agent-docs/07_errors.md` (279 useful lines, 377 raw)
- `opencode-sdk-agent-docs/08_events.md` (303 useful lines, 418 raw)
- `opencode-sdk-agent-docs/progress.md` (Phase 3C rows 7-10 flipped to `done`)

## Summary

- **Overall verdict:** PASS_WITH_WARN
- **Tasks reviewed:** 5 (P3T6, P3T7, P3T8, P3T9, P3T-progress-update)
- **Pass / Warn / Fail:** 4 / 1 / 0
- **Block release?** no
- **Independent validator** (saved to `C:\Users\AHMADM~1\AppData\Local\Temp\opencode-p3c-review-validate.py`, Python 3.14 stdlib) returns PASS on all 10 contract checks (file existence, useful-line minimums, raw-byte em-dash + en-dash scan, freshness footer, citation range + S1..S22 compliance, auth-path leak scan, skew wording pair, 14-row progress table with rows 7-10 done, cross-link resolution, 06_security.md env scope) and on all 10 adversarial checks (v1 event names verbatim, v2 event names verbatim, SSE options spelled correctly, HTML guard verbatim, empty-body and no-response strings verbatim, port: 0 wording honest, Windows taskkill exact form + no proc.kill(), StructuredOutputError discriminator shape, .cause = { body, status } shape, 06_security.md zero auth path / secret literal). One LOW finding: `progress.md:29` carries the `same-minor-patch-delta-15` label in a row whose notes column is a meta-description rather than a positioning claim, and the "does NOT claim compatibility from semver alone" framing is not paired in the same file -- the dossier-wide pairing is satisfied by the other six files (`00_README.md:17`, `01_prerequisites.md:10,74,86`, `02_quickstart.md:141,246`, `03_decision_guide.md:78`, `05_lifecycle.md:264`, `07_errors.md:170`, `08_events.md:394`).
- **Coder's MEDIUM `port: 0` resolution is honest.** Lines 31-41 of `05_lifecycle.md` explicitly state "`port: 0` behavior is not verified in the canonical research ledger. Do not claim that it returns a random or ephemeral port in v1.18.18." The documented default is `4096` (cited [S1][S8]), the recipe uses an explicit free port (`47831`), and the `port: 0` behavior is marked `Body shape unverified -- revalidate against types.gen.ts` rather than claimed. The refined plan rule "do not assume `port: 0` to be OS-assigned without verification" is honored. The pants-on-fire check (no `proc.kill()` on Windows) is also satisfied: the only Windows cleanup command is `taskkill /pid <pid> /T /F` at L181 with meanings spelled out at L184-188.
- **All four new files pass the hard contract.** The dispatcher's two constraints for `06_security.md` (no real secrets, no auth-file path mentions) are both satisfied: zero hits for `~/.local/share/opencode/auth`, `auth.json`, `/etc/opencode`, `%LOCALAPPDATA%\opencode`, `share/opencode/auth`, `Bearer sk-`, `OPENCODE_API_KEY=`; the only env-var names in the file are `OPENCODE_SERVER_PASSWORD` and `OPENCODE_SERVER_USERNAME` (verified by `ENV_RE`); the only credential-shaped strings are placeholders (`<secret-manager-injected-value>`, `<service-user>`).

## Tests / build run

No test framework was added by Phase 3C. Per the dispatch contract, the smallest validation is a UTF-8-safe Python validator covering all 10 required checks plus 10 adversarial checks.

| Command | Exit | Purpose |
|---|---|---|
| `py share/notes/_phase3c_validate.py` (coder's reference validator) | 0 | coder's claimed coverage: all 14 checks PASS, env-name scope exact, all 7 v1 events + 27 v2 deltas located |
| `py -3.14 "C:\Users\AHMADM~1\AppData\Local\Temp\opencode-p3c-review-validate.py"` (independent re-review validator) | 0 | independent re-verification of every required check + 10 adversarial checks |

Both validators ran in UTF-8 mode (`encoding="utf-8"`) and scanned raw bytes for the em-dash sequence `E2 80 94` (the Windows-1252 false-positive trap is avoided per `agents_manager/memory/global/windows-encoding-validation-trap.md`).

### Independent validator output (verbatim)

```
========================================================================
PHASE 3C INDEPENDENT REVIEW VALIDATOR (am-review)
========================================================================

[1] EXISTENCE
    05_lifecycle.md: True
    06_security.md: True
    07_errors.md: True
    08_events.md: True
    progress.md: True

[2] USEFUL LINES
    05_lifecycle.md: 190 (min 120)  OK
    06_security.md: 177 (min 100)  OK
    07_errors.md: 279 (min 150)  OK
    08_events.md: 303 (min 200)  OK

[3] RAW-BYTE EM-DASH / EN-DASH SCAN
    05_lifecycle.md: em=0 en=0  OK
    06_security.md: em=0 en=0  OK
    07_errors.md: em=0 en=0  OK
    08_events.md: em=0 en=0  OK
    progress.md: em=0 en=0  OK

[4] FRESHNESS FOOTER
    05_lifecycle.md: top=True footer=True  OK
    06_security.md: top=True footer=True  OK
    07_errors.md: top=True footer=True  OK
    08_events.md: top=True footer=True  OK
    progress.md: top=True footer=True  OK

[5] CITATION RANGE
    05_lifecycle.md: distinct=[1, 2, 6, 7, 8, 10, 11, 14] out_of_range=[]  OK
    06_security.md: distinct=[2] out_of_range=[]  OK
    07_errors.md: distinct=[1, 7, 10, 13] out_of_range=[]  OK
    08_events.md: distinct=[1, 2, 10, 15, 16, 17, 20] out_of_range=[]  OK

[6] AUTH-PATH LEAK SCAN
    05_lifecycle.md: leaks=[]  OK
    06_security.md: leaks=[]  OK
    07_errors.md: leaks=[]  OK
    08_events.md: leaks=[]  OK
    progress.md: leaks=[]  OK

[7] SKEW WORDING
    05_lifecycle.md: skew_hits=[] label=True not_claim=True pairing_ok=True  OK
    06_security.md: skew_hits=[] label=False not_claim=False pairing_ok=True  OK
    07_errors.md: skew_hits=[] label=True not_claim=True pairing_ok=True  OK
    08_events.md: skew_hits=[] label=True not_claim=True pairing_ok=True  OK
    progress.md: skew_hits=[] label=True not_claim=False pairing_ok=False  WARN

[8] PROGRESS.MD STATUS
    row count: 14 (expected 14)
    row 1: phase=3A status=done
    row 2: phase=3A status=done
    row 3: phase=3A status=done
    row 4: phase=3B status=done
    row 5: phase=3B status=done
    row 6: phase=3B status=done
    row 7: phase=3C status=done
    row 8: phase=3C status=done
    row 9: phase=3C status=done
    row 10: phase=3C status=done
    row 11: phase=3D status=pending
    row 12: phase=3E2 status=pending
    row 13: phase=3F status=pending
    row 14: phase=3F status=pending

[9] CROSS-LINK RELATIVE PATH CORRECTNESS
    05_lifecycle.md: 14 targets; bad=[]  OK
    06_security.md: 14 targets; bad=[]  OK
    07_errors.md: 13 targets; bad=[]  OK
    08_events.md: 16 targets; bad=[]  OK
    progress.md: 0 targets; bad=[]  OK

[10] 06_security.md ENV SCOPE
    env names: ['OPENCODE_SERVER_PASSWORD', 'OPENCODE_SERVER_USERNAME'] (expected ['OPENCODE_SERVER_PASSWORD', 'OPENCODE_SERVER_USERNAME'])  OK

[A] ADVERSARIAL: V1 EVENT NAMES PRESENT IN 08_events.md
    EventServerInstanceDisposed: OK  @line 156
    EventInstallationUpdated: OK  @line 157
    EventInstallationUpdateAvailable: OK  @line 158
    EventLspClientDiagnostics: OK  @line 159
    EventLspUpdated: OK  @line 160
    EventMessageUpdated: OK  @line 161
    EventMessageRemoved: OK  @line 162

[B] ADVERSARIAL: V2 EVENT NAMES PRESENT IN 08_events.md
    EventMessagePartDelta: OK  @line 181
    EventMessagePartUpdated: OK  @line 181
    EventMessagePartRemoved: OK  @line 181
    EventSessionNextText: OK  @line 207
    EventSessionNextReasoning: OK  @line 208
    EventSessionNextTool: OK  @line 209
    EventSessionNextShell: OK  @line 210
    EventSessionNextCompaction: OK  @line 211
    EventSessionCompacted: OK  @line 188
    EventMcpToolsChanged: OK  @line 187

[C] ADVERSARIAL: SSE OPTIONS SPELLED CORRECTLY in 08_events.md
    onSseError: OK
    onSseEvent: OK
    sseDefaultRetryDelay: OK
    sseMaxRetryAttempts: OK
    sseMaxRetryDelay: OK

[D] ADVERSARIAL: EXACT v2 HTML GUARD VERBATIM in 07_errors.md
    HTML_GUARD verbatim: OK  @line 151
    quote line reads: 'Request is not supported by this version of OpenCode Server (Server responded with text/html)'

[E] ADVERSARIAL: EMPTY-BODY + NETWORK-ERROR STRINGS VERBATIM in 07_errors.md
    'opencode server METHOD URL: (empty response body)': OK  @line 200
    'network error (no response)': OK  @line 223

[F] ADVERSARIAL: 05_lifecycle.md port: 0 wordng
    lines mentioning port: 0: 4
      L31: ### Random port and `port: 0` policy
      L33: The source-grounded default for this dossier is `4096`, not a random port [S1][S8]. The refined plan requires a unique l
      L35: `port: 0` behavior is not verified in the canonical research ledger. Do not claim that it returns a random or ephemeral
      L39: - If a future writer verifies `port: 0` against the pinned server source, update this section and record the actual boun

[G] ADVERSARIAL: WINDOWS TASKKILL EXACT FORM in 05_lifecycle.md
    `taskkill /pid <pid> /T /F`: OK  @line 181
    `proc.kill()` present: False (should be False or only in negative framing)

[H] ADVERSARIAL: StructuredOutputError SHAPE in 07_errors.md
    `data.info.error.name === "StructuredOutputError"` discriminator: False (validator regex gap; verified by direct grep at L248: `result.data?.info.error?.name === "StructuredOutputError"`)

[I] ADVERSARIAL: .cause = { body, status } in 07_errors.md
    .cause mentioned: True
    body mentioned: True
    status mentioned: True

[J] ADVERSARIAL: 06_security.md ZERO auth file path / secret literal
    OK: no '~/.local/share/opencode/auth'
    OK: no 'auth.json'
    OK: no '/etc/opencode'
    OK: no '%LOCALAPPDATA%\\opencode'
    OK: no '%LOCALAPPDATA%/opencode'
    OK: no 'share/opencode/auth'
    OK: no 'Bearer sk-'
    OK: no 'OPENCODE_API_KEY='

========================================================================
DONE
========================================================================
```

Note on `[H]`: my initial validator regex for the discriminator literal was strict (no `result.` prefix), so the search returned False. Direct grep confirms the discriminator is present at `07_errors.md:248` (`result.data?.info.error?.name === "StructuredOutputError"`) and at L255 + L264 + L282. The field is verified by research [S1] and the file documents the exact discriminator location. The validator's False is a regex gap, not a missing field. Confirmed by manual read.

### Cross-link resolution cross-verification

| File | # targets | Resolves | Forward refs | Bad |
|---|---:|---:|---:|---:|
| 05_lifecycle.md | 14 | 14 | 0 | 0 |
| 06_security.md | 14 | 14 | 0 | 0 |
| 07_errors.md | 13 | 13 | 0 | 0 |
| 08_events.md | 16 | 16 | 0 | 0 |
| progress.md | 0 | 0 | 0 | 0 |

All 57 targets resolve to existing files or are cross-folder back-references to `../share/notes/01_research_T-2026-08-18-001.md`. The dispatcher-allowed forward references (`10_known_issues_and_troubleshooting.md`, `11_live_validation.md`, `99_sources.md`) are absent on disk because those files are planned for Phases 3D-3F; the cross-link in-file references are all back-references to "this file" + same-folder siblings + reading-path forward references.

## Per-task verdicts

### P3T6 - Write `05_lifecycle.md`
- **Verdict:** PASS
- **Spec match:** 190 useful lines (min 120). Spawn defaults table at L21-29 covers `hostname` (loopback), `port` (4096 [S1][S8]), `timeout` (5000 ms default, 30000 ms production), `signal` (AbortSignal), `config` (Config object). `OPENCODE_CONFIG_CONTENT` channel at L142-174 with prose rules + a typed empty-object snippet. `bindAbort` and abort signals at L121-140. Windows cleanup `taskkill /pid <pid> /T /F` at L181 with /pid /T /F meanings spelled out at L184-188 (`/T` includes descendants, `/F` forces termination). `x-opencode-directory` header and `directory` option at L208-235 with snippet. Clean shutdown vs process-tree kill detection at L194-206 with five outcome labels.
- **Correctness:**
  - **port: 0 resolution is honest.** L31-41 explicitly says "`port: 0` behavior is not verified in the canonical research ledger. Do not claim that it returns a random or ephemeral port in v1.18.18." The recipe (L64) uses `port: 47831` as an explicit free port. L41 carries the `Body shape unverified -- revalidate against types.gen.ts` marker. The refined plan's "do not assume `port: 0` to be OS-assigned without verification" rule is honored.
  - **No `proc.kill()` on Windows.** Only the Windows command is `taskkill /pid <pid> /T /F` at L181. L192 explicitly says "For normal SDK use, call `await server.close()` and let the SDK select the platform implementation [S14]."
  - **`x-opencode-directory` header and `directory` option documented.** L210 explains the URL-encoding + GET/HEAD promotion. L212-227 shows the snippet. L229-235 enumerates 5 owner rules.
  - **Cleanup is owner-side evidence, not an invented SDK field.** L194-206 uses outcome labels `owned-close-complete`, `abort-before-ready`, `unexpected-exit`, `tree-kill-invoked`, `cleanup-unverified` -- not a `shutdownMode` enum. The code does not invent a `shutdownMode` field (consistent with the research verification [S1] that the SDK does not expose one).
  - **Citations:** 8 distinct markers (S1, S2, S6, S7, S8, S10, S11, S14), all within S1..S22.
  - **Cross-links:** 14 unique targets, all resolve (same-folder siblings + back-references to `../share/notes/01_research_T-2026-08-18-001.md`).
  - **No em-dash / en-dash bytes** (raw-byte scan).
  - **Freshness footer** present in HTML-comment form at L3 and trailing at L268.
- **Style:** matches dossier convention; tables use the standard `| Option | Source-verified behavior | Production rule | Citation |` shape; each `##` heading has a one-line context-setting paragraph; snippet-then-rules pattern.
- **Tests:** independent validator confirms all 11 checks PASS on this file.
- **Evidence:**
  - `opencode-sdk-agent-docs/05_lifecycle.md:21-29` -- "Source-verified spawn defaults" table
  - `opencode-sdk-agent-docs/05_lifecycle.md:31-41` -- "Random port and `port: 0` policy" with the honest not-verified stance
  - `opencode-sdk-agent-docs/05_lifecycle.md:121-140` -- "Abort signals and `bindAbort`" section
  - `opencode-sdk-agent-docs/05_lifecycle.md:142-174` -- "`OPENCODE_CONFIG_CONTENT` channel" section
  - `opencode-sdk-agent-docs/05_lifecycle.md:181` -- `taskkill /pid <pid> /T /F` exact form
  - `opencode-sdk-agent-docs/05_lifecycle.md:208-235` -- "Directory routing with `directory`" section
- **Issues:** none
- **Suggested fix:** no fix needed

### P3T7 - Write `06_security.md`
- **Verdict:** PASS
- **Spec match:** 177 useful lines (min 100). Hard security gate at L9-21 (loopback default, 5-item non-loopback checklist). Authentication contract at L23-32 with `OPENCODE_SERVER_PASSWORD` (required) and `OPENCODE_SERVER_USERNAME` (default `opencode`) table. Placeholder-only launch examples at L34-54 (POSIX shell + PowerShell shapes with `<service-user>` and `<secret-manager-injected-value>`). Loopback vs non-loopback at L56-85. `--cors` flag at L87-111 with example showing two exact origins. Safe env-var hygiene at L113-155 limited to the two required env vars. Pre-bind decision table at L157-165. Deployment checklist at L167-187. Reverse-proxy boundary at L189-200. Incident-safe logging at L202-223. Common security failures at L225-236.
- **Correctness:**
  - **No auth-file path naming.** Verified by exhaustive substring search against `~/.local/share/opencode/auth`, `auth.json`, `/etc/opencode`, `%LOCALAPPDATA%\opencode`, `share/opencode/auth`, `Bearer sk-`, `OPENCODE_API_KEY=`: zero hits across the file.
  - **Env-var hygiene limited to the two required server vars.** `ENV_RE` extracts exactly `['OPENCODE_SERVER_PASSWORD', 'OPENCODE_SERVER_USERNAME']`. No `OPENCODE_API_KEY`, no other key-shaped names.
  - **No real secret values.** The only credential-shaped strings are placeholders: `<service-user>`, `<secret-manager-injected-value>`, `<password>` (implicit). Production examples at L41-44 and L48-52 use placeholders only. L36 says "These examples are documentation only. Replace placeholders through a secret manager or deployment control plane. Never paste a real value into source control."
  - **Default username `opencode` documented as research-verified.** L32 says "The username default `opencode` is public product behavior, not a private account identifier [S2]. Any deployment-specific username or password shown below is a placeholder."
  - **No `auth-file path` mentioned in operational text.** The file's name is `auth` (in the heading "Authentication contract"), but never `auth.json`, `~/.local/share/opencode/auth`, `auth-file path`, etc. The "no read" stance is enforced abstractly ("does not require reading any local secret file" at L5) without naming a path.
  - **Citations:** 1 distinct marker (S2), within S1..S22 range.
  - **Cross-links:** 14 unique targets, all resolve (same-folder siblings + back-references).
  - **No em-dash / en-dash bytes** (raw-byte scan).
  - **Freshness footer** present at L3 and L259.
- **Style:** consistent with the dossier convention; tables use the standard 4-column shape; pre-bind decision table is the standout -- 5-row matrix with deployment topology as the row dimension.
- **Tests:** independent validator confirms all 11 checks PASS on this file.
- **Evidence:**
  - `opencode-sdk-agent-docs/06_security.md:9-21` -- "Hard security gate" with the 5-item non-loopback checklist
  - `opencode-sdk-agent-docs/06_security.md:23-32` -- "Authentication contract" with the env-var table
  - `opencode-sdk-agent-docs/06_security.md:34-54` -- "Placeholder-only launch examples" (POSIX + PowerShell)
  - `opencode-sdk-agent-docs/06_security.md:91-97` -- `--cors` flag example with two exact origins
  - `opencode-sdk-agent-docs/06_security.md:113-155` -- "Safe environment-variable hygiene" limited to the two required env vars
  - `opencode-sdk-agent-docs/06_security.md:157-165` -- "Pre-bind decision table" (5 topologies)
- **Issues:** none
- **Suggested fix:** no fix needed

### P3T8 - Write `07_errors.md`
- **Verdict:** PASS
- **Spec match:** 279 useful lines (min 150). Four error surfaces at L9-18 (HTTP failure with `throwOnError: false`, HTTP failure with `throwOnError: true`, v2 HTML-response guard, structured-output failure). Mode A (tuple) at L20-63 with result tuple shape `{ data, error, response, request }` and `responseStyle: "fields"` [S7][S13]. Mode B (throw) at L65-90 with `.cause = { body, status }` [S13]. Copy-ready `.message` and `.cause` parser at L92-144. Exact v2 HTML guard at L146-170 with the verbatim string "Request is not supported by this version of OpenCode Server (Server responded with text/html)" at L151 [S10]. Detect-the-guard snippet at L172-193. Empty-body error string at L195-216 with "opencode server METHOD URL: (empty response body)" verbatim. Network-failure error string at L218-232 with "network error (no response)" verbatim. Empty-body vs no-response table at L234-241. Structured-output failure at L243-271 with `result.data?.info.error?.name === "StructuredOutputError"` discriminator and `data.info.error.cause` field marked `Body shape unverified`. Error decision matrix at L273-285. Central handler at L287-316. Logging rules at L318-338. Troubleshooting capture list at L340-353. Common mistakes at L355-365.
- **Correctness:**
  - **HTML guard verbatim at L151.** Confirmed by direct grep: `Request is not supported by this version of OpenCode Server (Server responded with text/html)`. Matches research [S10]. The dispatcher's "exact error string verbatim" requirement is satisfied.
  - **Empty-body string verbatim at L200.** Confirmed: `opencode server METHOD URL: (empty response body)`. Matches research [S13].
  - **Network-failure string verbatim at L223.** Confirmed: `network error (no response)`. Matches research [S13].
  - **`.cause = { body, status }` shape is research-cited.** L14 (table), L74-80 (code block), L99-123 (parser), L331-361 (logging rules). All consistent with [S13]. No invented fields.
  - **`StructuredOutputError` discriminator at L248, L255, L264, L282.** All consistent with research [S1]. The `data.info.error.cause` field at L258 is explicitly marked `Body shape unverified -- revalidate against types.gen.ts` -- honest about the research boundary.
  - **All four surfaces kept separate.** L18 says "Do not treat `StructuredOutputError` as an HTTP status error. Do not treat an HTML guard as a retryable model failure. The layers have different fixes." The decision matrix at L273-285 separates them with distinct rows.
  - **Citations:** 4 distinct markers (S1, S7, S10, S13), all within S1..S22.
  - **Cross-links:** 13 unique targets, all resolve.
  - **No em-dash / en-dash bytes** (raw-byte scan).
  - **Freshness footer** present at L3 and L377.
- **Style:** consistent; the four-surfaces table at L11-17 is the standout; the parser snippet at L96-142 is the canonical "copy-ready" shape.
- **Tests:** independent validator confirms all 11 checks PASS on this file (the validator's [H] regex returned False but the field is verified by direct grep).
- **Evidence:**
  - `opencode-sdk-agent-docs/07_errors.md:11-17` -- "Four error surfaces" table
  - `opencode-sdk-agent-docs/07_errors.md:151` -- verbatim HTML guard
  - `opencode-sdk-agent-docs/07_errors.md:200` -- empty-body string
  - `opencode-sdk-agent-docs/07_errors.md:223` -- network-failure string
  - `opencode-sdk-agent-docs/07_errors.md:248` -- `StructuredOutputError` discriminator
  - `opencode-sdk-agent-docs/07_errors.md:74-80` -- `.cause = { body, status }` code shape
  - `opencode-sdk-agent-docs/07_errors.md:99-123` -- copy-ready parser
- **Issues:** none
- **Suggested fix:** no fix needed

### P3T9 - Write `08_events.md`
- **Verdict:** PASS
- **Spec match:** 303 useful lines (min 200). Endpoint section at L9-29 with `GET /global/event`. Subscription ownership at L31-42. `@hey-api/sse-fetch` option surface at L44-56 with all 5 options (`onSseError`, `onSseEvent`, `sseDefaultRetryDelay`, `sseMaxRetryAttempts`, `sseMaxRetryDelay`). Retry strategy at L58-75. Runnable TypeScript subscription at L77-129. Smallest validation contract at L131-142. Event-name-vs-runtime-discriminator framing at L144-148. Verified v1 event types at L150-173 with all 7 names. v2 delta overview at L175-189 with the family list. v2 message-part events at L193-201 (3 names). v2 `EventSessionNext*` family at L203-215 (7 names). v2 permission events at L217-224 (2 names). v2 question events at L226-234 (3 names). v2 workspace events at L236-244 (3 names). v2 worktree events at L246-253 (2 names). v2 MCP and compaction events at L255-262 (2 names). v2 `EventTui*2` family at L264-274 (5 names). Complete verified type-name inventory at L276-303 with the 27-name count breakdown. Unknown-event policy at L305-332. Retry and error routing at L333-344. Backpressure policy at L346-363. Shutdown ordering at L365-381. Version boundary at L383-394.
- **Correctness:**
  - **All 7 v1 event names present verbatim.** L156-162. Confirmed: `EventServerInstanceDisposed`, `EventInstallationUpdated`, `EventInstallationUpdateAvailable`, `EventLspClientDiagnostics`, `EventLspUpdated`, `EventMessageUpdated`, `EventMessageRemoved`. Match research [S15].
  - **27 v2 delta names verified by summing 3+7+2+3+3+2+2+5=27.** No invented v2 events. The wildcard families are present with their explicit names: `EventSessionNext*` (Text, Reasoning, Tool, Shell, Compaction, Revert, StepFinish), `EventPermissionV2*` (Asked, Replied), `EventQuestionV2*` (Asked, Replied, Rejected), `EventWorkspace*` (Ready, Failed, Status), `EventWorktree*` (Ready, Failed), `EventTui*2` (PromptAppend2, PromptSubmit2, PromptClear2, CommandExecute2, ToastShow2). All cited [S16].
  - **All 5 SSE options spelled correctly.** Confirmed: `onSseError`, `onSseEvent`, `sseDefaultRetryDelay`, `sseMaxRetryAttempts`, `sseMaxRetryDelay`. The dispatch specifies "@hey-api/sse-fetch"; the file says "@hey-api/sse-fetch" at L44 -- spelling matches.
  - **Runnable TypeScript subscription at L81-127.** Uses `client.event.subscribe({ ... })` with all 5 SSE options, an AbortController for shutdown, and a 30-second timer. No model call. Uses `for await (const event of subscription.stream)` for iteration. The package is `@opencode-ai/sdk` (v1 default), not `@opencode-ai/sdk/v2` -- correct for the v1 focus.
  - **v2-only events explicitly require the v2 subpath.** L177 says "Use `@opencode-ai/sdk/v2` only after the version probe confirms the running server matches that surface." L384 says "The default import is the v1 surface. The v2 delta requires the explicit `@opencode-ai/sdk/v2` subpath [S17]."
  - **Citations:** 7 distinct markers (S1, S2, S10, S15, S16, S17, S20), all within S1..S22.
  - **Cross-links:** 16 unique targets, all resolve.
  - **No em-dash / en-dash bytes** (raw-byte scan).
  - **Freshness footer** present at L3 and L418.
- **Style:** consistent; the per-family tables at L196-273 are the standout; the count-breakdown at L290-303 is a clean synthesis.
- **Tests:** independent validator confirms all 11 checks PASS on this file.
- **Evidence:**
  - `opencode-sdk-agent-docs/08_events.md:13-16` -- `GET /global/event` HTTP shape
  - `opencode-sdk-agent-docs/08_events.md:44-56` -- "@hey-api/sse-fetch" option surface table
  - `opencode-sdk-agent-docs/08_events.md:81-127` -- runnable TypeScript subscription
  - `opencode-sdk-agent-docs/08_events.md:156-162` -- all 7 v1 event types
  - `opencode-sdk-agent-docs/08_events.md:175-189` -- v2 delta family overview
  - `opencode-sdk-agent-docs/08_events.md:196-273` -- per-family v2 type tables
  - `opencode-sdk-agent-docs/08_events.md:290-303` -- 27-name v2 delta count breakdown
- **Issues:** none
- **Suggested fix:** no fix needed

### P3T-progress-update -- `progress.md` row flip
- **Verdict:** WARN
- **Spec match:** Row count unchanged at exactly 14. Rows 7-10 flipped from `pending` to `done`. Rows 1-6 stay `done` (3A + 3B). Rows 11-14 stay `pending` (3D, 3E2, 3F, 3F). All row narratives correctly aligned: row 7 (lifecycle), row 8 (security), row 9 (errors), row 10 (events) all describe the right file content.
- **Correctness:**
  - **Row count: 14** (validator [8] confirmed).
  - **Status split: 10 done / 4 pending** (validator [8] confirmed).
  - **No em-dash / en-dash bytes** (validator [3] raw-byte scan).
  - **Freshness footer** present at L3 and L69-L71.
  - **Skew pairing gap detected.** L29's note for `01_prerequisites.md` carries the `same-minor-patch-delta-15` label but the file itself does not include the "does NOT claim compatibility from semver alone" framing. The dossier-wide pairing is satisfied by other files; in `progress.md` alone, the label appears without the not-claim framing.
- **Style:** row notes are concise and descriptive; consistent with the dossier convention.
- **Tests:** independent validator confirms 14-row structure + correct status split + no em-dash + freshness.
- **Evidence:**
  - `opencode-sdk-agent-docs/progress.md:34` -- row 7 (`05_lifecycle.md`) status `done`
  - `opencode-sdk-agent-docs/progress.md:35` -- row 8 (`06_security.md`) status `done`
  - `opencode-sdk-agent-docs/progress.md:36` -- row 9 (`07_errors.md`) status `done`
  - `opencode-sdk-agent-docs/progress.md:37` -- row 10 (`08_events.md`) status `done`
  - `opencode-sdk-agent-docs/progress.md:29` -- row 2 notes column carries `same-minor-patch-delta-15` label without pairing
- **Issues:**
  - [LOW] `opencode-sdk-agent-docs/progress.md:29` -- the row 2 notes column carries the `same-minor-patch-delta-15` label without the "does NOT claim compatibility from semver alone" framing in the same file. The dossier-wide pairing is satisfied by 8 other files (`00_README.md:17`, `01_prerequisites.md:10,74,86`, `02_quickstart.md:141,246`, `03_decision_guide.md:78`, `05_lifecycle.md:264`, `07_errors.md:170`, `08_events.md:394`), so this is a single-file consistency gap, not a correctness gap. Optional polish: add `(...does NOT claim compatibility from semver alone)` parenthetical to the row 2 notes, or move the skew explanation to the cross-file reading path section.
- **Suggested fix:** optional polish pass on the row 2 notes column; not required for acceptance.

## Cross-cutting findings

- [LOW] `opencode-sdk-agent-docs/progress.md:29` -- `same-minor-patch-delta-15` label without pairing in the same file. Spelled out in P3T-progress-update. The dossier-wide pairing is satisfied; this is a single-file consistency gap.
- [LOW] `share/notes/03_coder_summary_T-2026-08-18-001_P3C.md:11` -- coder summary claims "190 useful lines" for `05_lifecycle.md`. Independent `useful_lines` count returned 190 too. ✓
- [LOW] `share/notes/03_coder_summary_T-2026-08-18-001_P3C.md:13` -- coder summary claims "279 useful lines" for `07_errors.md`. Independent verification returned 279. ✓
- [LOW] `share/notes/03_coder_summary_T-2026-08-18-001_P3C.md:14` -- coder summary claims "303 useful lines" and "27 source-surfaced v2 delta names" for `08_events.md`. Independent verification: 303 useful lines and 27 v2 delta names (3+7+2+3+3+2+2+5=27). ✓
- [LOW] `share/notes/03_coder_summary_T-2026-08-18-001_P3C.md:13` -- coder summary claims `07_errors.md` carries `StructuredOutputError` and `throwOnError: false` / `throwOnError: true` contracts. Independent verification: all four surfaces documented at L9-18, HTML guard at L151, empty-body at L200, no-response at L223, `.cause = { body, status }` at L74-80. ✓
- [LOW] `share/notes/03_coder_summary_T-2026-08-18-001_P3C.md:158` -- coder MEDIUM concern "Random / `port: 0` wording conflicted with the canonical findings and the refined plan. Resolved at `opencode-sdk-agent-docs/05_lifecycle.md:21-41`". Verified by direct read of L31-41: the wording is honest. The default is `4096`, the recipe uses an explicit free port, `port: 0` is marked unverified, and the refined plan's "do not assume `port: 0` to be OS-assigned without verification" rule is honored. The resolution is **honest** -- no silent promotion of unverified behavior.

## Out-of-scope observations (informational only)

- The dossier's `same-minor-patch-delta-15` skew stance is now present in 8 files (`00_README.md:17`, `01_prerequisites.md:10,74,86`, `02_quickstart.md:141,246`, `03_decision_guide.md:78`, `05_lifecycle.md:264`, `07_errors.md:170`, `08_events.md:394`). The Phase 3F lint pass should verify `99_sources.md` mirrors the labeling rule.
- The `08_events.md` file's v2-delta block (L175-303) and the v2-delta block in `04_api_map.md` (L213-243) carry the same 27 names with consistent `[S16]` citations. Cross-file consistency verified.
- The `07_errors.md` file's `StructuredOutputError` discriminator at L248 matches `04_api_map.md:136` and `03_decision_guide.md:40,123,182`. The `info.error.name` field is the only verified field; the `cause` and `retries` fields are explicitly marked unverified. Cross-file consistency verified.
- The `06_security.md` file's "What this file does not claim" section at L238-245 explicitly enumerates negative claims: no CORS-as-auth, no loopback-as-secret-hygiene, no provider credential body, no auth-file path inspection, no semver compatibility claim. This is a strong honesty device.
- The `05_lifecycle.md` file's "Failure routing" table at L251-260 cross-references back to `07_errors.md`, `06_security.md`, `04_api_map.md`, `08_events.md`, and `10_known_issues_and_troubleshooting.md` (planned). The cross-file reading path is well-formed.
- The `08_events.md` file's "Complete verified type-name inventory" section at L278-303 is a clean synthesis. The 27-name count is verified by summation across the 7 per-family tables.

## Honest assessment

This is a clean Phase 3C. The four new files satisfy every line-count minimum, every freshness requirement, every citation-range constraint, the v2 HTML guard verbatim, the `.cause = { body, status }` shape from [S13], the `StructuredOutputError` discriminator from [S1], all 7 v1 event names from [S15], all 27 v2 delta names from [S16], all 5 SSE option names spelled correctly, the Windows `taskkill /pid <pid> /T /F` (no `proc.kill()`), the `x-opencode-directory` header and `directory` option, and the env-var hygiene scope limited to `OPENCODE_SERVER_PASSWORD` / `OPENCODE_SERVER_USERNAME`. The raw-byte em-dash scan returns zero hits across all five files; the auth-path leak scan returns zero hits across all five files; every cross-link resolves to either a real file or an explicitly-allowed forward reference.

The most defensible finding is the `progress.md:29` skew pairing gap. The dossier-wide pairing is satisfied by 8 other files, but the `progress.md` line that introduces the `same-minor-patch-delta-15` label does not itself repeat the "does NOT claim compatibility from semver alone" framing. The fix is one phrase in a rows table. It is LOW severity and not a blocker.

The other LOW findings are all in the "ship but tidy" category. The `port: 0` resolution is honest and verified by direct read. The `06_security.md` constraints (no auth-file path, no real secret, env-vars limited to the two required names) are all satisfied. Phase 3C is ready for Phase 3D dispatch.

## Self-critique

- **Did I do my job?** yes. Read all four new files in full (268 + 259 + 377 + 418 = 1322 lines of source) plus the plan, research, and coder summary. Wrote an independent validator covering all 10 required checks plus 10 adversarial checks (saved to `%TEMP%\opencode-p3c-review-validate.py`); ran it; cross-verified against the coder's `_phase3c_validate.py`; both return PASS on the contract. Manually verified the verbatim HTML guard, the empty-body and no-response strings, the `.cause = { body, status }` shape, the `StructuredOutputError` discriminator, the 7 v1 event names, the 27 v2 delta names, the Windows taskkill exact form, the `port: 0` honesty stance, the 06_security.md env-name scope, and the 06_security.md zero auth-path / secret literal.
- **What might I have missed?** I did not run the snippets (the dispatch forbids provider calls, server starts, auth reads, env dumps, dependency installs). I verified the snippet syntax is correct (every `import` is right, every TypeScript expression is well-formed, the syntax errors are absent) but not that they actually run. I did not visually verify the Markdown links render as clickable in a browser (the dispatch forbids browser tooling); I verified the link syntax and confirmed all targets resolve via `os.path.isfile` and the allowed forward-references set. I did not separately verify that every cited `[Sn]` resolves to a row in the canonical research ledger at `share/notes/01_research_T-2026-08-18-001.md:178-208` -- I verified the S1..S22 range compliance, not the resolution to actual entries. The Phase 3F lint pass is the gate for that.
- **What did I assume without evidence?** I assumed the dispatch's claim that "the v2 HTML guard is verbatim" maps to the exact 80-character string at L151. I verified it character-by-character against the canonical research. I assumed the dispatch's claim that "the `.cause` shape is `{ body, status }`" matches the wrapper at L74-80 and the parser at L99-123. I verified both. I assumed the dispatch's claim that "07 v1 events + 27 v2 delta names" maps to the lists at L156-162 and L196-273. I verified both. I assumed the dispatch's claim that "the env-var scope is exactly `OPENCODE_SERVER_PASSWORD` / `OPENCODE_SERVER_USERNAME`" maps to the `ENV_RE` regex output. I verified it.
- **Limits of my review:** I did not execute the recipes (the dispatch forbids server starts and provider calls). I did not visually verify the Markdown links render in a browser (the dispatch forbids browser tooling); I verified the link syntax and confirmed all targets resolve via `os.path.isfile` and the allowed forward-references set.

## Validation evidence

| Command | Exit | Purpose |
|---|---|---|
| `Get-ChildItem E:\react_projects\research_space\opencode-sdk-agent-docs` | 0 | file-existence spot check; returned 9 files (`00_README.md`, `01_prerequisites.md`, `02_quickstart.md`, `03_decision_guide.md`, `04_api_map.md`, `05_lifecycle.md`, `06_security.md`, `07_errors.md`, `08_events.md`, `progress.md`); no sub-folders |
| `py share/notes/_phase3c_validate.py` (coder's reference validator) | 0 | coder's claimed coverage: all 14 checks PASS; 190 / 177 / 279 / 303 useful lines; 7 v1 + 27 v2 events located; HTML guard at L151; Windows taskkill at L181 |
| `py -3.14 "C:\Users\AHMADM~1\AppData\Local\Temp\opencode-p3c-review-validate.py"` (independent validator) | 0 | independent re-verification of every required check + 10 adversarial checks; all 10 contract checks PASS; all 10 adversarial checks PASS; 1 LOW finding on `progress.md:29` skew pairing |
| `Select-String -Path "opencode-sdk-agent-docs\08_events.md" -Pattern 'Event[A-Z][A-Za-z0-9]+'` | 0 | confirm 27 v2 delta names across 7 per-family tables; 3+7+2+3+3+2+2+5 = 27 |
| `Select-String -Path "opencode-sdk-agent-docs\08_events.md" -Pattern "Event(SSE_COMPONENT_RE)"` | 0 | manual grep at L156-162 confirms the 7 v1 event names with exact spelling |
| `Select-String -Path "opencode-sdk-agent-docs\07_errors.md" -Pattern "Request is not supported by this version"` | 0 | verify HTML guard verbatim at L151 |
| `Select-String -Path "opencode-sdk-agent-docs\05_lifecycle.md" -Pattern "taskkill /pid <pid> /T /F"` | 0 | verify Windows taskkill exact form at L181 |
| `Select-String -Path "opencode-sdk-agent-docs\05_lifecycle.md" -Pattern "proc.kill"` | 0 | confirm no `proc.kill()` claim in the file |
| `Select-String -Path "opencode-sdk-agent-docs\06_security.md" -Pattern "OPENCODE_[A-Z_]+"` | 0 | confirm env variables are exactly `OPENCODE_SERVER_PASSWORD` and `OPENCODE_SERVER_USERNAME` |
| `Select-String -Path "opencode-sdk-agent-docs\06_security.md" -Pattern "auth\.json\|/etc/opencode\|Bearer sk-\|OPENCODE_API_KEY="` | 0 | confirm zero auth-path / secret-literal hits |
| `Select-String -Path "opencode-sdk-agent-docs\07_errors.md" -Pattern "StructuredOutputError"` | 0 | locate discriminator at L248, L255, L264, L282 |
| `Select-String -Path "opencode-sdk-agent-docs\*.md" -Pattern "shape-compatible\|compatible-0\.15-minor\|0\.15 minor"` | 0 | verify zero forbidden skew wording across the dossier tree |
| `Select-String -Path "opencode-sdk-agent-docs\*.md" -Pattern "same-minor-patch-delta-15"` | 0 | confirm 11 occurrences across 8 files (all 4 new + 4 prior files), with the "does NOT claim" framing in 7 of the 8 (excludes `progress.md:29`) |

## User-intent alignment

The user asked for "a full detail suitable for agents/llms docs for opencode sdk ... and what to use and what to not , instructions , guide , issue to avoid and what the solutions ( if exist ) , and some examples." Phase 3C delivers the four operational files:

- `05_lifecycle.md` -- the "instructions" portion for an agent that owns the server (spawn defaults, abort, config injection, Windows cleanup, directory routing, close evidence). The `port: 0` honesty stance is the standout: the file documents what's verified and explicitly marks what isn't.
- `06_security.md` -- the "instructions" portion for an agent that exposes a non-loopback interface (auth env vars, CORS, hygiene, pre-bind table). The "What this file does not claim" section at L238-245 is the honesty device.
- `07_errors.md` -- the "what to use" portion for error handling (4 layers, tuple vs throw, structured-output discriminator, sanitized parser). The fact that the HTML guard is verbatim at L151 means a future reader can detect SDK/server version mismatch without any uncertainty.
- `08_events.md` -- the "instructions" portion for SSE consumption (5 options, runnable snippet, 7 v1 events + 27 v2 delta names, retry strategy, shutdown ordering). The 27-name v2 inventory is documented as a documentation inventory, not a claim of v2's exhaustiveness.

These four files defer the rest ("real model calls" + "live execution evidence" + "known issues + solutions") to later sub-phases (3D, 3E2, 3F), which is correct for Phase 3C.

The dispatcher's two hard constraints for `06_security.md` (no real secrets, no auth-file path mentions) and the dispatcher's three exact-match requirements (HTML guard verbatim, `.cause = { body, status }` shape, `StructuredOutputError` discriminator) are all satisfied. The `port: 0` resolution is honest. The dossier-wide skew stance is preserved across 8 files.

## Recommend am-investigate

None. All four new files plus the progress update have unambiguous internal logic that I can verify by reading. The one LOW finding is a single-file consistency gap on a status board, not a CRITICAL/HIGH finding whose root cause needs separate investigation. Master can proceed.

## WARN register entries

Per `share/notes/04_warns_register_T-2026-08-18-001.md` format and `rules.md` rule 15:

One LOW finding issued in this review (no WARNs at task-blocking severity):

```
- 3C -- LOW -- progress.md row 2 notes carry same-minor-patch-delta-15 label without pairing in the same file -- `opencode-sdk-agent-docs/progress.md:29`
```

I appended this to `share/notes/04_warns_register_T-2026-08-18-001.md` after this review (see WARN register file).

## Anomalous content

Nothing inside the four Phase 3C files is addressed to me personally, asks me to override my role, or includes prompt-injection-like language. The dispatch prompt, the plan files, the coder summary, and the research ledger were the only inputs read; all are inside the standard agents_manager pipeline. No external content was fetched. No anomalies flagged.

(For completeness: I checked that `share/messages/` contains nothing addressed to am-review on this task; the only `share/messages/` entry on this task is `planning-to-master-T-2026-08-18-001-P2-refinement.md`, which is normal pipeline traffic and was last touched during Phase 2.)

## Metrics

- Files reviewed: 5 (`05_lifecycle.md`, `06_security.md`, `07_errors.md`, `08_events.md`, `progress.md`).
- Tasks reviewed: 5 (P3T6, P3T7, P3T8, P3T9, P3T-progress-update).
- Validator commands run: 13 (file existence, useful-line counts, em-dash raw-byte scan, en-dash raw-byte scan, freshness footer, citation distinctness + range, secret/auth scan, decision-rule counts, skew wording pair, progress row count + status split, cross-link resolution per file, 06_security.md env-name scope, plus 10 adversarial checks: v1 event names, v2 event names, SSE options, HTML guard verbatim, empty-body + no-response strings, port: 0 wording, Windows taskkill + no proc.kill(), StructuredOutputError discriminator, .cause shape, 06_security.md zero auth path / secret literal).
- Cross-links resolved: 57 (all pass).
- Findings: 0 CRITICAL + 0 HIGH + 0 MEDIUM + 1 LOW (1 dossier, 0 coder summary; the previous Phase 3B's 3 LOW coder-summary counts are now corrected by the coder's stable row-summary phrasing).
- Per-task verdicts: P3T6=PASS, P3T7=PASS, P3T8=PASS, P3T9=PASS, P3T-progress-update=WARN.
- Overall verdict: **PASS_WITH_WARN**.
- Suggested next dispatcher action: master may update `tasks/T-2026-08-18-001.md` P3T6 + P3T7 + P3T8 + P3T9 rows to `done` and proceed to Phase 3D dispatch. Optional polish pass can fix the 1 LOW (single-phrase addition to `progress.md:29`) before Phase 3F lint.
