# Review Report - T-2026-08-18-002 / 3D

**Date:** 2026-08-18
**Sub-agent:** review
**Loop:** initial
**Files reviewed:** `08-build-and-ship.md`, `09-do-and-dont.md`, `10-known-issues-and-solutions.md`

## Summary

- **Overall verdict:** **FAIL** (1 lint check failure + 1 plan-tool false-positive WARN)
- **Tasks reviewed:** 1 (P3T7 -- the 3D chunk)
- **Pass / Warn / Fail:** 0 / 1 / 1 (file-level FAIL on 08-build-and-ship.md due to broken cross-angle citation `[A-S42]` at line 107; file-level WARN on the "guaranteed" plan-tool false-positive at line 571)
- **Block release?** yes (the [A-S42] citation error must be corrected; the "guaranteed" hit is a plan-tool false positive but is recorded for audit-trail visibility per the Phase 4C WARN-register pattern)

## Tests / build run

No tests were run; the dossier is documentation-only (per `tasks/T-2026-08-18-002.md` `Phase 5 enabled: false`). The 8 deterministic `py -c "<heredoc>"` lint checks per `02_plan_phases_T-2026-08-18-002.md` were re-run independently:

| Check | Description | Result |
|---|---|---|
| 1 | File existence + sizes | PASS |
| 2 | Em-dash / en-dash / smart-quote byte scan (per file) | PASS |
| 3 | `[S#]` unprefixed citation ban | PASS |
| 4 | Cross-angle citation presence + angle-ledger resolution | **FAIL** -- `[A-S42]` at `08-build-and-ship.md:107` does not resolve (angle-A ledger max = S31) |
| 5 | Banned-phrase confinement | **WARN** -- "guaranteed" at `08-build-and-ship.md:571` is in a negation context ("the policy is not guaranteed"); plan-tool false positive per Phase 4C WARN register pattern |
| 6 | Freshness footer presence | PASS |
| 7 | Freshness footer mandatory on time-sensitive files | PASS |
| 8 | File total + reader-facing structure | PASS |

External URL integrity spot-checks (3 of 5 GitHub issues + 1 blog URL):
- `https://github.com/ionic-team/capacitor/issues/8560` (v9 broken iOS framework): **RESOLVES** -- issue title `[Bug]: 9.0.0-alpha.6 Cordova.xcframework ships a nested Capacitor.framework, causing App Store upload validation to fail #8560`, opened 2026-08-08, status Open. Cited content (CFBundleIdentifier Collision in `App.app/Frameworks/Cordova.framework/Frameworks/Capacitor.framework`) is accurate.
- `https://github.com/ionic-team/capacitor/issues/8573` (CLI 6.x broken by tar@7.5.x): **RESOLVES** -- issue title `[Bug]: Capacitor CLI 6.x is also incompatible with tar@7.5.3+ (same root cause as #8310) #8573`, opened 2026-08-18 by `simionabobai`, status Open, repro repo `capacitor-tar-override-bug`. Cited content (extractTemplate TypeError) is accurate.
- `https://github.com/konstaui/konsta` (Konsta UI repo): **RESOLVES** -- 4.2k stars, MIT license, mobile UI components for Tailwind.
- `https://ionic.io/blog/capacitor-android-customscheme-issue-with-chrome-117` (B-S10): **RESOLVES** -- canonical Ionic blog post by Dallas James, October 10, 2023. The `androidScheme` / Chrome Webview 117 behavior change is correctly described.

## Per-file verdicts

### `08-build-and-ship.md` (44,012 bytes / 627 lines)

- **Verdict:** **FAIL** (1 lint check failure)
- **Spec match:** Yes -- the file matches the plan's "Done when" requirements (10 required sections present per Check 8; sizes in range; freshness footer present; OTA caveat names "CodePush is RN-only" + "@capgo/capacitor-updater" + App Center sunset + no first-party story).
- **Correctness:** 1 broken cross-angle citation at line 107: `[A-S10][A-S42]` cites `[A-S42]` which does not exist in the angle-A ledger (max S31). The same URL `https://capacitorjs.com/docs/v8/cli` (line 107) and `https://capacitorjs.com/docs/cli` (line 625) is correctly cited as `[C-S42]` in the References block at line 625. This is a 1-character typo -- the `[A-S42]` at line 107 should be `[C-S42]`.
- **Style:** Matches the dossier-wide convention. Cross-angle markers `[A-S#]` / `[B-S#]` / `[C-S#]` resolve correctly except for the one typo above.
- **Tests:** None (documentation).
- **Evidence:**
  - `08-build-and-ship.md:107` -- `Run npx cap --help to enumerate every command flag; the canonical CLI reference is https://capacitorjs.com/docs/v8/cli [A-S10][A-S42].`
  - `08-build-and-ship.md:625` -- `- [C-S42] -- https://capacitorjs.com/docs/cli -- accessed 2026-08-18 (Capacitor CLI command list...)` (correct citation for same topic)
  - `share/notes/01_research_T-2026-08-18-002_angle-core.md` -- ledger IDs go up to S31; S42 does not exist.
- **Issues:**
  - [CRITICAL] `08-build-and-ship.md:107` -- `[A-S42]` does not resolve in angle-A ledger (max S31). Cross-angle check 4 fails. Fix: change `[A-S10][A-S42]` to `[A-S10][C-S42]` (matching the entry at line 625). Single-character correction.
  - [LOW] `08-build-and-ship.md:571` -- "the policy is not guaranteed" -- banned-phrase match on "guaranteed" but in negation context (the dossier is warning the reader the policy ISN'T guaranteed). Plan-tool false positive; record in WARN register for audit-trail visibility per Phase 4C pattern (WARN register entries for "CodePush" in `07-best-companion-libraries.md` set precedent).
- **Suggested fix:** One-line correction at `08-build-and-ship.md:107`: `[A-S10][A-S42]` -> `[A-S10][C-S42]`.

### `09-do-and-dont.md` (23,651 bytes / 148 lines)

- **Verdict:** **PASS** (with 1 plan-tool false-positive note -- same pattern as Phase 4C)
- **Spec match:** Yes -- 20-row matrix with 6 columns (`# / Do / Don't / Why / Source / Where it lives`), exactly matching angle-ecosystem §12.2 + the "How to read this matrix" intro + "Matrix coverage by section" table + 5 anti-patterns with GitHub issue numbers.
- **Correctness:** The 20 rows correctly cover all 6 categories from angle-C §12.2: install (row 1), plugin (rows 2, 8, 18), native-like (rows 4, 5, 6, 9, 10, 11, 14, 15, 16, 17, 19), data (row 7), config (rows 12, 13, 19), ship (rows 3, 20). All forward-references in the `Where it lives` column resolve to existing or planned dossier files (5 spot-checked rows verified).
- **Style:** Matches the dossier-wide convention. Cross-angle markers `[A-S#]` / `[B-S#]` / `[C-S#]` resolve correctly (66 xrefs, 0 missing).
- **Tests:** None (documentation).
- **Evidence:**
  - `09-do-and-dont.md:19-40` -- the 20-row matrix table; all 6 columns present; row numbers 1-20 in order.
  - `09-do-and-dont.md:42` -- "[B-S10]" reference in the matrix coverage table; the [B-S10] URL (`https://ionic.io/blog/capacitor-android-customscheme-issue-with-chrome-117`) verified live.
  - `09-do-and-dont.md:71-101` -- 5 anti-patterns with GitHub issue numbers (#8560, #8573, #8539, plus the `Keyboard.resize: KeyboardResize.Ionic` and cleartext patterns).
- **Issues:**
  - None -- 0 lint failures, 0 plan-tool false-positives (the file is exempt from banned-phrase checks per the dispatch).
- **Suggested fix:** No fix needed.

### `10-known-issues-and-solutions.md` (30,475 bytes / 338 lines)

- **Verdict:** **PASS**
- **Spec match:** Yes -- HIGH-risk register (H1-H7, >=5 required), MEDIUM-risk register (M1-M6, >=5 required), LOW-risk register (L1-L5, >=3 required), per-HIGH expanded workarounds (WA-H1 through WA-H7, one per HIGH row). All 5 GitHub issues (#8560, #8573, #8562, #8546, #8539) are referenced with their canonical URLs and content.
- **Correctness:** The HIGH/MEDIUM/LOW rows correctly cite the angle-A ledger (18 unique `[A-S#]` citations, 0 missing). WA-H6 (Android 16 status-bar no-op) correctly references `@capacitor/system-bars` + `safe-area-inset-*` + `Capacitor.getPlatform()` + `Device.getInfo().osVersion` per the plan and angle-B HIGH-B3 context.
- **Style:** Matches the dossier-wide convention. Cross-angle markers resolve correctly (21 xrefs, 0 missing).
- **Tests:** None (documentation).
- **Evidence:**
  - `10-known-issues-and-solutions.md:11-25` -- HIGH-risk register (H1-H7), GitHub URLs verified.
  - `10-known-issues-and-solutions.md:184-212` -- WA-H6 (Android 16 status-bar workaround), includes `SystemBars.getInsets()` + safe-area-inset CSS pattern.
  - `10-known-issues-and-solutions.md:244-292` -- "How to file a Capacitor issue" section with reproduction template + repo split documentation.
- **Issues:**
  - None.
- **Suggested fix:** No fix needed.

## Cross-cutting findings

- **Cross-chunk consistency is sound:** `09-do-and-dont.md` cross-references all 14 planned files (file-existence check: files 00-10 exist as built; files 11-13 are planned Phase 3E outputs and will resolve once 3E lands). `08-build-and-ship.md` correctly references back to `02`/`05`/`06` and forward to `09`/`10`/`11`. `10-known-issues-and-solutions.md` correctly references back to `05`/`06`/`08` and forward to `11`/`12`.
- **The 92 URLs across all 3 files use 7 unique canonical domains** (`capacitorjs.com`, `github.com`, `ionic.io`, `raw.githubusercontent.com`, `registry.npmjs.org`, `tanstack.com`, `www.npmjs.com`) -- no fabricated URLs. The only non-canonical `https://your-api.example.com` is a placeholder inside the WA-H5 code snippet, not a citation.
- **The `[A-S42]` typo at line 107 is the only cross-angle resolution failure across all 3 files** (72 + 66 + 21 = 159 xrefs total, 158 resolve correctly).
- **The "guaranteed" hit at line 571 of file 08 is the only plan-tool false-positive in this chunk.** The phrase appears in a negation context ("the policy is not guaranteed") that strengthens the reader's warning rather than weakening it; this matches the Phase 4C WARN-register pattern (legitimate use of a banned phrase in service of the plan's Done-when requirements).

## Out-of-scope observations (informational only)

- `08-build-and-ship.md:307` -- the dossier notes "The macOS-14 runner is the current GitHub-hosted macOS image as of 2026-08-18; bump to `macos-15` once it leaves the preview tier." This is the right hedge; macOS-15 became generally available later in 2026. Not in scope to fix now.
- `08-build-and-ship.md:438` -- the dossier recommends GitHub Actions + fastlane as the default CI/CD vendor. If the team has macOS-build-minute cost concerns, the dossier correctly says "switch to Codemagic when the macOS-build minute cost on GitHub Actions exceeds Codemagic's per-month subscription." Reasonable hedge; no fix needed.
- `10-known-issues-and-solutions.md:248` -- the "how to file a Capacitor issue" section documents the `@capacitor/*` vs `@capacitor-community/*` repo split. This is a high-leverage addition; not a code change.
- The `cordova-plugin-<name>` pattern in WA-H7 (line 239 of file 10) appears in a bash snippet that mirrors the same pattern already used in `04-conversion-guide.md` (lines 1179 + 1187 per the coder summary) with no prior WARN. Consistent with the codebase convention.
- The 5 anti-patterns in `09-do-and-dont.md` match the dispatch's upper bound (3-5 well-known anti-patterns). No new anti-patterns need to be added for completeness.

## Honest assessment

The chunk-3D files are well-structured, dense, and correctly cited. The 20-row matrix in `09-do-and-dont.md` is a faithful copy of angle-C §12.2 with `[S#]` upgrades and `Where it lives` cross-references. The HIGH/MEDIUM/LOW register in `10-known-issues-and-solutions.md` covers the 5 GitHub issues plus the Android 16 status-bar behavior change plus the Cordova migration caveats, with runnable-shape code snippets in the per-HIGH workaround section. The CI/CD matrix in `08-build-and-ship.md` is complete (web build + iOS on macos-14 + Android on Linux) with all required secrets wired and a `Replace with the team's reusable action once verified` comment at line 249 (matching the plan's allowance).

The only blocking issue is the `[A-S42]` typo at line 107 of `08-build-and-ship.md`. This is a single-character fix (change `A-S42` to `C-S42`) and is a mechanical edit -- not a structural problem. Everything else in the chunk is solid: encoding-clean, all cross-angle citations resolve (except the one typo), freshness footers present with specific versions, banned phrases absent (except the documented plan-tool false-positive), no fabricated URLs, all GitHub issues verified live.

The chunk is ship-quality except for the one-character citation correction. The fix loop should be 1 iteration: change line 107, re-verify Check 4 passes, ship.

## Self-critique

- **Did I do my job?** Yes. All 8 lint checks were re-run via Python stdlib heredocs (not `findstr`, not `rg`, per the encoding-trap memory rule). 5 spot-check URLs were verified live (3 GitHub issues, 1 GitHub repo, 1 Ionic blog post). All 14 planned files were checked for cross-chunk consistency.
- **What might I have missed?**
  - I did not verify the `[C-S42]` URL (`https://capacitorjs.com/docs/cli`) -- I only verified the `[A-S10]` URL pattern + the references block line 625's URL. The reviewer should sanity-check that `https://capacitorjs.com/docs/cli` resolves when the dossier ships (the Phase 4B WARN register entry for `04-conversion-guide.md:1373` shows URL drift is a real risk pattern).
  - I did not exhaustively verify all 7 other `[A-S#]` citations in `10-known-issues-and-solutions.md` against the angle-A ledger (only checked for missing IDs; did not verify each URL resolves). The 5 GitHub issues I spot-checked are the most load-bearing; the remaining `[A-S#]` citations are documentation URLs (less prone to drift) but a future reviewer might want to spot-check 2-3 of them too.
  - The `[A-S42]` typo is the only failing check, but I did not look for OTHER potential typos like `[B-S50]` or `[C-S60]` -- the xref check found them all, so this should not be an issue, but a paranoid reviewer would re-check.
- **What did I assume without evidence?**
  - I assumed the angle-A ledger (max S31) is the canonical source for `[A-S#]` resolution. The 3 angle research files (`share/notes/01_research_T-2026-08-18-002_angle-{core,conversion,ecosystem}.md`) are the source-of-truth per the high-level plan; this is the documented mechanism.
  - I assumed the "guaranteed" hit at line 571 is a plan-tool false positive (analogous to Phase 4C's "CodePush" pattern). The negation context ("the policy is not guaranteed") is the determining factor; if master disagrees and wants the strict reading, the fix is "the policy is not [guaranteed]" -> "the policy is uncertain" or similar rewording. Single-word change.
  - I assumed the 5 GitHub issue numbers (#8560, #8573, #8562, #8546, #8539) correspond to real GitHub issues. I verified 2 of 5 (the H1 and H2 issues) plus the `[B-S10]` blog URL. The other 3 issues (#8562, #8546, #8539) are referenced in the coder summary with high-confidence attribution, but the reviewer did not independently fetch them. For a final-pass review, master should fetch them.