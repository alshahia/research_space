# Review Report - T-2026-08-18-001 / Phase 3A / fix loop 1 of 3

**Date:** 2026-08-18 (Asia/Baghdad)
**Sub-agent:** am-review
**Loop:** re-review 1 of max 3
**Reviewing:** P3T1 (carried from initial), P3T2 (re-reviewed independently)
**Source files:**
- `opencode-sdk-agent-docs/00_README.md` (11634 bytes, 117 lines)
- `opencode-sdk-agent-docs/01_prerequisites.md` (12207 bytes, 147 lines)
- `opencode-sdk-agent-docs/progress.md` (6597 bytes, 71 lines)

## Summary

- **Overall verdict:** PASS
- **Tasks reviewed:** 2
- **Pass / Warn / Fail:** 2 / 0 / 0
- **Block release?** no
- **All four original CRITICAL/LOW findings resolved.** Independent validator re-run confirms Phase 3A "Done when" baseline checks all pass; raw-byte em-dash scan returns 0 hits across the dossier tree; stricter secret/auth-path scan returns clean.

## Tests / build run

No test framework was added by Phase 3A. Per the dispatch contract, the smallest validation is a UTF-8-safe Python validator covering file existence, useful-line counts, required fragments, citation distinctness + S-range, freshness footer, raw-byte em-dash scan, and secret/auth-path scan. Two independent validators were run for this re-review:

| Command | Exit | Purpose |
|---|---|---|
| `py -3.14 C:\Users\AHMADM~1\AppData\Local\Temp\opencode-p3a-fix1-validate.py` (coder's fix1 validator, broader pattern set incl. `chmod`) | 0 | coder's claimed coverage; flags `chmod` as false positive in legitimate "never chmods" prose |
| `py -3.14 C:\Users\AHMADM~1\AppData\Local\Temp\opencode-p3a-fix1-review-validate.py` (independent re-review validator, stricter auth-path patterns) | 0 | independent re-verification of every required check |

Both validators ran in UTF-8 mode (`encoding="utf-8"`) and scanned raw bytes for the em-dash sequence `E2 80 94` (the Windows-1252 false-positive trap is avoided).

### Independent validator output (verbatim)

```
[1] EXISTENCE: {'00_README.md': True, '01_prerequisites.md': True, 'progress.md': True}
[2] USEFUL LINES: {'00_README.md': 83, '01_prerequisites.md': 101, 'progress.md': 53}
[3] EM-DASH (UTF-8 raw bytes E2 80 94): {'00_README.md': 0, '01_prerequisites.md': 0, 'progress.md': 0}
    EN-DASH (UTF-8 raw bytes E2 80 93): {'00_README.md': 0, '01_prerequisites.md': 0, 'progress.md': 0}
[4] FRESHNESS FOOTER: {'00_README.md': True, '01_prerequisites.md': True, 'progress.md': True}
[5] DISTINCT CITES (sorted): {'00_README.md': [1, 2, 3, 17], '01_prerequisites.md': [2, 3, 10, 17], 'progress.md': [1, 22]}
[6] DROPPED FROM LEDGER (must be empty): []
[7] SECRET/AUTH SCAN: clean across all three files (no auth.json, no .local/share/opencode, no share/opencode/auth, no %LOCALAPPDATA%\opencode, no /etc/opencode, no Bearer sk-, no OPENCODE_API_KEY=, no sk-[A-Za-z0-9]{20,})
[8] README Markdown links found: 26 (targets cover all 14 planned files)
    MISSING FROM README LINKS (must be empty): []
[9] SHAPE-COMPATIBLE HITS (must be empty): []
[10] VERSION WORDING:
     - a_priori_negation (does NOT treat...as compatible a priori): True
     - coord_label_present (same-minor-patch-delta-15): True
     - position_not_verdict (coordinate label...not a compatibility verdict): True
[11] PROGRESS ROW WORDING:
     - row12_mentioned ('currently row 12'): True
     - old_14th_removed (no '14th row'): True
     - table row 12 file: 11_live_validation.md
[12] RUNNER CAVEAT PRESENT: True
[13] PROGRESS TABLE: 14 rows (expected 14), 3 done / 11 pending (expected 3/11)
[14] MD LINK DISTRIBUTION: Path A 13, Path B 12 (line 59 has 3 links), line 9 1 -> 26 total
```

All assertions passed; `ALL VALIDATIONS PASSED`.

### Markdown link coverage cross-verification

The dispatch contract requires that **relative Markdown links cover every planned dossier file and render as clickable links; missing set empty**. Independent verification:

```
Targets covered by 00_README.md links (basename set, 14 entries):
  00_README.md (self, x2)
  01_prerequisites.md
  02_quickstart.md
  03_decision_guide.md
  04_api_map.md
  05_lifecycle.md
  06_security.md
  07_errors.md
  08_events.md
  09_examples.md
  10_known_issues_and_troubleshooting.md
  11_live_validation.md
  99_sources.md
  progress.md (line 9)

Missing set (planned files NOT covered by an .md link): []
```

All 14 planned files are reachable from `00_README.md` via Markdown links. Self-links to `00_README.md` (line 32 "this file", line 50) render as expected in GitHub-flavored Markdown (anchor jump to top of file).

### Link-count prose discrepancy in fix summary (informational)

The fix summary states two slightly inconsistent numbers:
- Summary table: "Path A 13 entries + Path B 10 entries + line 9 progress.md reference" = 24
- Validator prose: "README MD LINKS extracted: 26 (Path A 14 + Path B 11 + line 9 progress.md)"

The actual link count is 26, confirmed independently. The Path B section has 10 numbered items (lines 50-59), but line 59 contains three links (`10_known_issues_and_troubleshooting.md`, `11_live_validation.md`, `99_sources.md`) packed into one numbered item, giving 12 links in Path B. The two prose counts are both slightly off; the total of 26 is correct. This is a prose inconsistency in the coder summary, not a defect in the dossier itself.

## Per-task verdicts

### P3T1 - Create `opencode-sdk-agent-docs/` folder
- **Verdict:** PASS (carried from initial review; folder still present)
- **Spec match:** folder created at exactly `opencode-sdk-agent-docs/` with no sub-folder; matches Plan §Approach and Phase 3A deliverables.
- **Correctness:** `Get-ChildItem -Path E:\react_projects\research_space\opencode-sdk-agent-docs -Force` returned exactly the three files: `00_README.md`, `01_prerequisites.md`, `progress.md`. No stray files; no sub-directories.
- **Style:** n/a
- **Tests:** existence check passed (independent validator `[1]`); file timestamps show the three files were written today (2026-08-18 10:45) and have NOT been re-touched since the fix loop (LastWriteTime identical for `01_prerequisites.md` and `progress.md`, within seconds of each other; `00_README.md` ~6 seconds earlier).
- **Evidence:**
  - `opencode-sdk-agent-docs/` (directory present; no sub-folders)
  - `share/notes/03_coder_summary_T-2026-08-18-001_P3A_fix1.md:12` (P3T1 row carries over from initial review)
  - `tasks/T-2026-08-18-001.md` P3T1 row (Phase 3A `todo` column to be updated by master after this review)
- **Issues:** none
- **Suggested fix:** no fix needed

### P3T2 - Write `00_README.md`, `01_prerequisites.md`, `progress.md`
- **Verdict:** PASS
- **Spec match:** every Phase 3A "Done when" line-count + fragment item passes independently (line minimums met, freshness footer present, all 14 progress rows present, 3 done/11 pending status mix correct, raw-byte em-dash scan returns 0, secret/auth-path scan returns clean). All four original CRITICAL/LOW findings from `share/reports/04_review_T-2026-08-18-001_P3A.md` are resolved with cited new code.
- **Correctness:**
  - **Finding 1 resolved (CRITICAL).** `opencode-sdk-agent-docs/00_README.md:9,32-44,50-59` now contains 26 relative Markdown links of the form `` [`NN_xxx.md`](NN_xxx.md) `` (visible filename preserved in code formatting per the dispatch example). All 14 planned dossier files are reachable from the README; missing set is empty. The Phase 3A writer constraint "`00_README.md` must ... link to the relevant subset of files for each [path]" is satisfied for both Path A (long-lived agent) and Path B (existing-server client).
  - **Finding 2 resolved (CRITICAL).** `opencode-sdk-agent-docs/01_prerequisites.md:10` was rewritten to deny a-priori compatibility for any 1.18.x CLI and to position `same-minor-patch-delta-15` as a coordinate label, not a compatibility verdict. `shape-compatible` is absent across all three files (grep confirms zero hits in `opencode-sdk-agent-docs/`). The plan's §Approach item 14 + R-NEW-4 honesty stance is upheld.
  - **Finding 3 resolved (LOW).** `opencode-sdk-agent-docs/progress.md:60` now reads "The live-validation row (`11_live_validation.md`, currently row 12) flips from `pending` to `done` only after Phase 3E2's `am-review` PASS lands." The narrative matches the table: table row 12 is `11_live_validation.md` (verified via `re.findall` of pipe-delimited rows). Old "14th row" wording removed.
  - **Finding 4 resolved (LOW).** `opencode-sdk-agent-docs/01_prerequisites.md:90` now carries the runner caveat: "Runner note: the `with: { type: "json" }` import-attribute form requires Node 22+ or a current Bun/tsx build. Older runners may need their own supported JSON-import form. If the snippet fails on your runner, swap to that runner's documented form rather than rewriting the dossier." The caveat is honest: it does not overstate exact older-version syntax, it scopes the supported form to Node 22+ / current Bun / tsx, and it points readers at their runner's documentation for the legacy alternative.
- **Style:** reading-map Path A (13 numbered entries) and Path B (10 numbered entries) are visually consistent with the prior dossier style; freshness footer consistent across all three files; citations limited to S1..S22 (no invented markers; all cited markers resolve to the canonical research ledger `share/notes/01_research_T-2026-08-18-001.md`).
- **Tests:** independent validator confirms all assertions pass (see `## Tests / build run` above).
- **Evidence:**
  - `opencode-sdk-agent-docs/00_README.md:9` (line 9 link to `progress.md`)
  - `opencode-sdk-agent-docs/00_README.md:32-44` (Path A reading map, 13 Markdown links)
  - `opencode-sdk-agent-docs/00_README.md:50-59` (Path B reading map, 10 numbered items, 12 links because line 59 packs three)
  - `opencode-sdk-agent-docs/01_prerequisites.md:10` (Local CLI assumption rewritten; `shape-compatible` removed; `same-minor-patch-delta-15` presented as coordinate label)
  - `opencode-sdk-agent-docs/01_prerequisites.md:90` (runner caveat for Stage-3 `with: { type: "json" }` import-attribute)
  - `opencode-sdk-agent-docs/progress.md:60` (live-validation row narrative aligned to row 12)
  - `share/notes/01_research_T-2026-08-18-001.md ## Citation ledger` (canonical [S1]..[S22] sources; all citations in dossier resolve here)
- **Issues:** none new. (See `## Cross-cutting findings` for non-blocking observations.)
- **Suggested fix:** no fix needed

## Cross-cutting findings

- [LOW] `opencode-sdk-agent-docs/00_README.md` carries two self-links to `00_README.md` (line 32 "this file" and line 50 in Path B). These are valid Markdown and render as expected in GitHub-flavored Markdown (line 32 renders as an in-page anchor; line 50 is a re-introduction for Path B readers). A future polish pass could remove the line 32 self-link since it is on a path that already starts with this file, but this satisfies the "navigable" contract and is not a defect.
- [LOW] `share/notes/03_coder_summary_T-2026-08-18-001_P3A_fix1.md:19` and `:29` give two slightly inconsistent per-section link counts ("Path A 13 + Path B 10 = 24" in the file-table prose vs "Path A 14 + Path B 11 = 26" in the validator-output prose). The actual total of 26 links is correct and independently verified; both prose breakdowns are mildly off (Path A has 13, Path B has 12 because line 59 packs three links). Informational only; not a dossier defect.

## Out-of-scope observations (informational only)

- The `chmod` substring appears twice in the dossier tree (`01_prerequisites.md:120` and `progress.md:18`), both inside negative-discipline prose ("never chmods one", "no path / mode / chmod inspection"). The original Phase 3A review's stricter secret/auth-path scan returned clean (no `auth.json`, no `.local/share/opencode`, no `share/opencode/auth`, no `Bearer sk-`, no `OPENCODE_API_KEY=`). The fix1 coder validator uses a broader pattern set that includes the bare word `chmod` and flags these two hits as false positives. The independent re-review validator uses the original review's stricter pattern set and returns clean. Both occurrences are documented commitments NOT to chmod any auth-file path.
- The dossier correctly defers `11_live_validation.md`, `10_known_issues_and_troubleshooting.md`, and `99_sources.md` to later phases (3E2 and 3F) and correctly tracks them as `pending` in `progress.md`. Phase 3A's foundation is complete: a reader can navigate the dossier, install the CLI + SDK, record the version skew, and probe the server before any real call.

## Honest assessment

This is a clean fix. The coder addressed all four findings from the initial Phase 3A review directly and at the exact lines the user dispatch specified, and the resulting files pass every Phase 3A baseline check. The fixes are surgical and honest: line 10 now denies compatibility a priori instead of using the unsupported word `shape-compatible`; the README reading maps are now navigable; the progress narrative now matches the actual table; the JSON import-attribute snippet carries an honest runner caveat. The only minor noise is a couple of false-positive `chmod` hits in the coder's broader scan (both in legitimate "never chmods" prose) and a slightly inconsistent per-section link count in the coder summary's prose (the total of 26 is correct). Phase 3A's foundation is solid; P3T2 is ready to ship.

## Self-critique

- **Did I do my job?** yes. Read the actual files (not just the fix summary), wrote an independent validator that covers all four original findings and every Phase 3A baseline check, ran both the coder's fix1 validator and my own, and cross-verified with `Get-ChildItem` and `git status --porcelain`.
- **What might I have missed?** I did not re-run `rg` against `share/notes/*.md` for `shape-compatible` to confirm the term is absent from the wider share tree (the original review did this and reported zero hits outside the dossier tree). The new fix removes `shape-compatible` from line 10 of `01_prerequisites.md`; I verified it is absent across the three dossier files but did not re-scan `share/notes/`. Not material for this re-review since the dossier is the deliverable.
- **What did I assume without evidence?** None that affects the verdict. I assume the coder's claim that the version-snippet runner caveat text is unchanged from the validator's stored `runner_marker` string ("Runner note: the `with: { type: \"json\" }`") — verified by substring search of the actual file. I assume the research ledger in `share/notes/01_research_T-2026-08-18-001.md` is the canonical citation source for S1..S22 — verified by `cite_pat.finditer` matching the cited markers to the ledger.
- **Limits of my review:** I did not visually verify the Markdown links render as clickable in a browser because the dispatch forbids browser tooling. I verified the link syntax (`[`text`](target.md)`) and confirmed the targets are valid filenames in the dossier tree, which is sufficient to guarantee renderability in GitHub-flavored Markdown.

## Validation evidence

| Command | Exit | Purpose |
|---|---|---|
| `Get-ChildItem E:\react_projects\research_space\opencode-sdk-agent-docs -Force` | 0 | file-existence spot check; returned exactly three files (`00_README.md`, `01_prerequisites.md`, `progress.md`); no sub-folders |
| `git -C E:\react_projects\research_space status --porcelain` | 0 | only `?? opencode-sdk-agent-docs/` outside the Aug-16 unrelated untracked files baseline; no commits; no other tracked-file changes |
| `git -C E:\react_projects\research_space log --oneline -5` | 0 | single commit `fb0ebff Initial commit: research-first agents-manager controller + 11 historical outputs`; no commits required from this review |
| `py -3.14 "C:\Users\AHMADM~1\AppData\Local\Temp\opencode-p3a-fix1-validate.py"` | 0 | ran the coder's fix1 validator (broader pattern set incl. `chmod`); 26 MD links confirmed; `shape-compatible` 0 hits; version wording checks pass; progress row wording checks pass; runner caveat present |
| `py -3.14 "C:\Users\AHMADM~1\AppData\Local\Temp\opencode-p3a-fix1-review-validate.py"` | 0 | ran the independent re-review validator (stricter auth-path patterns matching the original Phase 3A review); all 14 checks pass; 0 em-dash bytes; 0 en-dash bytes; 14 progress rows; 3 done / 11 pending; 26 MD links covering all 14 planned files |
| `grep chmod opencode-sdk-agent-docs/` | 0 | confirmed the two `chmod` hits are in legitimate "never chmods" prose (`01_prerequisites.md:120`, `progress.md:18`), not actual chmod operations |

## User-intent alignment

The user asked for "a full detail suitable for agents/llms docs for opencode sdk ... and what to use and what to not , instructions , guide , issue to avoid and what the solutions ( if exist ) , and some examples." Phase 3A's three files deliver the foundation: the reading map + verdict + scope + reading paths in `00_README.md`; the CLI/SDK install matrix + version probe + secure provider-presence discipline in `01_prerequisites.md`; and the live status board in `progress.md`. They defer the rest ("9 safe + 5 unsafe + 2 verified examples" + "known issues + solutions" + the bounded live-validation evidence) to later sub-phases (3B, 3C, 3D, 3E2, 3F), which is correct for the foundation phase.

The CRITICAL/LOW fixes from the initial review make this foundation MORE aligned with user intent: navigation now works on GitHub-flavored Markdown, and the version-skew honesty stance (`same-minor-patch-delta-15` as a coordinate label, not a compatibility verdict) holds across the dossier.

## Recommend am-investigate

None. All four original findings have unambiguous root causes that have been resolved cleanly inside the writer's text choices. No CRITICAL/HIGH finding whose root cause needs separate investigation remains.

## WARN register entries

Per `share/notes/04_warns_register_T-2026-08-18-001.md` format and `rules.md` rule 15:

No new WARNs issued in this re-review. The previous Phase 3A entries in the WARN register (the four CRITICAL + LOW findings from the initial review) are now resolved by the fix loop; the master may close them out. This review did not edit the register; status note for master's close-out:

```
- 3A -- CRITICAL -- RESOLVED -- Reading-map filenames are clickable Markdown links; missing set empty -- `opencode-sdk-agent-docs/00_README.md:9,32-44,50-59`
- 3A -- CRITICAL -- RESOLVED -- `shape-compatible` removed; line 10 denies compatibility a priori and positions `same-minor-patch-delta-15` as coordinate label -- `opencode-sdk-agent-docs/01_prerequisites.md:10`
- 3A -- LOW -- RESOLVED -- Live-validation row narrative aligned to row 12 -- `opencode-sdk-agent-docs/progress.md:60`
- 3A -- LOW -- RESOLVED -- JSON import-attribute snippet carries honest Node 22+ / current Bun / tsx runner caveat -- `opencode-sdk-agent-docs/01_prerequisites.md:90`
```

## Anomalous content

Nothing inside the three Phase 3A files is addressed to me personally, asks me to override my role, or includes prompt-injection-like language. The dispatch prompt and the plan files were the only inputs read; both are inside the standard agents_manager pipeline. No external content was fetched. No anomalies flagged.

(For completeness: I checked that `share/messages/` contains nothing addressed to am-review on this task; the only `share/messages/` entry on this task is `planning-to-master-T-2026-08-18-001-P2-refinement.md`, which is normal pipeline traffic and was last touched during Phase 2.)

## Metrics

- Files reviewed: 3 (`00_README.md`, `01_prerequisites.md`, `progress.md`).
- Tasks reviewed: 2 (P3T1, P3T2).
- Validator commands run: 6 (existence, useful-line counts, em-dash raw-byte scan, en-dash raw-byte scan, freshness, citation distinctness + range + ledger-resolution, secret/auth scan, MD link extraction + planned-file coverage, `shape-compatible` grep, version-wording check, progress row-12 alignment, runner-caveat presence, MD link distribution by section).
- Markdown links found in `00_README.md`: 26 (Path A 13, Path B 12, line 9 1). Coverage: all 14 planned files; missing set empty.
- Findings: 0 CRITICAL + 0 HIGH + 0 MEDIUM + 0 LOW issues. (Two LOW cross-cutting informational observations; no new WARNs.)
- Original findings resolved: 4 of 4 (2 CRITICAL + 2 LOW).
- Per-task verdicts: P3T1=PASS (carried), P3T2=PASS (re-reviewed).
- Overall verdict: **PASS**.
- Suggested next dispatcher action: ready to ship Phase 3A; master may update `tasks/T-2026-08-18-001.md` P3T1 + P3T2 rows to `done` and proceed to Phase 3B dispatch.