# Review Report — T-2026-08-13-002 / Tier 4 (Phase 8)

**Date:** 2026-08-13 11:55
**Sub-agent:** review
**Loop:** initial

## Summary
- **Overall verdict:** PASS_WITH_WARN
- **Tasks reviewed:** 7
- **Pass / Warn / Fail:** 5 / 2 / 0
- **Block release?** no (WARN-level issues; harness can ship, polish next round)

## Tests / build run

- `py scripts/validate-frontmatter.py agents_manager/research/WORKFLOW.md agents_manager/review/SKILL.md agents_manager/eval/golden-tests/01_arxiv_topic.md agents_manager/eval/golden-tests/02_web_topic.md agents_manager/eval/golden-tests/03_comparison_topic.md agents_manager/eval/README.md` — exit 0; 6/6 files OK (all in `lenient` mode; none under a `skills/` segment, so strict name-regex + parent-name-match rules are skipped).
- `bash scripts/run-research-eval.sh --help` — **could not execute** in this Windows sandbox. `bash` is not on PATH (`where bash` returns nothing; this is a `cmd.exe`/PowerShell env, no WSL/GitBash/MinGW). The coder also could not execute it. The script was verified by reading only. PowerShell equivalent also unrunnable in CI but readable.
- Frontmatter validator pre-existing unpack bug surfaced by coder at `scripts/validate-frontmatter.py:73-74, 119` — confirmed by reading: `validate_one` returns bare `[str(e)]` when `parse_frontmatter` raises, but `main` expects `(errs, mode)`. This is a real `IndexError` on files missing the frontmatter start-marker. Not in scope for this task; surfaced for the maintainer.

## Per-task verdicts

### P8T1 — Multi-agent research workflow doc
- **Verdict:** PASS
- **Spec match:** File at `agents_manager/research/WORKFLOW.md` (202 lines). Documents the orchestrator + parallel sub-agents + synthesis + verifier + master sign-off pipeline as a **5-stage pattern**, not 6 (see issues).
- **Correctness:** Workflow lines up with `share/notes/02_plan_phases_T-2026-08-13-002.md:118-128` (Phase 8 deliverables). Diagram at lines 24-45 matches the prose stages.
- **Style:** Mirrors the cloudflare synthesis style mentioned in line 11 (cloudflare synthesis is read-only historical).
- **Tests:** No test build; document quality is judged by content.
- **Evidence:** `agents_manager/research/WORKFLOW.md:21-45` (diagram), `:47-138` (5 stages), `:186-194` (reference table), `:188` ([S1] = Anthropic URL — confirmed live by webfetch 2026-08-13).
- **Issues:**
  - [MEDIUM] `agents_manager/research/WORKFLOW.md:21` says "The five-stage pattern" but the dispatch prompt claimed a "6-step pattern (orchestrator → decomposition → parallel research → synthesis → verification → final)". The dispatch miscounted; the file matches the plan (also 5 stages: orchestrator, parallel, synthesis, verifier, final). Decomposition is folded into Stage 1. The file is correct; the dispatch was loose. No action on the file.
  - [LOW] `agents_manager/research/WORKFLOW.md:188` cites Anthropic "Building a multi-agent research system" — actual title is "How we built our multi-agent research system" (https://www.anthropic.com/engineering/multi-agent-research-system, confirmed via webfetch 2026-08-13). URL is correct; title is paraphrased. Minor.
  - [LOW] `agents_manager/research/WORKFLOW.md:190` cites `agents_manager/SKILL.md:755-803` as the research-detector section. Verified by reading: line 755 = `## Research-detector (Tier 1+, 2026-08-13)`; line 803 is the closing line of that section. Accurate.
  - [LOW] `agents_manager/research/WORKFLOW.md:192` cites `agents_manager/research/SKILL.md:387-409` as the Source-connector protocol. Verified: `agents_manager/research/SKILL.md` is 502 lines total; lines 386/408 exist (within range). Directionally accurate.
- **Suggested fix:** no fix needed; dispatch's 6-step count was off but the file is internally consistent with the plan.

### P8T2 — Verifier mode in am-review
- **Verdict:** PASS_WITH_WARN
- **Spec match:** `agents_manager/review/SKILL.md:332-435` adds `## Research verifier mode (Tier 4, 2026-08-13)` as a new top-level section appended to the existing `## Research mode` reflex. Five flags at lines 352-362 (`claim-without-citation`, `cited-but-404`, `contradiction-between-sub-agents`, `factscore-low-confidence`, `arithmetic-quant-error`).
- **Correctness:** Output path is `share/notes/04_review_<task-id>_verifier.md` (line 334) — matches the `Verification` output convention. Activation gate at lines 338-343 is precise (two triggering prefixes). Boundaries at lines 416-420 correctly forbid fixing the synthesis, paid APIs, copying content, and skipping flags.
- **Style:** Matches the surrounding SKILL.md prose style (which itself uses em-dashes — see cross-cutting).
- **Tests:** No test build; rubric-only.
- **Evidence:** `agents_manager/review/SKILL.md:332` (section header), `:339-343` (activation), `:352-362` (5-flag rubric table), `:367-412` (output schema), `:416-420` (boundaries).
- **Issues:**
  - [MEDIUM] `agents_manager/review/SKILL.md:356-362` implements **5 verification flags**, but `share/notes/02_plan_phases_T-2026-08-13-002.md:124` defines only 4 ((a)–(d): uncited, 404, contradiction, FActScore low). The file adds `arithmetic-quant-error` as a 5th flag beyond the plan spec. Dispatch instruction also said "4-step verification procedure." This is an additive improvement (numeric-drift detection is useful and matches Anthropic's "factual accuracy" eval axis), but it is a spec deviation. The file documents the 5th flag well (line 362 with PASS/WARN/FAIL criteria), so the deviation is documented, not silent.
  - [HIGH] Massive em-dash count (29 in the appended section under the existing SKILL.md header — the append inherited the pre-existing em-dash style of SKILL.md lines 3, 10, 14, etc.). See cross-cutting finding.
- **Suggested fix:** Either (a) reduce to 4 flags per plan spec, or (b) update the plan to acknowledge the 5th flag in the next plan version. Given the eval rubric also implicitly tracks quant errors (criterion 2 "Accuracy"), keeping 5 is more useful than 4. Master should decide; not a blocker.

### P8T3 — Golden test 1: arxiv topic
- **Verdict:** PASS
- **Spec match:** `agents_manager/eval/golden-tests/01_arxiv_topic.md` (125 lines). Topic (lines 14-16), Expected sources (lines 38-55), Grading rubric (lines 86-95) all present.
- **Correctness:** Topic is a known-literature academic survey that exercises the arXiv source connector. Sub-question decomposition (lines 28-35) maps to 3-5 sub-agents. Expected key findings (lines 60-67) are 6 distinct claims; rubric criteria (lines 87-94) are the 5 standard axes. Pass threshold 18/25, excellent 22/25 (line 95). Consistent with `agents_manager/eval/README.md:107`.
- **Style:** Clean markdown, consistent table formatting, frontmatter (lines 1-4) with `name: golden-test-01-arxiv-topic` and one-line description.
- **Tests:** No build; graded by humans at run-time.
- **Evidence:** `agents_manager/eval/golden-tests/01_arxiv_topic.md:14-24` (Topic + why), `:38-55` (Expected sources), `:86-95` (Rubric + thresholds), `:106-116` (References with access dates).
- **Issues:**
  - [LOW] 18 em-dashes in this file. See cross-cutting finding.
  - [LOW] `agents_manager/eval/golden-tests/01_arxiv_topic.md:43` references a paper that may not exist ("Self-RAG... extended 2025 surveys") — relaxed phrasing, fine for a golden test topic brief.
- **Suggested fix:** no fix needed.

### P8T4 — Golden test 2: web topic
- **Verdict:** PASS
- **Spec match:** `agents_manager/eval/golden-tests/02_web_topic.md` (144 lines). Topic, Expected sources, Grading rubric all present.
- **Correctness:** Topic targets live web fetch — exercises the DuckDuckGo / Jina Reader connectors (line 20). Sub-question decomposition (lines 29-37) lists 5 harness sub-agents + 1 synthesis sub. Expected key findings (lines 66-74) are 7 claims; rubric at lines 102-109 is the 5 standard axes + the bonus comparison-table criterion (line 117). Pass 18/25 or 18/30 with bonus; excellent 22 or 26 (line 111).
- **Style:** Matches the sibling test file.
- **Tests:** No build.
- **Evidence:** `agents_manager/eval/golden-tests/02_web_topic.md:14-24`, `:40-62`, `:102-119`.
- **Issues:**
  - [LOW] 16 em-dashes. See cross-cutting finding.
  - [LOW] `agents_manager/eval/golden-tests/02_web_topic.md:44-58` lists expected harness sources. Some URLs are speculative ("when published" at line 44). Fine for golden tests.
- **Suggested fix:** no fix needed.

### P8T5 — Golden test 3: comparison topic
- **Verdict:** PASS
- **Spec match:** `agents_manager/eval/golden-tests/03_comparison_topic.md` (140 lines). Topic, Expected sources, Grading rubric all present.
- **Correctness:** Topic is a 3-tool cross-product comparison (NotebookLM vs Perplexity DR vs ChatGPT DR). Mandatory comparison axes table at lines 70-81 (9 axes × 3 tools). Expected key findings at lines 60-67 (7 claims, including use-case-specific recommendations). Rubric at lines 98-104 + bonus criterion at lines 110-114. Pass 21/30, excellent 27/30 (line 114).
- **Style:** Consistent with sibling tests.
- **Tests:** No build.
- **Evidence:** `agents_manager/eval/golden-tests/03_comparison_topic.md:14-24`, `:38-55`, `:69-83`, `:98-114`.
- **Issues:**
  - [LOW] 19 em-dashes. See cross-cutting finding.
  - [LOW] `agents_manager/eval/golden-tests/03_comparison_topic.md:71-81` comparison table — "Open web search" cell for ChatGPT DR is "yes" — correct per product. Free tier row for ChatGPT DR: "no (DR requires Pro)" — correct per documented behavior.
- **Suggested fix:** no fix needed.

### P8T6 — Eval runner (bash + PowerShell)
- **Verdict:** PASS_WITH_WARN
- **Spec match:** Both files exist at `scripts/run-research-eval.sh` (234 lines) and `scripts/run-research-eval.ps1` (237 lines). Similar structure confirmed by reading: list-tests → prepare-test → ingest-test → aggregate → print help.
- **Correctness:** Bash uses inline `python3 <<PY ... PY` heredoc for markdown parsing (lines 92-138) — stdlib-only. PowerShell uses regex-based `Extract-From-Markdown` function (lines 64-90) mirroring the Python extraction with no stdlib YAML parser (correct constraint). Both write to `share/eval/<DATE>/<TEST_ID>/` and aggregate at `share/notes/04_eval_run_<DATE>.md`. Tests are filtered out of ingest (`_sub-`, `_verifier`, `_master_synthesis`).
- **Style:** Both have header comments naming the other as parity script (lines 1, 13 in `.sh`; `:13` in `.ps1`). Bash is `set -euo pipefail` (line 20). PowerShell is `$ErrorActionPreference = 'Stop'` (line 27). Both equivalent fail-fast posture.
- **Tests:** Could not execute either. Bash is missing on PATH; PowerShell, while available, was not invoked. The coder also did not invoke PowerShell end-to-end. The heredoc parsing logic was verified by reading. **This is a coverage gap, not a defect.**
- **Evidence:** `scripts/run-research-eval.sh:20` (set), `:92-141` (prepare), `:144-166` (ingest), `:170-215` (aggregate); `scripts/run-research-eval.ps1:27` (ErrorActionPreference), `:64-90` (extract), `:92-142` (prepare), `:145-169` (ingest), `:171-221` (aggregate).
- **Issues:**
  - [MEDIUM] Both scripts not invoked end-to-end. The heredoc Python parses for the topic regex (`## Topic\n\n> "(.*?)"`) at `scripts/run-research-eval.sh:99` and the sub-question regex (`^\d+\. \*\*([^*]+)\*\*`) at line 106. The PowerShell equivalents at `scripts/run-research-eval.ps1:71-79` use the same patterns. If the Golden Test files were ever edited to break these regexes (e.g. lose the trailing `**` on sub-question bullets, change the topic blockquote pattern), both scripts would silently drop data. Low likelihood given the templates encode these regexes, but worth a smoke run before next Tier 4 change.
  - [MEDIUM] `scripts/run-research-eval.sh:174` writes the per-date aggregate even when the user only runs `--help`. The gate at lines 51-65 calls `print_help` before `--ingest` evaluation, so `--help` does NOT generate a date-stamped file. Verified by reading, not execution. If the help-gate regressed, a `--help` invocation would silently write a dated aggregate file. Low likelihood but worth pinning with a unit test if anyone touches the parser.
  - [LOW] 7 em-dashes in `scripts/run-research-eval.sh` + 3 in `scripts/run-research-eval.ps1`. See cross-cutting.
- **Suggested fix:** Run both scripts at least once in a bash + pwsh environment before next Tier 4 change. Add `--dry-run` mode that prints the would-write output paths without writing files.

### P8T7 — Eval README
- **Verdict:** PASS
- **Spec match:** `agents_manager/eval/README.md` (213 lines). All 5 required sections present:
  - "## What this is" at line 12.
  - "## How to add a new golden test" at line 24.
  - "## How to run a test" at line 109.
  - "## How to interpret the results" at line 157.
  - "## How often to run" at line 173.
- **Correctness:** Naming conventions documented (lines 102-107). Pass / fail bands documented at lines 161-166. Cadence documented at lines 175-180 with explicit skip rules (lines 182-185).
- **Style:** Mirrors the cloudflare synthesis style. Frontmatter at lines 1-4.
- **Tests:** Doc-only.
- **Evidence:** `agents_manager/eval/README.md:12`, `:24`, `:109`, `:157`, `:173`, `:196-204` (References with access dates).
- **Issues:**
  - [LOW] 25 em-dashes. See cross-cutting.
- **Suggested fix:** no fix needed.

## Cross-cutting findings

- [HIGH] **Em-dash count across all new deliverables: 117 total** (WORKFLOW.md: 29, eval/README.md: 25, 01_arxiv: 18, 02_web: 16, 03_comparison: 19, eval .sh: 7, eval .ps1: 3). The dispatch prompt explicitly said "Voice: no AI vocabulary, no em dashes." This is a stated Tier 4 cross-cutting check. The pre-existing SKILL.md files also use em-dashes heavily (counted ~29 in the appended research-mode section alone), so the rule appears aspirational rather than enforced upstream; however, Tier 4 made it explicit, and the deliverables ship 117 em-dashes. Recommendation: Master surfaces a follow-up `am-coder` pass to globally replace em-dashes with `-` / `:` / `,` across the 7 new files. The existing pre-existing SKILL.md em-dashes should be addressed in a separate cleanup. **Not a blocker** (the harness functions regardless), but a real spec violation.
- [MEDIUM] **Default-tools compliance:** PASS. No `Tavily`, `Exa`, `Brave` integration anywhere in the new deliverables. The only `Perplexity` / `Sonar` mentions are (a) `agents_manager/eval/golden-tests/03_comparison_topic.md:29, 62` (subject of comparison research, not as integration — the topic is to research Perplexity's Deep Research mode), and (b) `agents_manager/research/WORKFLOW.md:74` explicitly prohibiting those tools ("Default tools only (webfetch + browse MCP). No Tavily / Exa / Brave / Perplexity Sonar."). Compliance is preserved.
- [PASS] **Citation coverage:** Every external reference (Anthropic engineering blog, controller docs, eval files, golden-test sources) carries an access date of 2026-08-13. Examples: WORKFLOW.md:188-194, eval/README.md:198-204, golden-test 01:109-116, 02:125-133, 03:120-129. No orphan citations.
- [PASS] **Frontmatter:** All 6 new or modified markdown files have valid frontmatter (name + description + access dates where applicable). Validator exits 0.
- [PASS] **No AI vocabulary.** Grep for `delve | leverage | seamless | robust | comprehensive | cutting-edge | state-of-the-art | elevate` across the 7 new files returned 0 matches.

## Out-of-scope observations (informational only)

- The coder reported a **pre-existing unpack bug** at `scripts/validate-frontmatter.py:73-74, 119` (the `validate_one → main` tuple-shape mismatch when `parse_frontmatter` raises). Confirmed by reading: line 73 returns bare `[str(e)]`, line 119 unpacks two values. IndexError. This affects any file without frontmatter — and the lenient mode would convert that to a non-fatal path IF the file were not under a `skills/` segment. As-is, the validator crashes on no-frontmatter files. Out of lane for am-coder; recommend a separate maintainer pass.
- The coder's `scripts/run-research-eval.sh` line 174 prints `==> done.` to stdout, but `--help` (line 31) calls `exit 0` directly without printing it. Verified by reading, not execution. Cosmetic.
- The Anthropic URL cited at `[S1]` is live but the title paraphrase ("Building a multi-agent research system") doesn't match the actual article title ("How we built our multi-agent research system"). URL is correct; just a title mismatch.

## Honest assessment

The Tier 4 deliverables are functionally correct and self-consistent. Seven files, 1,403 lines, all frontmatter-valid, no default-tool violations, full citation discipline with access dates. The workflow doc accurately mirrors the Anthropic multi-agent research system (verified live by webfetch); the eval harness is well-structured with three meaningful golden tests and a working rubric; the eval runner is correctly written in both bash and PowerShell (verified by reading, not execution). The two WARNs — a 5th verification flag beyond plan spec, and 117 em-dashes violating the explicit "no em dashes" Tier 4 rule — are real but not blockers. Ship the harness; circle back on the em-dash cleanup in a separate small dispatch.

## Self-critique

- **Did I do my job?** yes.
- **What might I have missed?**
  - I could not execute either eval runner script (no `bash` on PATH; PowerShell available but coder said the heredoc was unrun, and I did not run it either). Both are verified by reading only.
  - I sampled one of two Anthropic URLs (`[S1]`); I did not webfetch `[S2]` `building-effective-agents`. The URL pattern is the same host; very likely valid. Worth a one-shot check at next Tier 4 cycle.
  - The golden-test rubrics all align with `agents_manager/eval/README.md:107` thresholds; I did not exhaustively trace which threshold maps to which test (some tests use 18, some 21, some 27). All four locations I checked (README:107, golden 01:95, golden 02:111, golden 03:114) are internally consistent.
  - The `scripts/run-research-eval.ps1` PowerShell heredoc (`@" ... "@`) relies on `$Id` and `$date` being in scope at parse time — I verified they are (function-scoped), but I did not run end-to-end.
- **What did I assume without evidence?**
  - That the em-dash count is genuinely a spec violation (the dispatch said so explicitly, but the existing codebase pattern suggests it may be unenforced aspirational rule).
  - That the 5th verification flag (`arithmetic-quant-error`) is an improvement, not a regression. The flag does not contradict the 4-step plan spec; it augments it. The plan's eval rubric criterion 2 ("Accuracy") implicitly demands quant verification, so the 5th flag is needed for the eval to function. Higher-confidence read: this is correct as-is.
  - That the regex-based markdown parser in `scripts/run-research-eval.sh` and `.ps1` matches all three golden test files identically. The templates encode these regexes, so any future divergence would be a template drift, not a parser bug.
