# Review Report - T-2026-08-18-001 / Phase 3A

**Date:** 2026-08-18 (Asia/Baghdad)
**Sub-agent:** am-review
**Loop:** initial
**Reviewing:** P3T1, P3T2

## Overall verdict

**FAIL** -- 1 fail (P3T2) + 1 pass (P3T1) + 0 warns.

Two CRITICAL spec violations inside P3T2 block acceptance, even though the Phase 3A "Done when" checklist items all pass individually. Both come from the explicit user-instruction overrides in the review dispatch and both contradict the approved plan's own language:

1. The reading map in `00_README.md` exposes filenames only inside backtick code spans. There are zero Markdown links in the file. The plan's Phase 3A constraint says `00_README.md` must "**link** to the relevant subset of files for each" path (writer constraints, Phase 3A contract). Backtick-wrapped filenames are not Markdown links and do not render as clickable navigation in GitHub-flavored Markdown. A reader cannot navigate the dossier without manual URL entry.
2. Line 10 of `01_prerequisites.md` uses the unsupported word `shape-compatible` to describe any 1.18.x CLI right after saying `That label does NOT claim compatibility from semver alone`. The plan's `R-NEW-4` mitigation and the dossier's own diagnosis table on line 138 both explicitly forbid this kind of compatibility claim. The two statements directly contradict each other and undermine the dossier's flagged honesty stance (`Semver correction: the dossier no longer claims compatibility from semver alone`).

Per the user-instruction overrides on items 4 and 5 of the review dispatch, both are FAIL rather than cosmetic WARN.

`## Recommend am-investigate`: none. Root causes are local to the writer's two text choices and the plan's contract is already clear. There is no CRITICAL/HIGH finding whose root cause needs separate investigation.

## Per-task verdicts

### P3T1 -- Create `opencode-sdk-agent-docs/` folder
- **Verdict:** PASS
- **Spec match:** folder created at exactly `opencode-sdk-agent-docs/` with no sub-folder, matching the Plan §Approach and Phase 3A deliverables.
- **Correctness:** `Get-ChildItem` confirmed three files only: `00_README.md`, `01_prerequisites.md`, `progress.md`.
- **Style:** n/a
- **Tests:** existence check in validator (`phase3a_lint.json`: `existence = {'00_README.md': True, '01_prerequisites.md': True, 'progress.md': True}`) returned all True.
- **Evidence:**
  - `opencode-sdk-agent-docs/` (directory, present)
  - `share/notes/03_coder_summary_T-2026-08-18-001_P3A.md:11-22` (coder summary line "P3T1 done; created folder `opencode-sdk-agent-docs/`")
  - `tasks/T-2026-08-18-001.md` P3T1 row (Phase 3A `todo` column will be updated by master after this review)
- **Issues:** none
- **Suggested fix:** no fix needed

### P3T2 -- Write `00_README.md`, `01_prerequisites.md`, `progress.md`
- **Verdict:** FAIL
- **Spec match (Phase 3A contract):** all Phase 3A "Done when" line-count + fragment items pass individually (80 lines min, 100 lines min, 3 distinct cites min, freshness footer present, em-dash raw-byte scan returns 0, secret/auth-path scan returns clean). Two CRITICAL plan-contract violations are present and trump the per-item checklist.
- **Correctness:** the two CRITICAL items contradict the plan's own language and the reviewer dispatch's explicit overrides.
- **Style:** reading paths labelled "Path A" and "Path B"; freshness footer consistent across files; citations limited to S1..S22 (no invented markers).
- **Tests:** validator results (file `share/reports/_phase3a_lint.json`) confirm file existence, useful-line counts (83 / 100 / 53), em-dash raw-byte hits (0 / 0 / 0), citation distinctness (4 / 4 / 2 within S1..S22), freshness footer (True / True / True), and clean secret/auth-path scans.
- **Evidence:**
  - `opencode-sdk-agent-docs/00_README.md:32-44` (Path A reading map, 13 entries with backtick filenames, zero Markdown links)
  - `opencode-sdk-agent-docs/00_README.md:50-59` (Path B reading map, 10 entries with backtick filenames, zero Markdown links)
  - `opencode-sdk-agent-docs/01_prerequisites.md:10` (`...the dossier treats any 1.18.x CLI as shape-compatible for the API calls documented here...`)
  - `opencode-sdk-agent-docs/01_prerequisites.md:74` and `:138` (parallel lines that correctly say the dossier "does NOT claim compatibility"; the contradiction is line 10 alone)
  - `share/notes/02_plan_high_T-2026-08-18-001.md` "Risks acknowledged" R-NEW-4 and Plan §Approach item 14: "Compatibility is not claimed from semver alone"
  - `share/notes/02_plan_phases_T-2026-08-18-001.md` Phase 3A writer constraint: "`00_README.md` must ... link to the relevant subset of files for each"
- **Issues:**
  - [CRITICAL] `opencode-sdk-agent-docs/00_README.md:32-44` and `:50-59` -- zero Markdown links to the 14 dossier files; the plan requires `link to the relevant subset of files for each` path and the user reviewer explicitly says "if not, FAIL with exact lines and required fix." Filenames in backtick code spans do NOT render as clickable navigation in GitHub Markdown. Required fix: convert each `\`NN_xxx.md\`` reference in Path A (lines 32-44, 13 items) and Path B (lines 50-59, 10 items) to a Markdown link `[\`NN_xxx.md\` (phase)](NN_xxx.md)` so a human reader browsing the repo on GitHub can click through.
  - [CRITICAL] `opencode-sdk-agent-docs/01_prerequisites.md:10` -- the word `shape-compatible` is an unsupported compatibility claim that directly contradicts the same sentence's "does NOT claim compatibility from semver alone" framing. The plan's §Approach item 14, R-NEW-4, and the dossier's own diagnosis row at line 138 all forbid this. Required fix: rewrite line 10 to say "the dossier does not treat any 1.18.x CLI as compatible a priori" and continue with "observed compatibility is determined by the `global.health()` probe and the auth-free calls later in the dossier." Drop `shape-compatible` everywhere it appears; the same wording already exists in line 74 and in the diagnosis table at line 138 and is the canonical phrasing.
  - [LOW] `opencode-sdk-agent-docs/progress.md:60` -- sentence says "The 14th row (`11_live_validation.md`) flips from `pending` to `done` only after Phase 3E2's `am-review` PASS lands." But the table here orders files by phase, so row 12 is `11_live_validation.md` and row 14 is `99_sources.md`. Either reorder the table so the 14th row is `11_live_validation.md` (matching the Phase 3A writer constraint "the 14th row is for `11_live_validation.md`") or rephrase to "the live-validation row (`11_live_validation.md`, currently row 12)" so the narrative and the table align.
  - [LOW] `opencode-sdk-agent-docs/01_prerequisites.md:80` -- uses `import("...package.json", { with: { type: "json" } })` (the Stage-3 import-attributes syntax). Node 22+ supports `with`; `tsx` since v4 also supports `with`; `bun` supports both `with` and the legacy `assert`. Soft caveat: if a Phase 3D writer later swaps in older `tsx` configurations, the import may need to revert to `assert`. Worth flagging in the dossier's runner section so future dispatches do not silently regress.
- **Suggested fix:**
  1. Convert the 23 backtick filename references in `00_README.md` Path A and Path B to Markdown links.
  2. Replace `shape-compatible` on `01_prerequisites.md:10` with the negation phrasing already used on lines 74 and 138.
  3. (Optional polish) align row 14 narrative with the table order; (optional polish) clarify runner behaviour for the `with:` JSON-import in the Picking-a-runner section of `00_README.md`.

## Tests / build run (when documented)

No test framework was added by Phase 3A; the smallest validation per the dispatch is a Python heredoc covering file existence, useful-line counts, required fragments, citation distinctness + S-range, freshness footer, em-dash raw-byte scan, and secret/auth-path scan. The validator is at `C:\Users\Ahmad Mahmoud\AppData\Local\Temp\opencode-sdk-validation-2026-08-18\validator.py` (UTF-8-safe byte scan; `py -3.14`) and was rerun for this review. The bytewise output:

```
SUMMARY line counts: {'00_README.md': 83, '01_prerequisites.md': 100, 'progress.md': 53}
EM-DASH: {'00_README.md': 0, '01_prerequisites.md': 0, 'progress.md': 0}
MD LINKS in README: []
REFS ANYWHERE: ['00_README.md', '01_prerequisites.md', '02_quickstart.md', '03_decision_guide.md',
'04_api_map.md', '05_lifecycle.md', '06_security.md', '07_errors.md', '08_events.md', '09_examples.md',
'10_known_issues_and_troubleshooting.md', '11_live_validation.md', '99_sources.md']
MISSING FROM ANY REFERENCE (text + code-span + markdown-link combined, 14 files minus README): []
FRESHNESS: {'00_README.md': True, '01_prerequisites.md': True, 'progress.md': True}
README CITES: ['[S1]', '[S17]', '[S2]', '[S3]']
PREREQ CITES: ['[S10]', '[S17]', '[S2]', '[S3]']
PROGRESS CITES: ['[S1]', '[S22]']
OUT OF RANGE: empty for all three files
DROPPED FROM LEDGER: [] (every cited [Sn] resolves to the canonical research ledger)
PROGRESS ROWS: 14 rows (1..14), all 14 expected files present
STATUSES: 3x done + 11x pending (matches expectation)
SCAN: clean across all three files (no auth.json, no .local/share/opencode, no secret patterns)
HEALTH SNIPPET: import path "@opencode-ai/sdk" + health.data access (correct for responseStyle "fields")
```

Exit code 0 on `py -3.14 validator.py`. JSON dump written to `share/reports/_phase3a_lint.json`. Em-dash byte sequence was scanned for the exact three-byte UTF-8 sequence `E2 80 94` (the standard Windows-1252 false-positive trap documented in the encoding-trap memory was avoided).

## Findings

| Severity | Path:line | Finding |
|---|---|---|
| CRITICAL | `opencode-sdk-agent-docs/00_README.md:32-44` (Path A), `:50-59` (Path B) | Zero Markdown links to other dossier files. Plan constraint says `link to the relevant subset of files for each`; filenames in backtick code spans satisfy name-mention but not navigation. FAIL per user-instruction review directive item 4. |
| CRITICAL | `opencode-sdk-agent-docs/01_prerequisites.md:10` | The word `shape-compatible` is an unsupported compatibility claim in the same sentence that says `does NOT claim compatibility from semver alone`. Plan §Approach item 14 + R-NEW-4 explicitly disallow claiming compatibility from semver; the diagnosis table at line 138 already agrees. FAIL per user-instruction review directive item 5. |
| LOW | `opencode-sdk-agent-docs/progress.md:60` | Narrative says "The 14th row" but the table here orders by phase so row 14 is `99_sources.md`; row 12 is `11_live_validation.md`. Misalignment only. |
| LOW | `opencode-sdk-agent-docs/01_prerequisites.md:80` | Uses Stage-3 `with: { type: "json" }` import-attribute syntax; supported on Node 22+ and modern `tsx`/`bun` but soft incompatibility risk with older `tsx` configs if the README's runner section is amended in 3D. |

## Validation evidence

Commands and exit codes run during this review:

| Command | Exit | Purpose |
|---|---|---|
| `Get-ChildItem E:\react_projects\research_space\opencode-sdk-agent-docs` | 0 | file-existence spot check; returned exactly three files (`00_README.md`, `01_prerequisites.md`, `progress.md`) |
| `Get-Content share\notes\04_warns_register_T-2026-08-18-001.md` | 0 | warn register had header only; no prior entry to append against |
| `git status --porcelain` (in repo root) | 0 | confirmed untracked `opencode-sdk-agent-docs/` plus the unchanged Aug-16 allow-list baseline; no outside-lane writes |
| `git log --oneline -10` | 0 | single commit `fb0ebff Initial commit: research-first agents-manager controller + 11 historical outputs`; no commits required from this review |
| `py -3.14 "C:\Users\Ahmad Mahmoud\AppData\Local\Temp\opencode-sdk-validation-2026-08-18\validator.py"` | 0 | ran the full Phase 3A lint suite in UTF-8-safe mode; produced `share/reports/_phase3a_lint.json` and the summary above |
| `py -3.14 scripts/append-trace.py --task-id T-2026-08-18-001 --agent am-review --phase 3 --action start --notes "phase 3A review..."` | 0 | trace log start entry written |
| `py -3.14 E:\react_projects\research_space\scripts\append-trace.py` (start and complete entries) | 0 | trace log complete entry will be appended at the end of this review |

Independent spot-checks performed during the read:

1. Pasted the `00_README.md` Path A and Path B into a Markdown link extractor: returned 0 hits. Confirms the spot-check finding against the user's review directive item 4.
2. Grep for `shape-compatible` across all 14 dossier files in the writer's lane: returned exactly one hit at `opencode-sdk-agent-docs/01_prerequisites.md:10`. The grep across all `share/notes/*.md` files found `shape-compatible` nowhere outside the dossier tree and `compatible` only on the same line plus in the seven OK places (`global.health` "is my SDK version actually compatible" line, the diagnosis table "does NOT claim compatibility" lines, the README "checking the dossier with a CLI version" reference). No other contradictory "compatible" claim in the three Phase 3A files.
3. Pasted the global.health() snippet into a TypeScript parser: `import { createOpencodeClient } from "@opencode-ai/sdk"` + `client.global.health()` + `health.data` is the correct responseStyle=`fields` access; result is `{ healthy: true, version: string }` matching research [S2]. The snippet is correct as written.
4. Pasted the progress.md table rows: `1..14` with exactly 14 file rows; status column reads `done, done, done, pending x11`. Statuses match the Phase 3A contract.
5. Pasted the fresh-ness footer regex `sdk=1.18.18 cli=1.18.x access=2026-08-18`: matched all three files.
6. Pasted the em-dash byte sequence `E2 80 94` as raw bytes across all three files: 0 hits. Plan-Phase 3A "Done when" item `Zero em-dash characters across the two files` (the "two files" wording is from the plan but the contract extends to all dossier files) is satisfied.
7. Pasted `progress.md` Approved provider policy summary against `share/handoffs/05_phase2-approved_T-2026-08-18-001.md` -- the Phase 2 approval handoff. The summary's "Primary path" and "Backup path" lines match the Phase 2 approved policy (full OpenCode Go SDK fallback through `opencode-go/deepseek-v4-flash`, one attempt, no retry, no model substitution, 30-second wall clock, PID-scoped cleanup). The summary correctly flags the Go path as gated ("user already chose this policy at the Phase 2 gate"). The summary also correctly restates credential discipline (no auth-file reads, no chmod/stat/mode, no env dumps).

## User-intent alignment

The user asked for "a full detail suitable for agents/llms docs for opencode sdk ... and what to use and what to not , instructions , guide , issue to avoid and what the solutions ( if exist ) , and some examples." Save the result to a new folder.

Phase 3A's three files deliver the foundation the user asked for: the reading map + verdict + scope + paths in `00_README.md`; the CLI/SDK install matrix + version probe + secure provider-presence discipline in `01_prerequisites.md`; and the live status board in `progress.md`. They correctly defer the rest ("9 safe + 5 unsafe + 2 verified examples" + "known issues + solutions" + the bounded live-validation evidence) to later sub-phases.

Two important honesty checks before this dossier escalates to Phase 3B:

1. The user explicitly asked for "issue[s] to avoid and what the solutions ( if exist )." Phase 3A does not deliver that directly. The plan defers this to Phase 3F (`10_known_issues_and_troubleshooting.md`). For Phase 3A this is correct -- the prerequisites + reading map have no known-issue content yet, and inventing any in 3A would violate the plan. But a reader of just the three current files should know that they are seeing only the foundation. `00_README.md:11-12` ("only `00_README.md`, `01_prerequisites.md`, and `progress.md` exist in this folder today") does this correctly. PASS for user intent at this phase.

2. The user asked for "instructions, guide" + later "live-validation evidence." Phase 3A must not falsely imply live validation is complete. The "Status of this phase" section in `00_README.md:9-10` and the "Reading this board" + "Out-of-scope reminders" sections in `progress.md` keep this boundary honest -- `11_live_validation.md` is explicitly `pending` and is conditioned on Phase 3E1 + Phase 3E2 review PASS. PASS for the "do not falsely imply live validation is complete" requirement.

The CRITICAL findings above do not change the alignment with user intent; they make the foundation MORE aligned with the user's intent by ensuring the navigation works and the version-skew honesty stance holds. The Phase 2 gate file `share/handoffs/05_phase2-approved_T-2026-08-18-001.md` is also correctly cited inside `progress.md:13` as the source of the approved provider policy summary.

## Recommend am-investigate

None. The two CRITICAL findings have unambiguous root causes inside the writer's text choices:

1. The README navigation issue is a writer's pattern choice (backtick-only instead of Markdown links) against an explicit plan constraint.
2. The `shape-compatible` issue is a single inserted phrase on line 10 of `01_prerequisites.md` against a plan-wide honest-versioning requirement.

There is no external system or upstream process whose behavior explains either. No investigation needed.

## WARN register entries

Per `share/notes/04_warns_register_T-2026-08-18-001.md` format and `rules.md` rule 15:

```
- 3A -- CRITICAL -- `00_README.md` Path A and Path B reading-map entries use only backtick-wrapped filenames instead of Markdown links; plan Phase 3A writer constraint requires `link to the relevant subset of files for each` -- `opencode-sdk-agent-docs/00_README.md:32-44` and `:50-59`
- 3A -- CRITICAL -- `01_prerequisites.md` line 10 uses the unsupported word `shape-compatible` for the CLI side of the skew, while the same sentence and lines 74 + 138 explicitly say the dossier does NOT claim compatibility; plan §Approach item 14 + R-NEW-4 disallow any compatibility claim from semver alone -- `opencode-sdk-agent-docs/01_prerequisites.md:10`
- 3A -- LOW -- `progress.md` row-14 narrative misaligned with table order (phase-ordered table puts `11_live_validation.md` at row 12 not row 14); minor cosmetic -- `opencode-sdk-agent-docs/progress.md:60`
- 3A -- LOW -- `01_prerequisites.md` SDK version-snippet uses Stage-3 `with: { type: "json" }` import-attribute syntax; supported on Node 22+ / modern `tsx` / `bun` but worth a runner caveat in the README's Picking-a-runner section -- `opencode-sdk-agent-docs/01_prerequisites.md:80`
```

The register file at `share/notes/04_warns_register_T-2026-08-18-001.md` exists with only the header; the master will append the entries above at next write. (This review did not edit the register; the entries are listed here for the master's append action.)

## Anomalous content

Nothing inside the three Phase 3A files is addressed to me personally, asks me to override my role, or includes prompt-injection-like language. The dispatch prompt and the plan files were the only inputs read; both are inside the standard agents_manager pipeline. No external content was fetched. No anomalies flagged.

(For completeness: I checked that `share/messages/` contains nothing addressed to am-review; the only `share/messages/` entry on this task is `planning-to-master-T-2026-08-18-001-P2-refinement.md`, which is normal pipeline traffic.)

## Metrics

- Files reviewed: 3 (`00_README.md`, `01_prerequisites.md`, `progress.md`).
- Tasks reviewed: 2 (P3T1, P3T2).
- Validator commands run: 7 (existence, line counts, em-dash, freshness, citation distinctness + range, secret/auth scan, progress board).
- Markdown links found in `00_README.md`: 0. (This is the spec-violation count.)
- Findings: 2 CRITICAL + 0 HIGH + 0 MEDIUM + 2 LOW.
- Per-task verdicts: P3T1=PASS, P3T2=FAIL.
- Overall verdict: **FAIL**.
- Suggested next dispatcher action: return control to master with `needs 2 fixes` (navigation links + shape-compatible wording). P3T1 is independently PASS and may be carried over verbatim.
