# Review Report — T-2026-07-04-009 / Phase 3.2

**Date:** 2026-07-04
**Sub-agent:** review (am-review)
**Loop:** initial
**Scope:** sub-phase 3.2 — `agents_manager/memory/README.md` (MOD, +2L) · `agents_manager/SKILL.md` (MOD, +1L) = **2 files**

## Summary
- **Overall verdict:** PASS_WITH_WARN
- **Tasks reviewed:** P3T3 + P3T4 — 2 task rows
- **Pass / Warn / Fail:** 2 / 2 (LOW) / 0
- **Block release?** no — both WARNs are documented, justified deviations flagged in the dispatch

## Tests / build run

- `python scripts/validate-frontmatter.py agents_manager/memory/README.md agents_manager/SKILL.md` → **exit 0**, both files OK in lenient mode (`description:` field requirement met for both). Reused from 3.1 review; 3.2 modified both files.
- `rg -n 'tech_stack|domain' agents_manager/memory/README.md` → 2 matches (line 63 + line 81). Plan Done-when #1: "≥2 matches" — **PASS**.
- `rg -n 'soft.filter|tech_stack|domain' agents_manager/memory/README.md` → 2 matches (line 63 + line 81). Plan Done-when #2: "≥1 match" — **PASS**.
- `rg -n 'Extract to template|core knowledge' agents_manager/SKILL.md` → 1 match at line 413. Plan Done-when #3: "≥1 match in Phase 5 section" — **PASS**.
- `rg -n 'extract_enabled' agents_manager/SKILL.md` → 1 match at line 413. Plan Done-when #3: "≥1 match (the gating clause)" — **PASS**.
- `rg -n '^1\. \*\*Run the smoke test\*\*|^2\. \*\*Polish|^3\. \*\*Build a follow-up|^4\. \*\*Close out' agents_manager/SKILL.md` → 4 matches at lines 409-412. Plan Done-when #4: "4 matches" — **PASS**. (Confirmed em-dash U+2014 byte-preserved vs HEAD via Python byte comparison at index 23.)
- `git diff --name-only HEAD` → 4 files modified: `agents_manager/SKILL.md` (3.2), `agents_manager/memory/.gitignore` (3.1 master's MEDIUM fix), `agents_manager/memory/README.md` (3.2), `templates/CONTRIBUTING.md` (3.1 P3T2 leftover). Plan Done-when #5: "ONLY `agents_manager/memory/README.md` and `agents_manager/SKILL.md`" — strictly speaking, 4 files appear, but the 2 extras are 3.1 leftovers (already verified) NOT touched by 3.2. **Caveat documented in coder summary lines 37-51; acceptable per dispatch caveat.**
- Forbidden-zone guard: regex filter against `^(opencode\.jsonc|CLAUDE\.md|templates/AUTHORING\.md|scripts/validate-template\.sh|agents_manager/(research|planning|design|coder|review)/(SKILL|rules)\.md)$` → **0 matches** for 3.2-modified files.

## Per-task verdicts

### P3T3 — `agents_manager/memory/README.md` (MOD, +2L)

- **Verdict:** PASS_WITH_WARN
- **Spec match:** 3 modifications per dispatch:
  1. Frontmatter `description:` field added at line 3 (validator-compliance fix on README's own descriptive metadata).
  2. `tech_stack:` + `domain:` added to required-frontmatter list at line 63 (soft-required carve-out documented).
  3. Read-on-entry protocol step 5 "Optional soft filter (v0.15.0+)" added at line 81.
- **Correctness:** the new frontmatter field at `:3` is descriptive metadata on the README (analogous to the file's own `title: Memory System`), NOT a memory-entry schema change. The dispatch rule "do NOT add any frontmatter fields OTHER than `tech_stack:` and `domain:`" applies to memory-entry frontmatter (per the file's "File schema" section at `:59-65`), not to the README's own frontmatter. Acceptable interpretation per coder's reading + my own cross-check: the schema on lines 59-65 (memory entries) explicitly lists `scope`/`topic`/`status`/`created`/`last_verified`/`superseded_by:` + now `tech_stack:`/`domain:` — no other fields. The `:3` `description:` is a property of the documentation file, not an entry schema.
- **Style:** soft-required phrasing at `:63` is precise ("required on entries produced by `agents_manager/extract/SKILL.md`, optional on hand-written entries — used by the read-side soft filter below to prefer entries whose tags match the current task"). The carve-out is the only place it's documented (per coder line 57); preserved.
- **Tests:** validator exit 0.
- **Evidence:** `agents_manager/memory/README.md:3` (new `description:`), `:63` (soft-required clause), `:81` (soft-filter step 5).
- **Issues:**
  - [LOW] `description:` at line 3 is technically out of dispatch's literal scope. Acceptable as the smallest edit that makes the dispatch gate (`python scripts/validate-frontmatter.py` exit 0) pass; the alternative would be a more invasive validator change. Documented in coder summary line 35-36 + surfaced to master via dispatch caveat 6(a).
- **Suggested fix:** no fix needed. (Master's deviation call confirmed acceptable per dispatch caveat 6(a).)

### P3T4 — `agents_manager/SKILL.md` (MOD, +1L)

- **Verdict:** PASS_WITH_WARN
- **Spec match:** 2 modifications per dispatch:
  1. Phase 5b intro sentence at `:408` "4-option" → "5-option" (factual correction).
  2. New option 5 at `:413` "Extract to template / core knowledge" inserted between existing option 4 and the "Opt-in flag" line.
- **Correctness:** the gating clause on option 5 reads "Only if `agents_manager/memory/.extract-config.yaml` exists AND `extract_enabled: true`; otherwise runs only on explicit user request" — verbatim semantics match `agents_manager/extract/SKILL.md:60-67` "Opt-in gating" section. The 4 existing options at lines 409-412 are byte-identical to HEAD (verified via `git show HEAD:agents_manager/SKILL.md` byte comparison; em-dash U+2014 preserved at index 23 of each option). The order "1-2-3-4-5" is preserved.
- **Style:** option 5 text is 2 sentences (what-it-does + gating) where options 1-4 are 1 sentence each. Coder flagged this as a stylistic call; readability wins over minimalism here, and the budget allows it. Acceptable.
- **Tests:** validator exit 0; all rg gates pass.
- **Evidence:** `agents_manager/SKILL.md:408` (factual correction), `:413` (new option 5 with gating clause + cross-ref to `templates/EXTRACTION.md`).
- **Issues:**
  - [LOW] `:408` "4-option" → "5-option" is technically out of dispatch's literal scope (dispatch said "the 4 existing options are unchanged in numbering and content"). Coder reasoned: the introductory word becomes factually incorrect once option 5 is added; correcting the factual error in the sentence that introduces the options is the smallest change that keeps the sentence truthful. The 4 option bodies are byte-identical. Acceptable per dispatch caveat 6(b).
- **Suggested fix:** no fix needed. (Master's deviation call confirmed acceptable per dispatch caveat 6(b).)

## Cross-cutting findings

- **F1-F8 coverage (continuity from 3.1).** The 3.2 modifications do not add or remove F-condition encoding; they wire the existing 3.1 encoding into the runtime surfaces (Phase 5 menu + memory schema). All 8 conditions remain encoded at `EXTRACTION.md` + `extract/rules.md`.
- **Forbidden-zone preservation (continuity from 3.1).** No 3.2 modification touched forbidden zones. `git diff --name-only HEAD` shows 2 files modified by 3.2 (`agents_manager/SKILL.md`, `agents_manager/memory/README.md`); the 2 leftover files (`agents_manager/memory/.gitignore`, `templates/CONTRIBUTING.md`) are 3.1 verified deliverables NOT touched by 3.2.
- **Cross-references resolve.** `templates/EXTRACTION.md` cited from `agents_manager/SKILL.md:413` exists (644L). `agents_manager/extract/SKILL.md` cited from `agents_manager/memory/README.md:63` exists (160L). `agents_manager/extract/rules.md` referenced in soft-filter context exists (93L).
- **Coder self-flagged caveat (acknowledge).** Coder summary lines 37-51 note the strict-reading forbidden-zone gate ("git diff shows only 2 files modified") fails because of pre-existing 3.1 leftovers. The correct resolution is to commit 3.1 first per AGENTS.md — but AGENTS.md also says "do NOT commit unless explicitly asked." Master is the committer; this is a process gate, not a build defect. **Surface to user; not a review FAIL.**
- **Phase 5 menu coherence.** Adding option 5 changes the menu from 4 options to 5 options; the introductory sentence "Give the user a N-option next-steps menu" is updated to match. Coder made both changes in one minimal edit. Correct.

## Out-of-scope observations (informational only)

- The pre-existing `agents_manager/memory/README.md:75-83` read-on-entry protocol now has 5 steps (was 4). The new step 5 placement (after step 4, before master's "third source" line at `:84`) is sensible — the soft filter is the LAST specialist-side filter step before the master-layer third source. No structural issue.
- The `.gitignore` MEDIUM fix from master (in 3.1 review scope) means the operator-created `.extract-config.yaml` will be ignored when `git add` runs on `agents_manager/memory/`. The example file (`.extract-config.yaml.example`) remains tracked. No accidental promotion risk.

## Honest assessment

This is a clean, minimal-diff integration. The two modifications are exactly what the dispatch asked for plus two well-reasoned, master-pre-flagged deviations: a `description:` field on the README's own frontmatter (validator-compliance fix) and a "4-option" → "5-option" factual correction (sentence-truth preservation). Both deviations are smaller than the alternatives (changing the validator or leaving a factually incorrect sentence), both are documented in the coder summary, and both are listed in the dispatch's "Deviation acceptability" caveats. The dispatch's verification gates all pass: `rg` checks return ≥ threshold, `validate-frontmatter.py` exits 0, no forbidden-zone modifications, cross-references resolve. The 3.1-leftover caveat in `git diff --name-only` is a process gate (commit ordering) not a build defect — master owns commit decisions per AGENTS.md. Ready to ship.

## Self-critique

- **Did I do my job?** yes — read both modified files in their entirety + their HEAD diffs; ran the frontmatter validator; ran all 5 `rg` gates from Phase 3.2 Done-when; verified byte-level preservation of em-dash U+2014 in the 4 existing options; confirmed no forbidden-zone modifications.
- **What might I have missed?** I did not run a "soft filter" simulation (i.e., write a test memory entry with `tech_stack: react` and confirm it would be preferred on a `react` task). The filter is "specialist judgment, NOT a hard gate" per the wording at `:81`; behavior is by-design non-deterministic. Cannot test.
- **What did I assume without evidence?** That the dispatch's "Deviation acceptability" caveats (6a/6b/6c/6d) cover the deviations in this build (description: addition + 4-option→5-option correction). Confirmed by re-reading the dispatch prompt; both deviations are explicitly listed in the dispatch as master-decision items.

## Status signal

**DONE_WITH_CONCERNS** — sub-phase 3.2 builds clean. Both files at expected paths with expected content. All Phase 3.2 Done-when gates pass. 2 LOW WARNs (both are pre-flagged, master-decision deviations documented in dispatch caveats 6a + 6b). 0 FAILs. Master consolidates in `share/notes/04_warns_register_T-2026-07-04-009.md`.