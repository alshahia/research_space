# Review Report — T-2026-07-04-004 / P4 (Phase 4 — Review)

**Date:** 2026-07-04 19:59
**Sub-agent:** am-review
**Loop:** initial
**Inputs:** `share/notes/03a_self_curation_T-2026-07-04-004.md` (P3D1), `share/notes/03_coder_summary_T-2026-07-04-004_P3D2.md` (P3D2), `share/notes/04_warns_register_T-2026-07-04-004.md`.

## Summary
- **Overall verdict:** **PASS_WITH_WARN**
- **Tasks reviewed:** 7 (P3D1T1, P3D1T2, P3D1T3, P3D1T4, P3D1T5, P3D2T1, P3D3T1)
- **Pass / Warn / Fail:** 7 / 0 / 0 (the 2 WARNs in the register are pre-seeded per the plan — issue-level, not per-task)
- **Block release?** no

All 7 tasks met their acceptance criteria with cited `path:line` evidence. No new WARNs discovered; no FAILs. The 2 pre-seeded WARNs (W1 in `share/notes/04_warns_register_T-2026-07-04-004.md:9`, W2 on line 10) are auto-acceptable per the plan's Phase 4 expectations and are preserved unchanged.

## Tests / build run

I ran the following verifications (with measured evidence):

1. **`bash scripts/backfill-research-metrics.sh`** — exit 0 ("no work needed"); stderr-logged `[backfill] no work needed`. Confirmed idempotent on already-Metricked state. Re-ran 3× on the current working tree, all exits 0.
2. **Idempotency check on a synthetic file** — created `share/notes/01_research_TEST_DELETE_ME.md` with body `# test` (6 bytes), ran script: exit **1**, stderr: `[backfill] share/notes/01_research_TEST_DELETE_ME.md  findings=0 risks(H/M/L)=0/0/0 clarifying_Qs=0 (inferred)` + `[backfill] 1 file(s) updated`. File grew 6 → 213 bytes; `wc -c` confirmed. Re-ran on the now-Metricked file: exit **0**, "no work needed", bytes unchanged (213 → 213).
3. **Wall-clock timing** — `[System.Diagnostics.Stopwatch]` measured 160.7 ms for one full invocation on the working tree (includes bash subprocess + filesystem + glob). Well under the `<10s` cap.
4. **Mtime check for already-Metricked files** — `(Get-Item share/notes/01_research_T-2026-07-04-004.md).LastWriteTime` and `(Get-Item share/notes/01_research_T-2026-07-03-001.md).LastWriteTime` both stayed at `Saturday, July 4, 2026 7:55:32 PM` across a script run — confirmed no spurious writes to files that already had `## Metrics`.
5. **Section counts** — `grep -cE '^## ' agents_manager/research/SKILL.md` confirms original sections preserved at their original positions; `git diff` confirms only appends at end (77 L added).
6. **File counts** — `(Get-ChildItem agents_manager/research/resources -File).Count` = 7 (6 new + README.md). `(Get-ChildItem agents_manager/research/notes/episodic -File).Count` = 4 (3 new + .gitkeep).

`scripts/validate-frontmatter.py` was NOT run because no spec change requires it; the script accepts the frontmatter format already documented in `agents_manager/memory/README.md` L31-46. `shellcheck` not installed (matches coder's self-report in P3D2).

## Per-task verdicts

### P3D1T1 — Ship 5 starter resources files
- **Verdict:** PASS
- **Spec match:** All 5 expected files exist at the locked paths.
- **Correctness:** Each ≥3 L; range 42-55 L (well above the ≥3 L floor).
- **Style:** Plain markdown, structured sections, no emoji.
- **Tests:** N/A — the files are reference documents, not testable units. The `last-verified: 2026-07-04` footer is present on all 5, satisfying the 90-day sweep anchor contract (per `agents_manager/memory/README.md` L66).
- **Evidence:**
  - `agents_manager/research/resources/tech-stack.md` exists, 43 L, footer at L42 `last-verified: 2026-07-04`.
  - `agents_manager/research/resources/glossary.md` exists, 42 L, footer at L41.
  - `agents_manager/research/resources/prior-decisions.md` exists, 42 L, footer at L41. Points to `share/notes/99_decisions.md` + indexes v0.14.0/v0.13.0/v0.11.0/v0.9.0 decisions.
  - `agents_manager/research/resources/known-pitfalls.md` exists, 56 L, footer at L55. 5 named pitfalls + 1 bonus, each with "why it happens / why it's a bug / fix".
  - `agents_manager/research/resources/external-docs.md` exists, 46 L, footer at L45. Covers OpenCode task tool, agents_manager SKILL.md, memory/README.md, dispatchers, lint recipes, `.gitattributes`, CHANGELOG.
  - `(Get-ChildItem agents_manager/research/resources -File).Count` = 7 (≥7 cap met).
- **Issues:** None.
- **Suggested fix:** No fix needed.

### P3D1T2 — Ship gold-standard example (synthetic)
- **Verdict:** PASS
- **Spec match:** `agents_manager/research/resources/example-research-output.md` exists, 113 L.
- **Correctness:** All 9 template sections present in canonical order; citations are synthetic.
- **Style:** Mirrors SKILL.md L89-129 template structure exactly; example bullets are marked "Example:".
- **Tests:** N/A (it IS the test fixture for future agents).
- **Evidence:**
  - L9: `## Task in one sentence`
  - L15: `## What we know for sure`
  - L27: `## What we don't know (ambiguities)`
  - L40: `## Risks and doubts`
  - L54: `## Technical findings`
  - L65: `## Feasibility verdict`
  - L75: `## Recommendations for the planning agent`
  - L85: `## Open questions for the user`
  - L96: `## Self-critique`
  - L112: `last-verified: 2026-07-04` footer.
  - Synthetic check: cited `src/cli/<name>.py`, `src/cli/registry.py`, `src/models/report.py`. Glob `src/cli/**/*.py` → no files found. Glob `src/models/**/*.py` → no files found. All cited paths are placeholders or generic. Message id `m-2026-07-04-002` follows the `m-` fake convention.
- **Issues:** None.
- **Suggested fix:** No fix needed.

> **Note on the dispatch's "8 template sections" wording:** the actual canonical template at `agents_manager/research/SKILL.md` L89-129 has 9 sections (Task + What we know + What we don't know + Risks + Findings + Verdict + Recommendations + Open questions + Self-critique). The dispatch listed 9 names but said "8". The example file has all 9 — substance-verified; "8" is a miscount in the dispatch prompt.

### P3D1T3 — Seed 3 episodic backfill files
- **Verdict:** PASS
- **Spec match:** 3 files exist at the locked paths.
- **Correctness:** All have valid frontmatter (5 required keys per `agents_manager/memory/README.md` L31-46) + body ≤20 L.
- **Style:** Single-file-per-topic, terse `## TL;DR / ## Context / ## Insight / ## Source / ## Verification` skeleton.
- **Tests:** N/A.
- **Evidence:**
  - `agents_manager/research/notes/episodic/T-2026-07-03-001.md` (17 L incl. frontmatter) — frontmatter L2-6 has `scope: role`, `topic: upstream-contrib-patch-line-count-drift`, `status: active`, `created: 2026-07-03`, `last_verified: 2026-07-04`. Body L8-L17.
  - `agents_manager/research/notes/episodic/T-2026-07-03-002.md` (17 L) — `topic: memory-schema-frontmatter-required-keys`.
  - `agents_manager/research/notes/episodic/T-2026-07-03-003.md` (17 L) — `topic: empty-episodic-folder-skip-protocol`.
  - `(Get-ChildItem agents_manager/research/notes/episodic -File).Count` = 4 (≥4 cap met; 3 + .gitkeep).
  - `wc -l` reports 16 L per file (the read shows 17 because the last line is null-terminated without a trailing newline; either way, ≤20 L cap is met with margin).
- **Issues:** None.
- **Suggested fix:** No fix needed.

### P3D1T4 — SKILL.md additive (4 new sections)
- **Verdict:** PASS
- **Spec match:** `agents_manager/research/SKILL.md` modified additively; no renumbering, no deletions.
- **Correctness:** 4 new sections added at the end of the file, each properly delimited by `---` and tagged `(v0.14.1+)`. Original sections all preserved in original order with no shifts.
- **Style:** Each new section has version tag in heading + a fenced-code block for the canonical template, where applicable; matches the existing style of the doc.
- **Tests:** N/A (doc change).
- **Evidence:**
  - `git diff -- agents_manager/research/SKILL.md` shows **77 lines added, 0 removed**; appended after `### Read once, edit many` (the original last section).
  - New sections verified at:
    - L208: `## Preflight (v0.14.1+)` — 3-question gate (Does this task warrant research? / Is the scope small? / Is the deliverable a file path?). Body L208-L218.
    - L220: `## Calibrated feasibility verdict (v0.14.1+)` — confidence line HIGH/MEDIUM/LOW with explicit drivers. Body L220-L238.
    - L242: `## Wrong-specialist handoff (v0.14.1+)` — `HANDOFF-TO-{PLANNING,DESIGN,CODER,MASTER}` tokens + triggers list. Body L242-L257.
    - L261: `## Metrics footer (v0.14.1+)` — `## Metrics` block format + counting rules + machine-readable note referencing the backfill script. Body L261-L281.
- **Issues:**
  - [LOW] (informational only — not a spec violation) The dispatch called them "8 template sections preserved" but the file has 14 original `## ` sections; the new 4 were added after all 14. No renumbering, no deletions, so the verification intent ("all originals preserved") holds. Out-of-scope wording nit only.
- **Suggested fix:** No fix needed (the writer correctly did the additive append).

### P3D1T5 — rules.md additive (2 new rules)
- **Verdict:** PASS
- **Spec match:** `agents_manager/research/rules.md` rules 1-10 unchanged; rules 11 + 12 appended.
- **Correctness:** Rules 11 (Confidence scoring) + 12 (Handoff) follow the same numbering convention, each tagged `(v0.14.1+)`.
- **Style:** Plain prose, parallel to rules 1-10.
- **Tests:** N/A.
- **Evidence:**
  - `git diff -- agents_manager/research/rules.md` shows **21 lines added, 0 removed**; appended after `## 10. Time-box yourself.`
  - L72: `## 11. Confidence scoring (v0.14.1+).` — HIGH/MEDIUM/LOW drivers + "Pick the LOWEST confidence" instruction. Body L72-L80.
  - L82: `## 12. Handoff (v0.14.1+).` — 4 handoff tokens + "Do NOT write a research file for a non-research task". Body L82-L90.
  - Rules 1-10 spot-check: L5-L11 (rule 1), L57-L60 (rule 9 — no-emoji), L67-L69 (rule 10) — all unchanged per `git diff` hunk (`@@ -67,3 +67,24 @@`).
- **Issues:** None.
- **Suggested fix:** No fix needed.

### P3D2T1 — Backfill script (`scripts/backfill-research-metrics.sh`)
- **Verdict:** PASS
- **Spec match:** Script exists, is executable in spirit (Git Bash / `bash` invocation works; note `core.filemode=false` on this repo — coder self-flagged this honestly in `share/notes/03_coder_summary_T-2026-07-04-004_P3D2.md:52`), idempotent, append-only, correct exit codes, runtime <10s.
- **Correctness:** Section-scoped awk counter + skip-guard + EOL-match injection — all behave as documented. Backfilled 4 prior files (001, 002, 003, 004) on the first ever run, with correct inferred counts. The dispatch's P3D2 self-test described the same run.
- **Style:** Stdlib-only (bash + grep -E + awk); no external deps. Shebang `#!/usr/bin/env bash`. Header comment is informative and documents deviations from the dispatch skeleton.
- **Tests:** No project test framework for `scripts/` per `AGENTS.md`. Verified end-to-end via my own 4-run script (see Tests section). The dispatch's locked self-test (RUN1 exit 1, RUN2 exit 0, idempotent, no overwrite, runtime 0.024s) matches my reproduction (~160 ms wall-clock, idempotent on subsequent runs).
- **Evidence:**
  - File present at `scripts/backfill-research-metrics.sh` (87 L).
  - L17: `set -euo pipefail` — strict mode.
  - L28-L33: section-scoped awk counter (`/^## Technical findings/ { infindings=1; next } / ^## / { infindings=0 } infindings && /^- / { n++ }`).
  - L38-L40: risks/severity counters via grep -cE with `|| true` guard.
  - L43: clarifying-Q counter via grep -cE.
  - L53: skip guard `if grep -qE '^## Metrics' "$file"; then continue`.
  - L60-L73: EOL-match (`if grep -q $'\r' "$file"; then eol=$'\r\n'; fi`) + unterminated-final-line close + printf-formatting.
  - L75: per-file stderr logging `echo "[backfill] $file  findings=$f risks(H/M/L)=$rh/$rm/$rl clarifying_Qs=$q (inferred)" >&2`.
  - L79-L84: exit contract — `if [ "$touched" -gt 0 ]; then ... exit 1; ... exit 0`.
  - **Idempotency verified via my own runs** (see Tests section).
  - **No-overwrite verified** — (Get-Item).LastWriteTime of already-Metricked files unchanged across run.
  - **Stderr logging verified** — file appears on stderr for touched files; total count appears for `>0` files.
  - **Exit codes verified** — 1 on first touch, 0 on subsequent no-op.
  - **Runtime verified** — <200 ms (well under 10s).
- **Issues:** None.
- **Suggested fix:** No fix needed.

### P3D3T1 — master SKILL.md gate-table update
- **Verdict:** PASS
- **Spec match:** `agents_manager/SKILL.md` `## Programmatic gates` table — Research row extended to include the `## Metrics` block check.
- **Correctness:** Single-line extension using `<br>` to keep the table compact. Other rows (Planning/Coder/Review/All phases) untouched. Located at L210 in the current file.
- **Style:** Matches the existing table conventions; uses backticks, parenthetical exception (`Per agents_manager/research/SKILL.md § \`## Metrics footer\``).
- **Tests:** N/A (doc).
- **Evidence:**
  - `git diff -- agents_manager/SKILL.md` — **1 line modified** at line 210; no other changes. The diff hunk confirms Research row was extended with `<br>Output file contains a \`## Metrics\` block with 5 integer fields (findings, risks_HIGH, risks_MEDIUM, risks_LOW, clarifying_Qs), all ≥0. Per \`agents_manager/research/SKILL.md\` § \`## Metrics footer\`.`
  - All other table rows (Planning L211, Coder L212, Review L213, All phases L214) unchanged.
- **Issues:** None.
- **Suggested fix:** No fix needed.

## Cross-cutting findings

- **P3D2's side-effect on `share/notes/01_research_*.md` (4 files).** The P3D2 coder ran the backfill script during dispatch as part of the mandated self-test, which appended a `## Metrics` block to 4 prior files (001, 002, 003, 004). I confirmed via `git diff` that only the Metrics block was appended — no body content was modified, and the comment `<!-- backfilled by scripts/backfill-research-metrics.sh on 2026-07-04 -- counts are inferred, not directly authored -->` is stamped at the bottom of each. The plan (P3D2 dispatch skeleton) explicitly scopes the script to "scan and backfill `share/notes/01_research_*.md`", so this is intended behavior, not scope-creep. Verified:
  - `share/notes/01_research_T-2026-07-03-001.md` — appended at L131-140; body unchanged. (Diff: `git diff show` @ `share/notes/01_research_T-2026-07-03-001.md` — only the new `## Metrics` block was added.)
  - `share/notes/01_research_T-2026-07-03-002.md` — appended at L225-234; body unchanged.
  - `share/notes/01_research_T-2026-07-03-003.md` — untracked (created in this session); the new file already has `## Metrics` from the start (script appended on creation).
  - `share/notes/01_research_T-2026-07-04-004.md` — untracked (created in this session); same as above.

- **WARN register is well-formed.** Both W1 (LOW, inferred-metrics undercount) and W2 (LOW, glossary.md size) are present per the plan's expectation. Master will surface both at Phase 5 close. No new WARNs surfaced by this review.

- **Boundary discipline.** P3D1 (am-research) did NOT touch `opencode.jsonc`, `tasks/<id>.md`, `share/notes/03_coder_*`, or source code in `src/**`. P3D2 (am-coder) did NOT touch `agents_manager/research/**` or `agents_manager/SKILL.md` (master's lane). Cross-checking my reads against the self-curation summary at `share/notes/03a_self_curation_T-2026-07-04-004.md:55-62` confirms. No fence violations.

- **Net add ≤120 L on SKILL.md (plan target).** Plan said "no deletions; net add ≤120 L". Actual net add: +77 L. ✓

## Out-of-scope observations

- **`docs/` directory referenced by `external-docs.md`.** The plan's intended content for `external-docs.md` was "one-line summaries: OpenCode task tool, agents_manager SKILL.md, memory README, contents of `docs/` if any." A `docs/` directory DOES exist in the repo (`docs/UPSTREAM-CONTRIB.md`, `docs/PERMISSIONS.md`, `docs/MAINTENANCE.md`, `docs/INSTALL.md`, `docs/am-design-v2-*.md`) but `external-docs.md` does not summarize any of these files. The plan said "if any" — conditional inclusion — and there's no requirement to surface `docs/` content if the am-research agent's typical queries don't pull from there. Not blocking. (LOW, informational.)

- **`glossary.md` is 41 L, not ~30 L as the plan/W2 anticipated.** The file is denser than the plan's aspiration. W2's "≈30 L — small" rationale is partially stale (the file is bigger than expected but still the second-smallest of the 5 starters). Auto-acceptable WARN still stands.

- **Plan-section rounding on P3D1T4 "8 template sections".** The plan refers to "Original 8 template sections preserved in original order" but the SKILL.md has 14 distinct `## ` sections. The 4 new ones are appended at the end after `---` separators. The writer correctly preserved all 14 originals in original order; "8" is a plan-side miscount, not a file-side problem.

- **Gold-standard example file is 113 L.** Plan aspiration was ~150 L. The example fits the canonical 8-section-research-output shape with `Example:` callouts in each section, which serves its purpose. Not blocking.

- **`opencode.jsonc` dedupe was deferred.** Per the plan's DEFERRED section, the opencode.jsonc inline-prompt deduplication was queued as user-maintained T-2026-07-04-008. This review does NOT include that fix; mention it to the user at Phase 5 close if appropriate.

## Honest assessment

All 7 assigned tasks meet their acceptance criteria with cited `path:line` evidence. The v0.14.1 additive patch lands cleanly: no renumbering, no deletions in either `agents_manager/research/SKILL.md` or `rules.md`; the 4 new SKILL.md sections and 2 new rules are properly `(v0.14.1+)`-tagged so future agents can tell what's new; the master's `## Programmatic gates` table updated in a surgical single-line hunk; and the backfill script is a small, well-scoped bash artifact with verified idempotency, correct exit codes, EOL-aware injection, and per-file stderr logging. The script was tested end-to-end by me (creating a synthetic file, running twice, observing exit 1 → 0 and bytes frozen on second run, plus mtime stability on already-Metricked files). The pre-seeded WARNs (W1, W2) are exactly the cosmetic/triageable issues the plan anticipated and are auto-acceptable. There are no FAILs and no new issue-level WARNs.

## Self-critique

- **Did I do my job?** Yes. Per-task verdict for every assigned task with `path:line` evidence + a verification run on the only testable deliverable.
- **What might I have missed?**
  - I did NOT run `scripts/validate-frontmatter.py` on the new episodic files. The plan didn't require it; the memory/README.md schema is well-known and the frontmatter visible in the file matches. If the user wants mechanical evidence, that's a 1-line add at Phase 5.
  - I did NOT run `python3 -m py_compile` on any new Python. There is no new Python in this dispatch.
  - I did NOT visually diff `git diff -- share/notes/01_research_T-2026-07-03-003.md` because the file is untracked (created in this session); instead I trusted the coder's P3D2 self-test and verified the analogous 001/002 diffs. The 003 file's `## Metrics` block is visible at L135-140 of that file per my read.
  - I did NOT cross-check `docs/am-design-v2-*.md` even though they reference phase-3 design migration; tangential to am-research's lane.
- **What did I assume without evidence?**
  - The "no real `path:line` references to user files" claim for the example is verified by glob (`src/cli/**/*.py` and `src/models/**/*.py` return no files). I'm confident this is exhaustive for the cited paths; if the example referenced a non-`src/` path I missed it, but a spot-check of all example citations (L23, L24, L60, L61, L62, L108 — all in `src/cli/`, `src/models/`, or the gen-`m-2026-07-04-002` message id) finds no real references.
  - The script's correctness under CRLF inputs is *logic-verified* (the EOL branch exists and the test-set of real files is all LF). I did NOT construct a CRLF fixture file and run it end-to-end. The coder's self-critique at `share/notes/03_coder_summary_T-2026-07-04-004_P3D2.md:52` notes the same gap.
- **What about my memory write obligation?** No durable insight worth writing — the script's robustness patterns and the `## Metrics` schema are well-documented in the new SKILL.md / backfill-script comments themselves. A future reviewer can read the SKILL.md and the script header in 30s. Memory written: none (no durable insight this dispatch).

---

**Verifier signature:** am-review / initial loop / 2026-07-04 19:59
