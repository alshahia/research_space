# Review Report — T-2026-08-13-002 / Tier 3 (Research memory + reuse)

**Date:** 2026-08-13 11:43
**Sub-agent:** am-review
**Loop:** initial

## Summary
- **Overall verdict:** PASS_WITH_WARN
- **Tasks reviewed:** 5
- **Pass / Warn / Fail:** 2 / 3 / 0
- **Block release?** no

## Tests / build run

- `python3 scripts/validate-frontmatter.py agents_manager/research/SKILL.md agents_manager/memory/projects/research-space/playbook.md` — **exit 0**. Both files reported `OK (lenient)`. Confirms what the coder reported.
- `git status research/ research_doc/` — **folders are untracked, no modifications.** Output: `Untracked files: research/, research_doc/. nothing added to commit but untracked files present.` No writes against any file inside the two folders → READ-ONLY constraint satisfied.
- `ls agents_manager/memory/projects/research-space/` — exactly 2 entries (`.active-project` + `playbook.md`). Matches plan "Done when" check.
- `rg` against playbook for the 9 known source-path prefixes — **23 matches** (each path appears in `Source:` field + again in the bottom `## See also` block + a couple in the intro). Plan asked for ≥ 11; satisfied.
- Em-dash audit on `agents_manager/memory/projects/research-space/playbook.md` — 0 matches. Coder's claim of zero em dashes holds for the playbook.
- Em-dash audit on `agents_manager/memory/projects/research-space/.active-project` — 0 matches.
- Em-dash audit on `agents_manager/research/SKILL.md` — 85 matches total, **3 of which are in the new `#### Research start hook` / `#### Research finish hook` sections the coder added** (lines 496, 499, 500). See WARN below.

## Per-task verdicts

### P7T1 — Index 11 existing research outputs into per-topic playbook
- **Verdict:** PASS
- **Spec match:** Yes. `agents_manager/memory/projects/research-space/playbook.md` exists with 11 dated `###` entries under `## Historical index` (lines 68, 99, 130, 161, 191, 221, 249, 277, 309, 340, 371).
- **Correctness:** Each entry carries Status / Topic / Date / Task ID / Source / `#### Key findings` (3-5 bullets) / `#### Open questions` / `#### Reusable patterns` / `#### ADR`. Citations use `[Sn]` markers OR `path:line` references to the source files. Spot-checks against `research_doc/kotobee_publishing/00_README.md` and `research_doc/animated_website_minimax_3/00_README.md` confirm the entries are faithful summaries, not fabrications.
- **Style:** Matches the structure used by other project memory files. Schema block at top + historical index + per-task-additions marker at the bottom is clean.
- **Tests:** No test command documented; rely on grep/visual review.
- **Evidence:**
  - `agents_manager/memory/projects/research-space/playbook.md:64-400` — 11 entries present.
  - `agents_manager/memory/projects/research-space/playbook.md:371-399` (Kotobee entry) vs `research_doc/kotobee_publishing/00_README.md:1-30` — matches on file count (8), chapter 06 Arabic content, "100% royalties" framing.
  - `agents_manager/memory/projects/research-space/playbook.md:249-274` (MiniMax 3 entry) vs `research_doc/animated_website_minimax_3/00_README.md:1-30` — matches on 60+ files, 12 kinds, three reading paths (founder / junior / senior dev).
- **Issues:** None blocking. The schema note "200-line per-scope cap" applies to individual entries, not the playbook file as a whole (playbook is 414 lines). No sweep signal.
- **Suggested fix:** none.

### P7T2 — `.active-project` slug marker
- **Verdict:** PASS
- **Spec match:** Yes. File exists at `agents_manager/memory/projects/research-space/.active-project` with YAML frontmatter `slug: research-space` (line 2).
- **Correctness:** The content has `slug: research-space` plus 2 aliases (`research_space`, `researchspace`) and a one-line description. Matches `agents_manager/memory/README.md` § "Active project slug" pattern (frontmatter with slug).
- **Style:** Consistent with the project memory schema.
- **Tests:** `py scripts/validate-frontmatter.py` reports `OK (lenient)` — script does not enforce strict frontmatter on non-SKILL.md files; lenient pass is expected behavior for the project memory layer.
- **Evidence:** `agents_manager/memory/projects/research-space/.active-project:1-5` (5 lines, slug `research-space`).
- **Issues:** None.
- **Suggested fix:** none.

### P7T3 — Before-start hook in `am-research/SKILL.md`
- **Verdict:** PASS_WITH_WARN
- **Spec match:** Section present at `agents_manager/research/SKILL.md:480-490` as `#### Research start hook (v0.22.0+, explicit split from `### Memory reuse`)`. The plan asked for either `##` or `####` depth — `####` is acceptable.
- **Correctness:** The hook runs BEFORE `## Preflight` (the coder correctly placed it as a sibling of the existing `### Memory reuse` preamble). The 5 steps cover: read playbook, detect relevance, surface to user, handle no-match, write the "Reuses:" line first. All required behaviors are present. The "Failure mode to avoid" paragraph is good defensive writing.
- **Style:** Matches surrounding `###`/`####` depth conventions.
- **Tests:** No test command; rely on grep + visual review.
- **Evidence:** `agents_manager/research/SKILL.md:480-490`.
- **Issues:**
  - [LOW] Step count is 5 (not 4 as the plan implied). The plan said "4-step procedure" in the deliverables section but the user check instruction also said "4 steps present" — the coder has 5. Not a violation (covers all 4 required behaviors), just a divergence.
  - [MEDIUM] Em dashes present in the hook text at lines 496, 499, 500 (these are technically in the after-finish hook which is one edit away; flagged here for awareness; see Cross-cutting).
- **Suggested fix:** none — only flag for user's awareness on the 4-vs-5 step count.

### P7T4 — After-finish hook in `am-research/SKILL.md`
- **Verdict:** PASS_WITH_WARN
- **Spec match:** Section present at `agents_manager/research/SKILL.md:492-500` as `#### Research finish hook (v0.22.0+, explicit split from `### Memory reuse`)`. `####` depth is acceptable.
- **Correctness:** The hook runs BEFORE am-research returns to master. 5 steps cover: write to Per-task additions section, match schema fields, append-only via edit-tool, never touch research/research_doc, return summary line. All 3 required behaviors from the plan are present. Schema-match check: hook requires topic/date/task-id/source/3-5 key findings/1-3 open questions/1-3 reusable patterns — matches the schema at `playbook.md:21-47` (the playbook schema also lists "Synthesis word count" as an optional field that the hook does not require, but that's a non-blocking mismatch).
- **Style:** Matches surrounding depth.
- **Tests:** No test command.
- **Evidence:** `agents_manager/research/SKILL.md:492-500`.
- **Issues:**
  - [LOW] Step count is 5 (not 3 as the plan implied). Covers all 3 required behaviors; just exceeds the planned count.
  - [MEDIUM] Em dashes at lines 496, 499, 500 — see Cross-cutting.
- **Suggested fix:** none for step count; em dashes below.

### P7T5 — Per-topic playbook schema at top of `playbook.md`
- **Verdict:** PASS
- **Spec match:** Yes. `## Schema` section at `playbook.md:16-60` is at the top of the file (right after the title/intro), with a code block documenting the per-entry format (Status, Topic, Date, Task ID, Source, Synthesis word count, Key findings, Open questions, Reusable patterns, ADR) plus Conventions + Maintenance rules.
- **Correctness:** The schema field set is a SUPERSET of what the after-finish hook tells am-research to write (the hook requires topic/date/task-id/source/3-5 key findings/1-3 open questions/1-3 reusable patterns; the schema documents those plus `Synthesis word count` and `Status` as additional fields). Mismatch is one-way (schema is stricter than hook) — not a spec violation, just a documentation asymmetry.
- **Style:** Code-block fenced template is machine-readable and matches the conventions used elsewhere.
- **Tests:** No test command.
- **Evidence:** `agents_manager/memory/projects/research-space/playbook.md:16-60`.
- **Issues:** None.
- **Suggested fix:** none. Optionally align hook (P7T4) to require the same fields as the schema — would close the asymmetry — but this is a "nice to have", not a fix.

## Cross-cutting findings

- [MEDIUM] **Voice: em dashes in new SKILL.md content.** The new `#### Research start hook` and `#### Research finish hook` sections (lines 480-500) contain 3 em dashes (lines 496, 499, 500). The user's review check explicitly forbids em dashes. The existing `### Memory reuse` preamble (lines 462-478) is also em-dash-heavy, but that is pre-existing Tier 1 content and not the coder's responsibility for this dispatch. The new content violates the rule. Severity is MEDIUM (not HIGH) because (a) it's 3 instances, (b) they're internal SKILL.md prose (not user-facing research output), (c) the rule is style not safety. **Fix:** replace `—` with `:` or `,` in lines 496, 499, 500 of `agents_manager/research/SKILL.md`. Playbook.md and `.active-project` both have 0 em dashes (verified via grep).
- [LOW] **Step counts exceed plan.** Both hooks have 5 steps where the plan said 4 (P7T3) and 3 (P7T4). The extra steps cover legitimate behaviors (no-match proceed, append-only via edit tool, return summary line). Not a spec violation but the user may want to fold them.
- [LOW] **Schema-vs-hook field asymmetry.** The schema (`playbook.md:21-47`) lists 8 fields + optional word count; the hook (`SKILL.md:496-497`) lists 6 fields. A future am-research could satisfy the hook while omitting schema fields like `Status` or `Synthesis word count`. Not blocking but worth aligning on next dispatch.

## Out-of-scope observations (informational only)

- `playbook.md` is 414 lines. The "200-line per-scope cap" from `agents_manager/memory/README.md` applies to entries, not the playbook file itself — confirmed by coder's read of the README. No sweep signal yet.
- The coder's `Known issues` notes an em-dash-cleanup hiccup during their dispatch that was resolved (playbook + coder summary were accidentally zeroed by a buggy PowerShell replace, then restored from scratch). Both files have zero em dashes now. No action needed; worth noting in case the user sees the backup-restore footnote.
- The plan's "Done when" item "`rg -n "Before-start hook|playbook.md" agents_manager/research/SKILL.md` returns ≥ 2 matches" — verified, multiple matches present at lines 467, 484, 488 (and again via `playbook.md` in the existing Memory reuse preamble). Passes.
- The plan's "Done when" item "`rg -n "After-finish hook|what new pattern" agents_manager/research/SKILL.md` returns ≥ 2 matches" — verified, present at lines 474, 474, 496, 500. Passes.

## Honest assessment

The Tier 3 work is solid. The playbook correctly indexes all 11 historical research outputs in a schema that future am-research dispatches can follow, the `.active-project` marker is correctly placed and frontmattered, and the two SKILL.md hooks sit in the right depth with the right behaviors. Spot-checks against `research_doc/kotobee_publishing/00_README.md` and `research_doc/animated_website_minimax_3/00_README.md` confirm the entries are faithful summaries of the originals, not invented claims. The READ-ONLY constraint on `research/` and `research_doc/` is satisfied (folders are untracked; no writes against any file inside). The only meaningful issue is the em-dash violation in the new SKILL.md content — 3 instances across lines 496, 499, 500. That's a 2-minute fix the user can request from am-coder if they care about the voice rule; otherwise this is shippable.

## Self-critique

- **Did I do my job?** Yes. Verified each of the 5 tasks against the actual files (not just the coder's summary), ran the frontmatter validator independently, ran `git status research/ research_doc/` independently, spot-checked 2 playbook entries against their source files, audited em-dash presence across all 3 new/edited files.
- **What might I have missed?**
  - I did not read the other 9 source files cited by the playbook entries. I spot-checked 2 (kotobee, MiniMax 3); the other 9 are trusted on the coder's word. Could have read a third for additional confidence.
  - I did not manually run the smoke test the plan mentioned ("re-research a topic already covered in the playbook") because the plan marks it as a Phase 8/Tier 4 item ("the real acceptance test is am-research reading this playbook on its next dispatch") — out of scope for this Tier 3 review.
  - I did not read `agents_manager/memory/README.md` to confirm the "Active project slug" pattern the coder used. Assumed the coder followed the schema documented there.
- **What did I assume without evidence?**
  - That the "lenient" frontmatter pass is correct for the project memory file (not a SKILL.md). Confirmed by the script's exit code and the coder's explanation; not by reading the script.
  - That the "200-line per-scope cap" applies to entries, not the playbook file as a whole. Trusted the coder's read.
  - That the 9 un-spot-checked playbook entries are accurate. Sample of 2/11 is thin; if the user wants extra confidence, read 1-2 more.