# User task - T-2026-08-18-002 (verbatim)

**Captured:** 2026-08-18 10:09 (UTC+3)
**Task ID:** T-2026-08-18-002
**Channel:** chat

## Verbatim user request

> create a full detail suitable for agents/llms docs for capacitor  "https://capacitorjs.com/docs/" and what to use and what to not , instructions , guide , issue to avoid and what the solutions ( if exist ), how to convert  js/ts project to cross platform ( android/iphone/ ..etc ) , what the best to use with capacitor , how get the most of it and deliver app ( near native-like)   ,  and  add some examples , after the research add prompt to agents/llms to enhance/guide them to what the best path to follow and avoid the incorrect/bad path  , add a sets of question to agents to ask  himself when build the app to know what to do/not do .
> if you found things ( or know somethings ) that its must include plus to what i tell you , then add it .
> use if need it browsermcp 
> save the result/progress to new folder 

## Notes on interpretation (master)

- Produce an agent/LLM-facing Capacitor dossier, not application source code.
- Canonical deliverable folder: `capacitor-docs-2026-08-18/`.
- Cover Capacitor's official scope: installation, project structure, CLI, native runtime, plugins, configuration, native shells (iOS, Android, Web), live reload, native API access, storage, theming, accessibility, app store delivery, CI/CD, troubleshooting, and updated guidance for v6/v7.
- Explicit user demands:
  - What to use vs. what NOT to use (with rationale).
  - Instructions and step-by-step guide.
  - Issues/pitfalls to avoid and their known solutions (when they exist).
  - How to convert an existing JS/TS project into cross-platform.
  - Best companion tools/libraries/frameworks for Capacitor (UI, state, nav, native bridge, storage, etc.).
  - How to maximize native-like feel (performance, gestures, haptics, fonts, status bar, splash, animations).
  - Worked, runnable examples (config snippets + minimal code).
  - System prompt(s) for LLM agents: best path to follow, anti-paths to avoid.
  - Self-question set: questions the agent must ask itself when building the app.
  - Anything master/research knows to be "must include" beyond the explicit asks.
- Use BrowserMCP when plain fetch cannot access or validate rendered content; prefer the pre-fetch + Jina Reader (`https://r.jina.ai/<url>`) per `agents_manager/research/SKILL.md`.
- Source-of-truth: `https://capacitorjs.com/docs/` first; cross-validate with the Capacitor GitHub repo (issues, releases), npm package metadata for `@capacitor/*`, and recognized companion libraries (Ionic, Vue, React, Tailwind, Capacitor Awesome list).
- Every time-sensitive or version-specific claim must cite a source URL + access date.
- No external API key is required.
- This is a documentation deliverable only — no code is to be compiled into a native app. Treat "near-native" as an output-quality bar, not a requirement to actually ship a build.
