# Locked user decisions - T-2026-08-14-001 (build phase)

**Date:** 2026-08-14
**Source:** user reply in chat
**Status:** all 9 questions answered, locked, ready for am-planning.
**Supersedes:** `share/handoffs/00_decisions_T-2026-08-13-003.md`
  - Q6 was previously ambiguous ("A or B"); now locked to **A**.
  - Q8 was previously **C** (defer SaaS 6 months); now changed to **B** with build-order modifier (build last among tier2).

---

## Decisions table

| # | Question | Lock | Effect on the design |
|---|---|---|---|
| Q1 | Tier 2 tooling (bot/CLI in or out) | **D** — drop the whole kind | tier2-tooling removed. Bot / CLI / extension fold into tier1 + a layer when needed. |
| Q2 | Build order | **Order B** | tier1-standard first, then tier2 templates, then tier0-minimal last. |
| Q3 | Repair in place vs new beside | **B** — start new beside, archive old | New `templates/` family root. Old `resources/general-app-template/` archived with a pointer. |
| Q4 | Astro pin | **A** — `^7.2.1` | Already reflected in `02_STACK_MATRIX.md` row 5. |
| Q5 | `[UNVERIFIED]` verification policy | **A + C** | Planner runs `chub get <id>` at first use of every package. CI runs `scripts/verify-stack-claims.ts` weekly as drift gate. Maintainer quarterly review covers unpinned items. |
| Q6 | Maintenance owner | **A** — single named maintainer, no rotation | One person per quarter, named in `MAINTAINERS.md`. 1-2 days/quarter. |
| Q7 | Pilot before trusting cost headline | **B** — mini-pilot, N=3 builds, 1 agent, half-day | Three briefs: one tier0, one tier1+auth, one tier2 (mobile or ai-chat, picked at pilot time). Claude Code preferred. Headline stays `[MODEL]` until pilot validates it. |
| Q8 | Tier 2 SaaS | **B with build-order modifier** — promote to tier2-saas-bundle, build LAST among tier2 | Adds back `tier2-saas-bundle/`. Build order among tier2: ai-chat → mobile → storefront → saas-bundle (last). |
| Q9 | Handle the 5-7× → 2.7× correction | **C** — headline + footnote | Add one-line footnote to `00_README.md` Numbers section acknowledging earlier internal draft. |

---

## Net effect on the system shape

### Template family (8 templates)

| # | Template | Tier | Build order |
|---|---|---|---|
| 1 | `tier1-standard/` | 1 | **FIRST** (Q2 B) |
| 2 | `tier2-ai-chat/` | 2 | 2nd |
| 3 | `tier2-mobile/` | 2 | 3rd |
| 4 | `tier2-storefront/` | 2 | 4th |
| 5 | `tier2-saas-bundle/` | 2 | **LAST tier2** (Q8 modifier) |
| 6 | `cinematic-landing/` | specialist | 6th |
| 7 | `tier0-minimal/` | 0 | **LAST overall** (Q2 B) |

### Folder root and archive (Q3 B)

- New root: `templates/` (sibling to `research/`, not inside `resources/`).
- Archive: `resources/general-app-template/` → `resources/_archived/general-app-template/` with a `MIGRATION.md` pointer in the new `templates/` root.
- Per Q3 B, the old template is archived (not deleted) so any in-flight work can still reference it.

### Operational rules

- **Q5 (verification):** every `package.json` write must include a `chub get <id>` citation in the am-coder summary. Weekly CI gate runs `scripts/verify-stack-claims.ts` against the dossier's `[Sn]` markers.
- **Q6 (maintenance):** `MAINTAINERS.md` ships with one named maintainer per quarter (no rotation). 1-2 days/quarter.
- **Q7 (cost validation):** mini-pilot runs in Phase 3, before any cost headline is treated as measured. Until then, headline stays `[MODEL]`.

---

## What is unblocked

- am-planning can now produce the phased build plan from the dossier + this decision file.
- Phase 2 gate (user confirmation of the plan) still applies before am-coder dispatches.

## What is still blocked

Nothing. All 9 questions are locked. The build is gated only by the standard Phase 2 plan-approval step.

---

## Phase 2 Gate answers (2026-08-14 chat) — planner questions resolved

After the phased plan was written, am-planning surfaced 3 questions at the Phase 2 gate. User answered in chat:

| # | Question | Answer (verbatim) | Effect on the plan |
|---|---|---|---|
| P2G-A | `tier2-storefront`: Path A (Shopify) vs Path B (Medusa)? | "keep for later ( i will decide later ) , do not do it ." | **RE-GATE at the start of Phase 3.4** — master asks user to confirm A or B before scaffolding. Default remains Path A. Both `02_plan_high_T-2026-08-14-001.md` and `02_plan_phases_T-2026-08-14-001.md` Phase 3.4 updated to flag this. |
| P2G-B | `tier2-mobile` done-when: OK with `npx expo-doctor` exit 0 proxy? May add simulator later. **Also: add Capacitor support not only Expo.** | "yes , later may add a simulator , also add support to Capacitor not only expo ." | `tier2-mobile` becomes **multi-target**: Expo SDK 57 OR Capacitor 7, switched at intake via `mobile.config.ts`. SKILL.md and Phase 3.3 updated with both paths, doctor tests for both, and an explicit "real-device boot deferred" note. Estimate +0.5d (5.5d total). |
| P2G-C | Q7 mini-pilot: use Claude Code on Sonnet 4.6? | "just use sub agents from our sub agents to get it done" | Phase 3.8 rewired to dispatch **am-coder sub-agents via master** instead of an external Claude Code CLI. 3 briefs × 1 am-coder dispatch each. The measured USD/token numbers are now the production dispatch path's numbers (same model, same MCPs, same context rules), not a proxy. No external CLI needed. |

**Net effect on the plan:**
- Phase 3.4 storefront: re-gate added.
- Phase 3.3 mobile: Capacitor path added as alternate target; real-device boot deferred.
- Phase 3.8 mini-pilot: am-coder sub-agents replace external Claude Code.
- No other plan changes; 28.5d → 29d build estimate (+0.5d for Capacitor).

These 3 answers were applied to `02_plan_high_T-2026-08-14-001.md`, `02_plan_phases_T-2026-08-14-001.md`, `tasks/T-2026-08-14-001.md`, and this file. Plan ready for Phase 3.0 dispatch after user final plan approval.

---

## Out of lane (master does not edit; flagged for am-research maintenance phase)

- `00_README.md` Numbers section footnote (Q9 C) — minor copy edit.
- `02_STACK_MATRIX.md` rows 6a/6b/6c "unresolved decision" section (Q1 D) — now moot, replace with one short paragraph stating the kind dropped.
- `07_OPEN_QUESTIONS.md` (research/, read-only per AGENTS.md) — do not edit. Decisions are recorded here instead.

## Dossier chapters that need a small follow-up correction (post-build or as part of am-research maintenance phase)

1. `00_README.md` Numbers table: template count is now 8 (was 7 under Q8=C; SaaS added back).
2. `00_README.md` Numbers section: add Q9 C footnote.
3. `02_STACK_MATRIX.md` rows 6a/6b/6c: collapse into "kind dropped per Q1 D" paragraph.
4. `00_README.md` verdict table row 3: tier2 list is now `{ai-chat, mobile, saas-bundle, storefront}` (was `{ai-chat, mobile, saas-bundle, storefront, tooling}`).

These are cosmetic dossier touches. They can ship with the build or after — they do not block planning.

---

## Phase 2 Gate D answers (2026-08-14 chat) — model/provider decisions for tier2-ai-chat

After the Phase 2 Gate A/B/C approvals, user surfaced 4th-round questions on `tier2-ai-chat`'s model + provider strategy. Master researched the existing OpenCode CLI installation on this box + read OpenCode's public SDK/CLI docs, then surfaced the dual-path design for user approval. User approved via "continue" reply.

| # | Question | Decision | Effect on plan |
|---|---|---|---|
| D1 | "opencode go provider" → OpenCode as the provider? | **Yes — Path B added.** | New `src/lib/opencode.ts` + `scripts/start-opencode-server.ps1` + `tests/opencode-bridge.test.ts` in `templates/tier2-ai-chat/skeleton/`. |
| D2 | "minimax token plan provider" → MiniMax's openai-compat path? | **Yes — works in both paths.** | Path A: `@ai-sdk/openai({ baseURL: process.env.MiniMax_BASE_URL, apiKey: process.env.MiniMax_API_KEY })`. Path B: MiniMax is OpenCode's default model on this host (`minimax-coding-plan/MiniMax-M3`). |
| D3 | "gemini model" → default model? | **Path A default = `google/gemini-2.5-flash`** (cheapest, `GOOGLE_API_KEY` already on this host). **Path B default = `minimax-coding-plan/MiniMax-M3`** (whatever OpenCode is currently routed to). | Multi-vendor model registry in `src/lib/models.ts`; tier.config.json `modelFamily` axis picks at intake. |
| D4 | "support all allow the user to choose" → multi-provider? | **Intake-driven via `tier.config.json`.** | `template/tier2-ai-chat/prompts/intake-ai-chat.md` adds a "model family" question (single-select: google / anthropic / openai / MiniMax / opencode). Default = "google". |
| D5 | `.env.example` shape? | **Path A:** documents `GOOGLE_API_KEY`, `ANTHROPIC_API_KEY`, `OPENAI_API_KEY`, `MiniMax_API_KEY`, `MiniMax_BASE_URL`. **Path B:** documents nothing (auth is OpenCode's job; user's existing `~/.config/opencode/opencode.json` is the source). | `.env.example` ships with both sections, commented by default; intake flips the relevant block. |
| D6 | Path B same-box caveat? | **Document port-discovery step.** OpenCode defaults to random port (`--port 0`) + `127.0.0.1`; Vite dev server defaults to 5173; collision unlikely. | `SKILL.md` gains a "Path B setup" subsection explaining the port-discovery + `runtime/opencode-url.txt` flow. |

**Phase 2 Gate D follow-on effects (LOCKED):**
- 3.2 effort: 4d → **5d** (+1d for dual-skeleton; net vs the 5d from P2G-B Capacitor bump is 0d change, so plan estimate stays at 5d).
- New files in `templates/tier2-ai-chat/skeleton/`: `src/lib/opencode.ts`, `src/lib/models.ts`, `scripts/start-opencode-server.ps1`, `tests/opencode-bridge.test.ts`.
- New intake axis in `tier2-ai-chat/prompts/intake-ai-chat.md`: `modelFamily` (google/anthropic/openai/MiniMax/opencode).
- `agents_manager/MEMORY/projects/research-space/playbook.md` references both paths from now on.
- `share/notes/04_opencode_research_T-2026-08-14-001.md` is the research deliverable that backs D1-D6.

**Research deliverable saved at:** `share/notes/04_opencode_research_T-2026-08-14-001.md`.

**OpenCode CLI on this host (verified, READ-ONLY — no install/remove/kill per user directive):** v1.18.5 at `C:\Users\AhmadMhmoud\AppData\Roaming\npm\opencode.ps1`. Models list includes `opencode-go/minimax-m3`, `google/gemini-2.5-flash`, `opencode-go/gpt-5.6-luna`, etc. SDK surface = `session.prompt` (full agent end-to-end), `session.messages`, `find.text/files`, `file.read`. Per-message latency worse than Path A (full agent loop) — Path B is for "complex-task" UX, Path A for "snappy chat" UX.

---

## Phase 2 Gate A RE-DECISION — tier2-storefront Path B (Medusa 2 + Next.js 15 + Stripe)

**Original lock (2026-08-13 dossier):** **Path A (Shopify Storefront API + Hydrogen)** — 4d, hosted backend, default per dossier + plan. P2G-A (2026-08-14 chat): user said "keep for later ( i will decide later )" — call deferred to start of Phase 3.4.

**RE-GATE result (2026-08-14, at start of Phase 3.4):** user replied **"B"**. **Path B (Medusa 2 + Next.js 15 + Postgres + Stripe) is LOCKED.** No install/operate/abort on the existing template infra; same operational constraint as before.

| Field | Path A (REJECTED) | **Path B (LOCKED 2026-08-14)** |
|---|---|---|
| Backend | Hosted Shopify store | **Self-hosted Medusa 2 + Postgres (Docker + node-pg-mem for tests)** |
| Frontend | React Router 7 + Hydrogen | **Next.js 15 App Router** |
| Payments | Shopify checkout | **Stripe Checkout + webhook** |
| Files | 12 | **~40** (apps/backend 13 + apps/storefront 25 + shared root ~6) |
| Effort | 4d | **8d** |
| LOC | 2000 | **4500** |
| Risk | 3/5 | **4/5** (Postgres+Redis ops; Stripe webhook sig is load-bearing; Medusa 2 link-based; App Router) |
| Self-hostable | No (requires Shopify store) | **Yes** (no SaaS dependency) |
| Server cost | $29/mo Basic | **$0** (Vercel hobby + small VPS for Postgres/Medusa) |

**Why Path B (per user "B" reply, restated):** full data ownership + Next.js 15 server-rendering shared with 3.5 (tier2-saas-bundle) — both templates use App Router, so the 3.4 storefront unlocks a parallel build for 3.5 once 3.4 ships. Cost is ~2× scaffold effort (4d → 8d) but zero monthly SaaS cost thereafter. Postgres+Medusa operations live at deploy time, not scaffold time.

**Vite spine override (this template only — `tier2-storefront`):** Path B storefront uses Next.js 15 App Router (NOT Vite + React Router). All other templates stay Vite-spined. Documented in `share/notes/02_plan_high_T-2026-08-14-001.md:71` and `share/notes/02_plan_phases_T-2026-08-14-001.md` Phase 3.4.

**Path A preservation:** `templates/tier2-storefront/PATHS.md` ships a swap recipe (drop Next.js dir, swap `@/lib/medusa` for `@shopify/hydrogen` store import, drop Medusa backend). Path A remains viable as a future fork.

**Effect on plan totals:** **29d → 33d** (+4d for storefront Path B). Other phases unchanged. Phase 3.5 (tier2-saas-bundle) now has a HARD ordering dependency: **3.4 must ship first** so 3.5 reuses the Stripe webhook + checkout primitives.

**Effect on Q5 verification gate:** no new drift register rows expected. likely chub gaps: `medusa/medusa` + `medusa/medusa-js` + `medusa/medusa-cli` — coder will fall back to `npm view` + `https://docs.medusajs.com/`.

**Effect on Q7 mini-pilot:** the storefront brief is now a **strong 4th candidate** — it tests a backend AND a server-rendered frontend, unlike the other 3 (all client-only React). Recommending storefront over chat for the 4th brief slot if user wants N=4.

**Effect on Windows host (this build environment):**
- `medusa --version` exits 0 (CLI-only check).
- Postgres + Redis NOT installed → backend tests use `node-pg-mem` (in-memory) + no Redis. Recorded in `templates/tier2-storefront/apps/backend/SPEC.md` `## Deferred items`.
- `next build && next start` runs on Windows for the storefront.
- Stripe webhook signature verification uses Stripe Node SDK mock — no live Stripe call.

**User approval:** "B" reply at start of Phase 3.4 dispatch ask.

**Reversibility:** drop the apps/backend/` + `apps/storefront/` dirs, restore `templates/tier2-storefront/PATHS.md` Path A scaffold (saved as a 2-hour re-implement); 3.5 dependency un-tangles (saas-bundle re-derives its own webhook primitives). ~3 hours work.

**Plan files edited:** `share/notes/02_plan_high_T-2026-08-14-001.md` (3.4 row); `share/notes/02_plan_phases_T-2026-08-14-001.md` (3.4 section full rewrite). **Decisions log also updated:** `share/notes/99_decisions.md` got a `## 2026-08-14 — tier2-storefront Path B` section. **Tasks file:** `tasks/T-2026-08-14-001.md` P3T4 row rewritten + loop history row 11 appended.

---

## Phase 2 Gate E — Phase 3.5 spine pivot: Vite + React Router 7 + Express (was Next.js 15)

**Original lock (2026-08-13 dossier + 2026-08-14 plan):** Phase 3.5 (`tier2-saas-bundle`) was planned as **Next.js 15 App Router + Clerk pre-built UI + Stripe Billing + Resend + `@clerk/nextjs`**. Plan: `share/notes/02_plan_phases_T-2026-08-14-001.md` Phase 3.5 section (was lines 310-342). P3T5 row referenced `src/app/api/webhooks/stripe/route.ts` + `src/app/(auth)/sign-in/...page.tsx` Next.js patterns.

**Phase 3.5 disposition ask (2026-08-14 chat).** Master surfaced the React #31 env-block concern from Phase 3.4 (Windows + Next.js 15.5 static-prerender pipeline failure): "If Phase 3.5 also builds Next 15.5 on Windows and hits the same React #31 bug, the disposition B escape hatch (downgrade Next 14.2 LTS) becomes the recommended fix — and would invalidate the 'Next 15' lock for BOTH templates." Master offered Q1: A=stay Next.js 15 + accept pre-approved disposition B (env-deferred), or B=pivot to Vite + React Router 7.

**User reply (2026-08-14):** "Vite + react-router (Core / Full-Stack)8.3.0 Main active stable branch. Requires Node 22+ and React 19+" — Option B LOCKED.

**Spine pivot (locked 2026-08-14).**
- **Vite 8 + React 19 + TypeScript strict + Tailwind v4 + Vitest** (mirrors tier1-standard exactly — same as 3.1 + 3.2 + 3.3)
- **React Router 7.x (Declarative mode via `createBrowserRouter`)** — replaces Next.js App Router. User's "8.3.0 Main active stable branch" reference is from the React Router docs landing page; coder will pin via `npm view react-router version` (latest stable is 7.x; if 8.x exists in npm by 2026-08-14, pin that). The user also referenced "(Core / Full-Stack)" — Core = Declarative mode, Full-Stack = Data mode (formerly Remix). Master picked Declarative for SaaS bundle (no SSR needed).
- **Express 4 (single Node process)** — replaces Next.js API routes. Serves `POST /api/webhooks/stripe` + `POST /api/checkout` + `GET /api/portal` + serves the Vite-built `dist/` SPA for everything else in production.
- **Clerk React** (`@clerk/clerk-react`) replaces `@clerk/nextjs`. Auth pages use Clerk's pre-built `<SignIn>` + `<SignUp>` components inside React Router routes.
- **Node 22+ required** per user.

**Why Declarative (Core) mode over Full-Stack (Data/Framework) mode.** SaaS bundle doesn't need SSR (the public pages are pricing + auth pages which are client-rendered). Declarative is the lightest. Express handles the 3 server routes cleanly. Single deploy unit (one Node process serves SPA + API). If user later wants SSR for SEO, swap to Data mode (single change: `createBrowserRouter` → `createStaticHandler` + `createBrowserRouter` with server bundle).

**Files rewritten (~25, was ~15 in Next.js plan).** Removed: `src/app/api/{webhooks/stripe,checkout,portal}/route.ts`, `src/app/(auth)/sign-in/[[...sign-in]]/page.tsx`, `src/app/(auth)/sign-up/[[...sign-up]]/page.tsx`, `src/app/(marketing)/pricing/page.tsx`. Added: `index.html`, `vite.config.ts`, `server.ts` (Express), `src/router.tsx`, `src/routes/{sign-in,sign-up,pricing,dashboard}.tsx`, `src/components/{PricingTable,PlanBadge,NavBar}.tsx`, `src/lib/stripe-server.ts`. Auth.ts updated from Clerk-Next.js helpers to Clerk-React helpers.

**Effect on Phase 3.4 (tier2-storefront).** 3.4 stays Next.js 15 (Path B storefront for server-rendered ecommerce). 3.5 conceptually reuses the Stripe webhook + checkout patterns from 3.4 BUT NOT code-share (3.4 is Next.js, 3.5 is Vite + Express). 3.5 ships its own Express webhook/checkout routes. The "3.4 MUST ship first so 3.5 can reuse primitives" dependency softens to "3.4 SHOULD ship first so 3.5 can reference patterns" — no code-reuse, just pattern-citation.

**Effect on Vite-spine canonical status.** Now ALL 5 web tier2 templates share Vite as their primary frontend (3.1 base + 3.2 AI SDK + 3.5 SaaS bundle), except 3.4 (Next.js 15 storefront override for server-rendered ecommerce). Only ONE Vite-spine override in the family.

**Effect on Q5 verification gate.** No new drift register rows expected. Likely chub gaps: `react-router` + `@clerk/clerk-react` + `@clerk/express` + `resend` — coder falls back to `npm view` + official docs (`https://reactrouter.com/`).

**Effect on React #31 risk.** **REMOVED.** Vite pivot means 3.5 never touches Next.js. Windows host can build + test 3.5 cleanly (Vite 8 build works on Windows per Phase 3.1 verification).

**Effect on total plan effort.** Unchanged: 5d. Express is simpler than Next.js server-routing for the 3 API routes (~0.5d saved on prod-deploy docs); React Router 7 routes are slightly more verbose than Next.js App Router (~0.5d added). Net 5d.

**Effect on Q7 mini-pilot.** SaaS bundle remains a strong 4th candidate for the brief list (tests auth + billing + webhook). Now uses Vite + React Router + Express — exercises a different runtime surface than the other 3 (Vite-only). Recommend including for N=4.

**User approval:** single-message pivot directive, 2026-08-14 chat.

**Reversibility:** ~3h of work. Drop `server.ts` + Express deps; revert `src/router.tsx` to use `createStaticHandler` + `createBrowserRouter` with server bundle; convert `src/routes/*.tsx` to `app/routes/*.tsx`; convert `src/components/*.tsx` to `app/components/*.tsx`; swap `@clerk/clerk-react` → `@clerk/nextjs`; add `next.config.mjs`. Plan stays validated against the original Next.js 15 lock.
