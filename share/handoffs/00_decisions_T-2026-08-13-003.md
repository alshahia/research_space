# User decisions on open questions - T-2026-08-13-003

**Date:** 2026-08-13
**Source:** user reply to `research/agent-app-templates-2026-08-13/07_OPEN_QUESTIONS.md`
**Status:** all 9 questions answered.

The user's terse answers, mapped to options verbatim from the open-questions file:

| # | Question | User pick | Verbatim mapping |
|---|---|---|---|
| Q1 | Tier 2 tooling (bot/CLI in or out) | **D** | "Drop the whole kind." No tier2-tooling at all. Browser extension, bot, CLI all become tier1 + a layer when needed. |
| Q2 | Build order | **Order B** | tier1 first, then tier2 templates, then tier0 last. From `07` row 31-46. Fast path to a usable Tier 1 app. |
| Q3 | Repair in place vs new beside | **Option B** | Start new family beside it. Archive `resources/general-app-template/` with a pointer, do not delete it. The recommended migration path. |
| Q4 | Astro pin | **Option A** | Upgrade to `^7.2.1`. Already reflected in the corrected `02_STACK_MATRIX.md` row 5. |
| Q5 | `[UNVERIFIED]` verification policy | **Option A combined with Option C** | Planner runs `chub get <id>` at first use; CI runs `scripts/verify-stack-claims.ts` weekly as a drift gate. Maintainer quarterly review covers unpinned items. |
| Q6 | Maintenance owner | **Option A or B** | Either a single named maintainer per quarter or rotating across a small team. The user did not pick between A and B; master flags this as needing one word of clarification if Q6's answer is to be specific in any downstream document. The shape (single named owner, possibly rotated) is what matters. |
| Q7 | Pilot before trusting the cost headline | **Option B** | Mini-pilot: N=3 builds, one agent (Claude Code), half a day. Three briefs: one Tier 0, one Tier 1 with auth, one Tier 2 (mobile or ai-chat, to be picked at pilot time). |
| Q8 | Tier 2 SaaS graduation | **Option C** | Defer. Track the auth + billing cluster over 6 months of usage telemetry. Promote to `tier2-saas-bundle` only if the cluster holds. `02_STACK_MATRIX.md` row 3 stays as written (tier1 + layers). |
| Q9 | Handle the 5 to 7x correction | **Option C** | Both: corrected headline in the Numbers table, AND a one-line footnote acknowledging the earlier internal draft. This is a presentation-only call. |

## Net effect on the recommended design

The decisions land as follows on the system shape:

- Template count drops from 8 to **7** (Q1 removes tier2-tooling entirely). The family is: tier0-minimal, tier1-standard, tier2-ai-chat, tier2-mobile, tier2-storefront, plus cinematic-landing. SaaS is tier1 + auth + billing layers, content is tier1 + content layer, CRUD is tier1 + admin layer. CRUD dashboard is also tier1 + admin layer per `02` row 8.
- The folder that gets built first per Q2 is `templates/tier1-standard/`. `tier0-minimal/` is the last priority, not the first.
- Q3 commits to a new `templates/` family root beside (not inside) `resources/general-app-template/`. The old template gets archived. Nothing in `resources/` is touched in this task; the archive is a separate file move that should ship with the new family.
- Q5 commits to writing `scripts/verify-stack-claims.ts` as part of the build pipeline and to a chub-call convention at scaffold time.
- Q7 commits to a 0.5-day pilot before the cost headline is treated as measured. Until then, the headline stays labelled `[MODEL]`.
- Q9 is purely additive: one footnote line in `00_README.md` Numbers section. Trivial.

## Dossier chapters that need a small correction pass

Q1 removes tier2-tooling from the recommended family, which changes the count in three places in the dossier:

1. `00_README.md` line 16 row 3 of the verdict table reads "5 tier2 (ai-chat, mobile, saas-bundle, storefront, tooling)" — tooling drops.
2. `00_README.md` "The numbers that matter" line 38 reads "Template count | 8" — drops to 7.
3. `02_STACK_MATRIX.md` rows 6a, 6b, 6c, the bot/extension/CLI deep dives, and the "## The unresolved decision" section are now moot and should be replaced with a single short paragraph that says the kind dropped and the use cases fold into tier1 + a layer.

These are corrections to research-deliverable text. Per master lane, I do not edit dossier chapters directly (they are am-research output); a small dispatch to am-research is the clean way to apply Q1's correction and Q9's footnote.

## What is now unblocked

After the correction pass:
- A planning task can be dispatched to turn the seven decisions + the corrected dossier into a phased build plan. This task is research-only; the planning task is a separate task the user opens when ready.
- The pilot (Q7) can run against any agent that is available locally. The 0.5-day budget fits inside a single session.

## What is still blocked

Nothing. All nine are answered. The block was scope (research-only at the Phase 0 gate), and only the user can lift that.

## Open clarification

Q6 was answered as "Option A or B" rather than picking one. The two options differ only on whether the single named maintainer rotates or stays put. For the planning task, the answer matters for the `MAINTAINERS.md` shape but not for the template shape. Master flags this for the user's one-word follow-up if needed; otherwise the planning task can write the file to accept either via a documented rotation convention.