# User task - T-2026-08-18-003

**Captured:** 2026-08-18 (master)

## Literal request

> create 3 agent(s) exmples in "opencode-sdk-agent-docs" in new folder and ensure its working
> you can read and follow 'opencode-sdk-agent-docs'

## Restated goal

Create **3 runnable agent example programs** in a **new folder inside `opencode-sdk-agent-docs/`**
(that folder is the source-grounded SDK dossier from task T-2026-08-18-001). The examples must
follow the dossier's verified recipes and safety labels exactly (explicit model pin, `server.close()`
in `finally`, not-verified discipline, 4-line comment header convention, freshness footer).
"Ensure its working" = each example must be **live-run** (bun) and pass its smallest-validation
contract before the task closes.

## Context facts (verified read-only by master on 2026-08-18)

- CLI `opencode` 1.18.3 on PATH; bun 1.3.14; Node v24.11.1; PowerShell host (win32).
- Providers configured: MiniMax, OpenCode Go, Google, Hugging Face, OpenCode Zen (5 credentials).
- Dossier primary model pin: `opencode / deepseek-v4-flash-free` (Zen free tier).
- Dossier fallback (approved at T-2026-08-18-001 phase-2 gate): `opencode-go / deepseek-v4-flash`
  (verified working on this host, single run ~USD 0.009).
- SDK `@opencode-ai/sdk@1.18.18` NOT installed in repo root; the examples folder will own its own
  package.json + install. `node_modules/` stays untracked; user retains commit authority.
- `opencode-sdk-agent-docs/` currently holds 11 of 14 dossier files (3A-3D done, 3E1/3E2/3F pending).
- This task is NEW work; it does NOT edit any existing dossier file. Non-scope is inherited from
  `00_README.md` (docs folder; no library code) -- the new folder is the first code artifact subset.

## Boundaries

- New files ONLY in the new examples folder (+ task tracker/handoff/summary artifacts under share/).
- Do NOT modify the 11 existing dossier docs (they belong to T-2026-08-18-001's review gates).
- No commits; no global CLI upgrade; no auth-file reads; synthetic prompts only.
- Bounded provider execution: same ceilings as the approved T-2026-08-18-001 policy (30 s wall
  clock, tiny JSON schema, one attempt, no retry, PID-scoped cleanup).