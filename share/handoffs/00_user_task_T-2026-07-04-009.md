# User task — T-2026-07-04-009 (verbatim capture)

**Captured:** 2026-07-04

## User's literal intent

> i would like to add extract/convert to template tool/instruction that allow the agents to convert the current project ( when used  agents-manager in other projects) to template for later use or extract the most need/common/core part to guide other agents when used in other project to create a similar result or get benefit from it to not recreate the well or not repeat the same issues/problems when working on the same field or similar project.
> conduct a research (multi if need it ) then discuss/brainstorm with other agents ( get all opinion so its biased to certain specialization/role) and then provide me with you thought/report/analysis/solution

## What the user is asking for

Two related but distinct capabilities, both meta-tooling for the `agents_manager` controller:

1. **Extract-to-template tool/instruction** — an agents-manager feature that lets a user (or the system itself) take a current project that was built *with* agents-manager and convert it into a reusable template (alongside the existing `templates/_blank/`, `templates/cinematic-landing/`, `templates/dashboard/`, `templates/docs-site/`) for later use in OTHER projects.

2. **Extract-the-core-to-guide-other-agents** — a related but lighter-weight feature: from a finished project, extract the "most needed/common/core part" (e.g. recurring pitfalls, design decisions, conventions, knowledge) so that other agents — when dropped into a similar project — can benefit from that prior work and avoid recreating the wheel or repeating the same issues.

The user explicitly wants:
- Multi-angle research
- Multi-agent brainstorm (opinions from ALL roles, not biased to one specialization)
- Master's synthesized thought/report/analysis/solution

This is a discovery-and-design task. No code yet. Phase 1 (research) + master synthesis is the deliverable for this turn. Phase 2 (planning) is gated on user confirmation.

## Out of scope for this task (for now)

- Implementing the tool/instruction (Phase 3+)
- Choosing a single implementation approach
- Modifying `templates/AUTHORING.md`, any specialist SKILL, or `opencode.jsonc`

These are downstream of user confirmation on the report.