# Ship Report - T-2026-08-18-001 (Phase 5 close-out analysis)

**Date:** 2026-08-19 09:50 (Asia/Baghdad)
**Sub-agent:** ship (in-line; am-ship skill not in available skill catalog; protocol read from `agents_manager/ship/SKILL.md`)
**Verdict:** N/A — am-ship pre-flight correctly aborts. Documenting the analysis so the user can decide what to do next.
**Branch:** `master` (base branch — cannot be tagged per am-ship hard rule)
**Working tree:** dirty (80 paths)

## Pre-flight (per am-ship Step 1)

```
git branch --show-current  -> master
git status --porcelain    -> 80 paths (3 modified, 77 untracked)
```

Per `agents_manager/ship/SKILL.md` Step 1:
- **Branch is base** (`master`) — STOP. Abort; never tag the base branch.
- **Working tree is dirty** — STOP. Surface what's uncommitted; do not auto-include untracked files.

Both STOP rules trigger before the validator suite runs.

## What am-ship would do — and why it does not apply here

`am-ship` is the **release specialist for the agents-manager controller** itself. It bumps `VERSION`, inserts a CHANGELOG block, commits the change, tags `vX.Y.Z`, and pushes to `origin`.

For the OpenCode SDK research dossier task `T-2026-08-18-001`:

| Pre-flight check | Result | am-ship action |
|---|---|---|
| Are there controller code changes to ship? | **No.** Task produced `opencode-sdk-agent-docs/` (untracked, downstream deliverable). No edits to `agents_manager/<role>/SKILL.md`, no edits to `bin/agents-manager*`, no edits to `scripts/`, no edits to `opencode.jsonc`. | abort - nothing to ship |
| Is current branch a release branch? | **No.** On `master`. | abort - hard rule |
| Is working tree clean? | **No.** 80 paths dirty (most are notes/handoffs/task files for in-flight research work). | abort - hard rule |
| Does latest review pass? | YES (PASS_WITH_WARN per `share/reports/04_review_T-2026-08-18-001_P3.md`). | proceed |

Three of the four pre-flight rules STOP. am-ship correctly aborts.

## What the dossier needs instead

The OpenCode SDK dossier is a **research deliverable**, not a controller release. The Phase 5 close-out for a deliverable like this is the **task close-out flow**, not `git tag`:

1. **Commit the dossier** to the controller repo (when and how is up to the user; project hard rule "Do NOT commit unless explicitly asked").
2. **Update `tasks/T-2026-08-18-001.md`** with the final phase rows + status (DONE / DONE_WITH_CONCERNS).
3. **Archive the task files** per project convention:
   - `share/handoffs/00_user_task_T-2026-08-18-001.md` -> task archive
   - `share/handoffs/0[1-5]_*.md` (handoffs) -> task archive
   - `share/notes/01_research_T-2026-08-18-001*.md` -> task archive (or stay as research-input)
   - `share/notes/02_plan_*.md`, `03_coder_summary_*.md`, `04_review_*.md`, `04_warns_register_*.md` -> task archive
   - `share/reports/04_review_*.md` -> task archive
   - `opencode-sdk-agent-docs/**` -> stays at repo root (intended location per agent convention; or move to a `tasks/T-2026-08-18-001/dossier/` folder if a sub-archive is preferred)
   - `tasks/T-2026-08-18-001.md` -> task archive
4. **Append a CHANGELOG entry** somewhere *user-visible* — note that `agents_manager/CHANGELOG.md` is for controller changes (not dossier deliverables). For a dossier, the user-facing artifact is the dossier itself; downstream consumers will read `opencode-sdk-agent-docs/00_README.md`.

I have NOT done any of these — they all need explicit user authorization per project hard rules.

## Health check (cross-reference)

`share/health/2026-08-19.json` + `share/notes/05_health_2026-08-19.md`: composite **5.0 / 10 (FAIL)**.

- Frontmatter validator crash (`scripts/validate-frontmatter.py:74`) on 3 SKILL.md files with UTF-8 BOM.
- Python `py_compile` clean (10/10 on all 3 controller scripts).
- Shellcheck unavailable (npx download rate-limited 403; shell dimension skipped with weight redistribution).
- Recommended next action: spawn `am-coder` to fix findings 1-4.

The health finding is independent of the dossier task; it pre-dates this work and would surface on any controller-only phase close-out.

## Self-critique

- **Did I follow the checklist?** No (could not — pre-flight aborted at step 1, before validation, before VERSION, before CHANGELOG, before tag). That is the correct behavior for `am-ship` on this task — the abort is the work.
- **What might I have missed?** none for am-ship. The dossier close-out checklist above is a separate human-action workflow, not an `am-ship` concern.
- **Did I avoid force-push / pre-base-branch tags / etc.?** yes — none of those paths were reachable because pre-flight stopped at step 1.
- **Did I avoid skipping validation?** yes — but no validator was run because pre-flight aborted first.
- **Did I avoid force-inserting a CHANGELOG block?** yes — none inserted.

## Status

**NEEDS_CONTEXT** — `am-ship` correctly reports it has no controller release to ship for this task. Phase 5 close-out for the dossier requires user authorization per the project's "do NOT commit unless asked" hard rule.

The Phase 5 specialist outputs (`share/health/2026-08-19.json`, `share/notes/05_health_2026-08-19.md`, this file) are written and trace-verifiable. The user can read them, then decide whether to invoke `am-coder` for the controller health findings and whether to authorize the dossier close-out actions.
