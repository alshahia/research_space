# Plan — T-2026-07-03-003 (consolidated brief)

**Date:** 2026-07-03
**Sub-agent:** planning
**User intent:** Compare our applied v0.12.0 cinematic-landing template state against the source project's post-T-005/T-004 state and integrate the v2-axis deltas (a) 4 source memory files, (b) source PROPOSED_PATCH.md §F update, (c) extend our skeleton to absorb v2 axes.
**Q-answers (locked):** Q1=(d) cp+update MOVED pointers, Q2=(a) copy demo+proposal ~189 KB, Q3=(b) EXTEND skeleton (~271 LOC), Q4=(b) RENUMBER (11/12/13/14), Q5=(b) EXTENDED (full 7-zone §F).

---

## 1. Goal (one sentence)

Bring our applied v0.12.0 cinematic-landing template up to the source's post-T-005/T-004 state — by copying 4 v2 memory files (renumbered 11/12/13/14), copying the worked-example + proposal for traceability, extending our skeleton ~271 lines to absorb the v2 axes (dark theme + a11y + DPR + JS controller + FOUC script), and updating the source PROPOSED_PATCH.md §F in all 7 zones — all under a new top-level v0.12.1 CHANGELOG entry (peer of v0.12.0 and v0.13.0).

## 2. Non-goals

- Renumbering `04-locale-handoff.md` (out of scope; user picked Q4=(b), not (c)).
- Editing `opencode.jsonc` (no changes needed).
- Editing `agents_manager/SKILL.md` line 72 ("9 memory files" → "14 memory files") — soft wall; flagged as follow-up.
- Adding frontmatter to new template memory files (template convention forbids it; v0.13.0 schema does not apply to `templates/<name>/memory/`).
- Touching `agents_manager/<role>/SKILL.md` files (planning's wall).

## 3. Approach (chosen, with rationale)

The work decomposes into 4 phases (3a cp-and-stage → 3b skeleton extension → 3c §F sync → 3d cleanup). The phases are dependency-ordered so that the substantial skeleton extension (3b) sits on top of a fully-staged file base, and the final cleanup (3d) is the last to touch files (so its counts are correct).

**Why this order:**
- cp demo folder FIRST (P3T2) → gives us the source-side state to read from for the skeleton extension.
- cp 4 v2 memory files (P3T1) → installs the template-author content; later MANIFEST update needs them present.
- MANIFEST update (P3T3) → locks the canonical file list before substantial work.
- Skeleton extension (P3T6, ~271 LOC, ~30 min) → the workhorse; done while state is consistent.
- §F sync (P3T4, 7 zones) → needs the skeleton-extension LOC delta as a "source state" reference.
- MOVED pointers (P3T7) → needs the destination path to be final.
- 05-theming shrink (P3T5) → needs the new `14-dark-theme.md` to be in place.
- CHANGELOG + count fixes (P3T8) → final cleanup; needs everything else done.

**Why v0.12.1 not v0.14.0:** This is a back-fill of v0.12.0's memory folder (4 files added to an existing template) + a source-proposal update + a skeleton-extension + a count-fix. It's not a new feature. v0.14.0 would imply a new stacked feature.

**Why renumber to 11/12/13/14 (Q4=b):** Avoids the existing duplicate-`04-` pattern in our memory folder. Number 10 stays empty (gap is fine). Verified: our 10 existing template memory files all have no frontmatter — matches source convention.

---

## 4. Phases (one-line each)

1. **Phase 3a — cp-and-stage (P3T1, P3T2, P3T3)** — Copy demo folder + proposal + 4 v2 memory files + MANIFEST update. ~5 min.
2. **Phase 3b — skeleton extension (P3T6)** — Substantial ~271 LOC extension of our skeleton to match source's 1167-line state. ~30 min.
3. **Phase 3c — §F sync (P3T4)** — 7-zone text edits to source PROPOSED_PATCH.md. ~15 min.
4. **Phase 3d — cleanup (P3T5, P3T7, P3T8)** — MOVED pointer update, 05-theming shrink, CHANGELOG + count fixes. ~10 min.

Total: ~60 min wall-clock; ~271 LOC added + ~10/-2 lines PROPOSED_PATCH + ~30 lines CHANGELOG + ~16 lines demo-folder headers + (-15/+4 lines 05-theming) + +6 lines MANIFEST.

---

## 5. Sub-task decomposition (8 tasks, dependency-ordered)

### P3T2 — Copy worked-example demo folder + proposal to our root [Phase 3a]
**Source → Target:**
- `E:\js_projects\3d_website\1_website_minimax_3\cinematic-landing-kit-demo\` → `E:\context_gen\cinematic-landing-kit-demo\` (3 files + 4 memory files, ~106 KB)
- `E:\js_projects\3d_website\1_website_minimax_3\agents_manager\upstream-contrib\PROPOSED_PATCH_v0.5.x_2026-07-01_cinematic-landing-template.md` → `E:\context_gen\agents_manager\upstream-contrib\PROPOSED_PATCH_v0.5.x_2026-07-01_cinematic-landing-template.md` (~83 KB)

**Mechanical steps:**
1. `Copy-Item -Path "E:\js_projects\3d_website\1_website_minimax_3\cinematic-landing-kit-demo" -Destination "E:\context_gen\cinematic-landing-kit-demo" -Recurse -Force`
2. `Copy-Item -Path "E:\js_projects\3d_website\1_website_minimax_3\agents_manager\upstream-contrib\PROPOSED_PATCH_v0.5.x_2026-07-01_cinematic-landing-template.md" -Destination "E:\context_gen\agents_manager\upstream-contrib\PROPOSED_PATCH_v0.5.x_2026-07-01_cinematic-landing-template.md" -Force`
3. Verify: SHA-256 of `cinematic-landing-kit-demo/index.html` and the proposal file matches source.

**Files expected:** 1 new folder + 1 new file, ~189 KB total.

**Acceptance:** Both `Test-Path` returns True; SHA-256 matches.

### P3T1 — Copy 4 v2 memory files to `templates/cinematic-landing/memory/` (RENUMBERED 11/12/13/14) [Phase 3a]
**Source → Target (renumbered per Q4=b):**
- `…\cinematic-landing-kit-demo\memory\09-canvas-a11y.md` → `…\templates\cinematic-landing\memory\11-canvas-a11y.md`
- `…\cinematic-landing-kit-demo\memory\10-reduced-motion-listener.md` → `…\templates\cinematic-landing\memory\12-reduced-motion-listener.md`
- `…\cinematic-landing-kit-demo\memory\12-keyboard-nav.md` → `…\templates\cinematic-landing\memory\13-keyboard-nav.md`
- `…\cinematic-landing-kit-demo\memory\13-dark-theme.md` → `…\templates\cinematic-landing\memory\14-dark-theme.md`

**Mechanical steps (per file):**
1. `Get-Content <source> | Select-Object -Skip 5` (skips the 5-line `<!-- TODO(template-migration): -->` header) → save to `<target>` (with appropriate renumbering).
2. OR: `Copy-Item` the source, then `edit` to replace the L1-5 TODO comment with nothing (strip).

**Files expected:** 4 new markdown files, ~45 KB total (8,520 + 10,567 + 7,496 + 18,814 bytes).

**Acceptance:** Each new file matches source content byte-for-byte (except stripped TODO header). NO frontmatter added.

### P3T3 — Update `templates/cinematic-landing/assets/MANIFEST.txt` [Phase 3a]
**Target file:** `E:\context_gen\templates\cinematic-landing\assets\MANIFEST.txt`

**Mechanical steps:**
1. Read current MANIFEST.txt (17 file entries listed).
2. Add 6 new entries (sorted, to match alphabetical order):
   - `templates/cinematic-landing/memory/04-locale-handoff.md` (W3 was missing)
   - `agents_manager/assets/SKILL.md`
   - `agents_manager/assets/rules.md`
   - `agents_manager/assets/README.md`
   - `agents_manager/assets/notes/branch-decisions.md`
   - `agents_manager/assets/resources/landing-review-checklist.md`
3. Update the top-of-file comment block: "17 files" → "23 files".

**Files expected:** 1 modified file, +6 lines (17 → 23 file entries).

**Acceptance:** `find templates/cinematic-landing/ agents_manager/assets/ -type f | sort` minus comments matches MANIFEST.txt sorted entries, returns empty diff.

### P3T6 — Extend `templates/cinematic-landing/skeleton/index.html` to absorb v2 axes [Phase 3b]
**Target file:** `E:\context_gen\templates\cinematic-landing\skeleton\index.html` (currently 896 lines, will become ~1167 lines)

**Source of v2-axis content:** `E:\context_gen\cinematic-landing-kit-demo\index.html` (1167 lines, post-T-004)

**What to add (research §J matrix — 24 absences):**

| # | Item | Where in skeleton (target) |
|---|---|---|
| 1 | `:root[data-theme="dark"]` 14-token counter-table | After existing `:root{...}` block (L36) |
| 2 | Mode-aware `--gold-text-top: var(--gold-deep)` token | Inside `:root` |
| 3 | `#ritual .idx { color: var(--gold-bright); }` (K2 reverted) | After existing `#ritual` block |
| 4 | 6× `data-ambient-dark="..."` per-section attributes | hero, film, reveal, ritual, cta, editions |
| 5 | FOUC prevention `<script>` in `<head>` (before any CSS) | Top of `<head>` (before stylesheet link) |
| 6 | Theme controller JS (~40 LOC): setTheme + localStorage + prefers-color-scheme auto-detect + IntersectionObserver for ambient tween | Bottom of main `<script>` block (after CTA section logic, before close) |
| 7 | Header theme toggle `<button>` (id="theme-toggle") | Inside `<header>` next to brand |
| 8 | Film `role="img" aria-label="..."` + hidden `<ol class="film-transcript">` + 6 `<li>` | In `<section id="film">` |
| 9 | Film `aria-current="step"` toggled per scrub | In the GSAP scroll callback for film |
| 10 | Memo on `_lastFilmN` (avoid setAttribute/frame at 60Hz) | In the GSAP scroll callback |
| 11 | `tabindex="-1"` on all 6 sections (hero, film, reveal, ritual, cta, editions) | On each `<section>` opening tag |
| 12 | Section `:focus-visible` gold-deep ring (already have universal; tighten to gold-deep) | Inside `:focus-visible` rule |
| 13 | `aria-keyshortcuts="PageDown PageUp"` on `<section id="film">` | On `<section id="film">` opening tag |
| 14 | PageDown/PageUp section-snap keydown handler | In main `<script>` block (document-level listener) |
| 15 | CTA-scope gate on arrow keys (IntersectionObserver threshold 0.35) | Replace L876-880 unconditional handler |
| 16 | `if(e.target.tagName === "BUTTON") return;` guard | Inside the gated arrow key handler |
| 17 | Lenis `lenis.scrollTo(tgt, {offset:0})` for PageDown/PageUp | In section-snap handler |
| 18 | DPR srcset ladder (600/1200/1800/2400w) on hero img | On the `<img>` inside `<section id="hero">` |
| 19 | DPR `sizes` attribute (mobile-first) | Same `<img>` |
| 20 | Memo on `_lastFilmN` (avoid setAttribute/frame) | In GSAP scroll callback for film |
| 21 | `__reducedMotionActive` flag (window scope) | In the reduce-motion listener (L611-615 area) |
| 22 | `__lastReduce` defensive guard | Same listener |
| 23 | Safari < 14 `addListener` fallback | Same listener |
| 24 | Theme toggle `aria-pressed` toggle | In theme controller |

**Mechanical steps:**
1. Open `templates/cinematic-landing/skeleton/index.html` (current 896 lines).
2. Open source `cinematic-landing-kit-demo/index.html` (1167 lines) for reference.
3. Add items 1-24 one logical block at a time. After each block, verify by viewing the diff (no syntax breakage).
4. Total expected LOC delta: +271 lines (896 → ~1167).

**Files expected:** 1 modified file, +~271 lines (896 → ~1167 lines).

**Acceptance:** `wc -l` returns 1100-1230; all 24 items in research §J matrix are present via grep; syntax-validates as HTML (no unbalanced tags).

### P3T4 — Update source PROPOSED_PATCH.md per §F (7 zones) [Phase 3c]
**Target file:** `E:\context_gen\agents_manager\upstream-contrib\PROPOSED_PATCH_v0.5.x_2026-07-01_cinematic-landing-template.md` (1888 lines)

**Zones to edit (per research §G matrix):**

| # | Section | Line | Edit |
|---|---|---|---|
| Z1 | §1 Executive summary | L147 | INSERT new paragraph after L147: "**Post-apply status (2026-07-03):** all 24 atomic units landed in v0.12.0..." |
| Z2 | §1 TL;DR | L158-160 | EXTEND: append "**Applied 2026-07-03 → v0.12.0. Actual merge effort:** ~30 minutes human + ~15 minutes W2-W6 review-driven fixes..." |
| Z3 | §2 Evidence table | L173 | REPLACE: "885" → "885 (proposal-time) → 896 (post-apply in v0.12.0...) → 1167 (post-v2-axis absorption T-2026-07-03-003)" |
| Z4 | §3.5 MANIFEST.txt | L1639 | REPLACE: "22 new files + 2 modified" → "23 new files + 2 modified" (W3 added 04-locale-handoff.md); update total "24 atomic units" → "25 atomic units" |
| Z5 | §5.1 What worked | L1719 | INSERT new bullet: "**W3 follow-up arrived cleanly after merge.** 04-locale-handoff.md was a 30-line follow-up..." |
| Z6 | §5.3 Things I would change tomorrow | L1738-1743 | INSERT new item #4: "**Fold v2 axes (a11y + DPR + dark theme) into the template's memory + skeleton.** T-2026-07-03-003 in progress..." |
| Z7 | §7 Owner action checklist | L1804-1805 | REPLACE: "G6_locale_handoff: PENDING" → "G6_locale_handoff: RESOLVED (W3 follow-up — 04-locale-handoff.md added 2026-07-03)"; APPEND: "G8_v2_axes_fold: PENDING — T-2026-07-03-003 in progress (memory files landed, skeleton extension landed, this proposal updated)" |

**Mechanical steps:**
1. For each of Z1-Z7, locate the `oldString` (research §G has exact text), `edit` with the new text from research §G.
2. Verify total line count changed by +8/-2 (net +6 lines).

**Files expected:** 1 modified file, +~10/-2 lines across 7 zones.

**Acceptance:** All 7 substring searches in AG8 return matches.

### P3T7 — Update MOVED headers in demo-folder copies [Phase 3d]
**Target files:** `E:\context_gen\cinematic-landing-kit-demo\memory\{09,10,12,13}-*.md`

**Mechanical steps (per file):**
1. Open the file (current L1-5 is `<!-- TODO(template-migration): ... -->`).
2. Replace L1-5 with `<!-- MOVED → templates/cinematic-landing/memory/1[1-4]-<name>.md (T-2026-07-03-003, 2026-07-03) -->`.
3. Specifically:
   - `09-canvas-a11y.md` → `MOVED → templates/cinematic-landing/memory/11-canvas-a11y.md`
   - `10-reduced-motion-listener.md` → `MOVED → templates/cinematic-landing/memory/12-reduced-motion-listener.md`
   - `12-keyboard-nav.md` → `MOVED → templates/cinematic-landing/memory/13-keyboard-nav.md`
   - `13-dark-theme.md` → `MOVED → templates/cinematic-landing/memory/14-dark-theme.md`

**Files expected:** 4 modified files, ~16 lines net (5 lines → 1 line each).

**Acceptance:** AG4 — `rg 'MOVED →' cinematic-landing-kit-demo/memory/` matches all 4 files.

### P3T5 — Shrink `templates/cinematic-landing/memory/05-theming.md:58-72` dark-mode sketch [Phase 3d]
**Target file:** `E:\context_gen\templates\cinematic-landing\memory\05-theming.md` (currently 80 lines, dark-mode section at L58-72)

**Mechanical steps:**
1. Read L58-72 of `05-theming.md` (the 15-line dark-mode sketch).
2. Replace L58-72 with:
   ```
   ## Dark mode — see `14-dark-theme.md`

   The 14-token dark counter-table, K1 mode-aware `--gold-text-top`, K2 revert story, per-section `data-ambient-dark`, FOUC prevention, WebAIM contrast audit, and deuteranopia sim live in [`memory/14-dark-theme.md`](./14-dark-theme.md) as the canonical recipe.
   ```
3. Verify no `data-theme` or divergent hex values remain.

**Files expected:** 1 modified file, -15 / +4 lines (80 → 69 lines).

**Acceptance:** AG9 — `rg 'data-theme' templates/cinematic-landing/memory/05-theming.md` returns 0 matches; section now reads "## Dark mode — see `14-dark-theme.md`" + 3-line pointer text.

### P3T8 — CHANGELOG + count fixes [Phase 3d]
**Target files:**
- `E:\context_gen\agents_manager\CHANGELOG.md` (add v0.12.1 sub-section + fix L13)
- `E:\context_gen\templates\cinematic-landing\00-readme-first.md` (fix L13)

**Mechanical steps:**
1. **CHANGELOG L13 fix:** Change "9 `memory/*.md`" → "14 `memory/*.md`".
  2. **CHANGELOG v0.12.1 entry:** INSERT a new top-level `## v0.12.1` heading between `## v0.12.0` (L5-83) and `## v0.13.0` (L84+). Conventional top-level placement (peer of v0.12.0 and v0.13.0). Content (~30 lines):
    - **Date:** 2026-07-03
    - **Summary:** v2-axis memory files integrated; source PROPOSED_PATCH.md §F sync; skeleton extension to absorb v2 axes.
    - **What's new:** 4 new memory files (11/12/13/14); skeleton extension (~271 LOC); demo folder + proposal copied for traceability; MANIFEST.txt updated to 23 files.
    - **Files touched:** bullet list of P3T1-P3T8.
  3. **00-readme-first.md L13 fix:** Change "9 memory files" → "14 memory files".
  4. **agents_manager/SKILL.md:72 fix:** Change "9 memory files" → "14 memory files" (1-line edit; included per user Q-C decision; keeps count consistent across all 3 doc references).

**Files expected:** 3 modified files, ~33 lines net (CHANGELOG +30, 00-readme-first 0, SKILL.md +1/-1 for the count fix).

**Acceptance:** AG10/AG11/AG12 — substring searches return matches.

---

## 6. Sub-dispatch order for am-coder (verbatim)

```
P3T2 → P3T1 → P3T3 → P3T6 → P3T4 → P3T7 → P3T5 → P3T8
```

(See task tracker §"Sub-dispatch order" for rationale.)

---

## 7. Risks acknowledged (from research, with how this plan handles each)

- **R1 — content duplication:** Mitigated by `05-theming.md` shrink (P3T5) + topical overlap documented in research §K.
- **R2 — frontmatter on template memory files:** Mitigated by NOT adding frontmatter (template convention).
- **R3 — §F update scope ambiguous:** Mitigated by user Q5=(b) EXTENDED answer; this plan covers all 7 zones.
- **R4 — Where do the 4 v2 files land:** Q1=(d) cp + update MOVED headers; this plan handles both.
- **R5 — `14-dark-theme.md` documentation-vs-code mismatch:** Mitigated by Q3=(b) EXTENDED — skeleton extension (P3T6) absorbs dark theme + DPR + a11y patterns.
- **R6 — skeleton already has W4+W5; cp redundant:** Mitigated by audit in P3T6 — only the absences are added; existing listener is REPLACED with the full 16-LOC contract (flag + guard + Safari fallback).

## 8. Open assumptions (resolved via user Q-A/Q-B/Q-C clarifications)

- **CHANGELOG version = v0.12.1** as a **top-level `## v0.12.1`** heading (peer of v0.12.0 and v0.13.0). Per user Q-A (conventional CHANGELOG style). NOT nested under v0.12.0.
- **No `share/notes/99_decisions.md` entry.** This file does not yet exist in our repo; decisions accumulate in this task tracker + planning doc.
- **Use renumbered source filenames (11/12/13/14).** Per Q4=(b); gap at 10 is acceptable.
- **Memory file count = 14** (10 existing + 4 new). Per user Q-B (the math).
- **P3T8 includes 4 edits:** (1) CHANGELOG L13 fix, (2) new top-level `## v0.12.1` entry, (3) 00-readme-first.md L13 fix, (4) agents_manager/SKILL.md:72 fix. Per user Q-C (include SKILL.md fix to keep counts consistent in one pass).

## 9. Plan self-score

- **Testability** (1–5): **4** — All 8 sub-tasks have a concrete `Done when` clause + AG gate numbers.
- **Scope** (1–5): **4** — Right size: 4 phases × 8 sub-tasks = 32 cells. Skeleton extension is substantial but bounded.
- **Dependencies** (1–5): **5** — Sub-dispatch order is explicit + reason-coded (cp first, then MANIFEST, then work, then cleanup).
- **Risks covered** (1–5): **4** — All 6 research risks mapped to mitigations; R5 documented as addressed via Q3=(b).

## 10. Self-critique

- **Did I do my job?** Yes. Plan is concrete, dependency-ordered, with acceptance gates.
- **What might I have missed?**
  - The user's counting inconsistency (research says "13", math says 14, user prompt says 14). I went with 14 (the math).
  - The user's prompt mentions "Sub-dispatch order for am-coder" — I produced that explicitly in the task tracker.
  - The PROPOSED_PATCH.md update will require exact-string `edit` calls; if line numbers shifted from the source's pre-edit state, those exact strings may not match. am-coder should grep for the unique substrings in research §G ("Lines of demo (T-002)", "Estimated merge effort", "G6_locale_handoff", etc.) rather than relying on line numbers.
- **What did I assume without evidence?**
  - I assumed the existing `04-locale-handoff.md` file stays as-is (not renumbered). Per Q4=(b), correct.
  - I assumed v0.12.1 sub-version (under `## v0.12.0`) is the right CHANGELOG placement rather than a new `## v0.12.1` top-level heading. This matches semver intent (sub-version = back-fill, not new feature). Master can override if user wants top-level.
- **Was I complete?** Yes on the planning surface (3 artifacts + task tracker + acceptance gates). The sub-task decomposition is granular enough for am-coder to execute without re-asking.