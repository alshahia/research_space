# Phased Plan — T-2026-07-04-009

**Date:** 2026-07-04
**Sub-agent:** planning
**Phase 2 artifact:** planning complete; awaiting user confirmation per `agents_manager/SKILL.md:122`

---

## Phase 2 — Planning (this dispatch)

**Goal:** Produce high-level + phased plan + task rows. Stop and wait for
master/user confirmation before Phase 3 dispatch.

**Deliverables:**
- `share/notes/02_plan_high_T-2026-07-04-009.md` — high-level plan + self-score
- `share/notes/02_plan_phases_T-2026-07-04-009.md` — this file
- New task rows appended to `tasks/T-2026-07-04-009.md` (P3T1-P3T4, P4T1-P4T2, P5T1)

**Done when:** all three artifacts exist on disk; task tracker shows P2T1
status `done`; high-level plan has self-score in all 4 dimensions.

**Tasks:** see `tasks/T-2026-07-04-009.md` row P2T1.

**Notes:** This plan respects the single-phase trigger threshold
(`agents_manager/SKILL.md:130-145` + `agents_manager/planning/rules.md` §12).
Whole-phase LOC estimate from synthesis v2 is 1500-1700 → Phase 3 splits into
3.1 (authoring) + 3.2 (integration). See Phase 3 Complexity block below.

---

## Phase 3 — Build

### Phase 3 — Goal

Ship the 7 locked deliverables from synthesis v2 in scope A: 4 new files
(`templates/EXTRACTION.md`, `agents_manager/extract/SKILL.md`,
`agents_manager/extract/rules.md`,
`agents_manager/memory/.extract-config.yaml.example`) + 3 modified files
(`agents_manager/SKILL.md` Phase 5 menu, `agents_manager/memory/README.md`
schema, `templates/CONTRIBUTING.md` cross-ref).

### Phase 3 — Deliverables (per task row)

- **P3T1** (sub-phase 3.1) — `templates/EXTRACTION.md` — extraction rulebook
- **P3T2** (sub-phase 3.1) — `agents_manager/extract/SKILL.md` +
  `agents_manager/extract/rules.md` +
  `agents_manager/memory/.extract-config.yaml.example` + `templates/CONTRIBUTING.md`
  (modification: add "via extract" cross-ref subsection)
- **P3T3** (sub-phase 3.2) — `agents_manager/memory/README.md` (modification:
  add `tech_stack:` + `domain:` to required frontmatter list at line 62; add
  soft-filter step to read-on-entry protocol at lines 75-83)
- **P3T4** (sub-phase 3.2) — `agents_manager/SKILL.md` (modification: add
  Phase 5b option 5 "Extract to template / core knowledge" at lines 406-412
  area, gated on `agents_manager/memory/.extract-config.yaml` opt-in flag) —
  **explicit maintenance task, not bundled**

### Phase 3 — Files touched (summary)

| File | Action | Phase 3 sub | LOC delta |
|---|---|---|---|
| `templates/EXTRACTION.md` | NEW | 3.1 | +500 to +700 |
| `agents_manager/extract/SKILL.md` | NEW | 3.1 | +200 to +250 |
| `agents_manager/extract/rules.md` | NEW | 3.1 | +100 to +150 |
| `agents_manager/memory/.extract-config.yaml.example` | NEW | 3.1 | +5 to +15 |
| `templates/CONTRIBUTING.md` | MOD | 3.1 | +20 to +30 |
| `agents_manager/memory/README.md` | MOD | 3.2 | +25 to +40 |
| `agents_manager/SKILL.md` | MOD | 3.2 | +30 to +50 |
| **Total** | — | — | **~880 to ~1235** |

(Whole-phase estimate from synthesis v2 includes worked-example prose and
verification scaffolding that pushes past the per-file floor to 1500-1700.
This phased plan's lower-bound estimate puts the build JUST under the 1200
trigger; master may override `split_recommended: true` if the actual build
lands under 1200.)

### Phase 3 — Tasks

See `tasks/T-2026-07-04-009.md` rows P3T1, P3T2, P3T3, P3T4.

### Phase 3 — Complexity

- **novel_abstractions:** 1 (`extract` skill as user-triggered soft-skill
  without a roster slot — closest analog is the specialist-skill convention
  but with no `opencode.jsonc` entry and no master dispatch routing)
- **LOC_estimate:** 1600 (midpoint of synthesis v2's 1500-1700 range)
- **files_estimate:** 7 (4 new + 3 modified)
- **review_difficulty:** medium
- **split_recommended:** true
- **reason:** LOC > 1200 trigger fires (synthesis v2 HIGH-confidence
  estimate). Natural sub-phase seam at authoring-vs-integration boundary:
  sub-phase 3.1 produces the 4 new capability files + a documentation
  cross-ref (can be reviewed as one PR-shaped chunk); sub-phase 3.2 wires the
  capability into existing rulebooks (memory README schema + master SKILL
  Phase 5 menu) and references paths/fields established in 3.1. The seam is
  clean because P3T3 and P3T4 both reference output paths from P3T1/P3T2 —
  the dispatch order matches the dependency order.

#### Sub-phase 3.1 — Complexity

- **novel_abstractions:** 1 (same as whole phase)
- **LOC_estimate:** ~920 (4 new files + 1 modification)
- **files_estimate:** 5
- **review_difficulty:** medium
- **split_recommended:** false (under threshold)
- **reason:** Single coder dispatch; all 5 deliverables are new content or
  additive docs (no existing logic touched). Files do not depend on each
  other for content but share a common authoring voice (the EXTRACTION.md
  style mirror) — pairing them keeps the voice consistent.

#### Sub-phase 3.2 — Complexity

- **novel_abstractions:** 0 (additive modifications to existing rulebooks)
- **LOC_estimate:** ~85 (2 modifications)
- **files_estimate:** 2
- **review_difficulty:** low
- **split_recommended:** false (far under threshold)
- **reason:** Both modifications are small, additive, gated on output paths
  from 3.1 (extract/SKILL.md path + memory README frontmatter field names).
  Review is structural: "did the existing rulebook stay coherent; is the new
  content properly gated?"

### Phase 3 — Done when

**Sub-phase 3.1 Done when:**

1. `templates/EXTRACTION.md` exists at the path; contains all required
   sections per `templates/AUTHORING.md` style (TL;DR + numbered rules +
   folder structure section + 9-step recipe parallel to AUTHORING.md:248-290 +
   pre-flight checklist covering license/secrets/Jaccard/WARN-register +
   worked-example section + "what the agent can't decide" section +
   companion-files cross-refs + versioning block). Body length ~500-700L.
   Verify: `wc -l templates/EXTRACTION.md` reports ≥500; `rg -n
   'TL;DR|Pre-flight|9-step|worked example|can.t decide|Versioning'
   templates/EXTRACTION.md` returns ≥6 matches.
2. `agents_manager/extract/SKILL.md` exists; YAML frontmatter parses
   successfully; `description:` field contains the trigger phrases per
   Anthropic Skills convention; `paths:` lists `templates/**`,
   `agents_manager/memory/**`, `share/templates/drafts/**`. Body describes
   the 8-step procedure (read source → pre-flight → scaffold → fill memory
   → skeleton → verify.sh → emit audit log → hand back to master). Verify:
   `python3 -c "import yaml,sys; d=yaml.safe_load(open('agents_manager/extract/SKILL.md').read().split('---')[1]); assert 'description' in d and 'paths' in d"`.
3. `agents_manager/extract/rules.md` exists; has 8 numbered hard rules R1-R8
   matching the synthesis v2 specification verbatim (R1 no
   `templates/<name>/memory/` writes; R2 no secrets; R3 LICENSE with
   attribution; R4 invoke `tests/verify.sh`; R5 run
   `scripts/validate-memory.sh`; R6 never auto-default to `global/`; R7 never
   overwrite, suffix on collision; R8 emit audit log at
   `share/notes/03_extracted_<task-id>.md`). Verify: `rg -n '^##? R[1-8]:'
   agents_manager/extract/rules.md` returns ≥8 matches.
4. `agents_manager/memory/.extract-config.yaml.example` exists; valid YAML;
   contains at least an `extract_enabled: bool` field. Verify: `python3 -c
   "import yaml; yaml.safe_load(open('agents_manager/memory/.extract-config.yaml.example').read())"`
   exits 0; `rg -n 'extract_enabled' agents_manager/memory/.extract-config.yaml.example`
   returns ≥1 match.
5. `templates/CONTRIBUTING.md` has a new "via extract" subsection (suggested
   heading: `### I want to extract a finished project into a template`)
   inserted between the existing "I want to add a new template" and "I want
   to fix a bug" sections; subsection references the extract skill.
   Verify: `rg -n 'extract' templates/CONTRIBUTING.md` returns ≥2 matches (1
   new + 1 in any preserved prose).
6. `python3 scripts/validate-frontmatter.py` exits 0 against the 3 new
   files (EXTRACTION.md is optional per its frontmatter design but
   extract/SKILL.md + extract/rules.md + CONTRIBUTING.md if frontmatter is
   added).

**Sub-phase 3.2 Done when:**

1. `agents_manager/memory/README.md` line 62 area now includes `tech_stack:`
   and `domain:` in the required-frontmatter list (in addition to existing
   `scope`/`topic`/`status`/`created`/`last_verified`/`superseded_by:`).
   Verify: `rg -n 'tech_stack|domain' agents_manager/memory/README.md`
   returns ≥2 matches (frontmatter list + read-protocol step).
2. `agents_manager/memory/README.md` lines 75-83 read-on-entry protocol now
   contains a soft-filter step (suggested addition: "Optional: if your
   current task's tags match any memory entry's `tech_stack:` or `domain:`
   frontmatter, prefer those entries; specialist judgment, not a hard gate.").
   Verify: `rg -n 'soft.filter|tech_stack|domain' agents_manager/memory/README.md`
   returns ≥1 match in the read-on-entry protocol section.
3. `agents_manager/SKILL.md` Phase 5b menu (line 406 area) has a new option
   5 entry "Extract to template / core knowledge" with a clear gating clause
   ("only if `agents_manager/memory/.extract-config.yaml` exists AND
   `extract_enabled: true`"). Verify: `rg -n 'Extract to template|core
   knowledge' agents_manager/SKILL.md` returns ≥1 match in the Phase 5
   section; `rg -n 'extract_enabled' agents_manager/SKILL.md` returns ≥1
   match (the gating clause).
4. The existing 4 Phase 5b options (1. Run smoke / 2. Polish WARNs / 3.
   Build follow-up / 4. Close out) are unchanged in numbering and content.
   Verify: `rg -n '^1\. \*\*Run the smoke test\*\*|^2\. \*\*Polish|^3\.
   \*\*Build a follow-up|^4\. \*\*Close out' agents_manager/SKILL.md` returns
   4 matches.
5. No other files modified by Phase 3.2. Verify: `git diff --name-only`
   against the Phase 3.2 sub-phase start shows ONLY `agents_manager/memory/README.md`
   and `agents_manager/SKILL.md` in the modified set.

**Combined Phase 3 done when (sub-phase 3.1 + 3.2):**

- `git status` shows 4 new files + 3 modified files (matches the 7 deliverable
  scope; no accidental new/modified files in forbidden zones).
- `python3 scripts/validate-frontmatter.py` exits 0.
- The 4 new files reference each other correctly:
  `rg -n 'EXTRACTION.md' agents_manager/extract/SKILL.md` returns ≥1 match;
  `rg -n '.extract-config.yaml' agents_manager/extract/SKILL.md` returns ≥1
  match; `rg -n 'tech_stack|domain' templates/EXTRACTION.md` returns ≥1 match
  (memory schema referenced from the rulebook).
- No edits to: `opencode.jsonc`, `CLAUDE.md`, any
  `agents_manager/{research,planning,design,coder,review}/SKILL.md`, any
  `agents_manager/{research,planning,design,coder,review}/rules.md`,
  `templates/AUTHORING.md`, `scripts/validate-template.sh` (all deferred to
  v0.15.x patches per synthesis v2 and dispatch prompt). Verify: `git diff
  --name-only` against Phase 3 start shows ZERO matches against these
  forbidden paths.

---

## Phase 4 — Review

### Phase 4 — Goal

Run am-review against the 8 FAIL conditions F1-F8 from the operations angle.
Create the WARN register at first dispatch.

### Phase 4 — Deliverables

- `share/notes/04_warns_register_T-2026-07-04-009.md` — created at first
  dispatch per `agents_manager/SKILL.md:195`
- `share/reports/04_review_T-2026-07-04-009_p3-1.md` — review of sub-phase 3.1
- `share/reports/04_review_T-2026-07-04-009_p3-2.md` — review of sub-phase 3.2

### Phase 4 — Files touched

- `share/notes/04_warns_register_T-2026-07-04-009.md` (NEW, master-created)
- `share/reports/04_review_T-2026-07-04-009_*.md` (NEW, am-review)

### Phase 4 — Tasks

See `tasks/T-2026-07-04-009.md` rows P4T1, P4T2.

### Phase 4 — Complexity

- **novel_abstractions:** 0 (review is read-only verification)
- **LOC_estimate:** ~0 (review produces reports, not source files)
- **files_estimate:** 3 (1 register + 2 review reports)
- **review_difficulty:** medium (8 FAIL conditions × 2 sub-phases = 16
  checks; structural verification of frontmatter + cross-refs + forbidden-zone
  preservation)
- **split_recommended:** false (no code changes; review per sub-phase is the
  standard per-chunk pattern)
- **reason:** Standard per-sub-phase review. The 8 FAIL conditions F1-F8
  apply to FUTURE extraction outputs (templates/<name>/ produced by an
  extract run), not to THIS build's outputs. For this build, am-review
  verifies: (a) the 7 deliverable files exist at the right paths with the
  right content; (b) the 8 FAIL conditions F1-F8 are *encoded* in
  `EXTRACTION.md` pre-flight and `extract/rules.md` so future runs check
  them; (c) no soft-wall violations in the build output.

### Phase 4 — Done when

1. `share/notes/04_warns_register_T-2026-07-04-009.md` exists; contains the
   `## Phase 3.1 — <date> — <verdict>` block (per SKILL.md:195 format);
   subsequent blocks appended per sub-phase review.
2. `share/reports/04_review_T-2026-07-04-009_p3-1.md` exists with overall
   verdict ∈ {PASS, PASS_WITH_WARN, FAIL}. Per-task verdicts cover P3T1 + P3T2
   individually.
3. `share/reports/04_review_T-2026-07-04-009_p3-2.md` exists with overall
   verdict ∈ {PASS, PASS_WITH_WARN, FAIL}. Per-task verdicts cover P3T3 + P3T4
   individually.
4. The 8 am-review FAIL conditions F1-F8 are explicitly checked in one of
   the two review reports (each condition either PASS, N/A, or FAIL with
   path:line evidence):
   - **F1** — Scaffolded template's `_blank/tests/verify.sh` contains no
     placeholder PASS lines. (N/A for this build; PASS in EXTRACTION.md
     forward-looking gate.)
   - **F2** — Manifest references files that exist. (N/A for this build;
     PASS in EXTRACTION.md pre-flight.)
   - **F3** — Every memory file has `USE THIS WHEN:` on H1. (N/A for this
     build; PASS in `extract/rules.md` R4 + EXTRACTION.md acceptance.)
   - **F4** — H1 number matches filename prefix. (N/A for this build;
     PASS in EXTRACTION.md pre-flight.)
   - **F5** — `templates/<name>/LICENSE` exists and is on whitelist. (N/A
     for this build; PASS in `extract/rules.md` R3 + EXTRACTION.md
     pre-flight.)
   - **F6** — No secrets in skeleton. (N/A for this build; PASS in
     `extract/rules.md` R2 + EXTRACTION.md pre-flight.)
   - **F7** — `INDEX.md` has real trigger phrases. (N/A for this build;
     PASS in EXTRACTION.md acceptance.)
   - **F8** — Source project's `04_warns_register_*.md` has 0 OPEN entries
     at extract time. (N/A for this build; PASS in `extract/rules.md` R8 +
     EXTRACTION.md pre-flight.)
5. If any FAIL surfaces, fix-loop dispatched per `agents_manager/SKILL.md:191-194`
   (max 3 fix-loops per chunk). Each fix-loop iterates P3 coder call → P4
   review call.
6. All WARNs auto-acceptable per the existing triageable list
   (`agents_manager/SKILL.md:373-388`) are tagged `[auto-accepted triageable]`
   in the WARN register. New extraction-specific WARNs (5 proposed in
   synthesis v2 F10: W-extract-secrets-near-miss, W-extract-license-unattributed,
   W-extract-overlap-with-existing, W-extract-source-has-open-warn,
   W-extract-skip-fatigue) are NOT in this build's triageable list — they are
   in the Future work section. If am-review flags them in this build, they
   surface to user; do not auto-accept.

---

## Phase 5 — Close (conditional)

### Phase 5 — Goal

If `phase_5_enabled: true` on `tasks/T-2026-07-04-009.md` task tracker
header, append the `## Completion` block and stamp final metrics. Otherwise
skip (status `skipped` per dispatch prompt).

### Phase 5 — Deliverables

- `tasks/T-2026-07-04-009.md` ## Completion block (final commit/branch stamp)
- `share/notes/99_progress_T-2026-07-04-009.md` final entry

### Phase 5 — Files touched

- `tasks/T-2026-07-04-009.md` (append-only on Completion block)
- `share/notes/99_progress_T-2026-07-04-009.md` (append-only ledger entry)

### Phase 5 — Tasks

See `tasks/T-2026-07-04-009.md` row P5T1.

### Phase 5 — Complexity

- **novel_abstractions:** 0
- **LOC_estimate:** ~10 (completion block + ledger line)
- **files_estimate:** 2 (append-only)
- **review_difficulty:** low
- **split_recommended:** false
- **reason:** Pure close-out work; existing per-task-close protocol.

### Phase 5 — Done when

1. `## Completion` block appended to `tasks/T-2026-07-04-009.md` with date,
   final commit/branch, latest clean review report path, open WARNs status.
2. Final entry appended to `share/notes/99_progress_T-2026-07-04-009.md`
   (one-line: "Phase 5 (master) — DONE (artifact: tasks/T-...md ## Completion)").
3. `## Metrics` block in `tasks/T-2026-07-04-009.md` stamped with Closed
   date.

If `phase_5_enabled: false` (default), P5T1 status flips to `skipped` and
Phase 5 does not run. Master records the skipped reason in the task tracker
loop history.

---

## Future work (deferred to v0.15.x patches)

Per the dispatch prompt's "Deferred to v0.15.x patches (NOT in scope A)"
section. None of these are build tasks; they are tracked here so they do not
get forgotten.

| Item | Source | Trigger | Why deferred |
|---|---|---|---|
| `scripts/validate-template.sh` (~80 LOC) | Synthesis v2 §Polish #8 | User feedback on T-2026-07-04-009 ship | Adds enforcement script for `templates/AUTHORING.md` Rules 1-8; not needed until a second extraction run validates the recipe |
| 5 new triageable WARNs in `agents_manager/SKILL.md:373-388` (W-extract-secrets-near-miss, W-extract-license-unattributed, W-extract-overlap-with-existing, W-extract-source-has-open-warn, W-extract-skip-fatigue) | Synthesis v2 §Polish #9 | First real extraction attempt | WARN-list expansion needs field data from first real extraction; defer until ship proves the WARN surface is useful |
| 2 new Rule 8 acceptance items in `templates/AUTHORING.md:190-206` (LICENSE check + secrets-scan) | Synthesis v2 §Polish #10 | Companion to validate-template.sh | Rule 8 additions make sense AFTER an enforcement script exists to back them; defer together |
| Retroactive worked-example trace against `templates/cinematic-landing/` (a worked-example entry demonstrating the recipe in reverse, reconstructing `templates/cinematic-landing/` from its own git history) | Synthesis v2 §Polish #11 | After v0.15.0 ships; user feedback | Heavy lift (~hours); only worth doing if the recipe proves sound via first real extraction |
| Verify script for the extract skill itself (analogous to `templates/cinematic-landing/tests/verify.sh`) | Planning self-critique | If master wants self-check on the 4 new files | The 8 FAIL conditions F1-F8 apply to extraction outputs, not to the skill itself; defer until a second use case demands |

None of the above block v0.15.0 ship. The shipped capability is the 7
deliverables in Phase 3; the deferred items are polish that follow real-world
usage.

---

## Cross-references

- `share/notes/02_plan_high_T-2026-07-04-009.md` — high-level plan + self-score
- `share/notes/01_research_T-2026-07-04-009.md` — canonical merged research
  (10 deduped findings, 5H/4M/3L risks, 5 gating Qs all resolved)
- `share/notes/01_master_synthesis_T-2026-07-04-009.md` v2 — user's resolved
  scope A (items 1-7)
- `share/notes/01_research_T-2026-07-04-009_angle-operations.md:178-185` — 8
  am-review FAIL conditions F1-F8
- `agents_manager/SKILL.md:130-145` — single-phase trigger threshold
- `agents_manager/planning/rules.md` §12 — complexity block schema
