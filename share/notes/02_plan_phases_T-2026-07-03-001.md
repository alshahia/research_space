# 02 · Plan (phased) — T-2026-07-03-001

## Phase 1 — Research (DONE)

**Done when:** `share/notes/01_research_T-2026-07-03-001.md` exists, captures the proposal's 24 atomic units, wall conflicts, and risks.

**Status:** DONE — `share/notes/01_research_T-2026-07-03-001.md`.

## Phase 2 — Plan + PAUSE (this phase)

**Done when:** plan note exists AND user has confirmed the opencode.jsonc wall crossing.

**Sub-steps:**

1. Write high-level plan note — DONE — `share/notes/02_plan_high_T-2026-07-03-001.md`.
2. Write this phased plan note — DONE.
3. **PAUSE** — ask user via `question` tool:
   - Q1: "OK to cross the opencode.jsonc wall as deliberate maintenance?" (yes/no)
   - Q2 (conditional on yes): "Apply all 24 atomic units, or cherry-pick?"
4. Update task tracker row P2T1 → done after user confirms.

**Status:** PAUSED — awaiting user OK on Q1.

## Phase 3 — Apply (the verbatim routine)

**Done when:** all 24 atomic units applied + verification block passed.

**Sub-steps:**

- **P3T1 — Create 17 files under `templates/cinematic-landing/`**
  - Done when: `find templates/cinematic-landing/ -type f | sort` matches `cat templates/cinematic-landing/assets/MANIFEST.txt | sort` (empty diff).
  - Files:
    1. `templates/cinematic-landing/00-readme-first.md`
    2-10. `templates/cinematic-landing/memory/01-builder-flow.md` through `09-quality-bar.md` (9 files)
    11. `templates/cinematic-landing/skeleton/index.html` (via `cp` from source project)
    12-14. `templates/cinematic-landing/prompts/image-gen.md`, `video-gen.md`, `asset-spec.md`
    15. `templates/cinematic-landing/decisions/decision-log.md`
    16. `templates/cinematic-landing/assets/manifest.schema.json`
    17. `templates/cinematic-landing/assets/MANIFEST.txt`

- **P3T2 — Create 5 files under `agents_manager/assets/`**
  - Done when: `find agents_manager/assets/ -type f | sort` adds 5 paths not previously listed.
  - Files:
    1. `agents_manager/assets/SKILL.md`
    2. `agents_manager/assets/rules.md`
    3. `agents_manager/assets/notes/branch-decisions.md`
    4. `agents_manager/assets/resources/landing-review-checklist.md`
    5. `agents_manager/assets/README.md`

- **P3T3 — Edit `agents_manager/SKILL.md`**
  - Done when: `## Templates (v0.9.0+)` section exists between line 66 ("Never speak to the next agent out-of-band") and line 68 ("## The mandatory pipeline").

- **P3T4 — Edit `opencode.jsonc`**
  - Done when: `am-assets` agent entry exists between line 61 (closing `}` of am-review) and line 62 (closing `}` of agent object). Also update header comment on line 5 from "5 specialists" to "6 specialists" + line 11 from "all 6 agents" to "all 7 agents".

- **P3T5 — Verification block**
  - Run the 4 acceptance signals:
    - `diff -u /tmp/expected.txt /tmp/actual.txt` (expected: empty)
    - JSON validity check
    - markdown header check on `templates/cinematic-landing/memory/*.md`
    - visual spot-check on skeleton HTML (open in browser or `Get-Content` for the 5 hard rules)
  - Done when: all 4 signals return success.

## Phase 4 — Review

**Done when:** `share/reports/04_review_T-2026-07-03-001.md` exists with verdict (PASS / PASS-WITH-NOTES / FAIL).

**Sub-steps:**

- P4T1 — Dispatch `am-review` with prompt:
  - Read proposal §0 (the apply-with-llm block).
  - For each of the 22 new files: confirm content matches the proposal's fenced code block (path:line evidence).
  - For the 2 controller edits: confirm anchor + content matches the proposal.
  - Run the verification block.
  - Issue PASS/PASS-WITH-NOTES/FAIL verdict.
- P4T2 — Read review verdict.
  - If PASS or PASS-WITH-NOTES: proceed to Completion.
  - If FAIL: enter fix-loop (max_fix_loops=3). Each fix-loop:
    - Re-dispatch `am-coder` (NOT me) to fix the issue.
    - Re-dispatch `am-review` to re-verify.
  - If max_fix_loops hit: surface to user, stop.

## Phase 5 — Next-steps menu (opt-in)

Per master SKILL.md Phase 5 is opt-in. Will not offer unless user asks. If user requests:
- "Commit?" → user-driven; will offer a `git add` + commit suggestion (but execute only on user OK).
- "Smoke test?" → open the skeleton HTML in a browser to verify the worked example still renders.
- "Update CHANGELOG?" → propose a `## v0.12.0` entry summarizing the patch.

## Completion

**Done when:** all phases done or accepted.

**Sub-steps:**
- Append `## Completion` block + `## Phase productivity` table to `tasks/T-2026-07-03-001.md`.
- Update progress ledger with Phase 4 outcome.
- Surface to user: "Patch applied + reviewed. Ready for your commit + push."