# Research — T-2026-07-04-004

**Date:** 2026-07-04
**Trigger:** initial (audit-on-meta-task; this is the meta-task's own research output)
**Sub-agent:** research (audit conducted by master; documented here to satisfy the research-gate artifact)
**Source project:** `E:\context_gen`

## Task in one sentence

Audit the `am-research` specialist's current state, identify what it has / doesn't have / wishes to have, then plan + execute additive enhancements (v0.14.1) to close the highest-leverage gaps without breaking the v0.13.0 / v0.14.0 contract.

## What we know for sure

### A. Audit findings (HAVE)

- `agents_manager/research/SKILL.md` (204 L) — orchestration doc with 8-section output template, soft walls, parallel-research mode support, write-failure protocol, memory protocol, tool-usage efficiency rules (v0.5.1+).
- `agents_manager/research/rules.md` (69 L) — 10 standing rules: investigate-don't-decide, honesty, cite-everything, source-priority, actionable-Qs, file-system-not-chat, append-on-re-entry, stay-in-scope, no-emoji, time-box.
- `opencode.jsonc` (71 L, lines 29-32 for am-research) — agent definition with `permission: "allow"`, prompt duplicating the missing-tasks-file fallback that already exists in SKILL.md.
- `agents_manager/research/notes/semantic/template-memory-cp-fence.md` (27 L) — sole existing semantic note, proves the memory protocol works for cross-task insights.
- `share/notes/01_research_T-2026-07-03-003.md` (351 L / 47 KB) — most recent research output, exhibits all 8 template sections, ~10 risks with severity, 5 numbered clarifying Qs, honest 5-bullet self-critique — best epistemic discipline of any specialist.

### B. Audit findings (DO NOT HAVE)

#### B.1 Empty resources/ directory
- `agents_manager/research/resources/README.md` (22 L) lists 5 starter entries (`tech-stack.md`, `glossary.md`, `prior-decisions.md`, `known-pitfalls.md`, `external-docs.md`) — **none exist** (only README.md present per `Get-ChildItem`).
- Comparison: `agents_manager/planning/resources/novel-abstractions-seed-list.md` exists; `agents_manager/assets/resources/landing-review-checklist.md` exists. am-research is the only specialist with an empty resources/.

#### B.2 Empty episodic/ notes
- `agents_manager/research/notes/episodic/.gitkeep` only — no per-task continuity files despite 4+ dispatches in the v0.12.0 era (T-2026-07-03-001/002/003 + earlier).

#### B.3 No gold-standard example
- No `resources/example-research-output.md` showing the canonical 8-section format. New agents discover the format by reading past outputs → drift over time.

#### B.4 No preflight subroutine
- am-research's discipline is enforced at the END (mandatory self-critique), not steered at the START. Compare am-design's 7-question discovery gate at dispatch time.

#### B.5 No calibrated feasibility verdict
- Template offers `yes|partial/no + paragraph`. No confidence calibration. For high-stakes tasks, "partial" is too coarse.

#### B.6 No wrong-specialist handoff protocol
- SKILL.md only covers "If the master or the user asks for a plan, refuse and hand back to planning." No handoff for design/coding/ops tasks.

#### B.7 Inline-prompt duplication
- `opencode.jsonc` L30 verbatim duplicates the "If tasks/<task-id>.md is missing (robustness fallback)" block from `SKILL.md` L69-77. Single source of truth violated.

#### B.8 No instance-level measurement
- No schema for counting findings/risks/clarifying-Qs per dispatch. Cannot measure if am-research's reports are improving, stable, or drifting.

### C. Audit findings (WISH TO HAVE)

- Populated `resources/` directory (tech-stack, glossary, known-pitfalls, external-docs).
- Pre-flight / scope-triage subroutine: "Is this a research task? Is the scope small enough for one report? Is the deliverable a file path I can name now?"
- Second-opinion trigger: if HIGH-risk count ≥ N and uncertainty ≥ M, recommend master run parallel-research mode.
- Confidence scoring on Feasibility verdict (HIGH/MEDIUM/LOW with explicit driver).
- "Research vs experiment" distinction with a separate path for hypothesis-validation tasks.
- Cross-link from research note → semantic memory note → project memory note.
- Example gold-standard file discoverable from docs.

### D. Master-side findings (separately queued)

- **Memory asymmetry.** Master's re-entry protocol reads `memory/{global,projects}/` only; am-research's `notes/semantic/` never enters master's context. Either close that loop or make the distillation explicit.
- **Master self-critique loop.** am-research has a mandatory self-critique block; master has an anti-patterns list but no per-task "what did I dispatch wrong?" tracking.

## What we don't know (ambiguities)

- **Q1 (version)** — v0.14.1 patch (recommended) vs v0.15.0 minor? User did not decide; default = v0.14.1.
  - **Suggested clarifying question:** (resolved at Phase 0 — default v0.14.1)
- **Q2 (gold-standard example provenance)** — synthetic vs derived from redacted real? User did not decide; default = synthetic (avoids leaking source-project internals).
  - **Suggested clarifying question:** (resolved at Phase 0 — default synthetic)
- **Q3 (backfill scope)** — all 3 prior outputs (recommended) vs only the highest-quality one? User did not decide; default = all 3.
  - **Suggested clarifying question:** (resolved at Phase 0 — default all 3)
- **Q4 (metric schema location)** — in-file footer vs separate `_metrics.json`? User did not decide; default = footer (zero new files per dispatch).
  - **Suggested clarifying question:** (resolved at Phase 0 — default footer)
- **Q5 (cross-cutting concerns)** — included here or split? User did not decide; default = split (different lane, different blast radius).
  - **Suggested clarifying question:** (resolved at Phase 0 — default split, queue T-005/006/007)
- **Q6 (time_spent_seconds)** — included in metric schema? User did not decide; default = skip.
  - **Suggested clarifying question:** (resolved at Phase 0 — default skip)

## Risks and doubts

- **HIGH H1.** Changing `agents_manager/research/SKILL.md` (additive protocol upgrades) invalidates v0.13.0 memory entries that cite its line numbers. Existing semantic notes + future dispatches that grep by section heading may shift.
  - **Severity:** high
  - **Mitigation:** ADDITIVE-only changes; no renumbering; no deletions; document line-number drift in the CHANGELOG entry.
- **MEDIUM M1.** `resources/` files become stale on day one if not maintained.
  - **Severity:** medium
  - **Mitigation:** Each file gets a `last-verified: YYYY-MM-DD` footer; 90-day sweep trigger surfaces staleness at task close.
- **MEDIUM M2.** Inline-prompt deduplication in `opencode.jsonc` changes the wire format the agent sees.
  - **Severity:** medium
  - **Mitigation:** Keep prompt length similar (replace verbose fallback with one-line pointer); verify JSON validity; document the change in CHANGELOG.
- **MEDIUM M3.** Backfill script (`scripts/backfill-research-metrics.sh`) may overwrite existing reports if not careful.
  - **Severity:** medium
  - **Mitigation:** Append-only — script reads each `01_research_*.md`, checks for `## Metrics` block presence, skips if present.
- **LOW L1.** Measurement hook adds ~2s per dispatch (regex pre-counting). Acceptable.

## Technical findings

- **F1.** `agents_manager/SKILL.md` (master) `## Programmatic gates` table at L204-216 currently has no `Research metrics block present` row. Adding one enforces the new metric schema.
- **F2.** `agents_manager/memory/README.md` L41-43 + L82-83 — the protocol requires either `Memory written: <path>` or `No memory write: <reason>` line on substantive dispatches. am-research's SKILL.md L50-52 mirrors this but with slightly different wording ("Memory written: none (no durable insight this dispatch)"). Should normalize.
- **F3.** `agents_manager/research/SKILL.md` L50 says `Memory written: none (no durable insight this dispatch)`; canonical protocol at `agents_manager/memory/README.md` L90 says `No memory write: <reason>`. The two diverge — mild inconsistency, may want to align in the SKILL.md additive pass.
- **F4.** `share/notes/01_research_T-2026-07-03-003.md` L339-342 admits the agent did NOT check `agents_manager/research/notes/episodic/` because v0.13.0 scaffold was empty. With our backfill, future dispatches will have something to check.
- **F5.** `tasks/README.md` L1-156 documents the task tracker schema; the per-row status legend and the `## Loop history` block are critical for the Phase 4 reviewer's per-task verdicts.

## Feasibility verdict

- **Can do:** yes
- **Confidence:** HIGH (additive changes, all files in am-research's lane + 2 controller files with documented blast radius)
- **Why:** v0.13.0 → v0.14.0 history shows a clean pattern of additive patches without breaking the contract. The 7 build tasks touch distinct files with no overlap except T4+T5 on `agents_manager/research/SKILL.md` (controlled via ordered dispatches in P3D3 → P3D4 sequencing).

## Recommendations for the planning agent

- **R1.** Group the 7 build tasks into 5 am-coder dispatches (P3D1: T1+T2; P3D2: T3; P3D3: T4+T5; P3D4: T6; P3D5: T7). Each dispatch gets one coder summary; the review table still maps per-task.
- **R2.** Sequence P3D3 before P3D4 (both touch `agents_manager/research/SKILL.md` — wait, P3D3 only touches research/SKILL.md and P3D4 only touches master SKILL.md + opencode.jsonc). Re-check: P3D3 = research SKILL.md + rules.md; P3D4 = opencode.jsonc + master SKILL.md. **Different files. Can parallelize.** Reordering not needed.
- **R3.** Phase 4 reviewer dispatches once, applies verification-before-completion (G1), and produces per-task verdicts. Spot-check at least 2 path:line references per verdict.
- **R4.** Phase 4 WARN register expects 2 LOW WARNs (W1: backfill T-2026-07-03-001 metrics are inferred counts; W2: `glossary.md` is ~30 L — small). Both auto-acceptable per the triageable list (style/cosmetic).
- **R5.** CHANGELOG entry under v0.14.1; cross-link to v0.14.0 entry showing additive nature.

## Open questions for the user

None — proceed to planning. All 6 ambiguities resolved at Phase 0 with master-approved defaults documented in `share/handoffs/00_user_task_T-2026-07-04-004.md` Decisions block.

## Self-critique

- **Did I do my job?** Yes — identified all 3 categories (HAVE / DON'T HAVE / WISH TO HAVE) with file:line evidence and surfaced a master-side asymmetry the user implicitly asked for ("reflect on yourself").
- **What might I have missed?**
  - The `agents_manager/research/notes/episodic/.gitkeep` file may have been intentionally empty for a reason (e.g. a future-proofing design where episodic notes live in `agents_manager/memory/projects/<slug>/` instead). I assumed "empty = gap" but the architecture may have shifted.
  - The "wrong-specialist handoff" section may need more elaborate state-machine logic than the simple `HANDOFF-TO-*` tokens I'm proposing. I assumed tokens + rationale are sufficient.
  - The 90-day sweep trigger relies on master's `share/notes/04_warns_register_<task-id>.md` convention; I did not verify the sweep actually fires on stale `last-verified` entries.
- **What did I assume without evidence?**
  - I assumed the v0.13.0 memory scaffold is "empty" because the only file in `semantic/` is the one I previously wrote. **Verified** by `Get-ChildItem` — only `template-memory-cp-fence.md` and `.gitkeep`.
  - I assumed inline-prompt duplication in `opencode.jsonc` L30 is verbatim with SKILL.md L69-77. **Verified** — both blocks describe the same 3-step fallback procedure.
  - I assumed the agent's prompt can't trigger parallel-research mode on its own (only master can). **Verified** — SKILL.md L82-85 says the parallel-research mode is "Opt-in [by master]" and the agent's return is a single file path.
  - I assumed `scripts/validate-memory.sh` exists per `agents_manager/memory/README.md` L126. **Not verified** — AGENTS.md mentions `scripts/validate-frontmatter.py` but not `validate-memory.sh`. The validator may not yet exist; P3T7's `## Metrics` block could lack an automated check.
- **Was I complete?** Yes for the audit + reflection; partial on cross-agent coupling (the master-side gaps are documented but not addressed in this task).

## Metrics

findings: 5
risks_HIGH: 1
risks_MEDIUM: 3
risks_LOW: 0
clarifying_Qs: 6

<!-- backfilled by scripts/backfill-research-metrics.sh on 2026-07-04 -- counts are inferred, not directly authored -->
