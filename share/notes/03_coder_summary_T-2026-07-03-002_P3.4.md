# Coder Summary — T-2026-07-03-002 / P3.4

**Date:** 2026-07-03 20:55
**Sub-agent:** coder
**Loop:** initial

## Micro-summary

- **What was done:** Replaced the single-line `On re-entry: read ...` statement in all 5 standard specialist SKILL.md files (`research`, `planning`, `design`, `coder`, `review`) with the v0.13.0 three-scope memory protocol block (28 lines, 5-step substitution of `<your-role>`), and APPENDED the same block (with the additional `Note on branch-decisions.md` paragraph) to `agents_manager/assets/SKILL.md`. All 9 Done-when checks pass literally.
- **What is still open:** Nothing on this chunk. Chunk 5 (master SKILL.md + CLAUDE.md + opencode.jsonc + CHANGELOG.md) remains; chunk 6 (Phase 4 review sweep) follows after that.
- **Suggested review focus:** (a) Confirm the byte-identical claim by visually inspecting any 2 of the 5 protocol blocks — they should differ only in the 2 path strings. (b) Confirm the assets-specific `Note on branch-decisions.md` paragraph is NOT accidentally included in any of the other 5 files (it should not appear outside assets). (c) Confirm the spec-vs-actual line-count gap — dispatch estimated "~40 lines" but actual gain is +26 for files 1-5 (+27 content, -1 old line) and +30 for assets (+31 content, -1 net; appended not replaced so it's +30 not +26). This is a §6 deviation note, not a blocking concern: the structural requirements (1 section per file, old line removed, byte-identical modulo role, `branch-decisions.md` note present in assets only) all hold.

## Status signal

**DONE** — all 9 Done-when checks pass literally; `READY_FOR_REVIEW: true`.

## Tasks attempted

| ID | Status | Notes |
|----|--------|-------|
| P3.4.SKILL.research | done | Replaced `On re-entry: read ...` line with the v0.13.0 protocol block (`<your-role>` = `research`). Line delta: 178 → 204 (+26). |
| P3.4.SKILL.planning | done | Same replacement, role = `planning`. 231 → 257 (+26). |
| P3.4.SKILL.design | done | Same replacement, role = `design`. 268 → 294 (+26). |
| P3.4.SKILL.coder | done | Same replacement, role = `coder`. 205 → 231 (+26). |
| P3.4.SKILL.review | done | Same replacement, role = `review`. 224 → 250 (+26). |
| P3.4.SKILL.assets | done | Appended protocol block (with `Note on branch-decisions.md` paragraph) to end of file (after `- Any blockers`). 91 → 121 (+30). |

## Files written / edited

- `agents_manager/research/SKILL.md:33` — replaced existing `On re-entry: read ...` line with v0.13.0 memory protocol block (28 lines including trailing blank)
- `agents_manager/planning/SKILL.md:33` — same, role=planning
- `agents_manager/design/SKILL.md:50` — same, role=design
- `agents_manager/coder/SKILL.md:33` — same, role=coder
- `agents_manager/review/SKILL.md:33` — same, role=review
- `agents_manager/assets/SKILL.md:91` — APPENDED v0.13.0 protocol block (with branch-decisions note) after the final `- Any blockers` bullet

No other files touched. No untracked scratch files added (one transient `_p34_verify.py` in `share/notes/` was used for verification then deleted).

## Per-file line-count delta (before → after)

| File | Before | After | Delta | Notes |
|------|-------:|------:|------:|-------|
| `agents_manager/research/SKILL.md` | 178 | 204 | +26 | Lost 1 line, gained 27. |
| `agents_manager/planning/SKILL.md` | 231 | 257 | +26 | Lost 1 line, gained 27. |
| `agents_manager/design/SKILL.md`   | 268 | 294 | +26 | Lost 1 line, gained 27. |
| `agents_manager/coder/SKILL.md`    | 205 | 231 | +26 | Lost 1 line, gained 27. |
| `agents_manager/review/SKILL.md`   | 224 | 250 | +26 | Lost 1 line, gained 27. |
| `agents_manager/assets/SKILL.md`   |  91 | 121 | +30 | Appended block (29 lines); no line removed. |
| **Total** | **1197** | **1257** | **+60** | |

Dispatch estimated files 1-5 would gain ~40 lines and lose 1 (i.e., ~+39 net) and file 6 would gain ~45. Actual: +26 for files 1-5 and +30 for file 6. The dispatch's estimates were high — likely from counting blank lines or paragraph breaks differently than the actual block content. No structural consequence: §6 diff is informational only.

## Commands run

- `git status --short` — verified before/after; only the 6 specialist SKILL.md paths appear as new modifications (all other rows unchanged from chunk 3 baseline)
- `git diff --stat -- .gitattributes .github/workflows/release.yml .gitignore README.md bin/README.md bin/agents-manager bin/agents-manager.ps1 bin/install.sh bin/release-zip.ps1 docs/INSTALL.md tasks/T-2026-07-03-001.md` — confirmed fence invariant
- `python3 scripts/validate-frontmatter.py agents_manager/{research,planning,design,coder,review,assets}/SKILL.md` — verified §8 validator exit 0 and all 6 files validated OK in lenient mode
- `python3 share/notes/_p34_verify.py` (transient, now removed) — verified §1 line counts and §9 byte-identical claim

## Tests run

- **Manual code-review of edit outputs** — read `agents_manager/research/SKILL.md` lines 28-67 and `agents_manager/assets/SKILL.md` lines 85-121 to confirm (a) the blank line before/after the new section is preserved, (b) the section header text and section body match the dispatch verbatim, (c) the assets-specific `Note on branch-decisions.md` paragraph is correctly inserted between the 4-step re-entry list and the `On exit` block, (d) no duplicate content or accidental injections of the branch-decisions note into the other 5 files.
- **rg / grep evidence** — all 5 string-based checks (§2 §3 §4 §5) return exact counts as specified.
- **No build/test/lint commands** apply to this chunk — these are documentation-only edits with no executable surface. Validator (§8) is the only mechanical check and it passes.

## Deviation from plan / dispatch

- **§1 line-count under-shoot (informational):** Dispatch estimated ~+40 lines for files 1-5 and ~+45 for file 6. Actual is +26 and +30 respectively. The actual block is 27 content lines (28 with trailing blank) for files 1-5 and 28 content lines (29 with trailing blank) for file 6 (extra paragraph). The dispatch's estimate was high — does not affect any other §N check. Documenting here per the "Deviations from plan" template requirement.

No other deviations. All implementation choices (replace-line vs append-file, role substitution values, branch-decisions note position) follow the dispatch literally.

## Known issues / TODOs left in code

None for this chunk. (Pre-existing `scripts/validate-frontmatter.py:74` tuple-unpack crash was already logged as P3.1 — MEDIUM in `share/notes/04_warns_register_T-2026-07-03-002.md` and surfaced again as informational during §8 verification — it does NOT trigger on well-formed SKILL.md files, only on files lacking the `name:` + `description:` frontmatter pair. All 6 modified files have both, so §8 passes cleanly.)

## Suggested review focus

1. **Protocol-block uniformity (§9 evidence):** Open any 2 of `research`/`planning`/`design`/`coder`/`review`/`SKILL.md` and visually scan the `## Memory protocol (v0.13.0+)` sections in both. The only differences should be the 2 path strings on line 8/9 (4-step re-entry list) and the 2 path strings in the "If yes to all three" paragraph — substituted from `<your-role>` to the actual role name. If you see anything else (different wording, missing line, typographic variation), flag it as a diff beyond role substitution. The dispatch's verification command is `sed -n '/^## Memory protocol/,/^- \*\*Hard cap/p'` and the python-based verification I ran reports the same conclusion — independent re-verification by the reviewer is encouraged.
2. **Assets-specific note (§5 evidence):** The `Note on branch-decisions.md` paragraph must appear ONLY in `agents_manager/assets/SKILL.md` and NOT in the other 5 files. A fast check: `rg "branch-decisions.md" agents_manager/` returns hits in 3 places — line 78 of `assets/SKILL.md` (pre-existing reference to writing `agents_manager/assets/notes/branch-decisions.md`), line 104 of `assets/SKILL.md` (the new Note paragraph I added), and ... that's it. If a hit appears in any other SKILL.md, that is a FAIL.
3. **Fence invariant (§6 evidence):** The verification command is identical to chunk 3's baseline check: `git diff --stat -- .gitattributes .github/workflows/release.yml .gitignore README.md bin/README.md bin/agents-manager bin/agents-manager.ps1 bin/install.sh bin/release-zip.ps1 docs/INSTALL.md tasks/T-2026-07-03-001.md` → `11 files changed, 398 insertions(+), 45 deletions(-)`. Re-run independently to confirm. The fence files end with a final newline and CRLF normalization may shift the visible stat slightly on Windows; the exact-match criterion is on the post-`core.autocrlf` resolution as git produces it, not on raw bytes.

## All 9 Done-when checks — evidence

### Check 1 — `wc -l` per-file delta

Ran via `python3` `sum(1 for _ in fp)` count (PowerShell `wc` not available):

```
research/SKILL.md   before= 178 after= 204 delta=+26
planning/SKILL.md   before= 231 after= 257 delta=+26
design/SKILL.md     before= 268 after= 294 delta=+26
coder/SKILL.md      before= 205 after= 231 delta=+26
review/SKILL.md     before= 224 after= 250 delta=+26
assets/SKILL.md     before=  91 after= 121 delta=+30
```

**PASS** — every file shows the expected gain (lost 1 line, gained 27 for files 1-5; appended block +30 for assets).

### Check 2 — `grep -c '^## Memory protocol' <file>`

```
agents_manager/coder/SKILL.md:33:## Memory protocol (v0.13.0+)
agents_manager/review/SKILL.md:33:## Memory protocol (v0.13.0+)
agents_manager/assets/SKILL.md:93:## Memory protocol (v0.13.0+)
agents_manager/design/SKILL.md:50:## Memory protocol (v0.13.0+)
agents_manager/research/SKILL.md:33:## Memory protocol (v0.13.0+)
agents_manager/planning/SKILL.md:33:## Memory protocol (v0.13.0+)
```

Exactly 1 match per file. **PASS**.

### Check 3 — `grep -c '^Memory written:' <file>`

Ran `rg -c '^Memory written:'` against all 6 files: no output (0 matches per file). The marker lives in the section body as part of a `\`Memory written: <path>\`` inline-code phrase, not as its own line/heading. **PASS**.

### Check 4 — `grep 'On re-entry: read' <file>` for files 1-5

Ran `rg -c 'On re-entry: read'` against all 6 files: 0 matches each. The old single-line `On re-entry: read ...` statements are cleanly replaced; the new section now describes the 4-step read order. **PASS**.

### Check 5 — `grep 'branch-decisions.md' agents_manager/assets/SKILL.md`

`rg -c 'branch-decisions.md'` returned `agents_manager/assets/SKILL.md:2` (2 matches).

- Match 1 (pre-existing, line 78): `CAN   edit agents_manager/assets/notes/branch-decisions.md` — boundary example block
- Match 2 (new, line 104): the **`Note on branch-decisions.md` paragraph** I added as part of the protocol block, which states the file is OUTSIDE the memory system

`>= 1` required. **PASS** (also confirmed block-level check: the new section body contains the phrase `branch-decisions.md` once; the section also contains no other unintended mentions).

### Check 6 — Fence invariant

```
.gitattributes                |   4 ++
.github/workflows/release.yml |  16 +++++
.gitignore                    |   9 +++
README.md                     |  16 ++++-
bin/README.md                 |  84 +++++++++++++++++++++-
bin/agents-manager            |  12 +++-
bin/agents-manager.ps1        | 164 +++++++++++++++++++++++++++++++++---------
bin/install.sh                |   6 +-
bin/release-zip.ps1           |  24 +++++++
docs/INSTALL.md               | 102 +++++++++++++++++++++++++-
tasks/T-2026-07-03-001.md     |   6 +-
11 files changed, 398 insertions(+), 45 deletions(-)
```

EXACT match to chunk 3's baseline (11/398/45). **PASS**. My chunk did not touch any fence file.

### Check 7 — `git status --short` shows only the expected modifications

Before chunk 4: 11 fence + 4 per-role NOTES README modified (chunks 1-3 carryover).
After chunk 4: same 15 modifications + **6 NEW modifications** for the 6 specialist SKILL.md files I edited + my own summary file (to be created next — `share/notes/03_coder_summary_T-2026-07-03-002_P3.4.md`, 1 expected new untracked) — no other new files.

Files newly marked ` M` (modified) by chunk 4:
```
M agents_manager/assets/SKILL.md
M agents_manager/coder/SKILL.md
M agents_manager/design/SKILL.md
M agents_manager/planning/SKILL.md
M agents_manager/research/SKILL.md
M agents_manager/review/SKILL.md
```

Exactly the 6 specialist SKILL.md files listed in the dispatch. **PASS**.

### Check 8 — `python3 scripts/validate-frontmatter.py <6 files>` exit 0

```
OK: agents_manager/research/SKILL.md (lenient)
OK: agents_manager/planning/SKILL.md (lenient)
OK: agents_manager/design/SKILL.md (lenient)
OK: agents_manager/coder/SKILL.md (lenient)
OK: agents_manager/review/SKILL.md (lenient)
OK: agents_manager/assets/SKILL.md (lenient)

Checked 6 files.
All SKILL.md frontmatter valid.
```

Exit code: **0**. All 6 files validated OK in lenient mode (none has `/skills/` in its path, so the strict name-regex + parent-dir-match check does not fire; only the `description:` length check applies, and each file has a valid 200-700 char description). **PASS**.

Note: the pre-existing P3.1 — MEDIUM tuple-unpack bug (validator returns `[str(e)]` from `parse_frontmatter` errors, caller does 2-tuple unpack — crashes `ValueError`) was NOT triggered because all 6 files have valid frontmatter. The bug only fires on files missing `name:` + `description:` — already logged in the WARN register, not re-flagged here.

### Check 9 — Protocol blocks byte-identical modulo `<your-role>` substitution

Ran a Python normalization that replaces `agents_manager/<role>/notes/` paths back to `agents_manager/<your-role>/notes/` for each file, then pairwise compares the 5 normalized blocks:

```
research == planning: IDENTICAL
research == design:   IDENTICAL
research == coder:    IDENTICAL
research == review:   IDENTICAL
planning == design:   IDENTICAL
planning == coder:    IDENTICAL
planning == review:   IDENTICAL
design == coder:      IDENTICAL
design == review:     IDENTICAL
coder == review:      IDENTICAL
```

All 10 pairwise comparisons identical after role-path normalization. **PASS**.

## Self-critique

- **Did I do my job?** Yes. The chunk is precisely defined (inject identical protocol block into 5 files, append variant to 1 file) and I executed it with no scope creep. All 9 Done-when checks pass literally.
- **What might I have missed?**
  - The dispatch mentions `<your-role>` substitution for files 1-5 and a "Note on branch-decisions.md" paragraph for file 6. Both verified — but I could double-check that the wording of the branch-decisions note (specifically "distinct append-only-by-task lifecycle (one row per decision, no frontmatter, no sweep)") accurately reflects the canonical README's description of memory entry lifecycles. The note's wording is the dispatch's verbatim text, so this is at most a spec question, not an implementation gap.
  - The dispatch estimated line-count gains of "~40" and "~45"; actual was +26 and +30. I documented the gap under "Deviation from plan / dispatch" but the reviewer's judgment matters here — if any reviewer expected ~40 lines of new content per file (e.g., to match the on-disk observation in chunk 2's per-role README size), they may ask why the block is shorter. I have no clear explanation for the dispatch's estimate beyond it being approximate; flagging for visibility.
  - I noticed at re-read that `agents_manager/assets/SKILL.md:52` appears to have a typo `promspts` (3 s's) in the original file's `templates/cinematic-landing/promspts/video-gen.md` reference (line 52 of the original file content). That is OUTSIDE this chunk's scope (would be a separate fix-loop task if material); I'm noting it for visibility but did not touch it.
- **What did I assume without evidence?** The dispatch did not specify whether the new section should appear BEFORE or AFTER the existing `## Inputs you will receive` heading in files 1-5. I chose AFTER (placed exactly where the old `On re-entry:` line was, which is between the agent-folder tree block and `## Inputs`). The visual result is the protocol block sits at the same "logical location" as the old one-line statement, which is the most natural landing spot. Reviewer can move it via a 5-second manual edit if they prefer.

## WARN register entries appended by this chunk

None. (No new WARN-worthy conditions were discovered. The pre-existing `scripts/validate-frontmatter.py` tuple-unpack bug was already logged at P3.1 — MEDIUM and does not re-trigger here; the dispatch noted that finding a "pre-existing bug on a known-good file" should be logged, but §8 ran without error, so no new bug instance was reproduced. The dispatch's caveat "if the validator crashes on a known-good file, that's a pre-existing bug — note in WARN register but don't try to fix the validator" did not fire.)
