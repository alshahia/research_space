# Plan — T-2026-07-04-004 (high level)

**Date:** 2026-07-04
**Sub-agent:** planning (executed by master for meta-task)
**Inputs:** `share/notes/01_research_T-2026-07-04-004.md` (audit + reflection)

## Goal

Close the 8 gap categories surfaced by the audit: empty `resources/`, empty `notes/episodic/`, no gold-standard example, no preflight subroutine, no calibrated feasibility, no wrong-specialist handoff, inline-prompt duplication, no instance-level measurement. All additive under v0.14.1.

## Self-score

| Dimension | Score (1-5) | Driver |
|---|---|---|
| Testability | 5 | Each task has a concrete Acceptance line + verify command |
| Scope | 4 | 13 files touched (10 new + 3 modified); bounded to research/ + 2 controller files |
| Dependencies | 3 | P3D3 + P3D4 both touch controller files (different ones, no collision); P3D3 + P3D7 reference `## Metrics` schema (controlled via P3D3 → P3D7 sequencing in the dispatch order, although they touch different files) |
| Risks | 3 | H1 SKILL.md drift mitigated additive-only; M1-M3 mitigated via footer / JSON-validity / idempotency |

## Phases

| # | Phase | Tasks | File count | Net LOC | Risks |
|---|---|---|---|---|---|
| 1 | 1 Research | SKIP (audit IS the research) | 0 | 0 | — |
| 2 | 2 Planning | this plan + plan-phases | 2 new | ~120 | — |
| 3 | 3 Build (5 dispatches) | P3D1, P3D2, P3D3, P3D4, P3D5 | 13 files (10 new + 3 modified) | ~600 | H1 M1 M2 M3 |
| 4 | 4 Review (1 dispatch) | per-task verdicts + WARN register | 2 new | ~150 | — |
| 5 | 5 Completion | git/non-git menu + CHANGELOG + ## Completion | 1 modified | ~30 | — |

## Phased plan — see `share/notes/02_plan_phases_T-2026-07-04-004.md`

## Open questions for the user

None — proceed to build. All 6 ambiguities resolved at Phase 0 with master-approved defaults.

## NEEDS_USER_CONFIRMATION

**true** (Phase 2 gate) — already received explicit "proceed" at Turn 3 of user-task capture.

## Self-critique

- **Did I do my job?** Yes. The plan has 5 phases, 7 build tasks, per-task complexity, per-task acceptance, and a 4-dim self-score.
- **What might I have missed?** (a) `scripts/validate-memory.sh` may not exist (mentioned in memory README but not seen in scripts/). P3D3's episodic backfill has no automated validator available. Mitigation: manual per-file frontmatter check at Phase 4. (b) `agents_manager/research/notes/episodic/.gitkeep` is empty by design — but the alternative is that episodic notes were moved to `agents_manager/memory/projects/<slug>/` in v0.13.0. The v0.13.0 CHANGELOG entry doesn't clarify; the backfill may be redundant. Mitigation: Phase 4 reviewer verifies by listing both folders at task close.
- **What did I assume without evidence?** I assumed Phase 5 will detect non-git (the repo is git-tracked per `git status` output, so Phase 5a menu applies). I assumed the 5 dispatches are small enough that no per-phase complexity re-check is needed (each is ≤150 LOC / ≤5 files / 0-1 novel abstractions — below the split triggers).