# WARN register — T-2026-07-01-001

- T-2026-07-01-001.A (T1) — HIGH — PSScriptAnalyzer count regressed from 141 → 147 (+6 PSAvoidUsingWriteHost warnings on the new wizard scope-prompt at `bin/agents-manager.ps1:647-652`)
- T-2026-07-01-001.D (T4) — HIGH — `bin/__pycache__/` and `bin/standalone-installer/__pycache__/` are untracked; `.gitignore` was not extended (dispatch brief gate 12 required it as part of T4 acceptance)
- T-2026-07-01-001.B (T2) — MEDIUM — Python version floor inconsistency: `bin/agents-manager.py:27` requires 3.6+, `bin/standalone-installer/install.py:56` requires 3.7+. Pick one.
- T-2026-07-01-001.B (T2) — LOW — `bin/agents-manager.py:242` description uses U+2014 em-dash; cp1252 console mangles to replacement char.
- T-2026-07-01-001.B (T2) — LOW — Interactive `menu_install` (`bin/agents-manager.py:208-216`) and `menu_skills` (L240-243) also forward `--skills`; same broken hookup as the CLI install.
- T-2026-07-01-001 (cross-cutting) — MEDIUM — PowerShell `Parse-InstallFlags` rejects `-Git` / `-Yes` / `-DryRun` (PascalCase) despite `bin/agents-manager.ps1:603` help text advertising them, and despite `bin/README.md:51` documenting them. Pre-existing docs/behavior drift; surfaced by running install through fresh flags.
- T-2026-07-01-001 (cross-cutting) — LOW — `bin/release-zip.ps1` and `.gitattributes` show LF on working tree while `.gitattributes:8` declares `*.ps1 text eol=crlf`. Pre-existing; git normalizes on commit.
- T-2026-07-01-001.I (fix-loop 2) — MEDIUM — `bin/agents-manager.py:270-283` skills subcommand argparse has no `--skills|--scope` alias (only `--global|--local|--both|--skip`). Gate `python3 bin/agents-manager.py skills add --all --skills local --yes` fails at argparse (exit 2). Pre-existing drift surfaced by fix-loop-I gate; out of scope for that loop (dispatch constrained to bash + .ps1 only).
- T-2026-07-01-001.J (fix-loop 3) — RESOLVED — above J-loop-I WARN closed by adding `p_skills.add_argument("--skills", "--scope", dest="scope", choices=[...])` at `bin/agents-manager.py:284-286`. All 7 skills-gates + 2 install-gates pass. `menu_skills` (L186-190) already uses `f"--{scope}"` so no wizard change needed.
- T-2026-07-01-001.K (re-review loop 3) — LOW — `bin/agents-manager` bash dispatcher has CRLF line endings in the working tree (LF in HEAD / git index per `git ls-files --eol`: `i/lf  w/crlf  attr/`). `.gitattributes:5` declares `*.sh text eol=lf` but `bin/agents-manager` has no extension match. With `core.autocrlf=true` (this Windows host), direct `bash bin/agents-manager ...` fails with `set -euo pipefail\r: invalid option name`. Not in canonical dispatch path on Windows (Python wrapper routes to PowerShell via `IS_WINDOWS`); not in canonical path on Linux/macOS (autocrlf=false, file is LF). Pre-existing from prior commits; not introduced by G/I/J fix loops. Cosmetic-only on Linux/macOS, masked on Windows. Safe to defer to v0.11.1 (add `bin/agents-manager text eol=lf` to `.gitattributes`).
- T-2026-07-01-001.K (re-review loop 3) — LOW — `.gitignore:49-52` has a duplicate `agents-manager v0.11.0` block that's identical to the `v0.10.0` block at L40-43 (same three ignore entries). Cosmetic; not a bug; can be consolidated in v0.11.1.
- T-2026-07-01-001.K (re-review loop 3) — LOW — `bin/agents-manager.py:2` module docstring still has U+2014 em-dash (`agents-manager CLI — Python UX layer`). Original M2 was specifically about line 242 (argparse description, FIXED) — this is the docstring leftover. Not in argparse output; doesn't trigger cp1252 console crash. Cosmetic; safe to defer.
