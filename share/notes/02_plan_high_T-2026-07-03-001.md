# 02 · Plan (high-level) — T-2026-07-03-001

## Goal

Apply the upstream-contrib patch `PROPOSED_PATCH_v0.5.x_2026-07-01_cinematic-landing-template.md` to the agents_manager controller in this repo. The patch adds:
1. A vendor-neutral **cinematic-landing task template** at `templates/cinematic-landing/` (17 files).
2. A 6th specialist **am-assets** (asset gatekeeper, runs the 4-branch runtime asset decision tree) at `agents_manager/assets/` (5 files).
3. Two controller edits: `agents_manager/SKILL.md` (master) and `opencode.jsonc` (register `am-assets`).

This is **deliberate maintenance by master** — the apply routine is a literal verbatim execution of the proposal's `<apply-with-llm>` block, not a creative design task. No specialist dispatch is needed for Phase 3 because the proposal is the spec.

## Non-goals

- Did NOT spawn `am-planning`. The plan IS the proposal. Spawning planning would add noise.
- Did NOT spawn `am-research`. The research IS the proposal's §1–§8 + the 5 source files I already read.
- Will NOT spawn `am-coder` for Phase 3. I am acting as the executor of a verbatim apply routine.
- Will NOT spawn `am-design`. No design work needed; the proposal carries its own worked example.
- Will NOT commit. Per `AGENTS.md`: commits are user-driven.

## Approach

Execute the proposal's `<apply-with-llm>` block as a 7-step verbatim procedure:

1. Read `templates/cinematic-landing/memory/`, `templates/cinematic-landing/prompts/`, etc. sections of the proposal (re-read from disk in chunks of ≤1000 lines to avoid truncation).
2. Create the directory tree: `templates/cinematic-landing/{memory,skeleton,prompts,decisions,assets}/` + `agents_manager/assets/notes/`.
3. **Step 2.5**: `cp E:\js_projects\3d_website\1_website_minimax_3\cinematic-landing-kit-demo\index.html templates/cinematic-landing/skeleton/index.html`.
4. Write the 17 files under `templates/cinematic-landing/` from §3.1 of the proposal (one file per fenced code block, content extracted verbatim).
5. Write the 5 files under `agents_manager/assets/` from §3.2 of the proposal (verbatim).
6. Apply the 2 controller edits:
   - `agents_manager/SKILL.md` — append `## Templates (v0.9.0+)` section after `## Shared communication bus` (per §3.3 anchor — our SKILL.md does NOT yet have a `## Templates` section).
   - `opencode.jsonc` — append `am-assets` agent entry after `am-review`, update header comments "5 specialists" → "6 specialists" (per §3.4).
7. Run verification:
   - `cat templates/cinematic-landing/assets/MANIFEST.txt | sort > /tmp/expected.txt`
   - `find templates/cinematic-landing/ agents_manager/assets/ -type f | sort > /tmp/actual.txt`
   - `diff -u /tmp/expected.txt /tmp/actual.txt` (expected: empty)
   - `python -c "import json,re; json.load(open(re.sub(r'//.*','',open('opencode.jsonc').read())))"` (JSON validity)
   - markdown header check: each `templates/cinematic-landing/memory/*.md` starts with `# `

## Phases (one-line each)

- **Phase 0 — Ingest**: capture user task, create tracker, create progress ledger — DONE.
- **Phase 1 — Research**: write research note (proposal summary, wall conflicts, risks) — DONE.
- **Phase 2 — Plan (this file)** + `02_plan_phases` + **PAUSE for user on opencode.jsonc wall**.
- **Phase 3 — Apply 24 atomic units**: 17 + 5 new files + 2 controller edits + verification (no specialist dispatch — I execute as master, classified as deliberate maintenance).
- **Phase 4 — Review**: dispatch `am-review` to verify the applied patch matches the proposal byte-for-byte (compare each file to its proposal §3 source, confirm 2 controller edits have correct anchors, confirm verification block passed).
- **Completion**: append `## Completion` block + `## Phase productivity` table to `tasks/T-2026-07-03-001.md`.

## Risks acknowledged

- **R1 (opencode.jsonc wall)** — applying the patch requires editing `opencode.jsonc`, which is in master's "cannot do" list. Past precedent (v0.6.0, v0.7.0, v0.9.0) shows MiniMax-M3 has crossed this wall with user delegation. Surface to user at this Phase 2 PAUSE; require explicit OK.
- **R2 (working tree dirty)** — `git status` shows ~10 modified files + ~9 untracked files, all from in-flight v0.11.0 work. None of my Phase 3 writes will touch these. The 2 controller edits (SKILL.md + opencode.jsonc) do modify tracked files that are also in the dirty tree — those edits will stack on top of the dirty state. **Mitigation**: after Phase 3, run `git status --short` and explicitly call out the new files vs the stacked edits so the user can review/cherry-pick cleanly.
- **R3 (line count discrepancy)** — proposal claims skeleton is 885 lines; actual file is 841 lines. Cosmetic only; `MANIFEST.txt` doesn't list line counts, so `diff` still passes. Will note in `share/notes/03_coder_summary_T-2026-07-03-001.md` (will create at Phase 3 close).
- **R4 (prompt content truncation)** — the proposal §3.4 `am-assets` prompt text was truncated at 2000 chars in my initial read. Will re-read at offset in Phase 3 to get the full text before inserting.
- **R5 (no smoke test)** — no downstream web/ project in this repo to smoke-test (this IS the controller repo). Verification is limited to: file existence + JSON validity + markdown header sanity + visual spot-check on the skeleton HTML. The proposal's "verification via wc -l == 885" will be a no-op (or note the 841 vs 885 discrepancy as expected).

## Open assumptions

- The user is OK with me crossing the `opencode.jsonc` wall as part of "deliberate maintenance" (will ask).
- The user is OK with me crossing the `agents_manager/SKILL.md` wall as part of "deliberate maintenance" (per my SKILL.md this is in-lane but still surfaces for review).
- The user wants the entire patch applied (24 atomic units), not a cherry-picked subset.
- The user will review/commit manually; I will not commit.

## Plan self-score (1–5)

| Dimension | Score | Notes |
|---|---|---|
| Testability | 5 | Verification block has 4 explicit acceptance signals (diff, JSON validity, header check, manifest match) |
| Scope | 5 | Scope locked at 24 atomic units (22 new + 2 modified); the proposal is the spec |
| Dependencies | 4 | Depends on: user OK on opencode.jsonc wall (PAUSE); source skeleton HTML still exists at source path (verified at Phase 0) |
| Risks | 4 | R1 + R2 are the two real risks; both surfaced to user; R3-R5 are minor and contained |

Total: 18/20

## Self-critique

- **Am I being lazy by skipping am-planning/am-coder/am-design?** No — the proposal IS the plan. The apply routine is verbatim. Spawning specialists to re-derive what the proposal already specifies would add cycle time without value. This is exactly the case where "deliberate maintenance by master" applies.
- **Is the opencode.jsonc wall really crossed by me?** Yes — no specialist agent is allowed to edit it (per their Can/Can't lists). Master is the only agent with the proposal-delegated authority to edit it. This is the cleanest path; alternative would be to ask the user to do it manually, which adds friction.
- **What if verification fails?** I will not blindly fix it — surface the failure to user via the question tool. Per the proposal's `<review-with-llm>` block, the 10-question second-opinion prompt can be used to triage.
- **Should Phase 4 be am-review?** Yes — am-review is the right agent to verify "applied patch matches proposal byte-for-byte". It's a structural review, not a code review.

## Wall-conflict decision (PAUSE — awaiting user)

The proposal requires 2 controller edits:
1. `agents_manager/SKILL.md` — in-lane per "deliberate maintenance task with user review" exception.
2. `opencode.jsonc` — **OUT OF MASTER'S LANE** per SKILL.md "What you cannot do" § 1.

Past precedent shows this wall has been crossed by MiniMax-M3 for v0.6.0, v0.7.0, v0.9.0. The user has explicitly delegated this task. **Will proceed ONLY if user confirms at the Phase 2 PAUSE.**