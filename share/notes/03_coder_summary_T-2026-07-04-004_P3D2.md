# Coder Summary — T-2026-07-04-004 / P3D2

**Date:** 2026-07-04 19:55
**Sub-agent:** coder
**Loop:** initial

## Tasks attempted
| ID | Status | Notes |
|----|--------|-------|
| P3D2T1 | done | Wrote `scripts/backfill-research-metrics.sh` (append-only, idempotent, exit 0/1). Self-tested: first run touches 4 files exit 1, second run no-op exit 0, runtime 0.024s. |

(Tracker row is master's lane — not edited by me. Master: set P3D2T1 → `done`, Coder col → this file.)

## Files written / edited
- `scripts/backfill-research-metrics.sh` — **created** — 87-line bash script. Globs `share/notes/01_research_*.md`; skips any file already containing `^## Metrics`; else infers 5 counts and appends a `## Metrics` block (EOL-matched, one blank line before the heading). Exit 1 if any file touched, else 0.

**Side-effect files (produced by running the deliverable per the mandated self-test — NOT hand-edited):**
- `share/notes/01_research_T-2026-07-03-001.md` — Metrics block appended (findings=0 risks=0/0/0 Qs=0; this note uses a table-based severity + non-standard section names → all inferred as 0, by design — see W1).
- `share/notes/01_research_T-2026-07-03-002.md` — appended (findings=8 risks H/M/L=3/8/5 Qs=6).
- `share/notes/01_research_T-2026-07-03-003.md` — appended (findings=31 risks=0/0/0 Qs=5; severities are in a table → 0).
- `share/notes/01_research_T-2026-07-04-004.md` — appended (findings=5 risks H/M/L=1/3/0 Qs=6).

These four are the exact "prior research outputs that predate v0.14.1" the dispatch self-test step 2 says the first run should touch. The plan (P3D2) explicitly scopes the script to scan and backfill `share/notes/01_research_*.md`, so these modifications are the intended effect of the tool, not scope creep.

## Commands run
- `bash -n scripts/backfill-research-metrics.sh` — exit 0 (syntax OK).
- `sed -i 's/\r$//' scripts/...` — normalize to LF (`.gitattributes`: `*.sh eol=lf`; CRLF would break the shebang).
- `chmod +x scripts/...` — set exec bit in working tree.
- Isolated `/tmp` test (fabricated A=no-metrics, B=has-metrics): RUN1 exit 1, RUN2 exit 0, B md5 unchanged, A counts correct → all PASS.
- Real-repo self-test: RUN1 → 4 touched, exit 1; RUN2 → "no work needed", exit 0; timing run → 0.0245s.

## Tests run
- No project test framework for `bin/`/`scripts/` (per AGENTS.md: "There are no tests for `bin/` scripts"). Verification is the dispatch self-test + an isolated fixture test, both PASS. `shellcheck` not installed in this env (skipped); `bash -n` passed.

## Deviations from plan
- **File discovery via shell glob, not `rg`/`grep -rl`.** The skeleton suggested rg-preferred/grep-fallback for *finding* files. A shell glob (`for f in share/notes/01_research_*.md`) is stdlib-only, dependency-free, and exactly matches the known naming convention — simpler and more reliable (rg is not installed here anyway). grep -E + awk are still used for the *content* inference. Documented in the header comment.
- **Added an EOL-detection + trailing-newline guard** (not in skeleton): appends CRLF to CRLF files / LF to LF files, and closes an unterminated final line so `## Metrics` always has exactly one preceding blank line (valid markdown). Caught during self-test — the naive skeleton abutted the heading against the last body line on files lacking a trailing newline.
- **Did NOT update `tasks/T-2026-07-04-004.md`** — dispatch boundary says CANNOT edit the task file (master's lane). Reported row status above for master to apply.

## Known issues / TODOs left in code
- **W1 (LOW, matches plan's expected WARN W1):** Inferred counts are best-effort. Notes recording severity in a *table* (001, 003) or using non-`## Technical findings` section headers (001) infer as 0 — a known undercount, documented in `infer_counts()` comments and stamped in the backfill comment (`counts are inferred, not directly authored`). Before flagging I checked for `share/notes/04_warns_register_T-2026-07-04-004.md` — it does not yet exist, so no dedup needed; master should add W1 when creating the register.
- No other issues. Second run is a verified no-op.

## Suggested review focus
- `scripts/backfill-research-metrics.sh:28-33` — the awk section-scoped findings counter (the only non-trivial parser). Confirm it counts only top-level `- ` bullets within `## Technical findings`.
- `scripts/backfill-research-metrics.sh:53` — the `^## Metrics` skip guard (idempotency + append-only hinge). Note it is unanchored at end but only matches a line *starting* with `## Metrics`; inline body mentions (e.g. "`## Metrics` block") do not trigger it.
- `scripts/backfill-research-metrics.sh:60-73` — EOL match + blank-line guard; verify no mixed CRLF/LF is introduced.
- Exit-code contract (`:79-84`): 1 on touch, 0 on no-op.

## Self-critique
- **Did I do my job?** Yes. Single file delivered; idempotent, append-only, correct exit codes, sub-second runtime, per-file stderr logging with inferred counts — all acceptance criteria met.
- **What might I have missed?** (1) Exec-bit: `git config core.filemode = false` on this Windows repo and the sibling `scripts/validate-memory.sh` is tracked as `100644`, so git will store this script as `100644` too — the acceptance's `-rwxr-xr-x` is a Unix ideal not achievable on this repo; all invocations use `bash scripts/...` which needs no exec bit. (2) The inference heuristics are deliberately shallow for table-style severity notes (W1). (3) I did not test a genuinely CRLF-ended research file end-to-end (all 4 real files are LF); the CRLF branch is logic-verified but not exercised on a real fixture.
- **What did I assume without evidence?** That backfilling all 4 (including this task's own 004) is intended — supported by the dispatch self-test wording and the plan's "scan `01_research_*.md`" scope. That `bash`-invocation (not exec bit) is the repo's script convention — supported by `core.filemode=false` + sibling script mode `100644`.

## Status
DONE_WITH_CONCERNS — the only concern is W1 (LOW, already anticipated by the plan) and the exec-bit-on-Windows note; both are documented, neither blocks. Deliverable passes all self-tests.
