# Research — T-2026-07-03-003

**Date:** 2026-07-03
**Trigger:** initial (3rd dispatch of the day, after T-001=patch apply + T-002=memory system)
**Sub-agent:** research
**Source project:** `E:\js_projects\3d_website\1_website_minimax_3`
**Our project:** `E:\context_gen`

## Task in one sentence

Compare our applied v0.12.0 cinematic-landing template state against the source project's post-T-005/T-004 state and integrate the v2-axis deltas: (a) 4 demo memory files covering canvas-a11y, mid-session reduced-motion listener, keyboard nav, and dark theme; (b) update PROPOSED_PATCH.md per the user's §F (line counts, MANIFEST entries, insights, headline); (c) optionally copy the worked-example folder + proposal for traceability.

## What we know for sure

### A. Source project state (`E:\js_projects\3d_website\1_website_minimax_3`)

**Source PROPOSED_PATCH** lives at `agents_manager/upstream-contrib/PROPOSED_PATCH_v0.5.x_2026-07-01_cinematic-landing-template.md` (1888 lines, 83,724 bytes). Structure: §1 Executive summary → §2 Evidence → §3 Applied changes (24 atomic units, MANIFEST at L1604-1639) → §4 Workflow gaps (G1-G7) → §5 Workflow insights → §6 Recommended review order → §7 Owner action checklist → §8 About this file.

**Source worked example** at `cinematic-landing-kit-demo/` contains: `README.md` (321 lines, 20,874 bytes; has §11 T-005 hardening, §12 T-004 dark theme, §13 all v2 axes closed sections), `index.html` (1167 lines after T-004 dark theme, 76,472 bytes), `memory/` with 4 files (`09-canvas-a11y.md` 127 lines, `10-reduced-motion-listener.md` 128 lines, `12-keyboard-nav.md` 127 lines, `13-dark-theme.md` 191 lines — `8,520`/`10,567`/`7,496`/`18,814` bytes).

**Source task trackers**: `tasks/T-2026-07-01-004.md` (76 lines — dark theme, all phases DONE) and `tasks/T-2026-07-01-005.md` (82 lines — a11y+DPR, PASS-WITH-NOTES review at L80, 2 MEDIUMs fixed via surgical patch).

### B. Our project state (`E:\context_gen`)

**We DO have:**
- `templates/cinematic-landing/` (10 memory files, skeleton, prompts, decisions, assets/MANIFEST.txt, assets/manifest.schema.json). Verified via `Get-ChildItem`.
- `agents_manager/upstream-contrib/PROPOSED_PATCH_v0.5.x_2026-06-29.md` (31,249 bytes) + `PROPOSED_PATCH_v0.5.x_2026-06-29_part2_chunk-size.md` (25,197 bytes). The 07-01 file is NOT in our `upstream-contrib/`.
- `agents_manager/CHANGELOG.md` v0.12.0 entry at L5-83 (PASS-WITH-NOTES, 5 review-driven W2-W6 fixes). v0.13.0 memory-system entry at L84+.
- `share/reports/04_review_T-2026-07-03-001.md` (24,475 bytes; initial review PASS-WITH-NOTES, 1 WARN on `mix-blend-mode` heuristic vs hard rule).
- `templates/cinematic-landing/skeleton/index.html` (896 lines after W4 + W5: skip-link added at L371, mid-session `change` listener added at L611-615 with simplified `location.reload()` only — no `__reducedMotionActive` flag, no `__lastReduce` defensive guard, no Safari < 14 fallback).

**We DO NOT have:**
- `cinematic-landing-kit-demo/` folder at our root (`Test-Path` → `False`).
- `agents_manager/upstream-contrib/PROPOSED_PATCH_v0.5.x_2026-07-01_cinematic-landing-template.md` (`Test-Path` → `False`).
- The 4 v2 memory files (`09-canvas-a11y.md`, `10-reduced-motion-listener.md`, `12-keyboard-nav.md`, `13-dark-theme.md`).
- v0.12.0 memory entries populated at `agents_manager/memory/{global,projects}/` (only `.gitkeep` files exist; v0.13.0 scaffold is empty).
- `agents_manager/assets/` references in our `MANIFEST.txt` (it only lists 17 `templates/cinematic-landing/` files; the `agents_manager/assets/` 5 files are missing — though the source's proposal §3.5 also has them at L1630-1634, our MANIFEST.txt doesn't).

### C. Source memory files vs our skeleton — content absorption matrix

| Source memory file | Content summary | In our skeleton? | In our existing 9+1 memory files? |
|---|---|---|---|
| **09-canvas-a11y.md** (127 lines) | `role="img"` + `aria-label` on film wrapper; 6 hidden `<ol>` `<li>` transcript; `aria-current="step"` toggled per scrub; memo on `_lastFilmN` to avoid setAttribute/frame | NO — grep `role="img"`, `aria-current="step"`, `film-transcript` against skeleton returns 0 hits. Skeleton film section is at L416-450 with bare `<img>` stacked. | NO — our `memory/02-scroll-film-canvas.md` and `memory/09-quality-bar.md` don't mention film `role="img"` or transcript. Source's A7 = fully new content. |
| **10-reduced-motion-listener.md** (128 lines) | Mid-session `change` listener with `__reducedMotionActive` global flag (test observability), `__lastReduce` defensive guard (spurious-event no-op), Safari < 14 `addListener` fallback; `location.reload()` is the locked decision (Q1 m0099) | PARTIAL — skeleton has the listener at L611-615 but only the simplified version: `matchMedia("(prefers-reduced-motion: reduce)").addEventListener("change", () => { location.reload(); })`. No `__reducedMotionActive` flag exposure, no `__lastReduce` guard, no Safari < 14 fallback. | PARTIAL — our `memory/07-reduced-motion.md` L38-47 documents the listener pattern in pseudocode but doesn't mention the flag exposure or Safari fallback. Our `07-reduced-motion.md:38-46` matches source's intent but lacks the contract surface. |
| **12-keyboard-nav.md** (127 lines) | PageDown/PageUp section-snap with `tabindex="-1"` on all sections + `section:focus-visible` gold-deep ring; CTA-scope ArrowRight/Space/ArrowLeft via IntersectionObserver (threshold 0.35) gating preventDefault; `aria-keyshortcuts="PageDown PageUp"` on film; Lenis integration via `lenis.scrollTo(tgt, {offset:0})` vs native `scrollIntoView` fallback; wrap-on-ArrowLeft to match click handler modulo | NO — grep `tabindex="-1"`, `aria-keyshortcuts`, `PageDown`, `IntersectionObserver` against skeleton returns 0 hits. Skeleton sections (hero L385, film L416, reveal L451, ritual L478, cta L497, editions L540) have NO `tabindex`. Skeleton keyboard handler at L877-880 calls `e.preventDefault()` UNCONDITIONALLY (only `!reduce` gate, no CTA-scope gate) — this is the exact bug source's "Common pitfalls" warns about (breaks native arrow-key behavior for AT users outside CTA). | NO — our `memory/08-cta-frames.md:42-47` documents only the basic ArrowRight/Space advance without IntersectionObserver gating or PageDown/PageUp. Source's A12 = fully new content. |
| **13-dark-theme.md** (191 lines) | `:root[data-theme="dark"]` 14-token counter-table; mode-aware `--gold-text-top: var(--gold-deep)` (K1 root-cause fix); `#ritual .idx` `--gold-deep` swap (K2; **reverted at P5** to `--gold-bright` per V-K2 on-screen FAIL); 6 `data-ambient-dark` per-section values; FOUC prevention via `<head>` pre-`<style>` script; `localStorage.theme` persistence + `prefers-color-scheme: dark` auto-detect; WebAIM contrast audit (all 7 fg dark tokens ≥ AA body); deuteranopia sim PASS (R-DCB CLOSED) | NO — grep `:root[data-theme`, `data-ambient-dark`, `__refreshAmbient`, `setTheme` against skeleton returns 0 hits. Skeleton has no dark theme. | PARTIAL — our `memory/05-theming.md:58-72` has a 15-line sketch `[data-theme="dark"]` block with example dark values (which diverge from source's researched values: `--ink-soft: #B8A687` vs source's `#C9B89A`, `--ink-faint: #8E7B5F` vs source's `#8E7E66`). The sketch lacks K1 mode-aware `--gold-text-top`, K2 `#ritual .idx` story, per-section `data-ambient-dark` mapping, FOUC prevention `<head>` script, and the WebAIM audit table. |

### D. Source PROPOSED_PATCH.md — what §F updates touch

The user's "§F" reference (per the parenthetical "line counts, MANIFEST entries, insights, headline") maps to four distinct zones of the proposal:

1. **Line counts**: source L156 says "885 lines"; our applied skeleton is **896 lines** (after W4 + W5, +55 lines vs source) per `04_review_T-2026-07-03-001.md:35-36` (841 before W4/W5, 896 after). §2 evidence table at L173 also says "885". Both numbers need to become "841 (proposal-time) → 896 (post-apply with W4+W5)".
2. **MANIFEST entries**: source §3.5 at L1604-1639 lists 22 new files + 2 modified. Our MANIFEST.txt (`templates/cinematic-landing/assets/MANIFEST.txt:1-23`) only enumerates 17 `templates/cinematic-landing/` files. The `agents_manager/assets/` 5 files (which were created in our apply) are NOT listed. After W3, `templates/cinematic-landing/memory/04-locale-handoff.md` was added but not in MANIFEST.txt either. The MANIFEST.txt needs 6 added lines + line-count assertions could move here.
3. **Insights**: source §5 (L1703-1757) lists qualitative learnings (worked: 4-branch tree; needs work: am-assets weight, schema complexity; recommendations: do NOT add 7th specialist, do NOT lock to single asset source). After apply, new insights to surface: (a) the W3 `04-locale-handoff.md` arrived AFTER the proposal — demonstrates the "trivially-followup-able" property the proposal predicted; (b) the W4+W5 review-driven fixes showed the source skeleton had 2 latent a11y gaps that the template's own `memory/07-reduced-motion.md` flagged — apply-time verification caught them.
4. **Headline**: source §1 TL;DR (L158-160) ends with "Estimated merge effort: ~30 minutes for a human, ~3 minutes for an LLM applying §3." Post-apply actual was ~30 minutes including W2-W6 follow-ups. Headline should reflect "applied + review-driven fixes landed" status.

### E. v0.13.0 memory-system constraints that bound this work

Per `agents_manager/memory/README.md:113-116` (the canonical-schema source-of-truth):

> **No-write-into-templates.** Do NOT write into `templates/<name>/memory/`. That's the template author's lane.

The 4 source memory files are **template-runtime playbook content** (their content is about the cinematic-landing template's section implementation patterns), not controller-internal specialist knowledge. They belong in `templates/cinematic-landing/memory/`, NOT in `agents_manager/memory/{global,projects}/`. The v0.13.0 frontmatter requirement (`scope, topic, status, created, last_verified`) does NOT apply to `templates/<name>/memory/` files — those are template-author territory, not memory-system entries. Verified: our existing `templates/cinematic-landing/memory/01-09 + 04-locale-handoff.md` (10 files) all have ZERO frontmatter. The source 4 files also have zero frontmatter — they match our template convention.

### F. Worked-example traceability — context

Source proposal L146-147: "T-2026-07-01-002 (Cinematic Landing Kit Demo — SHIPPED, review PASS)" is referenced as the proof-of-life example. Our CHANGELOG.md v0.12.0 entry (L60-63) already lists `cinematic-landing-kit-demo` as a "source project" but does NOT copy the folder. The user wants the option to copy it for traceability — this means `cinematic-landing-kit-demo/{index.html, README.md, memory/}` (3 entries, ~106 KB total) plus the proposal file (83 KB). Total: ~189 KB of additional files at our root.

## What we don't know (ambiguities)

- **Q1 (placement of 4 v2 memory files)** — verbatim to `templates/cinematic-landing/memory/`? Or to `cinematic-landing-kit-demo/memory/` (preserving source folder structure)? Or both? Source files have `<!-- TODO(template-migration): -->` pointers at L1-5 saying "when T-003 applies, move to `templates/cinematic-landing/memory/`" — but T-003 already applied (v0.12.0) and the move was never done.
  - **Suggested clarifying question:** "The 4 v2 files have explicit `TODO(template-migration)` headers pointing at `templates/cinematic-landing/memory/`. After the T-001 apply that did not move them, should we (a) cp them to `templates/cinematic-landing/memory/` only, dropping the TODO comment, (b) keep them in `cinematic-landing-kit-demo/memory/` only (preserve the source folder), (c) cp to both folders, or (d) cp to `templates/cinematic-landing/memory/` and update the source pointer to `MOVED → ...`?"

- **Q2 (worked-example folder copy)** — the user said "Optionally copy the worked-example demo folder + the proposal file to our repo for traceability". Is this optional-but-recommended, or strictly optional?
  - **Suggested clarifying question:** "Should the worked-example folder + proposal file be copied (adds ~189 KB to our root for traceability), or skipped (no copy, files referenced by path only in CHANGELOG)?"

- **Q3 (skeleton absorption scope)** — the source skeleton had 1167 lines post-T-004 (vs our 896 lines). The gap (~271 lines) is dark theme + JS controller. Should the post-T-004 dark theme content be applied to our skeleton, OR should it stay source-only (as a worked-example enhancement, with our skeleton retaining light-only)?
  - **Suggested clarifying question:** "Source's skeleton is 1167 lines (post-T-004 dark theme); ours is 896 (light only). Should we (a) extend our skeleton to match source's dark theme (requires new `:root[data-theme=dark]` block + JS controller + FOUC `<head>` script), (b) leave our skeleton light-only and document the dark theme as a 'follow-up patch via 13-dark-theme.md', or (c) leave it source-only and don't touch our skeleton?"

- **Q4 (memory file numbering)** — our existing `04-cinematic-hero.md` and `04-locale-handoff.md` already have a duplicate "04-" prefix (a known cosmetic issue from W3, not in scope). If we cp source's `12-keyboard-nav.md` as `12-keyboard-nav.md` (matching source numbering), we get another duplicate prefix with `08-cta-frames.md` and `09-quality-bar.md`. Should we (a) renumber on copy, (b) keep source numbering and accept three duplicate-prefix pairs, (c) renumber the existing `04-locale-handoff.md` and our existing 01-09 to free up numbers 10-13 for the v2 files?
  - **Suggested clarifying question:** "Our memory folder already has a duplicate '04-' (04-cinematic-hero.md + 04-locale-handoff.md) from W3. Adding source's `12-keyboard-nav.md` would create a duplicate '12-' (12-keyboard-nav.md vs nothing else yet, OK). Should the 4 v2 files keep their source numbers (09, 10, 12, 13) or be renumbered to 11, 12, 13, 14 to avoid the existing duplication pattern?"

- **Q5 (PROPOSED_PATCH.md §F minimum viable update)** — the user mentioned "line counts, MANIFEST entries, insights, headline" as the four things to update. Is this a minimum (touch only these 4) or a recommendation (touch these plus any other post-apply drift)?
  - **Suggested clarifying question:** "Should §F update be strictly the four zones you named (line counts, MANIFEST entries, insights, headline), or also include other post-apply drift like the W2-W6 review-driven fixes and the W3 `04-locale-handoff.md` addition?"

## Risks and doubts

- **R1 — content duplication risk if v2 memory files overlap topics with our existing memory/01-09 files** — **severity: LOW**
  - Evidence: matrix in §C shows all 4 source files contain content NOT in our 9+1 existing memory files. `09-canvas-a11y.md` and `12-keyboard-nav.md` are fully new; `10-reduced-motion-listener.md` augments our `07-reduced-motion.md:38-46`; `13-dark-theme.md` augments our `05-theming.md:58-72`.
  - Mitigation: cp verbatim, then run a topical dedup pass against our 10 existing files. If any paragraph in source is a near-duplicate of a paragraph in ours, prefer the source version (more research + reviewed). This is a Phase 4 review check.

- **R2 — v0.13.0 memory-system constraints don't apply to template memory files** — **severity: LOW** (mitigated by understanding)
  - Evidence: `agents_manager/memory/README.md:113-116` fences off `templates/<name>/memory/` from the memory-system tree. The 4 source files have NO frontmatter (matching our 10 existing template files' style). The v0.13.0 schema (`scope, topic, status, created, last_verified`) applies only to `agents_manager/memory/{global,projects}/` and `agents_manager/<role>/notes/{semantic,episodic}/`.
  - Mitigation: do NOT add frontmatter to the cp'd files; cp them as plain markdown like our 10 existing template memory files. Document this in the cp plan (Phase 4 review check).

- **R3 — PROPOSED_PATCH.md §F update scope is ambiguous** — **severity: MEDIUM**
  - Evidence: source proposal is 1888 lines; §1 + §2 + §3.5 + §5 = ~80 lines; updating all of them post-apply is a non-trivial text-edit task. The user said "§F of the user's delivery manifest (line counts, MANIFEST entries, insights, headline)" — but the proposal's structure has no literal "§F" section (it's numbered 1-8). The user's mental §F may refer to a delivery-manifest structure not present in the proposal itself.
  - Mitigation: ask the clarifying question Q5 above before editing; default to updating the four named zones + their directly-dependent cross-refs (e.g., §7 checklist `G2_skeleton_file: RESOLVED` at L1804 is already correct, no change needed; §7 `G6_locale_handoff` at L1805 should flip from `PENDING` to `RESOLVED` because W3 landed `04-locale-handoff.md`). If user says minimum-viable, touch only those.

- **R4 — Where do the 4 v2 files land?** — **severity: MEDIUM** (depends on Q1)
  - Evidence: source files are at `cinematic-landing-kit-demo/memory/`; their own TODO comment says move to `templates/cinematic-landing/memory/`. Our apply of T-003 did NOT do the move. So currently neither folder exists in our project.
  - Mitigation: the right default is `templates/cinematic-landing/memory/` (per source TODO + the no-write-into-templates fence). If Q1 answer is (a) or (d), proceed. If (b) or (c), surface as a deliberate deviation from source's TODO comment.

- **R5 — `13-dark-theme.md` — template addition vs worked-example enhancement?** — **severity: HIGH**
  - Evidence: source's skeleton at index.html:1167 lines HAS the dark theme applied. Our skeleton at 896 lines does NOT. If we just cp `13-dark-theme.md` to `templates/cinematic-landing/memory/`, we have a memory file documenting dark theme behavior that our skeleton doesn't implement. This is a documentation-without-code mismatch.
  - Mitigation: surface this as Q3 above. Default plan: do NOT extend the skeleton (out of scope for a memory-file-cp task), but cp `13-dark-theme.md` as a "future-patch recipe" — its `Status:` section (L181-191) explicitly says "Applied to cinematic-landing-kit-demo/index.html" so the memory file is consistent with source. Document in our CHANGELOG that the template's runtime dark-theme recipe is in the memory file but not yet applied to the skeleton; future patch can apply it.

- **R6 — skeleton already has W4+W5; if `10-reduced-motion-listener.md` + `12-keyboard-nav.md` patterns are already absorbed, copy becomes redundant** — **severity: MEDIUM**
  - Evidence: verified via grep — our skeleton has the simplified mid-session reduce listener (L611-615) but lacks the `__reducedMotionActive` flag, `__lastReduce` guard, and Safari < 14 fallback. Our skeleton has NO section `tabindex="-1"`, NO `aria-keyshortcuts`, NO PageDown/PageUp section-snap, NO CTA-scope gate on arrow keys (the L876-880 handler fires unconditionally outside CTA — this is the bug source's "Common pitfalls" warns about).
  - Mitigation: the cp is NOT redundant. `10-reduced-motion-listener.md` augments our skeleton contract (add the flag + guard + fallback). `12-keyboard-nav.md` adds entirely new keyboard-handling layer. Phase 4 review should verify the cp didn't duplicate existing patterns. Note: source's `12-keyboard-nav.md` L126 `Status:` says "Applied to cinematic-landing-kit-demo/index.html" — so source's skeleton does have `tabindex="-1"` on all sections + the IntersectionObserver gate. Our skeleton does NOT. If Q3 is "extend our skeleton to match source's dark theme + a11y+DPR state", the skeleton extension would be substantial (not just dark theme).

## Technical findings

### Source v2 memory files — per-file deep dive

#### F1. `cinematic-landing-kit-demo/memory/09-canvas-a11y.md` (127 lines, 8,520 bytes)
- **Purpose:** A11y pattern for scroll-driven film section. Adapts v1 `<canvas>` + hidden `<ol>` transcript recipe to v2 demo's 6-still crossfade variant.
- **Key content** (line refs to source):
  - L10-12: Why this matters — film sections are completely opaque to SR without parallel text layer.
  - L20-23: v1 → demo adaptation table (canvas → stage wrapper, frames → `<img aria-hidden>`, transcript `<ol>`).
  - L27-43: Markup recipe (HTML for `<section id="film">` with `role="img"`, hidden `<ol class="film-transcript">`, 6 `<li>`).
  - L47-50: 1×1 px clipped CSS for visually-hiding transcript (NOT `display:none`).
  - L52-62: 6 sensory descriptions for the demo's stills (Pexels ID 4202325/3735160/3735181/4226265/1108572/9883737).
  - L66-95: GSAP callback pseudocode with memo on `_lastFilmN` (avoid setAttribute/frame at 60Hz).
  - L97-99: Why `aria-current="step"` not `"true"` (enumerated values: `page | step | location | date | time | true | false`).
  - L101-108: 6-step manual verification with screen readers (VO swipe, MutationObserver, etc.).
  - L110-112: Why not `<figure>` + `<figcaption>` per still (noisy — 6 stacked figures announce all captions).
  - L116: Why "Frame N of 6 — …" prefix on each `<li>` (reachable as static list).
  - L118-123: Common pitfalls (display:none breaks SR, aria-current on wrapper vs `<li>`, no memoization, removing `<li>` text on reduce).
  - L126: Status — applied to demo L463-528 + JS L940-960.
- **Verbatim-vs-augment vs skip:** **VERBATIM cp**. Content is fully new to our template; no overlap with our existing 10 files. Source file format matches our template convention (no frontmatter). The only adjustment is the TODO comment at L1-5 should be removed on copy (since the migration is happening).
- **Path:line refs:**
  - Source: `E:\js_projects\3d_website\1_website_minimax_3\cinematic-landing-kit-demo\memory\09-canvas-a11y.md:1-127`
  - Target: `E:\context_gen\templates\cinematic-landing\memory\09-canvas-a11y.md` (NEW)

#### F2. `cinematic-landing-kit-demo/memory/10-reduced-motion-listener.md` (128 lines, 10,567 bytes)
- **Purpose:** Handle user toggling OS reduce-motion preference mid-session. Documents the locked decision: `location.reload()` on change (Q1 m0099). Specifies a test-observable global flag.
- **Key content:**
  - L8-14: Why this matters — init-time-only reading misses OS toggles; P5T1 V5 is behavioral.
  - L16: The locked decision: `location.reload()`.
  - L18-27: Why not soft teardown (~80 LOC brittle; teardown-and-rebuild for long-lived apps).
  - L29-51: The verbatim module (16 LOC) — `__reducedMotionActive` flag, `__lastReduce` guard, Safari < 14 fallback.
  - L57-63: Why expose `window.__reducedMotionActive` (test observability, future consumers, novel abstraction).
  - L65-72: Defensive guard rationale (Playwright `emulateMedia` quirks, SPA back/forward cache).
  - L74-78: Safari < 14 fallback (no graceful degrade; pre-14 Safari refresh manually).
  - L80-107: P5T1 V5 verification — Playwright `emulateMedia({reducedMotion:'reduce'})` → assert `__reducedMotionActive === true` + `scrollY === 0` + `getComputedStyle(.motes span).animationName === 'none'` within 500ms.
  - L121-128: Status + follow-ups.
- **Verbatim-vs-augment vs skip:** **AUGMENT cp**. The 16-LOC verbatim module (L29-51) is what our skeleton needs but doesn't fully have (our skeleton has L611-615 with the listener but no flag, guard, or fallback). On cp, append a Phase 4 review note: "skeleton currently has simplified version; consider extending to full 16-LOC contract in future patch."
- **Path:line refs:**
  - Source: `E:\js_projects\3d_website\1_website_minimax_3\cinematic-landing-kit-demo\memory\10-reduced-motion-listener.md:1-128`
  - Target: `E:\context_gen\templates\cinematic-landing\memory\10-reduced-motion-listener.md` (NEW)

#### F3. `cinematic-landing-kit-demo/memory/12-keyboard-nav.md` (127 lines, 7,496 bytes)
- **Purpose:** Three additive key bindings: PageDown/PageUp section-snap, ArrowRight/Space/ArrowLeft CTA-frame advance/retreat. Designed to coexist with native scroll + form-field behavior.
- **Key content:**
  - L14-20: Three key bindings table.
  - L22-24: Why wrap on ArrowLeft (matches click handler modulo; avoids "at end" aria-live announcement).
  - L26-34: `tabindex="-1"` requirement + `:focus-visible` rule with gold-deep ring.
  - L36-45: Markup recipe (6 sections with `tabindex="-1"`).
  - L47-92: JS recipes — PageDown/PageUp document-level + CTA-scope arrow keys with IntersectionObserver threshold 0.35.
  - L94-96: Why IntersectionObserver (covers case 2: focus elsewhere but CTA visible).
  - L98-100: Why listener on `window` (not `#cta`).
  - L102-104: Why `preventDefault()` inside the gate (preserve native arrows elsewhere).
  - L106-108: Why `lenis.scrollTo` vs `scrollIntoView` (avoids Lenis fighting native smooth scroll).
  - L110-115: 4-step manual test.
  - L117-123: Common pitfalls — forgetting tabindex, preventDefault outside gate, focus without preventScroll, scrollIntoView block:center, tabindex="0" on sections.
- **Verbatim-vs-augment vs skip:** **VERBATIM cp**. Content is fully new to our template. Our skeleton's L876-880 handler has the exact bug source's pitfalls warn about (preventDefault unconditional). On cp, append a Phase 4 review note: "skeleton currently has unconditional arrow-key handler; consider applying the IntersectionObserver gate in future patch."
- **Path:line refs:**
  - Source: `E:\js_projects\3d_website\1_website_minimax_3\cinematic-landing-kit-demo\memory\12-keyboard-nav.md:1-127`
  - Target: `E:\context_gen\templates\cinematic-landing\memory\12-keyboard-nav.md` (NEW)

#### F4. `cinematic-landing-kit-demo/memory/13-dark-theme.md` (191 lines, 18,814 bytes)
- **Purpose:** Dark theme foundation (CSS only — JS controller is P4T3, not in this file). Documents the 14-token counter-table, K1 `.gold-text` mode-aware `--gold-text-top`, K2 `#ritual .idx` revert story, per-section `data-ambient-dark`, FOUC prevention, WebAIM contrast audit, deuteranopia sim.
- **Key content:**
  - L12-22: §0 Module comment — purpose, locked defaults (Q1/Q2/Q3), 3 HIGH risks (R-FOUC-1, R-K12, R-DCB).
  - L24-43: §1 — 14-token dark counter-table with WebAIM contrast ratios (all 7 fg dark tokens ≥ AA).
  - L49-75: §2 — K1 root-cause fix — `--gold-text-top: var(--gold-deep)` mode-aware token (caps read as `--gold-deep` in both modes — 5.24:1 light / 6.03:1 dark).
  - L77-104: §3 — K2 history — `#ritual .idx` `--gold-bright`→`--gold-deep` swap was **REVERTED at P5** to `--gold-bright` after V-K2 on-screen re-measure (literal-ambient math FAIL; actual photo+overlay composite FAIL on `--gold-deep` 2.08–2.80:1; PASS on `--gold-bright` 4.63–6.59:1).
  - L106-121: §4 — per-section `data-ambient-dark` (6 values; `#ritual` drops further in dark mode to keep relative position).
  - L123-146: §5 — 4 pass-gates (WebAIM contrast, deutan sim, grep preservation, file size).
  - L147-159: §6 — Don't-touch list (JS controller = P4T3; toggle button = P4T3; localStorage = P4T3; `<head>` FOUC script = P4T3; `#vignette`/`#glow` re-tunes = deferred).
  - L161-175: §7 — Why-not-per-mode-overrides — attribute selectors over `@media (prefers-color-scheme: dark)` (3 reasons: explicit toggle, localStorage persistence, specificity).
  - L179-191: Status + verification + follow-ups.
- **Verbatim-vs-augment vs skip:** **VERBATIM cp + SKIP-our-existing-05-theming-dark-mode-section**. Source `13-dark-theme.md` is the canonical dark-theme reference; our `05-theming.md:58-72` is a 15-line sketch that source supersedes. On cp, consider shrinking our `05-theming.md:58-72` to a pointer ("see `13-dark-theme.md` for the full recipe") rather than maintaining two divergent copies. Decision flagged for Phase 4 review.
- **Path:line refs:**
  - Source: `E:\js_projects\3d_website\1_website_minimax_3\cinematic-landing-kit-demo\memory\13-dark-theme.md:1-191`
  - Target: `E:\context_gen\templates\cinematic-landing\memory\13-dark-theme.md` (NEW)

### G. Source PROPOSED_PATCH.md — §F update zones

| Section | Line range | Current text | Post-apply text | Delta scope |
|---|---|---|---|---|
| §1 Executive summary | L147 | "Two downstream tasks (T-2026-07-01-001 ... + T-2026-07-01-002 ...). The synthesis surfaced **3 CRITICAL + 4 HIGH** template-level improvements..." | Add "**Post-apply status (2026-07-03):** all 24 atomic units landed in v0.12.0 with PASS-WITH-NOTES review verdict (1 WARN on mix-blend-mode heuristic, 5 LOW review-driven fixes W2-W6, +1 W3 follow-up `04-locale-handoff.md`). v2 axes (a11y + DPR + dark theme) landed in the demo at T-005 + T-004 but not yet folded back into the template." | INSERT new paragraph at L157 (between existing para and TL;DR) |
| §1 TL;DR | L158-160 | "Add `templates/cinematic-landing/` (17 files...)... Estimated merge effort: ~30 minutes for a human, ~3 minutes for an LLM applying §3." | Add: "**Applied 2026-07-03 → v0.12.0. Actual merge effort:** ~30 minutes human + ~15 minutes W2-W6 review-driven fixes. The biggest expected win (F2 am-assets specialist) was realized; am-assets now sits at Phase 3a in the pipeline. v2 axes remain source-only (demo at T-005 + T-004 done; template absorption = T-2026-07-03-003)." | EXTEND the existing TL;DR |
| §2 Evidence table | L173 | "Lines of demo (T-002) \| 885" | "Lines of demo (T-002) \| 885 (proposal-time) → 896 (post-apply in v0.12.0 template skeleton after W4 skip-link + W5 mid-session reduce listener added)" | REPLACE one row |
| §3.5 MANIFEST.txt | L1604-1639 | 22 new files + 2 modified = 24 atomic units | Add 1 file: `templates/cinematic-landing/memory/04-locale-handoff.md` (W3 addition). Total: 23 new files + 2 modified = 25 atomic units. | APPEND one line + update total |
| §5.1 What worked | L1705-1719 | 4 bullets | Add 1 bullet: "**W3 follow-up arrived cleanly after merge.** `04-locale-handoff.md` was a 30-line follow-up called out in §G6 — the apply process surfaced it as missing-references, and a separate W3 patch landed it. Demonstrates the patch's 'trivially-followup-able' property." | INSERT new bullet |
| §5.3 Things I would change tomorrow | L1736-1743 | 3 numbered items | Add 1 item: "**Fold v2 axes (a11y + DPR + dark theme) into the template's memory + skeleton.** They live in the source demo only. The 4 source memory files (`09-canvas-a11y`, `10-reduced-motion-listener`, `12-keyboard-nav`, `13-dark-theme`) should migrate to `templates/cinematic-landing/memory/` and the skeleton should extend to absorb them (currently skeleton lacks film a11y, mid-session flag, section `tabindex`, dark theme)." | INSERT new item |
| §7 Owner action checklist | L1804-1805 | "G2_skeleton_file: RESOLVED..." + "G6_locale_handoff: PENDING..." | Update G6 to: `RESOLVED (W3 follow-up — 04-locale-handoff.md added 2026-07-03)`. Add: `G8_v2_axes_fold: PENDING — fold source's 09/10/12/13 memory files into template + extend skeleton (T-2026-07-03-003 in progress)`. | REPLACE one line + APPEND one line |

Total §F edit scope: ~7 distinct zones, ~10 lines of changed/added text. Estimated edit effort: 15-20 minutes for am-coder.

### H. Source README §11/§12/§13 — context for the user's intent

`cinematic-landing-kit-demo/README.md`:
- L182-233: §11 — T-005 hardening (9 a11y items shipped: A1, A2, A4, A5, A6, A7, A9, A10, A11, A12; 4 verified n/a: A3, A8, A13; DPR D1-D3 shipped; 3 memory files added at L213-215). Review verdict PASS-WITH-NOTES (2 MEDIUMs fixed via surgical patch M1 `aria-keyshortcuts` + M2 button-tag guard).
- L237-298: §12 — T-004 dark theme (DT1-DT6 all shipped; K1 mode-aware `--gold-text-top`; K2 REVERTED after V-K2 on-screen FAIL; FOUC prevention `<head>` script; localStorage persistence; ambient tween; deuteranopia sim PASS; 1 memory file added at L285). Review verdict **FAIL initial → PASS after fix**.
- L302-321: §13 — All v2 axes closed (vendor, English, agent-ready, a11y, dark theme — all closed). Optional cosmetic follow-up + future work list.

Source's T-004 P4T2 at L70-71 row says: "all 14 dark tokens PASS AA · K1 cap AA · deutan sim PASS · K2 light-mode runtime math flagged as HIGH concern for P5 V5" — this is the K2 story.

### I. Source's plan files referenced in T-004/T-005

- T-2026-07-01-005 P4T2 row L77 + P4T3 row L78 + P4T4 row L79 — show the 4 a11y memory files were produced in T-005 P4T2 (A7 + A12), P4T3 (DPR only — no memory), P4T4 (A9 — the listener).
- T-2026-07-01-004 P4T2 row L72 — produced `13-dark-theme.md`.

### J. Skeleton currently has — confirmed via grep

| Item | In our skeleton? | Source skeleton (per §11 README) has it? |
|---|---|---|
| Skip-link | YES (W5 at L84-91, L371) | YES |
| Mid-session reduce listener | YES (W4 at L611-615) but simplified | YES |
| `__reducedMotionActive` flag | NO | YES |
| `__lastReduce` defensive guard | NO | YES |
| Safari < 14 fallback | NO | YES |
| Film `role="img"` + aria-label | NO | YES |
| Film hidden `<ol>` transcript | NO | YES |
| Film `aria-current="step"` | NO | YES |
| Section `tabindex="-1"` (all 6) | NO | YES |
| Section `:focus-visible` gold-deep ring | YES (universal `:focus-visible`) | YES |
| `aria-keyshortcuts="PageDown PageUp"` on `<section id="film">` | NO | YES |
| PageDown/PageUp section-snap | NO | YES |
| CTA-scope gate on arrow keys | NO (unconditional) | YES (IntersectionObserver threshold 0.35) |
| `if(e.target.tagName === "BUTTON") return;` guard | NO | YES |
| DPR srcset ladder (600/1200/1800/2400w) | NO | YES |
| DPR `sizes` attribute (mobile-first) | NO | YES |
| `:root[data-theme="dark"]` block | NO | YES |
| Per-section `data-ambient-dark` (6 values) | NO | YES |
| `#gold-text-top` mode-aware token | NO | YES |
| Header theme toggle button | NO | YES |
| FOUC prevention `<head>` script | NO | YES |
| `localStorage.theme` persistence | NO | YES |
| `prefers-color-scheme: dark` auto-detect | NO | YES |
| WebAIM dark contrast audit | NO (lives only in `13-dark-theme.md` table) | YES |
| Deuteranopia sim | NO | YES |

### K. Our existing 9+1 template memory files — topical overlap with v2 files

- `01-builder-flow.md` (56 lines): 6-stage build flow. Overlap with v2: NONE. Distinct from all 4 v2 files.
- `02-scroll-film-canvas.md` (61 lines): 4 paths (A canvas / B video / C stills / D fallback) + hard rules. Overlap with `09-canvas-a11y.md`: MINOR — both mention canvas; but `02` is about implementation paths, `09` is about a11y pattern for them. Distinct enough to keep separate.
- `03-scroll-ticker.md` (46 lines): Lenis + GSAP single ticker. Overlap: NONE.
- `04-cinematic-hero.md` (59 lines): 5-layer hero. Overlap: NONE.
- `04-locale-handoff.md` (39 lines, W3 addition): locale opt-in. Overlap: NONE.
- `05-theming.md` (80 lines): CSS tokens + dark mode sketch (L58-72). Overlap with `13-dark-theme.md`: SIGNIFICANT on dark-mode section. Recommendation: shrink `05-theming.md:58-72` to a pointer to `13-dark-theme.md` after cp.
- `06-asset-pipeline.md` (127 lines): 4-branch decision tree. Overlap: NONE.
- `07-reduced-motion.md` (71 lines): 3-layer reduced-motion. Overlap with `10-reduced-motion-listener.md`: MEDIUM — `07` covers all 3 layers generically; `10` covers Layer 2 (JS listener) in depth. Keep both; `07` is the spec, `10` is the implementation recipe.
- `08-cta-frames.md` (61 lines): 3-frame click-advance. Overlap with `12-keyboard-nav.md`: MINOR — both mention ArrowRight/Space; `08` is about the click-advance pattern, `12` is about keyboard navigation in general (PageDown/PageUp + arrow keys + IntersectionObserver gate).
- `09-quality-bar.md` (59 lines): 7 review dimensions. Overlap: NONE.

Verdict: only `05-theming.md` and `07-reduced-motion.md` have meaningful overlap with v2 files. Recommendation in Phase 4: shrink `05-theming.md:58-72` to a pointer; leave `07-reduced-motion.md` unchanged (the 3-layer overview complements `10-reduced-motion-listener.md`'s implementation recipe).

## Feasibility verdict

- **Can do:** **yes** (with 5 clarifying questions on placement/scope).
- **Why:** All 4 source memory files exist at known paths, are small (8-19 KB each, 127-191 lines), and follow our template-memory-file convention (no frontmatter, plain markdown). The cp operation is mechanical. The PROPOSED_PATCH.md §F update is bounded text editing in ~7 zones (per matrix in §G above). The worked-example + proposal copy is a single `cp` command + ~189 KB of new files at our root. The single material risk (R5: dark-theme as memory-file-without-skeleton) is manageable by treating `13-dark-theme.md` as a "future-patch recipe" rather than a runtime contract.

## Recommendations for the planning agent

### Candidate sub-tasks for am-planning

- **P3T1** — Copy 4 v2 memory files to `templates/cinematic-landing/memory/`
  - Source → Target map:
    - `E:\js_projects\3d_website\1_website_minimax_3\cinematic-landing-kit-demo\memory\09-canvas-a11y.md` → `E:\context_gen\templates\cinematic-landing\memory\09-canvas-a11y.md` (NEW)
    - `…\memory\10-reduced-motion-listener.md` → `…\memory\10-reduced-motion-listener.md` (NEW)
    - `…\memory\12-keyboard-nav.md` → `…\memory\12-keyboard-nav.md` (NEW)
    - `…\memory\13-dark-theme.md` → `…\memory\13-dark-theme.md` (NEW)
  - On each cp: strip the L1-5 `<!-- TODO(template-migration): -->` comment (the migration is happening).
  - Edit decision: keep source numbering (`09`, `10`, `12`, `13`) — surface the duplicate-`04-` issue separately (not in scope for this task).
  - Files expected: 4 new markdown files, ~45 KB total.
  - Acceptance: each new file matches source byte-for-byte except for the stripped TODO comment.

- **P3T2** — Copy worked-example demo folder + proposal to our root (conditional on Q2 answer)
  - `cp -r E:\js_projects\3d_website\1_website_minimax_3\cinematic-landing-kit-demo E:\context_gen\cinematic-landing-kit-demo` (3 files: README.md, index.html, memory/ — ~106 KB)
  - `cp E:\js_projects\3d_website\1_website_minimax_3\agents_manager\upstream-contrib\PROPOSED_PATCH_v0.5.x_2026-07-01_cinematic-landing-template.md E:\context_gen\agents_manager\upstream-contrib\` (83 KB)
  - Files expected: 1 new folder + 1 new file, ~189 KB total.
  - Acceptance: `Test-Path E:\context_gen\cinematic-landing-kit-demo` = True; SHA-256 of copied `index.html` matches source.

- **P3T3** — Update `templates/cinematic-landing/assets/MANIFEST.txt` for missing entries
  - Append 2 lines: `templates/cinematic-landing/memory/04-locale-handoff.md` (W3 was added but not in MANIFEST.txt) + the `agents_manager/assets/` 5-file block (was created but never enumerated).
  - Update header comment to reference the new total count (was 17 files → 23 files after the additions).
  - Files expected: 1 modified file, +6 lines.
  - Acceptance: `Get-Content templates/cinematic-landing/assets/MANIFEST.txt | sort` matches `Get-ChildItem templates/cinematic-landing/ -Recurse -File | sort` minus comment lines.

- **P3T4** — Update source PROPOSED_PATCH.md per §F (conditional on Q5 answer — minimum-viable 4 zones vs extended)
  - Edits in 7 zones (per §G matrix above): §1 new paragraph + TL;DR extend, §2 evidence row replacement, §3.5 MANIFEST.txt +1 line, §5.1 +1 bullet, §5.3 +1 numbered item, §7 checklist updates (G6 → RESOLVED + new G8 PENDING).
  - Files expected: 1 modified file (`agents_manager/upstream-contrib/PROPOSED_PATCH_v0.5.x_2026-07-01_cinematic-landing-template.md`), +10 / -2 lines.
  - Acceptance: each of the 7 zones shows the new text; SHA-256 of the file differs from pre-edit; the 10 added lines cite the source state correctly (no factual errors).

- **P3T5** (optional) — Shrink our `05-theming.md:58-72` dark-mode sketch to a pointer to `13-dark-theme.md`
  - Replace L58-72 with: "## Dark mode — see `13-dark-theme.md`\n\nThe 14-token dark counter-table, K1 mode-aware `--gold-text-top`, K2 revert story, per-section `data-ambient-dark`, FOUC prevention, WebAIM contrast audit, and deuteranopia sim live in [`memory/13-dark-theme.md`](./13-dark-theme.md) as the canonical recipe."
  - Files expected: 1 modified file, -15 / +4 lines.
  - Acceptance: pointer works (target file exists after P3T1); our `05-theming.md` no longer has divergent hex values vs source's researched values.

- **P3T6** (out of scope, NOT recommended in this dispatch) — Extend `templates/cinematic-landing/skeleton/index.html` to absorb the v2 a11y + DPR + dark-theme patterns
  - This is a substantial change (~271 lines to add to the skeleton to match source's 1167 lines). Not a "cp memory files" task — that's a separate "absorb v2 axes into template" task. Surface for a future task id (T-2026-07-03-004 or T-2026-07-04-001) but DO NOT include in T-003.

### Acceptance criteria for Phase 4 review

- All 4 new memory files exist at `templates/cinematic-landing/memory/` and match source content byte-for-byte (except stripped TODO comment).
- Our `MANIFEST.txt` now lists `04-locale-handoff.md` + the 5 `agents_manager/assets/` files; `find templates/cinematic-landing/` matches MANIFEST.txt.
- The source `PROPOSED_PATCH.md` shows updated line counts (885 → 896), MANIFEST entries (24 → 25 atomic units after W3), post-apply insights (§5.1 new bullet), future-work item (§5.3 new item), and checklist updates (G6 RESOLVED, G8 PENDING).
- Our `CHANGELOG.md` gets a new sub-section under v0.12.0 (or a new v0.12.1 entry) noting the v2-axes absorption: "v2-axis memory files (09/10/12/13) integrated into `templates/cinematic-landing/memory/`; source PROPOSED_PATCH.md updated per §F; skeleton absorption deferred to a future patch."
- `templates/cinematic-landing/00-readme-first.md:13` says "9 memory files" — should now say "13 memory files" (was missed at W3 — surface as a separate fix in Phase 4 review).
- `agents_manager/CHANGELOG.md:13` v0.12.0 entry says "9 `memory/*.md`" — should now say "13 `memory/*.md`".
- No `templates/cinematic-landing/memory/0N-*.md` file has frontmatter (preserves template convention; v0.13.0 schema does NOT apply to template files).
- No code edits; the skeleton stays at 896 lines (unchanged). v2-axis skeleton absorption is a future patch.

## Open questions for the user

1. **Q1 (placement of 4 v2 memory files):** The 4 v2 files have explicit `TODO(template-migration)` headers pointing at `templates/cinematic-landing/memory/`. After the T-001 apply that did not move them, should we (a) cp them to `templates/cinematic-landing/memory/` only, dropping the TODO comment, (b) keep them in `cinematic-landing-kit-demo/memory/` only (preserve the source folder), (c) cp to both folders, or (d) cp to `templates/cinematic-landing/memory/` and update the source pointer to `MOVED → ...`?

2. **Q2 (worked-example folder copy):** Should the worked-example folder + proposal file be copied (adds ~189 KB to our root for traceability), or skipped (no copy, files referenced by path only in CHANGELOG)?

3. **Q3 (skeleton absorption scope):** Source's skeleton is 1167 lines (post-T-004 dark theme); ours is 896 (light only). Should we (a) extend our skeleton to match source's dark theme + a11y+DPR state (substantial change, ~271 lines), (b) leave our skeleton light-only and document the dark theme + a11y+DPR as a 'follow-up patch via 09/10/12/13 memory files', or (c) leave it source-only and don't touch our skeleton in this dispatch?

4. **Q4 (memory file numbering):** Our memory folder already has a duplicate '04-' (04-cinematic-hero.md + 04-locale-handoff.md) from W3. Adding source's `12-keyboard-nav.md` would create another duplicate-prefix pattern. Should the 4 v2 files keep their source numbers (09, 10, 12, 13) or be renumbered to 11, 12, 13, 14 to free up numbers?

5. **Q5 (PROPOSED_PATCH.md §F minimum viable update):** Should §F update be strictly the four zones you named (line counts, MANIFEST entries, insights, headline), or also include other post-apply drift like the W2-W6 review-driven fixes and the W3 `04-locale-handoff.md` addition (extending to §5.3 future-work + §7 checklist G6/G8 updates)?

## Self-critique

- **Did I do my job?** Partial. I documented all 4 v2 memory files, did the comparison matrix against our existing 10 files, identified content absorption vs new content, mapped the §F update zones. But I did NOT:
  - Run the source `index.html` against a real browser to verify the a11y patterns actually work end-to-end. Source says they did; I trusted the source.
  - Verify that source's `13-dark-theme.md` hex values would actually pass WCAG AA in our skeleton if we applied them — I trust source's WebAIM audit but didn't re-compute.
  - Run our existing skeleton in a browser to confirm the simplified mid-session listener at L611-615 actually fires on `emulateMedia` change. The source's P5T1 V5 says yes; I trust source.

- **What might I have missed?**
  - I did not read the source proposal's `<review-with-llm>` block's 10 questions in depth (only skimmed L113-141). The 10 questions might surface additional §F update requirements beyond the 4 the user named.
  - I did not check `agents_manager/research/notes/episodic/` or `semantic/` for prior insights on the cinematic-landing template. v0.13.0 memory scaffold is empty, so this is moot, but the v0.12.0 work might have left notes I should re-read for context.
  - I did not check `share/notes/03_coder_summary_T-2026-07-03-001.md` in full — only inferred from review report. Coder summary might have documented the W2-W6 fixes more precisely.
  - I did not look at `agents_manager/CHANGELOG.md` lines 100+ (v0.13.0 entry); only read the v0.12.0 section. The v0.13.0 entry might bind this work to a larger memory-system rollout.
  - I did not read `tasks/T-2026-07-03-002.md` to see if T-002 (the memory system work) set any conventions for how new memory files should be added.

- **What did I assume without evidence?**
  - I assumed the user's "§F of the user's delivery manifest" refers to the four zones (line counts, MANIFEST entries, insights, headline) and not to a specific structured manifest file. **Verified** by the user's parenthetical listing the four items — this is the right reading.
  - I assumed the source proposal §3.5 MANIFEST.txt has the 22+2=24 list (not 23+1). **Verified** at L1639 "(24 atomic units total: 22 new files + 2 modified files.)" + L1613-1636 file list.
  - I assumed the 4 v2 memory files belong in `templates/cinematic-landing/memory/`, not `agents_manager/memory/{global,projects}/`. **Verified** by `agents_manager/memory/README.md:113-116` no-write-into-templates fence + the source's TODO comment + the files' runtime-playbook content nature.
  - I assumed our skeleton's L876-880 keyboard handler has the bug source's "Common pitfalls" warns about. **Verified** by direct read at L876-880: `window.addEventListener("keydown", e => { if(e.key === "ArrowRight" || e.key === " "){ e.preventDefault(); next(); } else if(e.key === "ArrowLeft"){ e.preventDefault(); show(idx - 1); } });` — unconditional preventDefault after `!reduce` gate only. The handler fires outside the CTA, blocking native arrow-key behavior.
  - I assumed the source PROPOSED_PATCH.md `885` line count claim is wrong for our applied state. **Verified** — source's proposal L156 says 885; our review report L35 says our skeleton is 896 lines after W4+W5.

- **Was I complete?** Yes on the comparison-matrix requirement. Partial on the optional worked-example copy (I gathered the facts but did not make a recommendation for whether to copy — left as Q2 for the user).

## Metrics

findings: 31
risks_HIGH: 0
risks_MEDIUM: 0
risks_LOW: 0
clarifying_Qs: 5

<!-- backfilled by scripts/backfill-research-metrics.sh on 2026-07-04 -- counts are inferred, not directly authored -->
