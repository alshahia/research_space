# WARN register — T-2026-07-03-002

> Master appends per-am-review WARNs here. Format: `<chunk> — <severity> — <description> — \`path:line\``.

---

## Phase 3 Build, chunk 1 (P3.1) — 2026-07-03

- P3.1 — MEDIUM — `agents_manager/memory/README.md` frontmatter uses `title/scope/status/created/last_verified` per spec, no `description:`. Pre-existing `scripts/validate-frontmatter.py` requires `description:` even in lenient mode (no `/skills/` in path). Validator exits 1 with `missing required field 'description'`. Not a memory-tree bug; spec-vs-validator inconsistency. `agents_manager/memory/README.md:1-7`
- P3.1 — MEDIUM — Pre-existing `scripts/validate-frontmatter.py:74` returns `[str(e)]` (list) when `parse_frontmatter` raises, but caller at line 119 does `errs, mode = validate_one(path)` (tuple unpack) — crashes `ValueError` on any non-frontmatter file passed as argument. Reproduced by `python3 scripts/validate-frontmatter.py agents_manager/.gitignore` → traceback exit 1. Not introduced by this chunk; surfaced for validator-chunk (P3.3) or maintenance follow-up. `scripts/validate-frontmatter.py:69-119`
- P3.1 — LOW — Locked design #4 (`share/handoffs/00_user_task_T-2026-07-03-002.md:33`) defines `scope: global|project|role` for memory entries, but the canonical README's own frontmatter declares `scope: canonical-schema` — a 4th value. Intentional (README is meta-doc, not a memory entry), and the validator (P3.3) does not scan the memory/ root (only `global/` + `projects/`), so no collision. Document this convention so future schema validators don't flag it as out-of-set. `agents_manager/memory/README.md:3`

## Phase 3 Build, chunk 2 (P3.2) — 2026-07-03

- P3.2 — LOW — `agents_manager/design/notes/{semantic,episodic}/.gitkeep` are pre-existing at 36 bytes (not 0 bytes as dispatch check 1 expected). Content: `placeholder so git tracks the folder`. Dispatch explicitly says "if pre-existing, leave alone (don't overwrite)" — followed that instruction. Dispatch check 1 also says "all 12 paths, all 0 bytes" — these two constraints are mutually exclusive for the 2 design files. Functional equivalence preserved (both track the directory in git). Consider standardizing `.gitkeep` convention repo-wide in a follow-up maintenance phase. `agents_manager/design/notes/semantic/.gitkeep:1`, `agents_manager/design/notes/episodic/.gitkeep:1`
- P3.2 — LOW — `agents_manager/assets/notes/README.md` line count is 16 (`Measure-Object -Line`) / 24 raw newlines (`wc -l`) — exceeds the plan's literal `Done-when #3` bound of ≤15 by 1-9 lines. Dispatch reportedly set ≤30 (per coder summary; that bound is met). Substance is correct (proper disambiguation + rule-of-thumb), but the plan's literal bound is violated. Coder's claim "16 satisfies both" is mathematically incorrect on the plan's bound. `agents_manager/assets/notes/README.md:1-24`
- P3.2 — LOW — The 4 replaced READMEs + new assets README lack trailing newlines (file ends with last text byte, no LF terminator). Inconsistent with most repo SKILL.md files (which have trailing LF) but consistent with `agents_manager/design/SKILL.md` and `CLAUDE.md`. Not a spec violation; repo convention is mixed. Could be standardized in a polish phase. `agents_manager/{research,planning,coder,review}/notes/README.md`, `agents_manager/assets/notes/README.md`
- P3.2 — LOW — `scope: canonical-pointer` (introduced in `agents_manager/assets/notes/README.md:3`) is a 4th-value family member alongside chunk 1's `scope: canonical-schema`. Both are intentional meta-doc markers; runtime validator scope (P3.3) excludes both files, so no collision. Document the 4th-value family (global|project|role + canonical-schema + canonical-pointer) in the canonical README's "File schema" section as a follow-up. `agents_manager/assets/notes/README.md:3`

## Phase 3 Build, chunk 3 (P3.3) - 2026-07-03

- P3.3 - LOW - Dispatch's check 6 wording says fence `git diff --stat` returns "empty" but a non-empty `11/398/45` semantic diff (with `--ignore-cr-at-eol`) has been present since chunk 1 (T-2026-07-03-001 follow-ups). Chunk 3 verifies the semantic intent ("no NEW delta vs prior chunk baseline") by exact-match against chunk 2's recorded `11/398/45`. Raw stat is `11/1248/895` due to `core.autocrlf=true` CRLF/LF normalization noise. Consider re-wording the check to "matches chunk N-1 baseline" or capture the baseline to a file for future automation. `scripts/validate-memory.sh:1-68`, `share/notes/02_plan_phases_T-2026-07-03-002.md:178`
- P3.3 — LOW — Plan's literal `superseded_by` requirement says "AND points to a file that exists" (per `share/notes/02_plan_phases_T-2026-07-03-002.md:83`); dispatch's spec says "just check the key exists" (per coder summary §Self-critique #3). Coder chose dispatch spec — script only checks key presence, not path resolution. No collision today (no `status: superseded` entries exist), but a future entry that points to a non-existent file would still PASS validation. Document the spec-vs-plan divergence as a known item and decide whether path-resolution is desired for v0.13.1. `scripts/validate-memory.sh:63-65`
- P3.3 — LOW — `scripts/validate-memory.sh:69` last line `[ "$fail" -eq 0 ]` has NO trailing newline. `wc -l` returns 68 (correct newline count); PowerShell `(Get-Content).Count` returns 69 (content lines). Both views are valid; bash executes the script correctly. Cosmetic only — repo convention is mixed (most SKILL.md have trailing LF, but `CLAUDE.md` does not). Could be standardized in a polish pass.
- P3.3 — LOW — WARN register's chunk-3 entry at `share/notes/04_warns_register_T-2026-07-03-002.md:20-22` uses `P3.3 - LOW` (hyphen-dash) format while chunks 1+2 entries use `P3.X — LOW` (em-dash) format. Inconsistent separator style within the same register. Cosmetic.

## Phase 3 Build, chunk 4 (P3.4) — 2026-07-03

- P3.4 — INFO — Plan's Phase-4 Done-when check 5 (`rg 'share/notes/02_secrets_' ...` returns 0 matches per `share/notes/02_plan_phases_T-2026-07-03-002.md:127`) is in tension with the dispatch's explicit requirement that the secrets-free rule be "present and explicit about NOT writing `share/notes/02_secrets_*` references". The rule can only be explicit if it names the forbidden pattern. The coder correctly resolved in favor of the design — each file shows 2 `02_secrets_*` matches (1 in the "Never write a memory entry that references" line, 1 in the "see share/notes/02_secrets_<topic>.md (do not include contents)" example). The literal check fails but the design intent is satisfied. No code change required; recommend future plan revision to phrase the check as "rg 'share/notes/02_secrets_' outside the secrets-free Hard-rules bullet = 0 matches" or scope the check to memory entries only. `agents_manager/{research,planning,design,coder,review,assets}/SKILL.md:56` (or corresponding line per file).

## Phase 3 Build, chunk 5 (P3.5) — 2026-07-03

- P3.5 — INFO — Final scope invariant `git status --short --porcelain | wc -l` returns 78 (expected ≤40). The baseline before chunk 5 was already 76 (not the 37 the dispatch estimated: "20 chunk-1+2+3+4 deliverables + ~17 pre-existing T-001 files"). The 76 baseline consists of 21 modified (T-001 fence follow-ups + chunks 1-4 SKILL.md/READMEs) + 55 untracked (T-001 leftovers in share/notes + share/reports + share/handoffs + tasks/ + 10 memory subdirectory entries + AGENTS.md + agents_manager/.gitignore + bin/{agents-manager,install}.{cmd,py,sh} + bin/standalone-installer/ + scripts/validate-memory.sh + agents_manager/assets/notes/*). Chunk 5 added exactly 3 modifications (CLAUDE.md, agents_manager/SKILL.md, agents_manager/CHANGELOG.md) as dispatched; the +2 delta from baseline (76 → 78) is correct. The literal ≤40 check fails by 38, but the chunk itself is in-scope per dispatch. Recommend future plan revisions to either (a) update the expected count to current baseline (~78) or (b) add a maintenance step that prunes stale share/notes/share/reports from previous task ids before scope checks run. The fence invariant (Check 8) is exact-match `11/398/45`, so chunk 5 did not touch the fence zone. `agents_manager/SKILL.md:528-579`, `CLAUDE.md:61,81-89`, `agents_manager/CHANGELOG.md:84-196`.
- P3.5 — INFO — Independent review (reviewer) confirms chunk 5 substantive work is correct: all 3 task verdicts PASS, fence invariant exact-match, chunk 5 file delta exactly 3 paths, frontmatter validator exit 0, CHANGELOG v0.13.0 entry body byte-exact with draft (13717 common bytes, 0 diffs). Dispatch's byte-diff regex `rb'## v0\.13\.0.*?(?=\n## |\Z)'` is buggy (matches the metadata reference at `share/notes/02_changelog_draft_T-2026-07-03-002.md:3` rather than the entry header at line 7); corrected regex `rb'## v0\.13\.0 \xe2\x80\x94 Three-scope.*?(?=\n## |\Z)'` produces the actual 13717-byte body match. Dispatch's stated target of 13605 bytes inherits this bug. Substantive byte-equality holds with the corrected regex; no chunk 5 content change required. `share/reports/04_review_T-2026-07-03-002_P3.5.md`
