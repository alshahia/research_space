# Plan — T-2026-07-04-004 (phased)

**Date:** 2026-07-04
**Sub-agent:** planning
**Pairs with:** `share/notes/02_plan_high_T-2026-07-04-004.md`

---

## Phase 0 — Ingest (DONE)

**Done when:** task file + user-task capture + progress ledger exist; git status checked; no secrets expected.

**Status:** DONE

---

## Phase 1 — Research (DONE — audit IS the research)

**Done when:** `share/notes/01_research_T-2026-07-04-004.md` exists with all 8 template sections + ≥1 risk with severity.

**Status:** DONE

---

## Phase 2 — Planning (DONE)

**Done when:** this file + `02_plan_high_*.md` exist; self-score filled; user has confirmed (Turn 3 of user-task capture).

**Status:** DONE (user confirmed with "proceed your are in master mode")

---

## Phase 3 — Build (2 specialist dispatches + 1 master self-edit)

> **Adjustment during Phase 3 prep:** original plan grouped 5 dispatches with am-coder. Soft-wall boundary check failed: am-coder CANNOT edit `agents_manager/research/**` or `opencode.jsonc`; master CANNOT edit `opencode.jsonc` either (controller fence, user-owned). Restructured to: am-research owns its own folder self-curation; am-coder owns the script; master owns its own doc; opencode.jsonc dedupe deferred to user-maintained T-008.

### Dispatch P3D1 — am-research (self-curate agents_manager/research/**)

**Complexity:** ≤420 LOC / 11 files (6 new resources + 3 new episodic + 2 modified) / 1 novel (preflight subroutine) / `split_recommended: false`

**Tasks in this dispatch:**
- **P3D1T1:** Ship 5 starter resources files
- **P3D1T2:** Ship 1 gold-standard example (synthetic)
- **P3D1T3:** Seed 3 episodic backfill files
- **P3D1T4:** SKILL.md additive (4 new sections: preflight, calibrated feasibility, wrong-specialist handoff, ## Metrics footer)
- **P3D1T5:** rules.md additive (2 new rules: confidence scoring, handoff)

**Files expected (new):**
- `agents_manager/research/resources/tech-stack.md` — pinned versions + rationale. ~30 L.
- `agents_manager/research/resources/glossary.md` — domain terms (master, specialist, soft wall, dispatcher, task id, severity, dispatch, handoff, preflight). ~30 L.
- `agents_manager/research/resources/prior-decisions.md` — pointer to `share/notes/99_decisions.md` + index of in-repo decision logs. ~25 L.
- `agents_manager/research/resources/known-pitfalls.md` — 5 common research traps (uncited, padded, scope-creep, missing-self-critique, missing-NEEDS_USER_INPUT). ~35 L.
- `agents_manager/research/resources/external-docs.md` — one-line summaries: OpenCode task tool, agents_manager SKILL.md, memory README, contents of `docs/` if any. ~30 L.
- `agents_manager/research/resources/example-research-output.md` — synthetic 8-section exemplar. ~150 L.
- `agents_manager/research/notes/episodic/T-2026-07-03-001.md` — frontmatter per `agents_manager/memory/README.md` L31-46; body ≤20 L.
- `agents_manager/research/notes/episodic/T-2026-07-03-002.md`
- `agents_manager/research/notes/episodic/T-2026-07-03-003.md`

**Files expected (modified):**
- `agents_manager/research/SKILL.md` — additive appends (NO renumbering, NO deletions):
  1. **Preflight (3 questions before writing).** New section after "Inputs you will receive": Does this task warrant research? Is the scope small enough for one report? Is the deliverable a file path I can name now? If any answer is "no" → STOP and ask master.
  2. **Calibrated feasibility verdict.** Modify the "Feasibility verdict" template to add `confidence: HIGH|MEDIUM|LOW` + paragraph citing the confidence driver.
  3. **Wrong-specialist handoff.** New section after "When to ask the user a question": if the user task is design/planning/coding/ops shaped, return immediately with one of {`HANDOFF-TO-PLANNING`, `HANDOFF-TO-DESIGN`, `HANDOFF-TO-CODER`, `HANDOFF-TO-MASTER`} + one-line rationale. Do NOT write a research file.
  4. **## Metrics footer.** Append to the "What you must produce" template — every research output ends with a `## Metrics` block listing `findings:`, `risks_HIGH:`, `risks_MEDIUM:`, `risks_LOW:`, `clarifying_Qs:` as integers.
- `agents_manager/research/rules.md` — additive appends (rules 1-10 unchanged, new rules numbered 11+12):
  11. **Confidence scoring rule.** Your Feasibility verdict must carry a confidence level (HIGH/MEDIUM/LOW). Drivers: HIGH = verified by direct read + path:line; MEDIUM = partial verification or single source; LOW = inferred / contradicted by other findings.
  12. **Handoff rule.** If you determine a task is not yours, return with one of the `HANDOFF-TO-*` tokens + one-line rationale. Do NOT write a research file.

**Acceptance:**
- 9 new files exist, non-empty
- 2 modified files preserve all original sections + add new sections additively
- Each new file has a `last-verified: YYYY-MM-DD` footer (or frontmatter per memory schema for episodic files)
- `Get-ChildItem agents_manager/research/resources/ -File` returns ≥7
- `Get-ChildItem agents_manager/research/notes/episodic/ -File` returns ≥4 (3 + .gitkeep)

**Boundary reminders:**
- CAN: write `agents_manager/research/**` (your folder); read any file.
- CANNOT: edit `agents_manager/{master,planning,coder,review,design,assets}/**`, `opencode.jsonc`, `CLAUDE.md`, `tasks/<id>.md`, source code in `src/**`, `share/notes/03_coder_*`, `share/reports/04_*`.
- This is self-curation, not a research investigation. Skip the standard 8-section template; write directly.

**Return:** list of 9 new file paths + 2 modified file paths + one-line summary per change + `Memory written: none (self-curation; no new cross-task insight)` + `DONE_WITH_CONCERNS` if any file has open questions.

---

### Dispatch P3D2 — am-coder (backfill script)

**Complexity:** ≤50 LOC / 1 file / 1 novel (parser) / `split_recommended: false`

**Files expected (new):**
- `scripts/backfill-research-metrics.sh` — bash, stdlib only. Scans `share/notes/01_research_*.md`. For any file missing `## Metrics`, infers counts via regex and appends a TBD-stamped Metrics block. Append-only; never overwrites a present Metrics block. Exits 0 if no work, 1 if any injection. Runtime <10s.

**Acceptance:** Idempotent (re-runs do nothing); does NOT modify any file that already has Metrics; logs each file it touches with inferred counts.

**Boundary reminders:**
- CAN: write source code, write `scripts/**`, run any bash.
- CANNOT: edit `agents_manager/research/**`, edit `opencode.jsonc`, edit `agents_manager/SKILL.md` (master's lane), edit `tasks/<id>.md`, dispatch subagents.
- This is a script, not a research investigation. Skip the 8-section template; write a brief summary instead.

**Return:** path to the script + one-line summary + `DONE_WITH_CONCERNS` if any test run produced unexpected behavior.

---

### Master self-edit — agents_manager/SKILL.md gate-table update

**Complexity:** ≤5 LOC delta / 1 file / 0 novel

**Files expected (modified):**
- `agents_manager/SKILL.md` (master's own orchestration doc) — modify `## Programmatic gates` table at L204-216: add a row to the "Research" entry: "Output file exists and contains `## Metrics` block with 5 fields (findings, risks_HIGH, risks_MEDIUM, risks_LOW, clarifying_Qs) |"

**Acceptance:** Master's gates table contains the new check.

**Method:** Master edits directly per its boundary `CAN: edit your own orchestration doc (agents_manager/SKILL.md)`. No dispatch.

---

### DEFERRED — opencode.jsonc dedupe

Per AGENTS.md hard rule, `opencode.jsonc` is a controller fence owned by the user. No specialist may edit it without explicit maintenance-phase authorization. The dedupe plan is queued as **T-2026-07-04-008** (user-maintained, separate session).

---

## Phase 4 — Review (1 am-review dispatch)

**Done when:** `share/reports/04_review_T-2026-07-04-004.md` exists with per-task verdicts (PASS/WARN/FAIL) for every P3D*T* task + cross-cutting findings + honest assessment + self-critique.

**Per-task verification:**
| Task | Verify |
|---|---|
| P3D1T1 | 5 files exist, ≥3 L each, `last-verified` footer present, `Get-ChildItem` ≥7 |
| P3D1T2 | All 8 template sections present in order; synthetic (no real path:line refs to user files) |
| P3D2T1 | Each file has frontmatter per `agents_manager/memory/README.md` schema; ≤20 L body |
| P3D3T1 | Original 8 sections preserved + 4 new sections; no deletions; net add ≤120 L |
| P3D3T2 | Rules 1-10 still numbered; new rules 11+12 appended |
| P3D4T1 | JSON valid; prompt length not longer than original; master gates table updated |
| P3D5T1 | Idempotent; no overwrite; exit codes correct; total time <10s |

**Master applies verification-before-completion (G1)** after the reviewer returns: spot-check at least 2 path:line references per verdict.

**Expected WARNs (WARN register at `share/notes/04_warns_register_T-2026-07-04-004.md`):**
- W1 (LOW): Backfill T-2026-07-03-001's metrics are inferred counts, not directly authored.
- W2 (LOW): `glossary.md` size ≈ 30 L — small. Future expansion expected.

Both auto-acceptable per the triageable list.

**`max_fix_loops = 3`.** Pause + user if loop exhausted.

---

## Phase 5 — Completion

**Done when:**
- All review verdicts PASS or WARN (auto-accepted).
- `tasks/T-2026-07-04-004.md` `## Completion` block filled.
- `agents_manager/CHANGELOG.md` v0.14.1 entry added.
- Phase 5 menu presented (5a git flavor since repo is git-tracked).

---

## Cross-cutting concerns (NOT in this task — separately queued)

- T-2026-07-04-005: Master memory-asymmetry fix (master should read `agents_manager/research/notes/semantic/` on re-entry).
- T-2026-07-04-006: Master self-critique loop (per-task `## Master-self-critique` block at Phase 4 close).
- T-2026-07-04-007: 90-day sweep tooling for stale memory entries.