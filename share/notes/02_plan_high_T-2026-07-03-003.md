# High-Level Plan — T-2026-07-03-003

**Date:** 2026-07-03
**Sub-agent:** planning
**Consolidated plan:** `share/notes/02_plan_T-2026-07-03-003.md` (full brief)

## Goal

Bring our applied v0.12.0 cinematic-landing template up to the source's post-T-005/T-004 state — by copying 4 v2 memory files (renumbered 11/12/13/14), copying the worked-example + proposal for traceability, extending our skeleton ~271 lines to absorb the v2 axes (dark theme + a11y + DPR + JS controller + FOUC script), and updating the source PROPOSED_PATCH.md §F in all 7 zones — under a v0.12.1 CHANGELOG sub-version.

## Non-goals

- Renumbering `04-locale-handoff.md` (out of scope; user picked Q4=(b), not (c)).
- Editing `opencode.jsonc` (no changes needed).
- Editing `agents_manager/SKILL.md` line 72 ("9 memory files" → "14 memory files") — soft wall; flagged as follow-up.
- Adding frontmatter to new template memory files (template convention forbids it; v0.13.0 schema does not apply to `templates/<name>/memory/`).
- Touching `agents_manager/<role>/SKILL.md` files (planning's wall).

## Approach

The work decomposes into 4 phases (3a cp-and-stage → 3b skeleton extension → 3c §F sync → 3d cleanup), each phase a user-visible milestone. Phases are dependency-ordered so that the substantial skeleton extension (3b) sits on top of a fully-staged file base, and the final cleanup (3d) is the last to touch files (so its counts are correct).

The chosen approach is **incremental consistency** — start by getting all the source-side state local (cp demo folder + 4 memory files + MANIFEST update), then do the substantial work (skeleton extension ~271 LOC), then sync the source-side text (PROPOSED_PATCH §F), then cleanup (MOVED pointers, 05-theming shrink, CHANGELOG + count fixes). Each phase's `Done when` is testable via specific shell commands + file-existence checks.

We chose **v0.12.1** (sub-version under v0.12.0) over v0.14.0 because this is a back-fill of v0.12.0's memory folder (4 files added to an existing template) + a source-proposal update + a skeleton-extension + a count-fix, NOT a new feature. v0.14.0 would imply a new stacked feature.

We chose **renumbering** (Q4=b: 11/12/13/14) to avoid the existing duplicate-`04-` pattern in our memory folder. Number 10 stays empty (gap is fine). Verified: our 10 existing template memory files all have no frontmatter — matches source convention.

## Phases (one-line each)

1. **Phase 3a — cp-and-stage (P3T1, P3T2, P3T3)** — Copy demo folder + proposal + 4 v2 memory files + MANIFEST update. ~5 min.
2. **Phase 3b — skeleton extension (P3T6)** — Substantial ~271 LOC extension of our skeleton to match source's 1167-line state. ~30 min.
3. **Phase 3c — §F sync (P3T4)** — 7-zone text edits to source PROPOSED_PATCH.md. ~15 min.
4. **Phase 3d — cleanup (P3T5, P3T7, P3T8)** — MOVED pointer update, 05-theming shrink, CHANGELOG + count fixes. ~10 min.

Total: ~60 min wall-clock; ~271 LOC added + ~10/-2 lines PROPOSED_PATCH + ~30 lines CHANGELOG + ~16 lines demo-folder headers + (-15/+4 lines 05-theming) + +6 lines MANIFEST.

## Risks acknowledged

- **R1 — content duplication** if v2 memory files overlap topics with our existing memory/01-09 files. **Mitigation:** shrink `05-theming.md:58-72` (P3T5) to pointer; topical overlap documented in research §K; 14-LOC gap-acceptable.
- **R2 — v0.13.0 memory-system constraints don't apply to template memory files.** **Mitigation:** NO frontmatter added to cp'd files (template convention verified).
- **R3 — §F update scope ambiguous.** **Mitigation:** user Q5=(b) EXTENDED; this plan covers all 7 zones.
- **R4 — Where do the 4 v2 files land.** **Mitigation:** Q1=(d) cp + update MOVED headers; this plan handles both.
- **R5 — `14-dark-theme.md` documentation-vs-code mismatch.** **Mitigation:** Q3=(b) EXTENDED — skeleton extension (P3T6) absorbs dark theme + DPR + a11y patterns.
- **R6 — skeleton already has W4+W5; cp redundant.** **Mitigation:** audit in P3T6 — only the absences are added; existing listener is REPLACED with the full 16-LOC contract.

## Open assumptions

- **CHANGELOG version = v0.12.1** (sub-version under v0.12.0). User picked at the "DECIDE" prompt.
- **No `share/notes/99_decisions.md` entry.** This file does not yet exist in our repo; decisions accumulate in this task tracker + planning doc.
- **Use renumbered source filenames (11/12/13/14).** Per Q4=(b); gap at 10 is acceptable.
- **Include 00-readme-first.md + CHANGELOG L13 fix in P3T8.** Single ownership; one review pass.
- **`agents_manager/SKILL.md` line 72 NOT touched.** Soft wall; surface as follow-up to a future task.

## Plan self-score

- **Testability** (1–5): **4** — All 8 sub-tasks have a concrete `Done when` clause + AG gate numbers.
- **Scope** (1–5): **4** — Right size: 4 phases × 8 sub-tasks = 32 cells. Skeleton extension is substantial but bounded.
- **Dependencies** (1–5): **5** — Sub-dispatch order is explicit + reason-coded (cp first, then MANIFEST, then work, then cleanup).
- **Risks covered** (1–5): **4** — All 6 research risks mapped to mitigations; R5 documented as addressed via Q3=(b).

## Self-critique

- **Did I do my job?** Yes. Plan is concrete, dependency-ordered, with acceptance gates.
- **What might I have missed?**
  - The user's counting inconsistency (research says "13", math says 14, user prompt says 14). I went with 14 (the math).
  - The user's prompt mentions "Sub-dispatch order for am-coder" — I produced that explicitly in the task tracker.
  - The PROPOSED_PATCH.md update will require exact-string `edit` calls; if line numbers shifted from the source's pre-edit state, those exact strings may not match. am-coder should grep for the unique substrings in research §G rather than relying on line numbers.
- **What did I assume without evidence?**
  - I assumed the existing `04-locale-handoff.md` file stays as-is (not renumbered). Per Q4=(b), correct.
  - I assumed v0.12.1 sub-version (under `## v0.12.0`) is the right CHANGELOG placement rather than a new `## v0.12.1` top-level heading. This matches semver intent (sub-version = back-fill, not new feature). Master can override if user wants top-level.
- **Was I complete?** Yes on the planning surface (3 artifacts + task tracker + acceptance gates). The sub-task decomposition is granular enough for am-coder to execute without re-asking.