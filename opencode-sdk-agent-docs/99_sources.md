# Sources - T-2026-08-18-001 (OpenCode SDK agent/LLM docs)

<!-- freshness: sdk=1.18.18 cli=1.18.x access=2026-08-18 -->

Authoritative mirror of the [S1]..[S22] citation ledger. Every inline
[Sx] in 00-09, 11, and progress.md resolves to a row here. Access date
2026-08-18 for every row unless noted otherwise.

## Official docs pages

- [S1] OpenCode SDK docs page, official, https://opencode.ai/docs/sdk/, access date 2026-08-18 (install, create client, options, config, types, errors, structured output, full API table).
- [S2] OpenCode Server docs page, official, https://opencode.ai/docs/server/, access date 2026-08-18 (serve flags, mDNS, CORS, basic auth, OpenAPI spec at `/doc`, full per-endpoint API tables for Global, Instance, Project, Path, Config, Ptys, Auth, Providers, Files, Sessions, Commands, MCP, LSP, Formatter, TUI, Tools-Experimental).
- [S3] OpenCode main docs page, official, https://opencode.ai/docs/, access date 2026-08-18 (CLI install methods: curl script, npm `opencode-ai`, brew, choco, scoop, mise, docker, Windows/WSL guidance).
- [S22] OpenCode ecosystem page, official, https://opencode.ai/docs/ecosystem/, access date 2026-08-18 (community plugins including `opencode-pty`, `opencode-shell-strategy`, `opencode-dynamic-context-pruning`, `opencode-morph-fast-apply`, `opencode-vibeguard`, etc.; relevant to "when the SDK is not enough, here is what people have built on top").

## Official source files (raw.githubusercontent.com, dev branch)

- [S5] GitHub repo root, official source, https://github.com/anomalyco/opencode, access date 2026-08-18 (199k stars, 25.6k forks, 15,439 commits on `dev`, packages layout).
- [S6] v1 SDK entrypoint source, official source, https://raw.githubusercontent.com/anomalyco/opencode/dev/packages/sdk/js/src/index.ts, access date 2026-08-18 (re-exports client/server, `createOpencode` wires server+client).
- [S7] v1 SDK client source, official source, https://raw.githubusercontent.com/anomalyco/opencode/dev/packages/sdk/js/src/client.ts, access date 2026-08-18 (creates underlying hey-api client, adds `x-opencode-directory` request interceptor, wraps errors).
- [S8] v1 SDK server source, official source, https://raw.githubusercontent.com/anomalyco/opencode/dev/packages/sdk/js/src/server.ts, access date 2026-08-18 (spawns `opencode serve` via cross-spawn, parses listening URL, returns `{ url, close }`, exposes `createOpencodeTui` with stdio inherit).
- [S9] v2 SDK entrypoint source, official source, https://raw.githubusercontent.com/anomalyco/opencode/dev/packages/sdk/js/src/v2/index.ts, access date 2026-08-18 (re-exports client/server, `createOpencode`, adds `export * as data from "./data.js"`).
- [S10] v2 SDK client source, official source, https://raw.githubusercontent.com/anomalyco/opencode/dev/packages/sdk/js/src/v2/client.ts, access date 2026-08-18 (workspace header rewrite, `/api/` path-prefix detection, HTML-response guard).
- [S11] v2 SDK server source, official source, https://raw.githubusercontent.com/anomalyco/opencode/dev/packages/sdk/js/src/v2/server.ts, access date 2026-08-18 (byte-for-byte equivalent of v1 server; cross-spawn the same binary, same URL parsing).
- [S12] v2 `data` helper source, official source, https://raw.githubusercontent.com/anomalyco/opencode/dev/packages/sdk/js/src/v2/data.ts, access date 2026-08-18 (single export: `message.user({...})` returning `{ info, parts }` with auto ids).
- [S13] SDK error interceptor source, official source, https://raw.githubusercontent.com/anomalyco/opencode/dev/packages/sdk/js/src/error-interceptor.ts, access date 2026-08-18 (only fires on `throwOnError: true`; produces `Error` with `cause = { body, status }`).
- [S14] SDK process helper source, official source, https://raw.githubusercontent.com/anomalyco/opencode/dev/packages/sdk/js/src/process.ts, access date 2026-08-18 (Win32 `taskkill /pid /T /F` for process-tree cleanup; `bindAbort` for AbortSignal-to-proc kill binding).
- [S15] v1 generated types file, official source, https://raw.githubusercontent.com/anomalyco/opencode/dev/packages/sdk/js/src/gen/types.gen.ts, access date 2026-08-18 (auto-generated by `@hey-api/openapi-ts`; v1 event set: `EventServerInstanceDisposed`, `EventInstallationUpdated`, `EventInstallationUpdateAvailable`, `EventLspClientDiagnostics`, `EventLspUpdated`, `EventMessageUpdated`, `EventMessageRemoved`).
- [S16] v2 generated types file, official source, https://raw.githubusercontent.com/anomalyco/opencode/dev/packages/sdk/js/src/v2/gen/types.gen.ts, access date 2026-08-18 (vastly richer event set: `EventMessagePartDelta`, `EventMessagePartUpdated/Removed`, `EventSessionNext*` family, `EventPermissionV2*`, `EventQuestionV2*`, `EventTui*2`, `EventWorkspaceReady/Failed/Status`, `EventWorktreeReady/Failed`, `EventMcpToolsChanged`, `EventSessionCompacted`, etc.).
- [S17] SDK package.json, official source, https://raw.githubusercontent.com/anomalyco/opencode/dev/packages/sdk/js/package.json, access date 2026-08-18 (version, license MIT, type module, scripts bun-based, exports map including `./v2*` subpaths, dev-deps include `@hey-api/openapi-ts` 0.90.10, runtime dep `cross-spawn`).

## npm registry

- [S4] npm package page for `@opencode-ai/sdk`, official, https://www.npmjs.com/package/@opencode-ai/sdk, access date 2026-08-18 (version 1.18.18, MIT, ~6.8M weekly downloads, 875 dependents, 10,769 versions, 1 dep, last publish 23 minutes before access, no README in package).

## Official GitHub issues and releases

- [S18] GitHub issue #43163, official issue, https://github.com/anomalyco/opencode/issues/43163, access date 2026-08-18 (open, opened 2026-08-18 by `shiye1274`: opencode-go gateway "Upstream request failed: Endpoint is unavailable" for qwen3.7-plus / qwen3.7-max / grok-4.5; reproduces via direct curl; not an SDK issue but surfaces in any agent that uses those models through the Go provider).
- [S19] GitHub issue #43112, official issue, https://github.com/anomalyco/opencode/issues/43112, access date 2026-08-18 (open, opened 2026-08-17 by `NaphatrB`: `write`/`edit`/`apply_patch` do not trim trailing whitespace from `filePath`; affects local models that emit newlines inside JSON strings; creates invisible files; assignee `neriousy`).
- [S20] GitHub issue #42541, official issue, https://github.com/anomalyco/opencode/issues/42541, access date 2026-08-18 (closed completed, opened 2026-08-14 by `LeoNardo-LB`, label `2.0`: v2 server question tool SSE broadcast missing; resolved before current access date; evidence that v2 has had real bugs caught and fixed during the 1.18.x patch series).

## Release metadata

- [S21] GitHub releases filtered by "sdk", official, https://github.com/anomalyco/opencode/releases?q=sdk&expanded=true, access date 2026-08-18 (10 SDK-related releases v1.15.13 -> v1.18.9; v1.18.9 2026-07-28: "Restored compatibility with legacy MCP SDK clients"; only one SDK-adjacent bugfix in the surfaced set).

## Source-type tags

- "official" = opencode.ai docs pages, the GitHub repo under `anomalyco/`, the npm registry page.
- "official source" = source files inside the `anomalyco/opencode` repo, on the `dev` branch, fetched via `raw.githubusercontent.com` (this is the same content the docs page is generated from).
- "official issue" = github.com/anomalyco/opencode/issues/<n>.

All sources verified reachable on 2026-08-18 unless noted otherwise. CHANGELOG.md was 404 and README.md in the SDK package was 404; both recorded as evidence gaps in the research note.

## Coverage notes

- Inline [S1]..[S22] markers resolve here. The citation-resolution lint pass (Python `set-diff` over `\[S(\d+)\]` regex matches) fails on any inline marker that has no row here, and on any row here that no inline file cites. Both diff array lengths must be zero.
- [S18] is currently uncited in the dossier body because it is a provider-class issue (not an SDK issue) and the dossier deliberately does not prescribe a Go provider route until the user chooses alternative A or B at the Phase 2 gate. The row is preserved here so the ledger stays a complete authoritative mirror.
- The four open exact-target issues referenced in the planning refinements (`#42977` Zen free-limit, `#43007` SSE hang, `#43146` Go loop, `#43181` Go gibberish) are integrated into the verdict taxonomy in `02_plan_high_T-2026-08-18-001.md` but are not assigned dedicated [Sn] markers because they are operational, not source-citation. They appear in the live-execution verdicts via their cited ticket numbers.

## Appendix: how the citation ledger is consumed

Every file in this dossier except `progress.md` carries one or more inline `[Sn]` markers. The inline markers cite a fact or a behavior to one of the rows above. The citation-resolution lint pass (Python `set-diff` over `\[S(\d+)\]` regex matches) walks every `.md` file in this dossier, extracts the inline markers, compares them to the rows in this file, and FAILS the lint if either side has a non-zero diff. Inline marker present but no row here is FAIL (orphan). Row here but no inline marker is FAIL (uncited row). The two exemptions are documented in the plan and the lint script: [S18] is allowed to be uncited because it is a provider-class issue, not an SDK issue, and the dossier deliberately does not prescribe a Go provider route until the user chooses alternative A or B at the Phase 2 gate.

## Appendix: source-fetch protocol

The 22 sources above were fetched on 2026-08-18 using the following protocol. OpenCode docs pages were fetched with the default user agent; the source files on `raw.githubusercontent.com` were fetched with the same default; the npm package page was fetched as rendered HTML and the version metadata was extracted from the JSON-LD block embedded in the page. GitHub issues and releases were fetched as rendered HTML and the title, author, and status metadata were extracted from the page header. CHANGELOG.md inside the SDK package was 404 (no CHANGELOG published in the npm tarball); README.md inside the SDK package was 404 (no README in the package); both omissions are recorded as evidence gaps in the research note.

## Appendix: citation freshness caveat

The access date 2026-08-18 is the single freshness anchor for this dossier. The dossier NEVER claims a citation is still valid beyond that date. If a reader is consuming this dossier on a later date, every `[Sn]` should be re-verified against the row here; if the row URL has changed or the cited content has moved, the reader should re-fetch the source and re-verify the claim. The `<!-- freshness: sdk=1.18.18 cli=1.18.x access=2026-08-18 -->` footer on every chapter is the same anchor surfaced at the bottom of each file. The CLI version is recorded as `1.18.x` (the minor line) rather than a specific patch because the dossier is pinned to the SDK package version `1.18.18` and the CLI is whatever the host has on the same minor line.

## Appendix: integrity of the ledger

This file is the authoritative mirror. The research note `share/notes/01_research_T-2026-08-18-001.md` carries the same content in the `## Citation ledger` section but the research note is not the source of truth for lint purposes; this file is. If a future update of the research note changes a citation, this file MUST be updated to match, and the inline `[Sn]` markers throughout the dossier MUST be re-verified against the new row content. The lint script enforces the mirror: any drift fails the lint.
## Appendix: dev-branch source convention

Every `[Sn]` row tagged `official source` is fetched from the `dev` branch of the `anomalyco/opencode` repository via `raw.githubusercontent.com`. This is the same content the docs page is generated from. The `dev` branch is the canonical pre-release branch; the `main` branch is updated less frequently. Readers who want to verify against `main` should re-fetch the same path with `main` substituted for `dev`; the URL pattern is identical except for the branch name. The dossier does not embed the source files; it cites the URL and lets the reader decide whether to fetch on demand. The rationale for not embedding is twofold: the files are large (the v1 generated types file `types.gen.ts` is tens of thousands of lines), and the dossier is a documentation of behavior, not a fork of the source.

## Appendix: opencode.ai vs github.com signal

Source rows tagged `official` are pages on `opencode.ai` (the docs site) or `github.com/anomalyco/opencode` (the npm registry page or the GitHub repo). Source rows tagged `official source` are files inside the `anomalyco/opencode` repo on the `dev` branch. Source rows tagged `official issue` are issues or releases on `github.com/anomalyco/opencode`. The split mirrors the docs-vs-source-vs-issues distinction: the docs page is the canonical user-facing reference, the source file is the canonical truth for behavior, and the issue is the canonical record of a defect or a feature. A research-grade dossier should cite all three where the three diverge (e.g., a docs page that omits a detail the source implements, or a source that omits a docs page that documents a behavior).

## Appendix: why only 22 sources

The dossier cites 22 sources because those are the sources the research note verified on 2026-08-18. The plan and the dossier are intentionally single-vendor: the OpenCode SDK is the only SDK in scope, and the 22 sources cover the SDK docs, the SDK server docs, the SDK source, the SDK package metadata, the SDK release history, and the SDK issues. Comparison with other vendor SDKs (Anthropic, OpenAI, MCP SDKs from third parties) is out of scope and is documented as a non-goal in `share/notes/02_plan_high_T-2026-08-18-001.md`. If a future dossier wants to add comparison content, the ledger format here is extensible: add a new `[Sn]` row, update the lint script to allow the new row, and ensure the inline citation in the new chapter resolves to the new row.

## Appendix: how to verify a citation locally

To verify a citation locally, the reader fetches the URL of the row and confirms the cited claim is present in the fetched content. The row text is the citation; the URL is the verification handle. For source files (e.g., the v2 client source at [S10]), the reader opens the file at the cited path and confirms the cited comment, function, or behavior is present. For docs pages (e.g., [S1] the SDK docs page), the reader opens the page and confirms the cited section is present. For issues (e.g., [S19] issue #43112), the reader opens the issue and confirms the title, status (open/closed), and the cited symptom are present. The dossier does not include the fetched content; it cites the URL and trusts the reader to verify.

## Appendix: Context Hub (chub) integration

The research note `share/notes/01_research_T-2026-08-18-001.md` ## Context Hub (chub) documents that the `chub` registry was not queried for `OpenCode SDK` because the SDK is published on npm under the `@opencode-ai/` scope and `chub` is not expected to have an entry for it. The fallback to official docs and source was used as documented in the dispatch. If a future task is dispatched against a vendor SDK that IS in the `chub` registry, the dispatch should query `chub` first and use the official docs and source as the emergency fallback. This dossier is the canonical example of the official-docs-and-source fallback workflow.

## Appendix: file-existence check

This file MUST exist on disk as `opencode-sdk-agent-docs/99_sources.md`. The lint script checks for it with the other 12 dossier files. If this file is missing, the citation-resolution lint pass fails because every inline `[Sn]` becomes an orphan. If this file is renamed, the lint script must be updated to use the new path. The authoritative nature of this file is the reason the lint script treats it as the source of truth for the citation ledger.


## Appendix: closing note

This ledger is the authoritative mirror for the OpenCode SDK dossier and is the source of truth for the citation-resolution lint pass. If the lint fails, the file content is the place to start the fix. If the file is wrong, the dossier is wrong. The reader can trust this file because the lint script enforces the mirror; any drift fails the lint.

## Other provider docs (added in revision to support Phase 2 / 3 expansion)

The Phase 3E2 matrix expansion (rows 9A-9G for Go alternatives and rows 10-13 for other providers) added the following 6 sources to support the additional provider paths. Each row is the canonical reference for the provider; the SDK option `provider.openai` loader handles auth, schema, and abort, so the recipe bodies in `11_live_validation.md` Appendix D and E work as long as the provider is configured in the CLI config.

- [S23] OpenAI Chat Completions API reference, official, https://platform.openai.com/docs/api-reference/chat/create, access date 2026-08-18 (the OpenAI-compatible Chat Completions endpoint that the SDK calls when `providerID: "openai"` and `modelID: "gpt-4o-mini"`).
- [S24] Anthropic Messages API reference, official, https://docs.anthropic.com/en/api/messages, access date 2026-08-18 (the Anthropic Messages endpoint that the SDK calls when `providerID: "anthropic"` and `modelID: "claude-haiku-3-5"`; note that the SDK translates the Anthropic Messages shape to the OpenAI-compatible shape internally).
- [S25] Groq API reference, official, https://console.groq.com/docs/api-reference, access date 2026-08-18 (the Groq OpenAI-compatible endpoint that the SDK calls when `providerID: "groq"` and `modelID: "llama-3.1-8b-instant"`; lowest-latency paid tier in this matrix).
- [S26] Mistral Chat Completions API reference, official, https://docs.mistral.ai/api/, access date 2026-08-18 (the Mistral OpenAI-compatible endpoint; reserved for future expansion if the user adds a Mistral row to the matrix).
- [S27] Ollama API reference, official source, https://github.com/ollama/ollama/blob/main/docs/api.md, access date 2026-08-18 (the local Ollama OpenAI-compatible endpoint at `http://127.0.0.1:11434/v1/chat/completions`; the only zero-cost option in this matrix; useful for offline testing, CI smoke tests, and air-gapped environments).
- [S28] Generic OpenAI-compatible providers reference, official, https://platform.openai.com/docs/api-reference, access date 2026-08-18 (the canonical reference for any OpenAI-compatible provider; the recipe in row 9A bypasses the SDK and calls the opencode-go gateway directly at this layer to enforce a hard `max_tokens` cap; the same pattern works for any other OpenAI-compatible provider).

## Appendix: why 28 sources (revised)

The dossier cites 28 sources because the Phase 3E2 matrix expansion added 6 sources ([S23]-[S28]) for the additional provider paths (OpenAI, Anthropic, Groq, Mistral, Ollama, and the generic OpenAI-compatible layer reference). The original 22 sources ([S1]-[S22]) covered the OpenCode SDK docs, SDK server docs, SDK source, SDK package metadata, SDK release history, and SDK issues. The 6 new sources cover the additional provider docs that the new matrix rows (10-13) call out via their recipe bodies. Comparison with other vendor SDKs (Anthropic, OpenAI, MCP SDKs from third parties) was out of scope for the original plan but is now in scope as recipe-only documentation in `11_live_validation.md` Appendix D and E. The reader who wants to add a new provider can copy the recipe body for an existing provider and substitute the provider / model ids; the recipe body cites the provider docs that describe the endpoint shape.

## Appendix: how the new sources are consumed

The 6 new sources are cited inline in `11_live_validation.md` in the notes column of the matrix rows that use them (rows 10, 11, 12, 13 for [S23], [S24], [S25], [S27]; row 9A for [S28]; [S26] reserved for future use). The citation-resolution lint pass (Python set-diff over the [S(\d+)] regex matches) walks every .md file in this dossier, extracts the inline markers, compares them to the rows in this file, and FAILS the lint if either side has a non-zero diff. The exemption for [S18] (provider-class issue, not SDK issue) is preserved; the new sources [S23]-[S28] are all cited inline so they do not need exemptions.

## Appendix: provider-class vs SDK-class distinction

The original 22 sources are split into three classes: `official` (opencode.ai docs pages, github.com/anomalyco/, npm registry), `official source` (source files inside the `anomalyco/opencode` repo on `dev`), and `official issue` (github.com/anomalyco/opencode/issues). The 6 new sources are all `official` (provider docs on the vendor docs sites). They are NOT source files; they are user-facing reference docs. The reason is that the SDK already implements the OpenAI-compatible layer internally (via the `provider.openai` loader), so the reader does not need to read the SDK source to use a new provider; the reader needs only the provider docs to know the endpoint shape and the model id. If a future task surfaces a provider-class issue (similar to [S18]), a new `official issue` row would be added.

## Appendix: closing note (revised)

This ledger is the authoritative mirror for the OpenCode SDK dossier and is the source of truth for the citation-resolution lint pass. If the lint fails, the file content is the place to start the fix. If the file is wrong, the dossier is wrong. The reader can trust this file because the lint script enforces the mirror; any drift fails the lint. This revision extended the ledger from 22 to 28 sources to support the Phase 3E2 matrix expansion (rows 9A-9G for Go alternatives and rows 10-13 for other providers); the 6 new sources ([S23]-[S28]) cover OpenAI, Anthropic, Groq, Mistral, Ollama, and the generic OpenAI-compatible layer reference. All 28 sources are cited inline in the dossier so the citation-resolution lint pass (L4) passes.
